# 上传尺寸差异裁决与变更范围

- 身份: UPLOAD-SIZE-001, 作者/执行方, 2026-09-23T20:18:33.019195+08:00.
- 来源: 用户明确要求判断并一并修复无idx上传尺寸不等阻断清单的问题, 同步P04和直接测试. 这是对旧严格清单规则的明确局部修改授权.
- 产品基线: main/10eb6e6f823972d254acf5e3e6f73e6b433b1874. P04由v11变v12; 基底九规范CURRENT-PC022-R01及其原manifest不改写, 本次增量身份由同目录p04-upload-size-r01.json独立绑定. 其他八份规范和G14已有原件保持原样. 后续消费本次P04必须显式纳入此增量, 不能把旧manifest的v11 hash当成v12通过证据.

## 事实与原因界限

作者在当前实际_deduplicated_uploads及FixedToolService调用链用良性合成元数据复现: file_size1000/uploaded_size1022在原_upload_record触发invalid_file_size; uploaded_size989在无idx分支触发invalid_sparse_pair. 文件相同而只改变大小即可决定成败, 不依赖VM/Prefetch名称. 最小复现prefetch-size-repro.py初次2errors, 修复后通过. service级prefetch-contract-tests.py初次2errors, 修复后完整清单与稳定文件下载通过.

现有只读G14-cycle1-rootcause.md记录真实Prefetch增长(+22B和另一个文件+99B). 用户另外报告后续缩小及增长. 本次未连接VM, 未独立取得后三次采集的全部原始元数据, 不把“187必定因完全稳定才成功”或“任何重试必然失败”当成已证事实. 采集期间修改是合理解释, 单靠大小字段不能排除不完整上传或丢失索引. 不读取样本或真实凭据.

## 固定处理规则

1. 两大小仍须非负非bool整数; 原Type/作用域/selector/身份/精确去重全部保留.
2. 无idx条目大小不等时清单保留两原值及file_id, 不重写为相等. warnings含全量去重差异数量与返回上限内逐文件ID/两大小, 使用possible_file_modified_during_collection明确原因未被证明. 不只对Prefetch路径豁免, 不读内容或落盘.
3. 有idx的稀疏对象仍严格要求uploaded_size<=file_size及配对镜像字段/identity/path完全一致; 孤儿idx、多义/损坏配对、碰撞继续拒绝. 自然命名.idx普通文件不误当索引.
4. 下载选中的无idx差异条目仍在内容I/O或目录创建之前BACKEND_ERROR/size_mismatch. 未确认完整性不伪造成成功原件. 同Flow其他合法条目正常可下载. 原file_size==成功DownloadResult.size、hash、安全发布及发布后错误合同均保持.
5. 不新增MCP入参、结果字段或工具, golden无变化. P04列举警告规则和下载边界、两README同步. 不修改P05/VPB runner、旧采集报告、快照或测试基础设施.

## 验证

- 红/绿原件: /home/adminn/projects/MalTrace/AI辅助逆向/.tmp/velo-transfer-sol-20260923-1404/prefetch-size-repro.log, prefetch-size-green.log, prefetch-contract-red.log, prefetch-contract-green.log. 红阶段各2errors, 两个绿阶段各1test通过.
- 直接回归: `PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_p04_fixed_tools tests.test_pc017_file_delivery tests.test_p04_contract_golden tests.test_p04_registration -v`, 最终exit0, 42tests中40通过/2native-Windows跳过. 原件/home/adminn/projects/MalTrace/AI辅助逆向/.tmp/velo-transfer-sol-20260923-1404/upload-size-regression-final.log.
- 首次回归新增测试错误读取异常.details已修正为正式error_model; 另发现原有Windows路径断言在Linux用Path.name失败, 从修改前原件单测复现同失败后改为PureWindowsPath, 保留双目录同名/不同ID断言. 原件upload-size-existing-path-failure.log. 不把这项既有失败归因产品修复.
- 修改3个Python py_compile及Velo scoped diff --check通过. 无临时调试日志注入生产. 本次诊断脚本仅留在明确.tmp协作证据目录.
- 未部署VM或重跑G14, 不宣布新的Triage/快照阶段通过. 需使用包含此提交的新制品及当前来源增量重绑后, 再运行受影响真实场景.
