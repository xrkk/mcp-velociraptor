# PLAN-CHANGE-022登记与作者方向：Windows刷新边界

2026-09-21，主方规划/作者/修复/调度；不是独立核验，不产生或关闭AUD/CHK。依据用户已授权主方负责总纲验收所需回流及决策。状态：方向候选，精确条款及保真待审；不取代CURRENT-PC021-R04，不开放生产实施门。

## 事实与原件

当前HEAD 8d2b2191a71541c704aeeb5ec9a086a0275c8f9b。PC021-WIN-DURABILITY R01官方资料、R02隔离Windows探针位于`.tmp/velo-codex-20260921-pc021-win-durability/`。
R02 `R02-evidence/probe.stdout.json` SHA-256 feb704e2b9eb31ac9de6d03baff5e7679434aa0745c0c8bb99ee786a78367c1c；脚本SHA a110de8a3276e91c7f31fac937b7fb1dc39fe896e81d2c94026f0d74668fbf48。

Windows10 build19045、Python3.13.7 x64、NTFS、既有管理员token：os.open(directory,O_RDONLY)实际errno13；GENERIC_READ目录handle的FlushFileBuffers返回错误5；GENERIC_WRITE目录handle刷新返回成功；普通文件对照、同卷MoveFileExW flags9和hard-link正负例可观察成立。身份前后不变、无owned后台进程，未操作.233。不能推断普通用户支持或掉电保证。

现有PC020 writer以目录fd/fsync实现Windows路径，隔离测试替换此步骤，尚未完成生产实现。P05包保全与P04产品下载为不同层，不能因P04没有目录同步就判P04违反P05契约。

## 作者方向与明确代价

选择推进“限定Windows运行环境、实际原生刷新确认、失败停止”的候选，不选忽略同步错误、不增加管理员提权或卷级flush、不开展VM断电试验。

候选必须明确区分：文件内容flush；目录句柄刷新API确认；原子可见与回读；硬件/宿主掉电持久性。拟接受的成功口径是规定API全部实际成功且身份/字节复核成立，不承诺官方未证明的所有存储栈掉电后目录项必存。这个保证边界必须作为规范差异公开审查，不能称为已证明POSIX等价或偷偷降低旧验收。

候选生产目录操作限定本地NTFS及经过相同进程身份/路径权限预检的环境：CreateFileW目录GENERIC_WRITE、OPEN_EXISTING、BACKUP_SEMANTICS及OPEN_REPARSE_POINT；完整handle身份/目录/reparse验证；FlushFileBuffers成功；CloseHandle与异常处理明确。不要求管理员身份本身为产品需求，不自动提权；实际权限不够拒绝。预检不能代替每个真实写入后的同步。共享flags、句柄生命周期及新建父目录的逐级同步须在精确候选中定义。

保留所有文件flush/fsync、独占创建/不覆盖、原件完整图、签发后一次性能力、.next→intent→compare→replace顺序、读回、失败现场保全和非COMMITTED拒绝。新目录、root/receipt、包成员发布、migration及activation writer各同步点都需覆盖，不能只修epoch8。现有os.replace是否改为Win32 write-through须在候选中明确且不削弱同卷原子替换；不将其成功当作完整持久性替代。

断电、OS/VM崩溃、存储异常或刷新/关闭返回不确定时，停止自动接续，保全可取得原件，由主方核实际canonical及图再决定；不从receipt/hash读回反推过去API成功，不自动重签/回滚/复用能力。P06/P07接收的是明确版本及来源绑定的操作事实，不是硬件无损证明。若正式需求确实要求硬件掉电持久性而无可接受限定，当前方向必须拒绝并回主方，不能由执行端移除该要求。

## 受影响边及重新裁决

当前需求恢复/验收边界→总纲OUT/ACC-020/023/026→P05迁移、签发、包保全、epoch8→P06严格接收/选择/聚合→P07终检/追溯。准确正式及累计ID必须从当前来源定位，不凭此摘要代替全量保真。P04产品工具、130注册、129+1/645、187/189阶段和用户业务目标不变。

环境/文件系统/权限/解释器/API行为改变、目录身份漂移、任何同步点不可兑现、字段语义无法无歧义绑定或正式来源不接受保证边界时重新裁决。候选不得增加网络/磁盘/服务/快照权限、兼容fallback或新的产品清理工具。

下一步仅形成P05精确候选与相关规范差异映射；主方审定后才能正式同步、完整保真自评及必要审核，不能凭研究/探针/本登记改生产代码或真实canonical。
