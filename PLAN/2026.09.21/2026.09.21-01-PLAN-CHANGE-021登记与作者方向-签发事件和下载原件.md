# PLAN-CHANGE-021 登记与作者方向：签发事件和下载原件

创建2026-09-21。作者为当前Codex主会话，角色规划/回流作者。状态：方向确定、精确契约待成稿；未取代CURRENT-PC020-R01，未完成需求保真、自评及必要审核，不开放新实现或现场门。不是AUD/CHK，不重置审核额度。

## 事实与触发

PC020-ACTIVATION R03/R04揭示：现有只读验证器只有内容DAG与issued_at格式，没有记录实际验证完成后取时签发的动作；下载响应local_path是原服务端绝对路径，测试搬运后改写SDK报告，尚无不可变原件到包成员的生产映射。R04定位文档SHA26a20e309b3a77cff716b74ea9788307d0120ade348dbdea63069de9146c528a，路径为.tmp/velo-codex-20260921-pc020-activation/R04-evidence/contract-location.md。70方法和ACT-002即时完成修复保留，不当整体通过。

## 作者选择与适用边界

1. 采用P05 issuer/controller实际签发及固定同层issuance-receipt.json方向；保持activation root和Phase现有exact keys，不让root引用未来epoch8或自身收据。签发器先验证完整内容图，成功后才取一次实际UTC Z签发值、冻结root、持久化并读回验证，随后记录成功事件。收据绑定同一次操作、完整输入/来源、cycle2事实、root字节与issued_at；消费者须重验原件，不信任布尔摘要。精确键、事件和故障条件待候选成稿，不在本文隐式创造可执行schema。
2. 采用确定性包成员downloads/<sha256(flow_id UTF-8)>/<file_id>/content.bin，保持SDK报告与mcp_result原字节不变。收件时按可信配置的产品下载根与既有后缀核源，非链接普通文件、流式复制、大小/hash、独占发布及读回，封入现有完整manifest。后续只解包内Ref，不解引用原机local_path，也不改写原响应。不另加复制sidecar，除非精确草案证明既有报告+确定路径+manifest仍无法表达必要身份。
3. 候选范围采用每个Phase真实完整Triage FINISHED、实际结果与文件列表，加固定ASCII/UTF-8 fixture两条list/download交付链；不新增对Triage全部上传的批量下载，不把未知空结果视成功。原因：当前文字未定义Triage文件选择/数量/空列表/额外下载预算，P06又将Triage与fixture下载分列；先明确固定范围，避免无依据重复下载4GiB级数据。该澄清须回流需求累计结论、总纲及负责Pxx并做保真，不能仅由实现解释生效。如正式来源明确要求Triage自身下载，则停止本候选范围并重新裁决选择和资源界限。

## 不变量、故障与重新裁决

保持三阶段独立事实、真实来源、P04显式单文件下载及错误模型、PC020状态代次、187/188保留、安全/资源门、129实测+1排除和645关系；无新产品工具、无PKI/外部可信时间或跨机时间大小排序门。issuer的动作次序由同一操作原件承接，不把时间格式等价签发发生；未来epoch8写入另有输入绑定和失败保全。

无效源、包目标冲突、复制中变化、链接/reparse、签发输入漂移、已存在root/receipt、fsync/读回或receipt失败均不能报告成功或放行P06；已创建本轮对象保全并明确失败状态，不删改历史或猜测重试。精确算法须解决封包自身排除、来源冻结、校验前无issued_at和收据未生成的引导问题，不能引入循环引用或绕过最终验证的开关。

若现有exact字段无法闭合、收据引入循环、目录持久性不支持、来源不符、Triage范围与正式要求冲突，停止受影响项重新裁决。本文不授权恢复/创建快照、部署、网络、服务或canonical写入。

## 影响与下一步

沿REQ/RACC-020/023及相关累计来源，经总纲恢复/验收项到P05签发/封包→epoch8→P06 receiver/selection/aggregate→P07；P04交付字段不变但下游收件职责需对应。下一任务仅完成P05精确契约候选及逐对象影响定位，既有稳定ID逐条核对，不发明既有边ID。之后主方审定，再同步规范、重做需求保真/自评及当前治理要求的必要审核，才派分开的下载保全/签发/消费验证实现。候选成稿不是正式全组通过。
