# GLM独立执行交接：剩余任务、执行步骤与验收条件

创建：2026-09-21。编写者：Codex主方。项目根：`/home/adminn/projects/mcp-velociraptor`。

本文件是执行清单，不是规范变更、实施许可或验收通过记录。它不覆盖当前需求、总纲、Pxx及用户授权边界。GLM应从G00开始，按依赖顺序逐包执行；不需要主会话的对话历史。当前状态是**规范回流尚未完成，生产实施门关闭**，不是只剩最后一次测试。

## 0. 先读这一页

### 0.1 要交付的两个结果

1. **全部有效工具可用**：130个注册工具准确列出；129个有效实测工具逐项有匹配当前部署的真实成功证据，另1个PacketCapture按PC014登记排除。不能称130个业务工具全部实测通过。
2. **总纲验收通过**：除工具可用外，完成有效P01—P07契约、P05恢复/激活、安全、P06全量情景、P07成本/清理、80正式项与67累计结论双向追溯，最后由未参与作者/实现/修复者独立核验最终HEAD。

GLM可以独立完成执行、自检、证据整理、修复和候选回流准备；**不能把自己的自检充作最终独立核验，也不能凭本清单自行降低规范要求**。遇到需作者采纳或独立角色的门，交付完整待审包并准确标“待验收”，不伪造角色通过。已明确授权范围内的普通实现细节自行解决，不按命令或文件逐项请批。

### 0.2 当前对象及冻结身份

| 对象 | 本次交接时事实 |
| --- | --- |
| 分支、HEAD | `main`，`8d2b2191a71541c704aeeb5ec9a086a0275c8f9b` |
| 当前九规范组 | `CURRENT-PC021-R04` |
| manifest | `PLAN/2026.09.21/current-normative-inputs-pc021-r04.json` |
| manifest SHA-256 | `fe6539153d030929b5743ee9fd7fb9d9b47fd49c4ef50bb6e4b0b138e844e0a6` |
| 版本 | 需求v9、总纲v22、P01v4、P02v7、P03v3、P04v11、P05v18、P06v19、P07v17 |
| 未采纳方向 | `PLAN/2026.09.21/2026.09.21-05-PLAN-CHANGE-022登记与作者方向-Windows刷新边界.md`；尚未提交、不是当前规范 |
| 正式需求/累计数 | 80＝26 REQ＋15 CON＋13 NON＋26 RACC；累计67；并集147 |
| 工具/情景口径 | 130注册、129实测＋1有效排除、645工具—情景覆盖关系；其余细则以当前P06为准 |

开始时重新读取`git status --short`、`git rev-parse HEAD`及manifest所有九对象。HEAD不同先解释差异，不回退现场。以上身份是交接快照，不是未来持续有效的“最新”断言。

三份未提交文件是**未整包验收的已有工作**，保留后逐项审查，不能删除或直接纳入合格提交：

| 路径 | 交接时SHA-256 |
| --- | --- |
| `tests/p05_pc020_activation.py` | `190d20edd96b1a61b15c26fcaa798f6ae7c17be120dc2536c6616b2d882daed4` |
| `tests/pc020_activation_fixture.py` | `207c9e19633506e20ad4e2cd59c888ba3c3f39948b3eafd28cf390343aebbfc0` |
| `tests/test_p05_pc020_activation.py` | `a8c3bea7b7db795d8732b0c1770c28f529894996bb3954d4f7a772087b837c27` |

### 0.3 当前交接：用户已结束Sol会话，GLM接续

2026-09-22更新：用户明确告知“原Sol会话已经被我结束了”。主控接受此事实，结束原Sol协作，不再续派；本轮交接文档修订结束后，由GLM接续唯一业务写入。**原先等待Sol回传的G00阻断已解除，不需要已关闭会话补发`WORK_STOPPED=true`。**这是用户终止确认，不伪装成执行端自报或后台全部结束的机器证明。

GLM现在执行G00基线盘点，再进入G01。对于原候选目录，可一次性读取现存遗留交付并核完整性；缺失或中断文件记未交付，从R01及四项修订要求继续，不轮询等待R02。只有发现具体残留写入、锁冲突或活动任务的证据时，才暂停其影响对象并处理；没有依据的后台可能性不能阻断整个交接。真实VM操作仍须满足后续现场准入。

以下为2026-09-21派发历史，不是当前等待状态：

- RUN：`velo-codex-20260921-pc022-contract`；TASK：`PC022-CONTRACT`；ROUND：2；状态`WAITING_RESULT`。
- 记录：`.tmp/velo-codex-20260921-pc022-contract/coordination.md`。
- 在途task：同目录`PC022-CONTRACT-R02-因果与无损闭合.task.md`。
- 预期主动回传result：同目录`PC022-CONTRACT-R02-因果与无损闭合.result.md`。**此处仅是预期路径，写清单时没有读取或声称它已生成。**
- Sol固定身份：`velo-sol-exec-0919 / w7:pV / 01a0b8bb-cf52-7411-bfc3-f18550674bf2`。
- 当前主控：`w7:p9 / 01a092d1-c83b-7f60-82ae-58b076a9f786`。

正常仍存活的执行器交接，使用其停写及派生任务结束确认；用户明确结束会话时，使用用户确认并对遗留现场做范围明确的接续检查。这两条路径不得混同。主控曾尝试STOP，返回`agent_not_found`，仅证明未投递，不作为本次解除依据；解除依据是随后用户的明确终止确认。禁止为找回旧执行器而查询其他会话或重建Sol。

### 0.4 现场边界

- 唯一允许的测试目标是既有`.232`，先按当前规范核实际主机身份；**.233完全禁止占用**。
- 已确认的最近探针目标为`DESKTOP-3FI41GR / 192.168.204.232 / 00-0C-29-83-B8-65`、DHCP Disabled。不得将这些旧观察直接当后续恢复后的身份。
- 产品入口为正式`28790/mcp`；`28787`仅控制面，不代理正式产品tools/call。固定IP、原自启保持，不改成DHCP。
- 产品代码、验证器和单元/协议/行为测试在指定Windows解释器运行。宿主只做静态阅读、文本/JSON/hash/Git及经授权的外部正式SDK客户端；不能因测试带mock就改在宿主跑产品测试。
- 文件互传已获用户持续授权，不反复索取同一权限；传输要绑定源/目的/hash，避开凭据。控制通道不通时报告阻碍，不用屏幕键入、不改网络/服务或转用.233。
- 当前不能执行真实迁移、恢复/创建/删除快照、部署切换、正式canonical或`.next`写入、批量Flow或kill。完成G01—G12并形成明确现场放行记录后，才能按有效方案执行G13以后；清单不是放行记录。
- Windows临时探针现场仍保留：`C:\Users\xxx\AppData\Local\Temp\velo-pc021-win-durability-r02`，9个小文件18094字节；其记录见WIN-DURABILITY R02。已无owned后台进程。这是诊断证据，不是垃圾，不递归清理或拿来当产品目录。

## 1. 状态表：按行更新，不用轮数或文件数量充当进度

状态只用：未开始／进行中／待验收／完成／阻断。每行完成必须填提交、验收记录及证据，不仅打勾。修复引起失效时标记受影响行重新验收，不覆盖旧记录。

| ID | 工作包 | 前置 | 初始状态 | 完成后填提交/验收记录 |
| --- | --- | --- | --- | --- |
| G00 | 接收唯一写入权与现场基线 | 用户终止确认及本次交接 | 完成 | `.tmp/velo-glm-20260922-082250/G00-baseline.md`；HEAD `fdbc966`（与快照差异=本清单自身提交，有归属）；manifest与三保护文件SHA复算一致；九规范对象全MATCH；R02 result缺失记未交付；Sol进程无匹配，无并发写入证据 |
| G01 | 验收PC022候选及四项修订 | G00 | 完成（候选待G02主方采纳） | `.tmp/velo-glm-20260922-082250/G01-pc022-r02-verdict.md`；R02缺件由GLM接续修订（candidate/impact/validation-r02），21锚点隔离应用+逐hunk删除语义审查通过，四项缺陷闭合 |
| G02 | 作者采纳、规范无损同步与新manifest | G01 | 完成 | `.tmp/velo-glm-20260922-082250/G02-pc022-adoption.md`；用户2026-09-22授权采纳R02候选，采纳记录2026.09.22-01；新组`CURRENT-PC022-R01`（manifest SHA `2ef040a9…f1274`），版本v10/v23/v19/v20/v18，P04仅组指向；自查全过，实施门关闭 |
| G03 | 147项完整规范保真、自评与必要治理 | G02 | 完成 | `PLAN/2026.09.22/2026.09.22-02-PC022主方完整规范自评与限定实现门.md`；80+67逐行复核（脚本验证行数/ID序列/判定），六计数80/80/0/0/0/0、67/67/0/0/0/0；异常ID无；限定G04起实现范围与.232测试环境；现场门关闭 |
| G04 | Windows文件/目录刷新与替换原语 | G03限定实现门 | 完成 | `.tmp/velo-glm-20260922-082250/G04-windows-refresh.md`；`tests/pc022_windows_refresh.py`+writer接入+probe；.232真实验收6成功/6拒绝/3注入/1未运行/0意外；Windows隔离副本9项transition回归OK；FileIdInfo→BY_HANDLE勘误候选与28790服务Stopped事实待后续裁决/核实 |
| G05 | P05真实下载→报告→包成员保全 | G03、G04 | 完成 | `.tmp/velo-glm-20260922-082250/G05-download-package.md`；`tests/p05_pc021_package.py`+probe+G04模块增量（CreateHardLinkW发布）；.232真实验收4成功/10拒绝/1注入/0意外；P04只读核一致；G04 probe与9项回归重跑通过；真实产品端到端留待G13+现场 |
| G06 | activation完整内容图及版本验证 | G03 | 完成 | `.tmp/velo-glm-20260922-082250/G06-activation-graph.md`；三份activation文件升级（receipt v2六事件/5/4键manifest/包内成员不解引用local_path/双policy source分层/download恒等二）；.232隔离副本15项测试全OK（501s）；G07起进入issuer实现 |
| G07 | 真实issuer、收据及一次性能力 | G04—G06 | 完成 | `.tmp/velo-glm-20260922-082250/G07-issuer.md`；五步issuer（preissue谓词复用G06组件/一次取时/六事件边界/PC022刷新）+不可序列化一次性能力（Lock恰一次）；.232隔离副本6项测试OK（120s，含16线程争用恰一/漂移/各I/O失败/不重签） |
| G08 | epoch7迁移与epoch8 writer故障/并发 | G04、G07 | 完成 | `.tmp/velo-glm-20260922-082250/G08-transition-writers.md`；6→7升v2 receipt+三分观察（9项回归OK）；7→8新writer（能力原子SPENT前置/十步顺序/v3 kind不串用/漂移三分/INDETERMINATE保全）；.232隔离副本16项测试OK（360s）；本轮VM再次关机已恢复并记录 |
| G09 | selector/controller/index/runner接入 | G06—G08 | 完成 | `.tmp/velo-glm-20260922-082250/G09-stage-rules.md`；统一stage规则表+index/restore/报告三方绑定验证器；runner常量与数据升级（candidate→189、7/8类kind集、6/7与6/8 canonical）；.232验收22项OK；生产命令清单已列 |
| G10 | package/receiver/selection/aggregate/P07消费 | G05—G09 | 完成 | `.tmp/velo-glm-20260922-082250/G10-p06-consumer.md`；统一消费谓词（G06完整图+v3 COMMITTED 15键四绑定/旧形状不升级/selection唯一success+理由/aggregate 129×5双向差集零/P07单向handoff/包内成员/hash域分离）；.232验收8项OK；188模块链保留历史可读 |
| G11 | Windows集成及受影响回归 | G04—G10 | 完成 | `.tmp/velo-glm-20260922-082250/G11-affected-regression.md`；19模块169项（165 ok+4跳过）零失败；修复G09未同步面（schema enum/P06数据/冻结hash/六处断言）；4跳过与真实服务类未运行已分类记录；产品与README未动 |
| G12 | 锁源、部署差异、保全和现场准入 | G11 | 完成 | `.tmp/velo-glm-20260922-082250/G12-site-admission.md`；实现集合冻结于HEAD d3e86bf；.232身份/canonical前驱/快照树/凭据布尔/资源全核；3产品文件差异保全（旧部署，G13替换）；服务Manual/Stopped异常记录并归G13处置；**现场放行记录已出具，G13+真实现场门开启** |
| G13 | 正式migration→187恢复→部署→initial | G12现场放行 | 阻断 | `.tmp/velo-glm-20260922-082250/G13-blocked-preservation-receiver.md`；真实source全集34件在位/前驱与快照树核实/平台定guest；**保全收件链collector契约预设Windows收件机（powershell.exe）与Linux host(.1)冲突，历史从未真实执行**——待用户裁决勘误方案（推荐C：collector改Linux原生观察） |
| G14 | 189创建、两恢复周期与epoch8激活 | G13 | 未开始 | — |
| G15 | 安全与HTTP/stdio完整资格 | G14 | 未开始 | — |
| G16 | 逐工具资源资格及129＋1真实验收 | G15 | 未开始 | — |
| G17 | 五场连续情景与645覆盖 | G16 | 未开始 | — |
| G18 | P06严格聚合与P07交接 | G17 | 未开始 | — |
| G19 | P07旧代码安全清理与全回归 | G18 | 未开始 | — |
| G20 | P07双边成本与两README终检 | G18、G19 | 未开始 | — |
| G21 | CHK账本、147项实现追溯与最终对象 | G19、G20 | 未开始 | — |
| G22 | 最终独立核验及问题闭合 | G21、独立角色 | 未开始 | — |

本表是可执行顺序，不把它与旧清单行数混成百分比。G13/G14中的安全与业务验收必须在对应Phase当时完成；G15只做最终有效性汇总/缺口补验，不允许把P05当时必需的安全检查推迟到激活之后。

## 2. 规范闭合：G00—G03

### G00 接收与盘点

步骤：以§0.3用户终止确认及主控交接为本次唯一写入权依据，不再等待Sol；记新执行者身份/起始时间；核Git分支/HEAD/dirty；复算九规范和保护文件；建立本次独立运行目录，例如`.tmp/velo-glm-<实际日期时间>/`，保留输入清单与本清单SHA。读取生效AGENTS，采用新增的“每个已验收功能/修复单独本地提交”规则。

验收：原Sol协作已按用户终止确认结束；所有差异有归属；没有覆盖未验收代码；没有提前接触VM。基线盘点完成即标本项完成。若出现具体并发写入证据，只阻断相应对象，不要求已关闭会话补回传。

### G01 验收PC022候选

读取05号作者方向、PC022-CONTRACT的R01主方verdict，按§0.3一次性盘点R02遗留候选及结果。存在的文件须独立核完整性，不能按文件名视为完成；缺失或不合格则从R01及以下四项要求在GLM新目录继续修订，保留原件，不等待或重建Sol，不重新做全部Windows研究。

必须逐项验证四个已发现问题：

1. PC22-001：完整preissue验证→event1→实际UTC取时一次并同时用于event2和root issued_at→exclusive root写入→文件刷新/关闭→目录刷新→root读回→receipt→能力；不得漏取时、预填或用mtime。
2. PC22-002：主句柄与前/后路径比对句柄有有限生命周期；每个有效handle都恰一次关闭尝试；CloseHandle失败保留错误，不能递归重开到无限循环。
3. PC22-003：本调用尚未尝试replace的失败，与外部canonical漂移分开记录；不能要求已被别人改变的字节还等于前驱，也不能回滚别人字节。已尝试replace或结果未知按不确定状态停止。
4. PC22-004：精确应用候选时，P07包内下载路径/禁旧local_path/Triage不下载及两fixture边界、P05 exact嵌套/Ref/并发loser/原件图均保留；source_inputs是规范决定，implementation_sources是代码/helper/tests。

验收方法：在隔离副本应用候选，对需求/总纲/P05/P06/P07逐hunk核每条删除语义；列正例、反例和未支持关系。通过条件：四项无未闭合项，候选算法无循环/额外fallback/来源混装；API成功仍不冒称硬件掉电保证。正式采纳前仍为候选。

### G02 正式回流及冻结

先提交候选审定包给具有作者采纳权的主控；不得把GLM自报COMPLETED当作者决定。获明确采纳后，按批准边界同步需求、总纲及负责Pxx，不全局替换历史身份，不删混合段中的有效条款。重新分配版本和current manifest，记录实际size/SHA/blob、前态和稳定PLAN-CHANGE-022来源；P04产品下载不因P05包复制要求而增加无依据义务。

验收：当前组唯一；新旧receipt版本/kind、精确键和事件语义一致；当前与历史来源可区分；所有Ref可定位；不存在未来hash或自身hash循环；旧记录不改。新manifest只冻结字节，不自行宣布自评/实施通过。正式文档修改验收后单独提交，不push。

### G03 完整保真和实施门

读取当前正式需求全部80项和累计67条的理由、取舍、前提、失效与重裁条件。以全部Pxx并集逐条恢复：参与者、输入输出、触发/顺序、状态/生命周期、不变量、异常/边界/结束、适用/重裁与可观察验收。

旧PC020主方完整自评可作未变契约导航：`PLAN/2026.09.20/2026.09.20-20-PC020主方完整规范自评与限定实现门.md`。PC021辅助定位文件只作索引，不能照搬其396/767/13或旧147/147结论。必要治理按当前有效作者记录执行；不重置旧审核额度，也不把作者自评当独立核验。

通过条件：两组六计数分别为80/80/0/0/0/0、67/67/0/0/0/0，且逐条证据确实支持计数；异常ID和裁决清晰；主控明确限定实现范围与Windows测试环境。未验证的生产可实现性须列出，不能用“资料数量正确”放行。现场门保持关闭。

## 3. 实现与隔离测试：G04—G11

以下每包都先完整读所改文件，做最小修复；使用已生效规范，而非本清单预先决定尚未采纳的schema。

### G04 Windows刷新原语

入口：`tests/p05_pc020_transition.py`及最终PC022定义。旧`os.open(directory,O_RDONLY)`在目标Windows实测errno13，不可保留为成功生产路径；旧目录同步mock仅证明状态机。

步骤：实现被批准的文件刷新、目录handle验证/刷新/关闭、有限路径身份比对、新建层级同步和同卷replace；绑定实际运行身份与支持的文件系统。保持无提权、无卷级刷新、无静默fallback。不要为支持未知平台引入大框架。

验收：在.232唯一owned隔离目录运行真实API正例，并分别覆盖权限/类型/reparse/身份漂移、open/info/flush/close失败、目录层级漏同步和跨卷拒绝。真实成功和故障注入分别计数，不能将mock成功称生产I/O通过；确认本包失败不会修改外部哨兵或真实canonical。

### G05 下载原件及Phase封包

入口：P05当前PC021 package段、`tests/p05_sdk_capture.py`、`tests/p05_http_evidence.py`及报告producer；P04产品`velociraptor_fixed_tools.py`只读核字段，发现真实产品错误才另开修复。

步骤：产品list/download完成→冻结最终SDK/report原字节→从同实例可信配置的下载根核源→独占复制到Phase中确定性`downloads/<sha256(flow_id UTF-8)>/<file_id>/content.bin`→完整hash/size及原生刷新→生成manifest。list真实数据字段为`data`，不能编造`files`或列表中的源hash。后续只解包内成员，不能搬运后改写`local_path`或SDK报告。

通过条件：普通、空文件及稀疏逻辑文件按各自契约验证；报告前后hash相同；两个fixture下载独立绑定；目标碰撞、源漂移、链接/reparse、越界/别名、不可信路径、复制/发布/刷新失败均拒绝并保全规定现场。exact成员集合按当前坐标系重算，自引用排除只限明确对象，不能排除整个嵌套目录。

### G06 activation完整图

入口：三份保护的未提交activation文件，以及已提交`tests/p05_pc020_evidence.py`、`tests/p05_pc020_creation.py`、`tests/p05_pc020_restore.py`。

步骤：先对现有实现逐功能比对当前规范，保留已正确部分；将root、issuance receipt、三Phase、preparation/migration/creation及source绑定的只读验证补齐。旧PC020 validator/test成功不自动满足PC021/022新版本。

通过条件：exact keys/类型/Ref/成员集和因果身份全部验证；三Phase各真实Triage FINISHED/results/list及两个fixture下载链，按规范九个独立Flow；不批量下载Triage上传。篡改、跨attempt、重复/缺件、错误kind及旧source稳定拒绝；public verifier只报告内容图有效，不返回可恢复的操作许可。验收前不把三文件整包提交。

### G07 issuer及一次性能力

步骤：实现完整preissue→实际签发→root/receipt写入刷新读回→complete verifier→同controller私有一次性能力。能力绑定canonical/root/receipt/source等当前规范字段，不能由bool、环境变量、磁盘token或可解析receipt重建。

通过条件：输入漂移、已有root/receipt、root各I/O失败、receipt可解析但刷新失败、签发后崩溃/能力丢失、二次使用、两线程争用都可观察；只有一个writer可消费，消费在任何writer副作用之前；失败不重签、复用、覆盖或fallback。完整receipt不证明过去操作成功。

### G08 两阶段transition writer

入口：`tests/p05_pc020_transition.py`及对应测试。旧epoch6→7提交`f4792fb`可复用状态机思路，但其目录同步未完成真实Windows证明。

步骤：按最终版本分别实现6→7与7→8；保留`.next`独占及实际持久化/读回→intent→canonical临近再比较→replace→目录刷新/读回→receipt。7→8须消费G07能力；两种receipt shape/kind不得串用。

验收：成功、非法前驱、未知.next/碰撞、外部canonical漂移、并发、所有文件/目录/关闭/replace故障。replace前证明本调用未替换；外部漂移另存实际字节/hash；replace后结果不确定保全且停止。不得自动隔离、删除、回滚或恢复旧188。单独核“receipt已完整但其最终刷新失败”仍不报成功。

### G09 恢复与runner接入

入口：`tests/p05_pc020_restore.py`、`tests/p05_ready_evidence.py`、`tests/p05_real_acceptance.py`、`tests/p05_external_acceptance.py`、`tests/data/p05_scenario_index.json`及按实际调用链定位的selector/controller。

步骤：消费当前source集合、状态/schema/epoch/stage、真实snapshot与canonical分字段语义；将index、restore、controller、runner、报告验证串成同一规则，不新增任意根/快照/跳过门的CLI。列出实际生产命令，不能只给测试API。

通过条件：initial用187、candidate恢复189但仍是epoch7准备状态、P06_ACTIVE用已激活epoch8/189；具体七/八类record按当前契约exact匹配。阶段串用、manifest错代、路径逃逸、缺原件、摘要替代真实控制原件全部拒绝；失败不自动操作VM。先在隔离fixture验证，不触真实恢复。

### G10 下游consumer集成

入口：`tests/p06_evidence.py`、`tests/p06_package.py`、`tests/p06_receive.py`、`tests/p06_aggregate_reports.py`、`tests/p07_cost_measurement.py`、相关contract/index/测试。

步骤：同步当前issuance/transition版本、canonical及完整递归图；封包枚举、接收登记、显式selection、严格聚合、P07 handoff统一谓词。固定P06根仍为`Logs/P06/wf-01a05d1d-p06-r3/`，不增加任意证据根入口。

通过条件：旧合法包仅历史可读，不能凭hash正确升级；未来时间/跨attempt/缺嵌套原件/extra/reparse/非COMMITTED拒绝；transport ZIP hash与manifest hash不混用；失败attempt不进入成功覆盖；没有P07反向成为P05前提；不会读取包外旧local_path。

### G11 受影响Windows回归

先静态盘点`tests/test_*.py`及相关runner入口，确认哪些是无现场单元/fixture测试，哪些需要真实配置。host不通过import、compile或unittest来运行产品代码；Windows限定包可用实际解释器执行，例如：

```text
C:\mcp-velociraptor\.venv\Scripts\python.exe -m unittest tests.test_p05_pc020_evidence tests.test_p05_pc020_transition tests.test_p05_pc020_creation tests.test_p05_pc020_restore tests.test_p05_pc020_activation
```

该命令仅为现存模块组合示例，先读测试的环境需求；新模块和受影响P06/P07、文件、状态分页、预算测试必须补入，不以示例为全集。迁移到隔离源码目录时使用经核定的实际路径，不覆盖已部署工作树。

通过条件：逐模块运行/失败/跳过/未运行分类完整；所有本包必需项通过，skip不能算通过；真实API与mock各自边界清楚；stdio/HTTP工具schema按130全集比对；受影响行为两README同迭代更新。每个已验收功能/修复分别提交代码、测试和文档，保留未验收差异，不push。

## 4. 实际设施与正式业务：G12—G18

### G12 锁源与现场准入

读取当前P05正式准入与PC020/021/022生效条款；参考历史`.tmp/velo-codex-20260920-deploy-preflight/`、`.tmp/velo-codex-20260920-pc020-evidence-read/`的结果/主方verdict，仅复用仍有效信息。

步骤：比较宿主待部署版本与guest实际文件/依赖/service配置；识别guest脏文件并保全；冻结已验收实现集合及source输入，避免输出/未来hash入source。只读取凭据是否配置和安全属性，不复制凭据原文进入证据。核.232身份、固定IP、自启、快照树、当前canonical原字节、可用空间/内存及存储拓扑。

通过条件：已有代码与现网差异全部可解释；保全/恢复路径真实可用；尚未执行真实migration；按当前规范出具准入结果和主控现场放行记录。旧部署HEAD/旧快照数量/旧容量读数不能直接复用。用户暂停追查历史创建证据的决定保留，不借准入恢复无限历史追证。

### G13 迁移、187恢复、部署与initial

只有现场门明确开启后执行。先按规范核当前前驱；不把历史schema5/epoch6/188硬写成现状。按准入/保全→migration到schema6/epoch7 PREPARATION_BASELINE→187恢复→部署已冻结版本/锁定依赖→initial执行。187完整名称按P05和实测树定位，禁止选DHCP快照。

通过条件：每个动作有真实控制原件、来源与目标身份；canonical/receipt及原始返回符合新版本；initial的服务/网络安全/资源门和Triage＋两个fixture链完整，前驱/后继状态一致。失败按责任层保全停止，不继续创建189。

### G14 189创建、两周期与activation

步骤：initial通过后才零到一创建189候选；保留规范要求的187/188，真实树集合不符就停。按当前P05完成两次独立189恢复周期，各自记录restore/run/会话/服务/业务、安全、资源及原件；全部通过后G07真实签发并由G08原子激活epoch8 NETWORK_ACTIVE/189。

通过条件：创建父边/恢复起点/marker/服务身份完整；允许共享的冻结来源可共享，运行事实不可跨周期复用；三个Phase内容图及九Flow业务对应无歧义；已提交canonical H8、issuance与transition全部有效。没有“先激活才能做candidate验证”的循环；未提交/不确定禁止P06。

### G15 安全与双传输资格

入口：P02/P05/P06的ACC-026；`tests/p05_network_collect.py`、`tests/p05_service_observation.py`及相关安全测试。

验收：实际外部官方SDK直连28790；正确/错误/缺Bearer、Host/Origin、允许/非允许来源、管理面权限/配置ACL/日志去敏逐项验证；固定服务实例及HTTP/stdio完整tools schema同一。不能用已成功的一次401代替全集，不能用28787控制面代理产品调用。存在权限/网络未知时只阻断受影响项，不为测试关闭认证或扩大监听范围。

### G16 资源资格、逐工具与剩余可用性

入口：`tests/p06_individual_acceptance.py`、`tests/p06_resource_qualification.py`、`tests/p06_resource_policy.py`、`tests/data/p06_coverage_manifest.json`、当前allowlist及P06正文。

步骤：冻结130集合及PacketCapture有效排除；按工具定义冻结输入/预期/允许空语义/资源和专用fixture；完成正式资源资格、预算与deadline后，分批真实执行全部129。Triage预算4GiB/2400秒、内存等特殊资源参数只按各自有效契约，不套用全局默认或未经批准提限。kill只用本轮已确认身份的专用测试进程；Hunt/cancel只操作本轮专属对象。

通过条件：逐项有参数→真实ID/初态→终态→结果/效果→必要文件链；错误、缺依赖、无意义空行不算成功。所需工具覆盖双向差集零；1项排除有当前依据。原A05—A07冒烟可导航，但不能替代新部署受影响工具复验。失败修复后完整重跑受影响项，旧失败保留。

### G17 五场连续情景

入口：当前P06五目的、`tests/data/p06_scenario_index.json`及正式runner。每场独立189恢复，真实官方SDK、同一HTTP会话及服务进程；按规范引用/等待/断言，不随机拼工具。

通过条件：129个有效工具各覆盖五个不同情景，645有效关系；每场至少10次真实tools/call，sleep不计，同场重复不增加关系；无未预期错误，空结果按冻结语义判断；会话/PID/来源/fixture/资源/原件一致。任一失败整场零覆盖，不能拼不同失败attempt组成成功。

### G18 接收、选择、聚合及handoff

步骤：host逐次接收原件并核hash，追加登记全部成功/失败；final-selection显式选择每场唯一有效run，写清其他attempt拒绝理由；严格只读重算aggregate；生成P07 handoff，绑定当前九对象、实现提交、源码、原始包及必要既有阶段核验。

通过条件：无凭时间“挑最新”、无挑最好指标、无旧报告重标、无自引用handoff；覆盖与来源全部可复算；P07入口仅消费当前epoch8/189且完整递归图成立。P07之后的最终独立核验不是本项前提，不能制造循环。

## 5. 收尾与最终验收：G19—G22

### G19 安全清理

逐项盘点旧wrapper/helper/注册/危险开关/profile与兼容路径。先证明等价替代已通过、无独立能力、生产/测试/文档无引用，再按P07授权删除；保留上游平台源码和明确需要复用的底座。不要把历史文档术语误当运行兼容层。

通过条件：全部删除有归属/理由/替代证据；发现独有能力只暂停该项回流；清理后Windows全回归、130注册和schema无意外改变；原失败及下载完成文件/赢家/用户对象不被清理。单独提交。

### G20 成本和文档

按P07当前§4双边实测schema、wire调用/重试、内部status重试、完整CallToolResult规范化字节和elapsed_ms。current侧只能取P06唯一selection中的对应子链；上游原样`9dc8054`按既定适配方法，五pair分别独立189恢复，保持相同fixture/参数/调查终点。原上游开关仅隔离子进程，不能写入当前产品配置。

通过条件：五pair、双方原始tools/list/SDK环境/输出和内部重试观测完整；不能用五场总量对一次上游，也不按耗时猜重试；不预设优化后更小。无法建立可比性则ACC-024阻断并回流，不用单边披露替代。两README、示例与实际部署一致，解释状态/分页/逻辑文件/稀疏成本/发布后残留、agent不支持及安全边界。

### G21 CHK账本与最终追溯

从当前需求/总纲规范性引用定位全部核查和PLAN-CHANGE记录，不只搜固定CHK范围。按P07追加事件账本，稳定键为来源记录OID＋CHK ID；作者修复不自行追加closed，开放/阻断原样承接。

构建双向链：需求条目→OUT/ACC→负责Pxx→当前代码/测试/记录实际落点→可解析提交→内容寻址的真实验收原件，并反向查无归属落点。正式80与累计67各行含源原文定位/hash、采纳/取舍/重裁、全部相关异常；14项P07直接责任单独展示，但不新增成14条需求。

通过条件：按P07`final-traceability.json`契约校验ID精确集合、状态、Ref/hash、commits、实现HEAD及六计数；未落点/弱化/不可验收/复核条件缺失任一非零不得过。并行写入全部停止，冻结最终HEAD、guest实际部署、依赖、schema、报告和排除范围；后续代码改变按影响重新回归。

### G22 独立核验与闭合

GLM准备独立核验输入包后停止写入，交给未参与本轮作者/实现/修复的合格核验方。原任务约定独立核验在Codex执行；GLM不能自签该角色。由主控安排，不由GLM伪造一份自测“核查通过”。

独立核验应固定当前需求/总纲路径、版本/blob、HEAD、Pxx边界和保护现场，亲自逐项追溯并重跑关键/跨Pxx/受影响回归。失败先对照实施前与diff归因；未恶化的范围外问题仅环境观察，无法归因只阻断受影响项。需要改规范只形成PLAN-CHANGE候选回作者，不由核验者改规范。

结论仅核查通过／核查不通过／核查被阻断。在总体验收记录同目录，按实际创建日扫描全部同日前缀Markdown取最大NN＋1、写前复查，创建不可变独立核查记录。每CHK有需求/总纲/Pxx、证据、反例、影响和最小闭合条件；不改旧记录、不产生/关闭AUD或重置审核额度。

最终完成条件：合格独立核验对最终对象给出通过，开放失败按规范闭合，必要修复均已提交且部署/证据匹配。若失败则作者修复/回流→受影响回归→新HEAD→新独立核验，不能改旧结论为绿。独立角色未到位时交付状态是“待独立核验”，不是总纲通过。

## 6. 每包统一记录、提交和阻碍处理

每个工作包记录：任务ID、角色、开始/结束实际时间、开始/结束HEAD、完整输入/输出身份、修改范围、实际命令/环境、正例/反例、通过/失败/skip/未运行、原件路径/hash、剩余问题、是否停写、提交ID及下一步。缺时间写未知，不估计填值。

每个功能或修复满足自己的验收后：先确认其他执行者停写→`git status --short`和完整diff→只stage本包明确路径→检查暂存范围/空白/敏感信息→单独本地commit→复核tree与测试身份。不要使用`git add -A`夹带未验收activation或作者草案，不push。没有变化不做空提交。

失败闭环：保存原始失败→确认是否环境/实现/契约→最小修复→重跑受影响集合。相同前提失败先查共同原因；不要扩大到全矩阵制造无意义失败。ROUND只追溯，不代表失败次数；不同卡点分别记录，主控缺前提不归咎执行能力。任何证据改变验收边界都必须回流，不用增加fallback求绿。

完成一项就在本清单状态表填记录链接和提交；一包未完可保留有效子集，但状态仍未完成。GLM独立执行不要求每小步回报，遇到必要决策、缺权限、对象不匹配或真实环境阻碍时输出具体事实、影响范围、已完成内容和最小解除条件。

## 7. 按任务读取的来源索引

- 规范入口：本目录`current-normative-inputs-pc021-r04.json`列出全部九对象路径和身份；正式新组采纳后以新manifest替代，不凭文件名猜最新。
- 当前需求：`PLAN/2026.09.02/2026.09.02-01-需求提炼-mcp-velociraptor全阶段设计.md`。
- 当前总纲：`PLAN/2026.09.02/2026.09.02-02-总纲-mcp-velociraptor-Windows-DFIR二次开发.md`。
- P05/P06/P07路径直接取manifest；04号记录说明P07 r3根、八类原件及入口前提勘误。
- PC021完整契约来源：本目录`PC021来源/p05-pc021-candidate.md`仅过程原件，正式P05正文优先；01—04号作者记录提供理由/边界。
- Windows研究及真实probe：`.tmp/velo-codex-20260921-pc021-win-durability/`的findings、R02结果和codex-R02-verdict；限当前环境可调用性。
- PC022遗留候选交接：`.tmp/velo-codex-20260921-pc022-contract/`，按§0.3一次性盘点现存交付，缺件由GLM接续，不等待旧会话。
- 未验收activation起点：`.tmp/velo-codex-20260921-pc020-activation/`历轮result/verdict，仅复用已限定接受部分。
- 已接受底座：PC020 evidence提交`5610b5b`、transition`f4792fb`、creation`5d0948a`、restore`51e9fcf`；各自Windows隔离证据不是未来现场通过。
- 已实现产品变更：逻辑文件/安全发布`e759d24`、triage参数/预算`1508f1b`、真实初态/终页`65c81ef`。按受影响身份复用，最终部署仍须真实验证。
- 原A/B清单：`PLAN/2026.09.19/2026.09.19-01-全部工具可用与总纲验收剩余任务清单.md`。其状态段停留在较早PC020阶段，只作历史和任务全集来源，不能覆盖本交接。

原清单覆盖对照：A01—A04→G12/G16；A05—A07旧冒烟→G16受影响复验；A08—A11→G04—G17；A12→G16—G18/G21。B01/B03/B04→G01—G03；B02→G21；B05→G12—G14；B06→各Phase安全及G15；B07→G09—G11；B08/B09/B10→G16—G18；B11/B12→G19/G20；B13/B14→G21；B15/B16→G22。没有因新清单省略旧未完成工作。

## 8. 交接完成时要给用户什么

简短总结：完成哪些G编号、哪些仍阻断、每项提交与实际验证范围、部署身份、关键验收记录、仍保留的临时现场、下一个必要动作。清楚区分“代码已提交”“隔离测试通过”“真实工具通过”“正式情景通过”“独立核查通过”。只有最后一项及所有前提成立，才称总纲验收通过。

2026-09-22交接修正：用户已结束原Sol会话，主控结束原业务派发并移交GLM；本清单经文本验收独立提交，05号作者方向及三份activation文件不夹带。本次仅修复交接条件，不表示G01或后续业务验收通过。GLM读取修订版完成G00盘点后继续执行。
