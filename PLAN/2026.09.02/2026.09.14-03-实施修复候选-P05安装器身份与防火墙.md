# P05 安装器修复候选：身份、防火墙和回滚

创建日2026-09-14，主会话修复方。HEAD ac1c58a9439aaa303d071a8a040e0f26db85bbdb。不可变实施记录，不是新恢复契约自评、部署授权、CHK/AUD关闭或验收通过。

## 改动范围

完整读取安装脚本、服务宿主、现有源码测试和两README后，修改 `tests/p05_service_install.ps1`、`tests/test_p05_service_script.py` 并同步两README：

- 服务命令指向现有SCM宿主，不再直接注册普通bridge入口；通过Win32_Service的StartName、精确PathName和StartMode及环境引用核对既有身份。
- 操作者显式提供实测HostAddress和非空AttemptId UUID，禁止把宿主来源硬编码成整个子网。绑定地址必须是本VM唯一Preferred/Manual IPv4，规则固定TCP28790、本guest地址和单一宿主地址。既有规则的方向、动作、启用状态、端口和地址均复核，不以名称存在当幂等成功。
- 服务环境引用用单个REG_SZ，避免将路径写成多字符串后由宿主错误解析。受保护配置不输出正文，拒绝重复项、host不匹配、空或过短token。
- install先核对所有既有对象再写；仅新建对象登记本attempt标记。rollback在首个删除前核对整个目标集合、标记及身份；服务必须已经Stopped，不强制停止进程，不删除不属于该attempt的对象。中途失败保留现场，不把未知部分状态自动清理。
- 固定命名mutex串行化安装器；不能获取或前一进程异常终止时拒绝继续。该互斥不是对其他管理员修改的事务保证，也不替代恢复原件。

范围为现有IMP-P05-009与ACC-P05-007的纯实现保持修复，不改变产品工具/权限/网络契约。HostAddress来自部署者实测，不产生新网络控制工具；固定IP和WindowsMCP自启不由此脚本改变。

## 检查与未闭合

脚本blob：c22ce62acdf94ced6bd71cd66d36e60c64d8d64c；测试blob：ab6413703f2903cc2a156ba5d330a514a88f4884。宿主仅Python语法检查和diff检查通过；没有PowerShell运行时，未在宿主执行行为测试。源码断言仅防止已知回归，不能证明实际幂等、安全回滚、ACL或服务生命周期。

已派无上下文gpt-5.6-terra/xhigh实施验证子任务，在目标Windows上仅对固定源码做内存ParseInput及纯源码检查；禁止执行install/verify/rollback主体、修改guest磁盘/服务/规则/网络、启动后端或操作快照。其结果另存，尚未据此宣称通过。

后续仍须：真实Windows解析及隔离行为正反例；保护路径/ACL完整检查；服务SCM回调及停止生命周期复核；失败阶段归属与回滚重跑；新187恢复契约完整自评。缺一项不得安装或创建候选。旧02号禁止旧脚本部署的结论不因本候选自动解除。

两份README明确候选未验证、不得部署，不把新的参数示例误写为当前产品可用。所有历史记录保留，整体目标未完成。
