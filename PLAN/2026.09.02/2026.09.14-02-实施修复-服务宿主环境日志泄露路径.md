# 服务宿主环境诊断修复（未部署、待Windows验证）

创建日2026-09-14；主会话修复方；HEAD仍为ac1c58a9439aaa303d071a8a040e0f26db85bbdb。此记录不可变，不是CHK/AUD，不关闭历史核验项。

## 源码事实及修复

完整读取 `tests/p05_service_host.py` 后发现 `_service_main` 枚举所有VELOCIRAPTOR环境变量，将TOKEN值前四字符与省略号写到 `Logs/service-host-env.log`。该逻辑违反现有P05 §0.3/0.4/0.7“不记录token正文或指纹”边界；不需要扩展产品需求或放宽验收来修复。

本次只移除整个环境字典及其落盘块，保持共享bridge入口调用不变；在 `tests/test_p05_service_script.py` 增加源级反例检查，禁止该环境日志路径、枚举环境值和旧token截断表达式再次出现。没有读取实际token、日志正文或改变凭据，没有清理用户证据，也没有同步/启动guest服务。

宿主仅执行两个文件py_compile及git diff --check，退出0；没有执行Windows行为测试。新增源级测试只是防止旧泄露代码回归，不证明其他异常日志绝对无秘密，不替代真实服务/日志安全验收。Windows实施方须在冻结同步后执行该检查及相关服务回归。

## 同时发现、尚未修复的部署设施缺口

`tests/p05_service_install.ps1` 当前仍直接注册python+bridge（没有使用现有SCM service host），防火墙RemoteAddress为整个192.168.204.0/24，使用Get-Service.UserName判断账户，且rollback只凭名称删除而无本attempt创建所有权。现有测试大量检查字符串存在，不能证明幂等和安全回滚。

这些是现行P05专用账户、准确宿主来源、真实服务、范围受限回滚契约的实现缺口。禁止运行该旧安装脚本；主会话须修复并由实施方在Windows验证后才能部署。不能把187缺少服务简单通过执行旧脚本“补齐”。

## 当前现场与后续

实施库存子智能体报告：目标MAC与固定.232匹配、DHCP禁用；16:07:38Z只读短JSON中mcp-velociraptor服务不存在、28790无监听、Velociraptor后端进程为空，fixture-instance存在。完整原件仍由该任务归档；本段仅为待核对的子任务报告，不冒充主会话完整验收。

PLAN-CHANGE-012和总纲v12/P05v8的恢复门保持关闭；源修复不授权恢复、安装、启动业务、创建候选或激活。下一步是接收原件、补齐部署前提和新恢复契约，再按主会话规划/修复、子智能体实施/独立核验的职责推进。
