# P06 逐工具与连续情景全量执行首次审核处理

> 会话角色：作者/执行方
>
> 审核性质：首次全面审核结果引入与作者自评闭环
>
> 审核批次：`P06-BATCH-df4e0372-de5603cc-43784f06-d142760e`
>
> 已用额度：全面审核1/1、定向复审0/1
>
> 原审计：`2026.09.05-31-审核-P06逐工具与连续情景全量执行.md`，Git blob `460fe03fbbe41ee9f4ef135ff2e73b808a848595`，SHA-256 `f5b9355f3047c46ad9a383c9cdcfc28e20685fba18464ad5a74f56382f34445b`
>
> 处理前方案：`2026.09.05-29-实施子方案-P06逐工具与连续情景全量执行.md` v0，Git blob `df4e037293e1296d40a5e036a24e3c9445d2bc1e`，SHA-256 `f019be45bd8f2c4aa1215291231741a7da613471e1ae711a9624727bbcb3c76d`
>
> 处理后方案：同路径 v1，Git blob `80847d148080263280562a611a03cc304941c84f`，SHA-256 `eaaf94da880e0e8bdf5dcf9f26c1e55887a3562f5ffc64ad6547197ec30ff4ae`
>
> 本地审核与处理基线：`d142760e29b350b0706869296444c317dfbba936`
>
> 处理结论：AUD-001～006全部同意并闭合；首次审核已引入，修改后由作者完整复评，无需再次第三方审核

## 1. 身份、漂移与处理边界

原审计绑定的P06 v0、需求v0、总纲v4、审核批次、HEAD和P01～P05提交身份均与派遣记录一致。审计写入后，决策关键路径没有来自第三方的漂移；作者只修改被审P06方案，并新增本处理记录。原审计保持不可变。

本轮未修改需求、总纲、业务代码、测试、VM、快照、Flow、Hunt、API配置或README。审核前只在Snapshot185当前运行态做了一次安全只读资源观察，证据为`Logs/P06/wf-01a05d1d-p06/resource-readonly-spike.json`；它只证明容量方向，不计资源资格、不计工具覆盖，也没有触发快照恢复。

受影响范围为P06方案第0～11章、IMP-P06-001～007、ACC-P06-001～005和FB-P06-001～007。继承且未改变的全局不变量是：最终工具面118动态+12固定=130；五个不同调查情景且每工具至少进入五个情景；每个正式情景独立恢复Snapshot185；失败不计覆盖；所有测试由作者在Win10VM执行；Snapshot183/184未经新授权禁止恢复；禁止冷启动、临时联网安装、凭据/二进制/运行证据入Git及提前执行P07。

## 2. 逐项处理

### AUD-001｜同意，已闭合

- 原因：v0只能证明五个标题和650个文字条目，不能证明执行层中的调查关系不同。
- 修改：第2章把每条关系冻结为`scenario_id/tool/step_ids/parameters_sha256/fixture_preconditions/assertion_ids/investigative_question/expected_evidence/allowed_empty_condition`，并要求manifest、scenario step、report call/assertion三向映射。复制调查理由、参数/断言漂移、孤立关系或仅改标题全部由golden反例拒绝；零参数工具也必须通过场景特定证据角色与断言体现差异。
- 最小闭合：最终650条三向差集为0，且逐关系可人工复核；已写入IMP-P06-001/003/004/006与ACC-P06-002。

### AUD-002｜同意，已闭合

- 原因：P05断言只核对当前调用，不能证明跨步骤Flow/Hunt/file/PID身份连续。
- 修改：第4章采用审核给出的第二条唯一方向：不扩张P05 schema，而在runner中增加固定、不可由CLI配置的跨步骤不变量验证层。覆盖动态start→status→results/files、collect→list→download、hunt start→status→stop→cancel、PacketCapture双Flow/trace路径和kill输入PID→结果PID；任何错配令整场failed且coverage为空，并冻结F1→F2、错hunt/file/PID反例。
- 最小闭合：由源码、反例测试和五报告不变量差集0机械证明；已写入IMP-P06-002～004与ACC-P06-001/003。

### AUD-003｜同意，已闭合

- 原因：随机UUID和可写报告目录不足以证明单一ClientSession，也不能防止改时间、复制或遗漏失败历史。
- 修改：第4、5章冻结runner/bridge进程身份、实际创建时间、单一ClientSession open/close和连续call sequence；每次回传由host向固定JSONL接收账本分配单调attempt并记录report/package及全部输入SHA。不可变`final-selection.json`显式列出五个选中报告和所有早期attempt/拒绝原因；聚合器重算哈希并拒绝缺历史、重复、时钟排序、路径重解析和跨session拼接。
- 最小闭合：最终选择、账本、报告和aggregate形成可复算SHA链；已写入IMP-P06-002/005/006与ACC-P06-002/003/004。

### AUD-004｜同意，已闭合

- 原因：P04已证实取消后端Flow终态是`ERROR`，v0失败总则与正确取消相冲突。
- 修改：第3.2、4、5章冻结三类状态账：`finished`、`expected_cancel_terminal`、`unexpected_error`。只有本scenario创建并登记的owned cancel-target、成功匹配的`cancel_flow`及前后ID/状态一致时，后端ERROR才属于expected cancel；它只计`cancel_flow`工具成功，不计PowerShell动态成功。其他任何ERROR使整场失败。
- 最小闭合：预期取消通过、未登记ERROR、错ID取消及普通动态ERROR四类正反例齐全；已写入IMP-P06-004～006与ACC-P06-001/002。

### AUD-005｜同意，已闭合

- 原因：P03没有证明五次全量完成的容量；v0没有峰值、时限、余量和确定性不足退出路径。
- 修改：第3.3章新增Snapshot185独立`resource-qualification` attempt。先重测RAM、C卷、datastore、下载根和报告目录，再按正式顺序完成9个resource-sensitive工具、Triage、PacketCapture和下载链，冻结逐步增量、峰值、证据reserve和时长；静态门为`2 × (physical RAM + 64 MiB) + 4 GiB`，正式门使用实测峰值加安全reserve，deadline使用实测2倍且不低于P05上限。每个大调用前重算余量；确定性不足禁止盲目重跑或降低标准，登记PLAN-CHANGE并按Q-065～066回流。
- 证据边界：只读观察值为物理RAM 4,293,971,968字节、C卷可用18,004,791,296字节、E卷可用76,995,567,616字节；这不是资格通过结论。正式资格仍须从Snapshot185独立执行。
- 最小闭合：资格报告、预算/deadline、低空间拒绝和回流分支齐全；已写入IMP-P06-003/005、ACC-P06-001/002/004和FB-P06-003。

### AUD-006｜同意，已闭合

- 原因：v0引用不存在的`velociraptor_core.py`和`velociraptor_dynamic_tools.py`，会使语法门和最小修复白名单失真。
- 修改：第0、8章及实际命令统一使用HEAD存在的`velociraptor_mcp_core.py`、`velociraptor_dynamic_artifacts.py`和`velociraptor_env.py`；旧名只保留为必须被机械拒绝的不存在路径。产品hunk仍仅在真实失败证明冻结契约被违反时允许。
- 最小闭合：路径与HEAD tree一致；实施时须在Win10VM运行准确`py_compile`并验证旧名拒绝。

## 3. 实际diff、影响传播与继承

处理产生六组契约diff：650关系三向绑定；固定跨步骤不变量验证；host单调接收账本与显式final selection；取消ERROR精确分类；资源资格/预算/deadline/确定性回流；真实模块路径。方案版本由v0升至v1，既有REQ/OUT/IMP/ACC/Pxx ID均未重编号、删除或复用。

受影响的既有通过主张是v0对REQ-021/022/023、CON-002/008和RACC-021/022/023“可完整验收”的判断，现均按v1从头复评。继承项是审计已核实的130工具面、P03安全invocation、P04固定schema与取消语义、P05 runner/fixture/Snapshot185基础设施、需求与总纲身份、Pxx顺序和P07排除边界。方案修改没有改变产品接口、权限、快照切换机制、核心数据流或跨项目契约。

## 4. 修改后完整复评与需求保真

- 身份与ID：方案v1、需求v0、总纲v4、P01～P05提交及Snapshot185身份唯一；IMP-P06-001～007、ACC-P06-001～005和FB-P06-001～007连续且双向可追溯。
- 可交接性：五情景、650关系、资源资格、动态/固定链、同会话证据、失败分账、整场重跑、聚合和Git边界均有唯一动作与退出条件，不依赖制定对话。
- 验收：每项ACC均具有前置、动作、精确预期、证据及通过/失败/阻断边界；尚未实施的命令未被虚构。
- 范围：P06仍只扩展测试设施并执行全量验收；产品修复受真实契约失败门约束，P07清理/成本没有前移。
- 需求保真：应承接10、完整10、未落点0、语义弱化0、不可验收0、失效或复核条件缺失0；异常ID无。

## 5. 再次审核逐项判定

1. 跨两个以上项目或多业务共享契约：未命中。修改仅限本仓库P06测试/证据契约。
2. 不可逆或高风险迁移、权限、安全、隐私、外部关键契约：未命中。没有产品权限或外部接口变化；运行态可由已授权Snapshot185恢复。
3. 总体架构、核心数据流、并发一致性、恢复或切换机制变化：未命中。新增host证据账本和资源资格属于P06验收闭合，不改变产品数据流或用户冻结的快照恢复方向。
4. 核心路径难以回滚或影响面无法限定：未命中。当前只改方案；未来实现范围和失败回流均已限定。
5. 修改后仍有多个有证据可行方向：未命中。AUD-002明确选择固定验证层；其余意见均采用唯一最小闭合方向。
6. 用户或生效项目约定明确要求再次审核：未命中。要求首次独立审核，现已完成并引入。

因此不消耗定向复审额度，不输出复审模式。本处理记录同时构成首次审核后的作者自评闭环依据。

## 6. 结论

P06 v1状态为`已审核，可以实施`。AUD-001～006全部接受并闭合；需求保真六项为`10/10/0/0/0/0`；无需再次第三方审核。长程实施授权继续有效，下一步进入P06实现。
