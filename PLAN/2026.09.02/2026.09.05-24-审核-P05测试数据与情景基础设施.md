# P05 测试数据与情景基础设施首次独立第三方审核

> 会话角色：独立第三方审核方
>
> 审核模式：首次全面审核
>
> 审核方式：只读核验；未参与方案制定或自评，未实施方案，未修改任何既有文件
>
> 审核批次：`P05-BATCH-c7ec47f7-de5603cc-43784f06-814dc829`
>
> 基线 HEAD：`814dc829a11b38e59288a4120b715438bd802f6b`
>
> 总体结论：存在不同意见；P05 v0 当前不得实施
>
> 阻断问题：10 项；未发现需要记录的非阻断问题：0 项

## 1. 被审对象与身份

- 被审方案绝对路径：`/home/adminn/projects/mcp-velociraptor/PLAN/2026.09.02/2026.09.05-22-实施子方案-P05测试数据与情景基础设施.md`
- 被审版本：v0；方案自述状态为“未审核，不得实施”。
- Git blob：`c7ec47f78e3a10d0a69fb4472d4e7fa88b24a8b7`；与审核批次和用户给定预期一致。
- SHA-256：`f32c9b3146b058ce37d9c7847108d93a0cdd1a3a8b358137069cfc40c4ae5172`。
- 作者自评：`/home/adminn/projects/mcp-velociraptor/PLAN/2026.09.02/2026.09.05-23-自评-P05测试数据与情景基础设施.md`；blob `66407cf7fc64b5f261ff372a25dd585c8e04f537`；SHA-256 `ce31d619b1d38ac171f2c735e447ce8292ab368e425cc3949bc68a8eb2b95527`。
- 需求文档：v0；blob `de5603cc9ca1c63f62feaba7ad397343d1513448`；SHA-256 `0596c3bc7e3e78712300e78b757982a335fc5e374485ce7b2af99649a2adf2a3`。
- 总纲：v4；blob `43784f06d8f253061579815b93ef3bc743555691`；SHA-256 `0e79f3ac01a7542167d7b1e14d0127b1efb967ff799d71de3ef505b9d80f8b62`；与用户给定预期一致。
- P01 基线报告：blob `582d5148fb376057e534a5502f9fbac5c4e71b39`；SHA-256 `9bacb33c1b5e44342f9428402292f2ff03003cfa61d4afd1235e3bed30bef0d2`。
- P04 最终实施记录：blob `6fed9c988e1b34aa25cd857a407ba632b5e84990`；SHA-256 `a84df43acc0d4637064d135c3860fe54e4978369ac33f994df7c05465a00ae49`。
- P05 source Spike：`Logs/P05/source-spike/p05-source-spike.json`；blob `efa2797bd7412fb71e17a2f819a7920a6a4c1f3e`；SHA-256 `4ce4539bb51b77afd99f35d8ad744f46f268629be2944443a92d4203676c8922`。
- 当前恢复状态 SHA-256：`e7d730da4336a6a377c1fe9b25a860fac99d0d39e06f31f07c1728a0c8588b41`；当前选择器 SHA-256：`7dbc8308b95719ef3d6dc07bfd11a44349748319e5fe766908de77fd88cf7617`。

写入前重新扫描同目录全部 `2026.09.05-NN-*.md`，最大序号为 23，序号 24 不存在，因此本记录分配为 24。启动时 `git status --short` 已有 `PLAN/`、`Logs/` 两个未跟踪目录；本审核不把它们冒充为本轮改动。

## 2. 来源切片与审核范围

本轮完整读取仓库 `AGENTS.md`、被审方案、自评、需求文档、总纲 v4，并针对下列规范性或事实切片复核：

1. 需求文档“正式需求 v0”中的 REQ-007/013/014/020/023、CON-002/004/005/006/007/008/011/012、NON-004/009、RACC-007/013/014/020/023，以及最终 P05/P06/P07 组织结果。
2. 总纲第 3.4、5.7、6、7、8、9、10、12 章，特别是 P05 固定来源 OUT-020/023/032/048、ACC-007/013/014 回填、ACC-020/023、P05/P06 边界和 FB-005/009/012。
3. P01 方案、组织结果、实施记录和阶段 0 基线报告中 Snapshot184、三项现存 inventory 初态、118 项依赖矩阵、唯一 client、SDK 2.1.1、triage 缺失和恢复治理证据。
4. P04 v3 方案、首次审核及处理、最终自评、实施记录和 `Logs/P04` 中 Flow/Hunt/file、两个缺失 artifact、130 工具协议与清理证据。
5. `Logs/P05/source-spike/` 的 JSON、两份 Autoruns 字节和 triage ZIP；当前长程执行状态、44 条事件、自动决定记录、恢复状态和恢复选择器。
6. `velociraptor_fixed_tools.py` 的 triage/kill 服务调用，`velociraptor_api.py` 的 root API/collection 参数编码，P01 原始 artifact/inventory 定义，以及 requirements 锁定文件。
7. Velociraptor 官方 VQL 参考中的 `inventory_add`、`artifact_set`、`pskill`：`inventory_add` 的本地 `file` 必须能由 server 读取且要求 `SERVER_ADMIN`；`artifact_set` 写全局 repository 且既有自定义同名定义会被无警告覆盖；`pskill(pid)` 的参数为必需 `int64` 且要求 `EXECVE`。

审核范围只判断 P05 v0 是否足以安全、确定、可复现地实施并向 P06 交接；不执行安装、Flow、快照、VMware、测试或 Git 提交，也不审查 P06 的 130 工具/约 650 关系全量正文。

## 3. 已独立核实且无异议的事项

- 被审方案、总纲和 HEAD 身份均准确；P01～P04 提交链与当前长程状态一致。
- 当前 canonical 恢复状态为 schema v1、epoch 2、`INSTALLED_ACTIVE`，active/allowlist 均只指向 `Snapshot 184-Velociraptor-MCP测试基线`，旁边不存在 `.next`；当前选择器 `--emit-active` 的实际只读输出也只给 Snapshot184。
- requirements 已锁 `mcp==2.1.1`；P04 最终证据确有 44/44 测试、130 工具和退出时活动 Flow/Hunt 为 0。P04 固定服务内部对 triage 使用 `Targets=["_BasicCollection"]`，对 kill 使用整数 `Pid`。
- triage ZIP 的本地 SHA-256 为 `44e59e...acbb`，仅含一个 80977 字节 `Windows.Triage.Targets.yaml`；解压正文 SHA-256 为 `1a699a...bb1c`，名称准确，`HighLevelTargets` 与 `Targets` 均含 `_BasicCollection`。两份 Autoruns 本地字节哈希与 Spike 一致。
- P01 原始定义证明 PacketCapture 精确消费 ZIP 内 `etl2pcapng/x64/etl2pcapng.exe`，Autoruns 精确按 `Autorun_` 加 Windows architecture 选择工具。方案锁定 v1.4.0 和 14.3、大小/哈希/签名且禁止替换最新版的方向正确。
- 官方语义支持使用 root server VQL 的 `inventory_add` 和 `artifact_set`，并支持 client 侧 `pskill(pid)`；产品 MCP 不新增安装入口的边界正确。
- P05 只以代表情景验收设施而把逐工具与全部五情景关系留给 P06，范围切分符合总纲；未发现提前物理删除旧源码或改变 P04 生产 schema 的要求。

上述通过项不抵消以下阻断问题。

## 4. 审核发现

### AUD-001｜首次 inventory 转换、干净起点和部分失败恢复未形成单一状态机

- 风险等级：高，阻断。
- 对应：REQ-020，CON-005/008，OUT-020/030/033，ACC-007/020，IMP-P05-004～007，FB-P05-002/003。
- 证据：方案第 75 行规定“相同 identity 只复核，不同 identity 拒绝覆盖”，但 P01 `artifact-inventory.json` 已存在三项不同于目标锁定态的合法基线条目：`etl2pcapng` 为 `admin_override=false/version=""/hash=""/materialize=false/serve_locally=false`；两个 Autorun 条目同样为空 version/hash、未 materialize，其中 amd64 还显示来源 `Notebooks.Demo`。首次安装本来就是把这些既有默认条目重配置为锁定本地字节，按现文字面会被“不同 identity”拒绝。方案第 127～133 行只检查 selector 返回 Snapshot184，并未要求在 IMP-004 首次写入前实际完成一次 Snapshot184 的单次恢复与 hostname/API/marker 验证；P03/P04/Spike 已对当前运行 VM 产生过临时对象和文件。FB-P05-002 对第一、第二个写入后后续写入失败只说修脚本，没有规定如何丢弃局部 inventory/repository/fixture 状态。
- 影响：实现者要么无法执行首次安装，要么自行放宽覆盖规则；也可能从被先前测试污染的运行态创建 Snapshot185，或在部分失败后从混合状态重试，违反固定可恢复数据、范围隔离和“不同 identity 拒绝覆盖”。
- 最小闭合条件：冻结一个明确的 P05 准备状态机：IMP-004 前先由 canonical selector 完成一次且仅一次 Snapshot184 恢复全链；manifest 逐字段记录并只接受 P01 已证的三项 predecessor identity；仅允许该 predecessor 到目标 locked identity 的一次转换、目标 identity 重跑为只读 no-op、其他 identity 拒绝。每个写操作前后保存三项 inventory、两项 artifact、fixture 根的差集；任何部分失败先回传证据，再用同一 Snapshot184 恢复链回到完整 predecessor 状态后才允许新 attempt，不用删除/卸载模拟回滚。创建 Snapshot185 前证明本 attempt 的所有状态都源于该干净起点。

### AUD-002｜schema v1 没有定义可实现的断言、引用和 cleanup 契约

- 风险等级：高，阻断。
- 对应：REQ-023，CON-004/008，OUT-023/029/033，ACC-023，IMP-P05-003，ACC-P05-005。
- 证据：方案第 101～115 行只枚举顶层字段、step 名和 10 个操作符，未给 assertion 对象的必需字段、实际值路径、期望值类型、缺失值语义、`in/contains` 左右操作数方向、`matches` 正则方言、数组/对象相等规则、多个断言是 all 还是 any、`is_error` 如何读取 MCP `isError`。`$ref`/`$fixture` 的路径转义、数组索引、禁止原型/私有属性和类型不匹配也未冻结。更直接地，顶层 `additionalProperties=false` 的允许字段中没有 cleanup，而第 121 行和 ACC-P05-005 又要求执行“精确 cleanup 列表”；该列表在 schema 中无可表达位置。`canonical JSON` 报告也没有冻结键集合、排序、数字/时间和可变字段规范化方式。
- 影响：两个合规实现可对同一情景给出相反结果；cleanup 只能硬编码或被忽略，无法由 P06 审阅；正反例可能用实现生成的规则验证自身，不能满足“可判定”和失败重跑要求。
- 最小闭合条件：在方案中冻结完整 JSON Schema，而不只列操作符：每种 assertion/repeat/cleanup 的 tagged union、精确 required/additionalProperties、受限 JSON Pointer（或等价单一语法）、操作数类型和错误语义、多断言组合规则、轮询成功/耗尽/调用错误规则。增加可表达的 scenario-level cleanup（或逐 step cleanup）并限定工具、参数、触发条件、顺序、重复调用和 cleanup 自身失败；冻结 canonical report schema/序列化规则。独立手写 golden 覆盖每个操作符、路径逃逸、前向/失败引用、轮询耗尽、正常失败、cleanup 成败和未执行步骤。

### AUD-003｜runner 接受任意输入路径和全部工具参数，仍可成为任意 VQL/命令代理

- 风险等级：高，阻断。
- 对应：NON-009，REQ-023，CON-004/006，OUT-023/029/031/048，ACC-023，IMP-P05-003。
- 证据：方案第 112 行只要求 tool 存在于运行时 130 工具列表，第 121 行只验证 schema/manifest；第 254 行把输入描述为“单个 scenario JSON、snapshot 名称和 fixture manifest”，没有限制 scenario 必须位于仓库受审目录、是普通文件、无 symlink/`..`、受 Git/哈希绑定。130 工具本身包含 `run_vql(query)` 和可执行命令的动态 Windows artifact，因此不需要 Python `eval` 或本机 shell，一个任意 JSON 已可让 runner 代理任意 VQL或 client 命令。`fixture_manifest: repo-relative path` 也没有规范化 containment 或 symlink 防护，和“不得任意文件读取”并不等价。
- 影响：该入口具备通用生产式调度/命令代理能力，违反 NON-009；攻击者或误操作可用未审情景读取仓库外 manifest、向 root Org 执行 VQL或向 endpoint 下发命令，且报告 SHA 只能事后标识输入，不能成为执行前授权边界。
- 最小闭合条件：runner 只接受规范化后位于固定 `tests/scenarios/` 树中的普通 JSON，拒绝绝对路径、`..`、symlink/reparse 和树外 realpath；要求 source SHA 与受审 inventory/index 中的值一致，P06 新情景须先进入该 index。fixture 输入也须采用固定目录/类型和哈希绑定。server argv 固定为当前 bridge 入口、`shell=False`，环境只继承明确 allowlist。对 `run_vql`、PowerShell/Cmd 等可承载任意文本的工具，情景中的准确参数必须是受审文件的固定内容，CLI 不能覆盖或插值。用未索引场景、树外/链接路径和参数覆盖做拒绝测试。

### AUD-004｜tracked fixture 规范与运行态 manifest 身份互相矛盾，fixture 副作用也未冻结

- 风险等级：高，阻断。
- 对应：REQ-020/023，CON-006/008，OUT-020/023/031/033，ACC-020/023，IMP-P05-002/003。
- 证据：核心提交第 27 行把 `tests/data/p05_fixture_manifest.json` 定义为路径模板和断言；第 93 行又让准备脚本生成含绝对 guest 路径、PID/start-time 的 fixture manifest；schema 第 107 行只允许 repo-relative manifest。方案没有说明 runner 读取的是 tracked 模板、每次变化的运行实例，还是脚本会改写 tracked 文件。文件名/字节/哈希、注册表键和值、事件 provider/id/message/token、计划任务 XML/主体/trigger/action/enabled 状态、专用进程实际解释器/参数/sleep 秒数和 ownership marker 均未固定。“固定任务定义”仍可被实现为会执行命令或联网的任务；“长 sleep”也不能证明可存活到快照创建及两轮验收。
- 影响：P06 无法稳定解析 PID/路径，或每次准备都会修改 tracked golden；两个恢复周期可能比较的是不同定义。任务、事件源和进程可能产生范围外副作用，现有目录也无法可靠判定是否属于本 workflow。
- 最小闭合条件：分离不可变 tracked `fixture-spec-v1` 与位于批准 guest fixture 根内、每次生成的 `fixture-instance-v1`；两者有各自严格 schema、spec SHA、workflow/attempt、ownership marker 和一一映射，runner 只按固定安全机制读取 instance。冻结全部文件内容/大小/哈希与对象全名/值；计划任务必须 inert/disabled、无网络且不执行外部 payload，事件的唯一检索键和重复判定明确；冻结 Python 路径来源、sleep 下限或存活门，并在创建快照瞬间复核 PID+start-time+token。定义首次创建、同态 no-op、仅进程重建、冲突拒绝和失败后恢复的精确差集。

### AUD-005｜`.next` 失败路径会阻断 Snapshot184，schema v2/epoch3 也不足以严格验证

- 风险等级：高，阻断。
- 对应：REQ-020/025，CON-005/008，OUT-020/025/030/033，ACC-020/025，IMP-P05-007，FB-P05-004。
- 证据：当前选择器第 187～188 行明确“只要 `.next` 存在，正常恢复选择即失败”。方案第 141、145 行沿用此门，并要求写 `.next` 后才用 `--emit-next` 做 epoch/evidence 验证；若候选 state 本身无效、证据哈希错或 selector 新实现有 bug，`.next` 已存在。第 147 行及 FB-P05-004 却只说 canonical 保持 Snapshot184，未规定如何处置本 workflow 自己生成的失败 `.next`，所以 canonical 虽未改写，正常 `--emit-active` 仍不可用。另有 `retired 列表` 只有自然语言，没有冻结 v2 exact keys、元素唯一性/排序、active/retired 不相交、marker 唯一性、epoch 只允许 2→3、证据字段和旧 v1 兼容边界。
- 影响：一次候选 state 验证失败即可把唯一常规恢复路径永久锁死；不同实现可生成不同 schema v2，并可能接受旧快照同时出现在 automatic allowlist 或错误 retired 状态，不能证明原子、fail-closed 且可恢复。
- 最小闭合条件：冻结 schema v2 的 exact keys 和全部交叉不变量；selector 单元测试覆盖 v1 epoch2、v2 epoch3、未知键、重复 retired、active/retired 重叠、错误 marker/phase/evidence、降级/跳 epoch。`.next` 必须以排他 create、完整写入/fsync 后出现；验证期间继续 fail closed。若验证失败，只允许在确认它由当前 P05 attempt 创建、记录其 SHA/错误并重新验证 canonical epoch2 完整有效后，将该 `.next` 原子移入隔离证据名或安全移除，再证明 `--emit-active` 恢复输出 Snapshot184；不得删除任何 VMware snapshot。成功 rename 后 fsync 目录并再次验证 185-only/183-184 exit 3。

### AUD-006｜PacketCapture 对系统全局 netsh trace 的所有权与失败清理不足

- 风险等级：高，阻断。
- 对应：REQ-007/020，CON-005/008，OUT-007/020/030/033，ACC-007/020，IMP-P05-005，FB-P05-003。
- 证据：P01 原始 artifact 的 stop 分支无条件执行 `netsh trace stop`，而 start 分支使用系统默认 trace 状态/路径，没有 workflow token 参数。方案第 79 行只说 finally 停止“本 workflow 启动”的 trace，却没有在启动前证明全局 trace 未运行、何时取得所有权、如果 start Flow 返回异常/超时/MCP 失联由哪个固定动作清理，以及如何避免停止一个先前存在的无关 trace。创建 Snapshot185 的“trace 未运行”终门只能发现残留，不能保护范围外 trace。
- 影响：验收可能终止用户或其他 workflow 的 trace；反之也可能在异常路径留下全局 trace、持续写盘或把它固化进候选快照。仅凭 FINISHED/ERROR 不能证明全局 OS 状态已还原。
- 最小闭合条件：调用前以固定只读命令/VQL记录 `netsh trace show status` 并要求 inactive，否则阻断且不 stop；start 成功且取得本次 ETL 路径、活动状态和时间边界后才记为本 attempt 所有。冻结同会话正常 stop/convert 路径及 MCP 不可用时由外层 P05 固定脚本执行的唯一紧急 stop，不允许 scenario 自带命令。每条成功/失败路径都回读 inactive、ETL/PCAPNG身份和无范围外文件差集；若不能证明所有权则从 Snapshot184 恢复而不对未知 trace 执行 stop。

### AUD-007｜最小 KillProcess artifact 的安全/权限/结果契约仍可由实施者自行决定

- 风险等级：中，阻断。
- 对应：REQ-014，NON-004，OUT-014/043，ACC-014，IMP-P05-001/004/005。
- 证据：方案第 49 行只冻结名称、单个 `Pid: int`、CLIENT 和“唯一动作 pskill”，没有冻结 Windows precondition、`required_permissions: [EXECVE]`、source 数量/名称、准确 VQL、返回列和 `pskill` 成功/失败值。官方 VQL 参考明确 `pskill(pid)` 要求 `EXECVE`。第 82、211～215 行记录 PID/start-time/token，但没有要求在 tools/call 紧邻前再次证明同一 PID 的 start-time/token，亦未冻结 PID 被复用时必须拒绝本轮；“系统关键进程”集合也不确定。
- 影响：artifact 可能编译但权限/错误表现不确定，或带有额外 source/precondition 行为；PID 在 manifest 生成后退出并复用时会错杀。不同“关键进程”取样仍可让 ACC-P05-003 得出不同结论。
- 最小闭合条件：在方案中给出独立手写 golden 的准确规范：一个 CLIENT artifact、Windows precondition、显式 `required_permissions: [EXECVE]`、唯一 Pid int 参数、无 tools/import/export、唯一 source 和唯一 `pskill(pid=Pid)` 查询及预期结果列；对 raw/compiled 定义逐字段核对。冻结受保护 PID 集合至少含 server/client、PID 0/4、当前执行器及父进程。在调用前紧邻复核 PID+start-time+token，任一变化拒绝；调用后既要目标 token 消失，也要全部受保护 PID/start-time 不变，并区分 pskill 返回错误、Flow ERROR 与真实结束。

### AUD-008｜“无原始 URL 访问”的观察面和 inventory 成功身份不足以判定

- 风险等级：中，阻断。
- 对应：REQ-007，CON-005/008，OUT-007/030/033，ACC-007，IMP-P05-004/005。
- 证据：方案第 69、80、173、193～199 行只写 `inventory()/inventory_get(probe=TRUE)`、网络观测和“不访问原始 URL”，未定义 readback 的完整字段、probe 是否会触发物化、观察窗口、server/client 进程、DNS 与连接数据源。当前 P01 初态中 Autorun 已有 localhost `serve_url` 但 `materialize=false/hash=""`，这证明“URL 看起来本地”本身不能证明固定字节已缓存；etl 条目仍直接指向 GitHub。
- 影响：实现可只截图 inventory 或只观察 endpoint，而 server 在 probe/Flow 前后从公网取字节；也可能漏 DNS/重试。ACC-P05-001 对供应链和离线可重复性的关键预期无法机械判定。
- 最小闭合条件：冻结三项安装后 identity 的必需字段（name/version/filename/admin_override/materialize/hash/invalid_hash/serve_locally/serve_url/versions 及本地 filestore 字节独立 SHA）；明确 `inventory_get(probe=TRUE)` 的调用时点和预期副作用。为安装后首次 FetchBinary 与两条真实 Flow 定义从调用前到终态后的 DNS/连接/服务日志观察窗口，同时覆盖 server 与 client；断言只允许 loopback/已知本地 public URL，三个原始域名/URL零请求。host-only 传输窗口与产品调用窗口分开记账。

### AUD-009｜Git/二进制扫描的“零命中”与现存 source Spike 字节冲突

- 风险等级：中，阻断。
- 对应：REQ-020，CON-006/007，OUT-020/031/032，ACC-020，IMP-P05-008，FB-P05-006。
- 证据：方案第 34 行称官方 Microsoft 工具只在 VM 和宿主临时传输区，第 256 行又要求永久保留 `Logs/P05/source-spike/`。当前该目录实际含两份 EXE 和一个 ZIP；作者自评第 20 行称宿主下载物在 `/tmp`，与现场不符。ACC-P05-006 要扫描“Git 对象/工作树”，FB-P05-006 的退出条件是“扫描零命中”，但完整工作树扫描必然看到这些受保护的未跟踪证据。另一方面，若笼统禁止“二进制入 Git”，也可能误伤非恶意的 schema/fixture 数据，而 CON-007 的规范是恶意或敏感二进制不得提交。
- 影响：验收要么永远无法零命中，要么通过未声明排除隐藏现场；也无法区分“未跟踪本地证据”“staged/reachable Git 对象”和“允许的非恶意数据”。自评据此给出的 Git 隔离通过结论不可复核。
- 最小闭合条件：明确分类：`PLAN/Logs` 为受保护、未跟踪的本地治理/证据，当前 source bytes 的准确清单和哈希单独报告但不计 staged/reachable Git；产品扫描分别覆盖 index、P05 commit tree、相对基线新增 reachable blobs 和 tracked worktree，要求秘密/恶意/官方工具字节零命中。对整个 worktree 的扫描必须报告已知排除而不是伪称零；新增排除必须失败。修正文档中“只在 /tmp/临时区”的失实表述，并冻结普通测试二进制数据的允许口径。

### AUD-010｜需求/OUT 双向追溯和“应承接 9”计数不成立

- 风险等级：中，阻断。
- 对应：总纲第 6、7、9、12 章，ACC-025；被审方案第 13 行；作者自评第 22～38 行。
- 证据：P05 不仅拥有 REQ-020/023，还实际回填 ACC-007/013/014；对应规范来源至少包括 REQ-007/013/014、CON-005/011、NON-004 以及 OUT-007/013/014/030/036/043。runner 本身还直接受 CON-004/OUT-029 约束。方案身份行漏掉 REQ-007/013/014、CON-004、NON-004，却列入已在 P01 完成的 CON-001；自评的 9 行“来源契约”没有展开这些 OUT，也没有映射方案身份行已经声称的 CON-002/005/006/011/012。把自然语言分组行数称为“应承接 9”既不是稳定 ID 数，也不是总纲固定 OUT 数。
- 影响：ACC-P05-001～003 可在不证明其原始 REQ/CON/NON 语义时被标为回填成功；后续 ACC-025 无法机械反查。遗漏 NON-004 也会削弱 kill 工具无联网控制旁路的来源约束。
- 最小闭合条件：建立两层、双向且无异常 ID 的矩阵：第一层是 P05 固定拥有的 OUT-020/023/032/048；第二层是受委托回填 ACC-007（OUT-007/030）、ACC-013（OUT-013/036）、ACC-014（OUT-014/043），再列出本阶段适用的交叉约束 OUT-027/029/031/033/037。规范稳定 ID 口径至少为 5 REQ（007/013/014/020/023）、8 CON（002/004/005/006/007/008/011/012）、2 NON（004/009）、5 RACC（007/013/014/020/023），共 20 项；若保留 CON-001，须单列为已由 P01 解除的继承前提而非 P05 新验收。每个 IMP/ACC/fallback 反向指向这些来源，并重新计算完整/未落点/弱化/不可验收数。

## 5. ACC 可验收性逐项结论

| P05 ACC | 前置/动作/预期/证据形式 | 通过/失败/阻断边界 | 本轮结论 |
| --- | --- | --- | --- |
| ACC-P05-001 | 表面齐全，但首次 inventory 转换、离线观察面和 trace cleanup 未冻结 | 有概括边界，无法机械判定首次转换、外联和全局 trace 所有权 | 不通过；AUD-001/006/008 |
| ACC-P05-002 | 来源、动作和 Flow/results/files 已列；本地 triage 字节身份可复核 | `known source`、精确 raw/编译 identity 仍须由实现证据补齐，但核心方向成立 | 有条件通过；依赖 AUD-001/008 的准备状态闭合 |
| ACC-P05-003 | PID/token/start-time 与 Flow 证据已列 | pskill 权限/返回、紧邻身份复核和受保护 PID 集合不确定 | 不通过；AUD-007 |
| ACC-P05-004 | 两恢复周期和 snapshot 证据已列 | 未保证首次干净 Snapshot184 起点，state `.next` 失败会封锁恢复，fixture/扫描不可判定 | 不通过；AUD-001/004/005/009 |
| ACC-P05-005 | SDK、代表情景、两周期和报告证据已列 | schema 无 cleanup/断言语义，入口可代理任意 VQL/命令 | 不通过；AUD-002/003/004 |
| ACC-P05-006 | 测试、diff、README、commit 证据已列 | 无独立“被阻断”分支，且扫描 universe 与零命中互相矛盾 | 不通过；AUD-009/010 |

因此作者自评“六项 ACC 均有精确预期和边界”的结论不能成立。本审核没有把缺少真实执行结果当成方案缺陷；问题在于即使未来执行，当前文字也不能唯一决定动作和通过标准。

## 6. 需求保真计数

### 6.1 规范稳定 ID 口径

| 类别 | P05 应承接或本阶段直接适用 | 被审方案身份行明确列出且正确 | 漏列 | 异常/错位 |
| --- | ---: | ---: | ---: | ---: |
| REQ | 5 | 2 | 3 | 0 |
| CON | 8 | 7 | 1 | 1（CON-001 应作为已解除继承前提单列） |
| NON | 2 | 1 | 1 | 0 |
| RACC | 5 | 5 | 0 | 0 |
| 合计 | 20 | 15 | 5 | 1 |

漏列稳定 ID 为 REQ-007、REQ-013、REQ-014、CON-004、NON-004。方案正文对其中部分行为有自然语言涉及，但这不等于双向追溯闭合。

### 6.2 保真判断

| 指标 | 数量 |
| --- | ---: |
| 应承接正式稳定 ID | 20 |
| 完整且可验收 | 10 |
| 有正文语义但追溯或验收不完整 | 10 |
| 完全无正文落点 | 0 |
| 明确语义弱化 | 3（runner 非生产边界、失败恢复、固定数据干净起点） |
| 当前不可唯一验收 | 10 |
| 异常/错位 ID | 1 |

这里的 20 是规范稳定 ID，不与作者自评的 9 个自然语言分组混用。OUT 层应按 AUD-010 分为 4 个 P05 固定拥有输出、3 个受委托回填组和 5 个交叉适用约束；当前自评未给出该口径，因此 OUT/ACC/P05 双向覆盖结论为不通过。

## 7. 总体裁决

P05 v0 的总体方向正确：四项依赖身份锁定、产品不联网安装、最小 kill artifact、fixture/runner 与 P06 全量执行分拆、两轮候选恢复和旧快照退役，均符合需求和总纲。然而，依赖写入的真实 predecessor 状态、runner 可判定格式及信任边界、fixture 双 manifest、PacketCapture 全局副作用、Snapshot `.next` 失败恢复、Git 扫描范围和正式追溯均没有闭合。

裁决为：**存在不同意见，当前 v0 不得实施。** AUD-001～AUD-010 全部是进入实施前必须闭合的方案缺口。作者应只修改方案/自评来引入意见，重新计算对象 blob/SHA-256和需求保真；是否需要再次第三方审核应按生效工作流规则判断。本记录保持不可变，不替代作者的审核处理记录，也不授权任何安装、VMware、源码或状态变更。
