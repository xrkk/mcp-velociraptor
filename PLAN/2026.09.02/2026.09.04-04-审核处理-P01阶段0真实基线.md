# P01 阶段0真实基线审核处理记录

> 会话角色：作者/执行方
>
> 记录性质：首次审核处理；创建后不可修改
>
> workflow_id：`wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2`
>
> 审核批次：`P01-BATCH-a0ff2d38-de5603cc-ee7c23e1-1f97a4f4`
>
> 原审计：`/home/adminn/projects/mcp-velociraptor/PLAN/2026.09.02/2026.09.04-03-审核-P01阶段0真实基线.md`；blob `bc87ae9dc52796623e633e003afe558dcab2195d`
>
> 处理前方案：`2026.09.04-01-实施子方案-P01阶段0真实基线.md` v1；blob `a0ff2d382ccf448e8332e11e355a8d3efcdf0168`；状态 `未审核, 不得实施`
>
> 处理后方案：同路径 v2；blob `afbcc52a050dcdc41fa6ed73a37f5bab2d4c8038`；状态 `已审核, 可以实施`

## 1. 身份、基线与批次一致性

- 原审计绑定的方案、需求、总纲、HEAD 和内容指纹在审计写入前一致，审计记录已实际持久化且保持不可变。
- 本地基线仍为分支 `main`、HEAD `1f97a4f4e64d27faf07fe684d8cbfe270b57757d`；产品代码没有 diff。
- 规范性来源仍为需求 v0 blob `de5603cc9ca1c63f62feaba7ad397343d1513448` 和总纲 v2 blob `ee7c23e1b12fb78e690c5b95d5a511de19bbce0c`，含义未漂移。
- 本批次全面审核额度使用 `1/1`，定向复审额度使用 `0/1`。当前唯一有效第三方结论是首次全面审计的“存在不同意见（AUD-001～AUD-005）”；本记录逐项引入并完成作者复评。

## 2. AUD 逐项处理

### AUD-001｜逐 artifact 外部工具、缓存和联网前提缺口

- 接受度：同意。
- 依据：总纲 P01 明确要求外部工具/缓存/联网前提；v1 的 IMP/ACC-P01-005 只冻结定义 metadata，后序确会悬空。
- 修改：v2 在 IMP-P01-005 增加逐项 `external_tools/cache_requirement/network_requirement`、证据位置、可满足性和影响字段，冻结五类结果；ACC-P01-005 要求规范集合与依赖集合双向差集为零且无 `unknown`；ACC-P01-008 明确交接完整依赖矩阵。
- 影响：只增强 P01 取证和 P03/P05/P06 交接，不改变 allowlist、联网产品边界或 Pxx 顺序。
- 最小闭合条件：已满足；实施时以完整定义和双向差集取证。

### AUD-002｜快照真实失败与被阻断混淆

- 接受度：同意。
- 依据：已执行命令的错误是可观察失败，不能和无权限/无观察通道混为被阻断。
- 修改：v2 将 revert/`vmware -x` 错误、checkpoint 异常复现、进程/hostname 不符归为失败；被阻断仅保留无法读取、启动或观察且无等价证据；明确失败不得被 ACC-P01-008 吞并。
- 影响：纠正 ACC-P01-001 三分边界，不改变用户提供的恢复顺序或异常恢复分支。
- 最小闭合条件：已满足。

### AUD-003｜四个上限没有具体交接值

- 接受度：同意。
- 依据：REQ-012、U-005 和总纲要求 P01 给证据化具体建议，P02 只固化。
- 修改：v2 的 IMP/ACC-P01-006 冻结 page_size 候选网格、输入/输出/耗时/工作集记录，要求阶段0报告给出具体整数的行数上限、字节上限、默认和最大 page_size，并含选择理由、前提和重新裁决条件；证据不足则 P01 不通过。
- 影响：只收紧 P01 产出，不提前把未实测数字写成产品常量。
- 最小闭合条件：方案层已满足；实施层必须产生四值表才能通过。

### AUD-004｜Git identity 未覆盖

- 接受度：同意。
- 依据：P01 标题已承诺“提交身份”，补充只读 identity/来源不会扩大产品范围。
- 修改：v2 的 IMP/ACC-P01-002 加入宿主/VM 的有效 `user.name`/`user.email`、`--show-origin` 来源、HEAD author/committer 和前后零漂移；P01 明确不修改 identity。
- 影响：只增加可复现 Git 基线。
- 最小闭合条件：已满足。

### AUD-005｜最小 collection/Hunt 选择与资源/结束语义未闭合

- 接受度：同意。
- 依据：临场任选有状态对象会造成不可复现和不受控副作用。
- 修改：v2 先只读复用完全匹配 workflow 对象；否则最多各创建一个固定 `Windows.System.Pslist`，使用固定无匹配正则和 workflow tag；冻结 120 秒/5 秒/10 行/1 MiB 预算、禁止动作、终止/取消顺序、终态回读、前后对象差异和异常重连幂等规则。artifact/参数不成立时阻断并回流，不临场换项。
- 影响：只限定 P01 的基线取证对象；不改变后续产品 Hunt/Flow 契约。
- 最小闭合条件：已满足。

## 3. 实际 diff、影响传播与继承

实际契约 diff：

1. 方案头部 v1 升为 v2。
2. IMP/ACC-P01-002 增加 Git identity、来源和 HEAD author/committer。
3. IMP/ACC-P01-005、ACC-P01-008 增加逐 artifact 外部依赖字段、分类和双向差集门禁。
4. IMP/ACC-P01-006 增加固定对象、参数、资源预算、结束/取消、前后差异、测量网格和四个具体建议值。
5. ACC-P01-001 纠正失败/被阻断边界。
6. 版本记录增加 v2；稳定 IMP/ACC 编号未变。

受影响并重新打开后已由本记录复评的项：八个规范切片中的 CON-001、CON-009/U-002/U-003、CON-005/U-004/U-005，以及 IMP/ACC-P01-001/002/005/006/008/009。

未受影响并继承：快照恢复动作顺序、VM/VMX/快照/hostname 身份；REQ-001/NON-010 仓库渐进扩展；stdio 取证；triage 身份；P01 零产品代码改动；P02～P07 顺序；其余 IMP/ACC 和明确排除范围。

全局不变量继续成立：Windows 单 VM/root Org；P01 只取证；无冷启动/stop/start；无联网控制接口；无凭据/恶意样本入库；无第二 server/整体重写；无后序依赖；失败不带入 P02。

## 4. 修改后作者完整复评

| 维度 | 结论 | 证据 |
| --- | --- | --- |
| 身份/来源/状态 | 通过 | 路径、v2、处理后 blob、需求/总纲/HEAD 唯一 |
| 稳定 ID/双向追溯 | 通过 | 9个 IMP、9个 ACC 不变；八个规范切片完整映射 |
| P01 范围/非目标 | 通过 | 新增内容均是 P01 取证与安全边界，没有产品实现 |
| 无后序依赖 | 通过 | 四值和依赖矩阵由 P01 交付，P02～P06 只消费 |
| 方向闭合 | 通过 | artifact 依赖分类、对象选择、参数、预算、退出和 Git identity 均唯一 |
| ACC 三分边界 | 通过 | 快照失败/阻断互斥；底座缺失取证与 P01 证据阻断分开 |
| 资源/数据一致性 | 通过 | 每类新对象最多1个、预算固定、只改本 workflow 对象、终态和差异必记 |
| fallback | 通过 | 未新增产品 fallback；artifact差异用既有 FB-003，恢复分支来自用户契约 |
| 可实施性 | 通过 | 已存在命令明确；运行值由证据生成，不虚构结果 |
| 审核意见闭合 | 通过 | AUD-001～005 全部同意并具有对应修改和实施证据门禁 |

## 5. 需求保真确认

修改后独立按八个 P01 规范切片复核参与者、输入输出、触发/顺序、状态/生命周期、不变量、异常/边界/结束、适用前提/重新裁决和可观察验收：

| 计数 | 结果 |
| --- | ---: |
| 应承接数 | 8 |
| 完整数 | 8 |
| 未落点数 | 0 |
| 语义弱化数 | 0 |
| 不可验收数 | 0 |
| 失效或复核条件缺失数 | 0 |

异常 ID：无。需求保真确认：通过。

## 6. 再次第三方审核判定

| 触发条件 | 是否命中 | 具体判断 |
| --- | --- | --- |
| 跨两个以上独立项目或多业务共享契约 | 否 | 仅当前仓库 P01 取证契约 |
| 不可逆/高风险迁移、权限、安全、隐私或外部关键契约 | 否 | 没有迁移或权限变化；恢复机制本身未改，只纠正状态归类 |
| 总体架构、核心数据流、并发一致性、恢复或切换机制改变 | 否 | 没有改变用户恢复机制或产品架构；只补执行门禁和证据 |
| 核心路径难回滚或影响无法限定 | 否 | 修改全为可回滚文档契约，运行 mutation 限定为两个新对象 |
| 多个均有证据方向且独立评审会实质改变选择 | 否 | 每条意见由来源与已证实事实唯一闭合 |
| 用户或项目约定明确要求再次审核 | 否 | 只要求首次全面审核；无再次审核强制项 |

判定：首次第三方审核已引入，修改后由作者自评闭环，无需再次第三方审核；不输出复审模式或定向复审请求。

## 7. 最终状态与实施入口

- 审核性质：首次全面第三方审核结果引入。
- 审核意见：AUD-001～AUD-005 全部闭合，无未解决或复开项，无审核循环熔断。
- 当前方案：v2，blob `afbcc52a050dcdc41fa6ed73a37f5bab2d4c8038`，状态 `已审核, 可以实施`。
- 长程实施授权：进行中，覆盖 P01 实施、验证、提交与后续 P02～P07。
- 下一检查点：进入 P01 `IMPLEMENT`，先创建实施记录，再执行 ACC-P01-001 快照恢复链。
