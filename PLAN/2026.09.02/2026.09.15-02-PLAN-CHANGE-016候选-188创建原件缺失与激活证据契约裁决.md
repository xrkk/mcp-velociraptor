# PLAN-CHANGE-016 候选：188 创建原件缺失与激活证据契约裁决

- 提出方：接手执行方（G4.D 执行中）
- 日期：2026-09-15
- 状态：候选（待原主会话裁决；裁决前不执行激活，不改验证器）
- 影响规范：P05 v10 §激活；tests/p06_evidence.py（verify_activation_bundle 终端阻断）；tests/p05_candidate_creation.py（verify_creation）；总纲 v14 快照代次语义

## 1. 事实链（全部可复核）

1. G4.D 连续相位管道已全部真实通过（单宿主进程 per 相位：ready→gate→formal→chain）：
   - initial：run `cb491168-63d3-4ff6-a4f4-9283c0d8b16d` @ 187 恢复 `p05-g4c-restore-a87d941b4514`
   - cycle-1：run `d2eeaf44-3cfa-43d4-8a52-ab15075086ec` @ 188 恢复 `p05-g4c-restore-c4523985a283`
   - cycle-2：run `4ab669ba-8ece-4eb6-8706-6ca89f7778e6` @ 188 恢复 `p05-g4c-restore-7236f7c5040c`
   - 各自 ready 12 类原样、七 HTTP 例、正式外部 streamable-http 代表情景 success、三链（PC-014 口径）真实通过。
2. 结构性发现（bundle 组装前）：正式报告的 snapshot-evidence.restore_records 使用扁平文件名（`snapshot-metadata.json` 等），三相位内容互不相同（6 件中 5 件 sha 不同，已列表核对）。verify_activation_bundle._verify_phase_restore 把这些路径统一解析到 `bundle/p05-originals/` 单一扁平命名空间 → 三相位必然碰撞。snapshot-evidence 字节被 report.snapshot_evidence_sha256 哈希绑定，不可重写 → **三相位必须以「<restore_attempt_id>/<file>」唯一路径重跑**（相位管道补丁已明确，改动点两处：current-restore 记录路径 + ready run_id 绑定正则）。
3. **核心阻塞**：`verify_creation`（p05_candidate_creation）要求四件 p05-snapshot-command-v1 命令信封：tree_before（仅 187 的 showTree 输出）、create_operation（那次 `vmrun snapshot` 本身）、tree_after、metadata_readback。188 已于 2026-09-14T18:17:50Z 创建（g4c-completion-ledger.md），**当时未按信封格式落档**；全仓检索确认无任何创建信封原件。tree_before（仅 187 的树状态）与 create_operation 均为历史一去事件，无法在 188 已存在的前提下真实重采；伪造信封被证据纪律禁止。
4. `verify_activation_bundle` 末端存在 §0.7 冻结的 fail-closed 终端阻断（"Snapshot188 raw transcript predicates are not implemented; activation remains blocked"），并有回归测试 test_p06_evidence_schema5.py:237 断言该阻断。选择器 `--emit-next` 激活路径调用该验证器且要求其成功返回 → 在移除该冻结阻断前，任何真实包都无法通过选择器激活。当年阻断理由（parser 未实现）现已不成立：p05_snapshot_raw / p05_candidate_creation / p05_http_evidence / p05_network_window 谓词均已实现并在 Windows 实测通过。
5. 物理快照约束（用户安全边界）：不得删除/改名/覆盖 187 或 188；G4.C.6 亦规定不覆盖同名 188。

## 2. 争议点

对"已创建但无信封原件的 188"，激活证据契约如何满足：

- 方案 A（新代次）：新建快照 189+ 并当场全信封采集。问题：tree_before 谓词硬编码"零→一（仅 187）"，189 的 before 是 [187,188]，同样过不了 verify_creation → 还需改谓词为 N→N+1；canonical/验证器多处硬编码 SNAPSHOT_188；三相位需按新代次全部重跑；成本最高。
- 方案 B（推荐，历史重建证据契约）：一次性修订 verify_creation，为"现存 188"接受受限的历史重建证据包：vmware.log 中该时刻的 Snapshot 创建/Revert 事件行（带时间戳原始日志）+ 创建后 vmsd 原字节 + 创建前 vmsd/树原件（g2b 恢复 attempt 的 vmsd-original/snapshot-metadata 原件）+ G4.C 完成账本，并在创建文档中显式标注 reconstruction 来源；同时按同一次修订移除 verify_activation_bundle 的终端阻断（其存在理由已消失）并同步回归测试。三相位以唯一记录路径重跑后组包 → 严格验证真实通过 → selector --emit-next → 原子 replace → 回读 5/6/188。
- 方案 C（维持冻结）：接受激活被 §0.7 阻断，G4.D 终止于"结构完整包 + 到达终端阻断"，G5/G6/G7 以 187 为活动基线继续（与总纲"P05 激活后才能进 G5"冲突，需一并改 G5 入口条件）。

## 3. 执行方意见

推荐 B：改动面最小且可全量留证（重建证据全部为当时段真实原件，无仿真无编造）；A 的谓词/常量改动更深且推翻"188 固定名"规划；C 放弃用户目标"执行完 0→…→7"的关键一环。

## 4. 裁决后执行序列（B 路径，供参考）

1. 修订 p05_candidate_creation（reconstruction 分支）+ p06_evidence（移除终端阻断）+ 两处回归测试 + 规范覆盖块（P05 v10/总纲 v14 补记）。
2. 相位管道两处补丁（ready run_id 绑定正则、current-restore 唯一记录路径）→ 三相位重跑（187×1 + 188×2，各含新恢复+重准备+管道）。
3. bundle-builder 重写（p05-originals/<attempt>/、ready 路径重写、创建重建包、七例/网络/依赖/manifest 组装）。
4. verify_activation_bundle 真实通过 → selector --emit-next → 排他 .next → 原子 replace → 回读 5/6/188。

## 5. 安全边界重申

- 不删除/改名/覆盖 187 或 188；不伪造任何信封；bearer 不入文件/报告；快照/防火墙/ACL 操作限于已授权范围。
