# 全面复审：P07 PLAN-CHANGE-010 回流方案

> 复审批次：`P07-PC010-20260913`（本新批次全面复审 `1/1`；本记录写入即消耗。旧批次额度不继承、不重置、不扩充；未授权定向复审。）
>
> 会话角色：第三方方案审核方；不是作者/实施方，也不是实施后独立核验方。
>
> 复审性质：对固定 P07 v3 字节的全面正式方案复审。只读检查；未运行 VM、网络、业务或回归测试，未修改需求、总纲、被审方案、代码、既有证据或 CHK。
>
> 结论：**不通过（回流待处理 4 项本批 AUD；不是对待实现代码或实施证据的否定）。**

## 1. 固定身份、授权和现场

| 对象 | 本次固定身份 |
| --- | --- |
| 被审方案 | `2026.09.12-18-实施子方案-P07清理与终检.md` v3；内容 OID `bc2c6a3e9899f62d3f4453b59ec5f610586d22a2` |
| 规范上限 | 需求 v2，内容 OID `4906941e0698ffe7476838c4479ce344ce30ef27`；总纲 v10，内容 OID `a2658be62622a373bf65bbebc50ae33e8a99654e` |
| P06 交接方案 | `2026.09.05-29-实施子方案-P06逐工具与连续情景全量执行.md` v7；内容 OID `94e9c29890680bf7e3c8120a880012442707caa4` |
| 授权 | `2026.09.13-11-自评与复审授权-PLAN-CHANGE-009-010.md`；本批只授予一次全面复审，等待上限 60 分钟 |
| Git 定位 | `main@ac1c58a9439aaa303d071a8a040e0f26db85bbdb` |
| 现场 | 写入前及写入时保留 README、P06 候选测试源码、`Logs/`、`PLAN/`、`tests/__pycache__/` 等 dirty/未跟踪对象；没有将其当作提交、P06 通过或 P07 通过证据 |

以上 OID 均以当前工作树字节用 `git hash-object` 复算；它们是内容 OID，不冒充已提交且可由 `git cat-file` 读取的对象。

旧 P07 审核及 AUD 不属于本批。为免与历史 `AUD-001` 等混淆，本记录的新发现只使用 `P07-PC010-20260913/AUD-*` 命名空间；未复用、关闭或改写历史 AUD/CHK。

`2026.09.13-12-全面复审-P06-PLAN-CHANGE-009.md` 已对 P06 **方案**作出通过裁决，但明确不认证 r3 实施证据且保持 CHK-024～027 开放。该记录不替代本方案所需的 P06 实施交接、严格聚合或独立实施后核验。

## 2. 覆盖、方法与不作出的结论

本审完整核对 P07 v3、需求 v2 的 REQ-015/016/024/025、CON-010/013/014、NON-003/011/013、RACC-015/016/024/025，以及总纲 OUT-015/016/024/025/035/038/039/042/050/052、ACC-015/016/024/025、P01→P06→P07 顺序与 80 正式项/67 累计有效结论的终检要求。交叉阅读 P01 上游基线与旧源码、P06 v7 的 r3 证据/选择/封包契约、技术建议 `2026.09.13-10`，以及独立核验 `-07/-08`；后两者仅用于形成可复核线索，未把其结论直接代入本审。

同时静态检查了上游 `9dc8054` 的 `collect_artifact`、`get_collection_results`、`run_vql`，当前 `list_flow_uploads`/文件前缀校验，以及现有成本脚本与 P06 聚合候选。源码支持本版保留“启动 → FINISHED → 结果行 → 文件清单”、上游隔离开关、文本 envelope `ok=true`、profile 只观察和五组 pair 的方向；没有把尚待实现的 r3 代码或新测量产物当作方案缺陷。

没有运行测试，也没有评估当前 `Logs/`、未提交候选源码、VM 状态或任何实施记录是否实际合格。尤其不能以 P06 方案复审、隔离单测或本报告代替 P06 新资格/五场/r3 聚合，或代替 P07 实施后的独立核验。

## 3. 需求保真六计数

P07 的直接终检责任按总纲双向追溯计 14 个稳定 ID：REQ-015/016/024/025，CON-010/013/014，NON-003/011/013，RACC-015/016/024/025。这里评的是**方案能否机械验收**，不是当前项目实现完成度；RACC 镜像独立计数。

| 指标 | 数量 | 异常 ID |
| --- | ---: | --- |
| 应承接 | 14 | — |
| 完整 | 10 | — |
| 未落点 | 0 | — |
| 语义弱化 | 0 | — |
| 不可验收 | 4 | REQ-024、RACC-024、REQ-025、RACC-025 |
| 失效或复核条件缺失 | 0 | — |

`REQ/RACC-024` 受 AUD-001、002 影响；`REQ/RACC-025` 受 AUD-002、003、004 影响。其余 10 项的方向、范围和验收动作没有发现本版新弱化：危险开关仅可在隔离的上游子进程中用于受限只读基线；当前产品不得恢复开关/兼容层；文档、agent_poc 支持边界、术语可理解性和旧接口物理清理仍须由终检矩阵逐行列证，不能以本表的“完整”预支实施认证。

全局终检还必须另行输出两个互不替代的集合：`26 REQ + 15 CON + 13 NON + 26 RACC = 80` 正式项，以及需求文档第 395 行起的 67 项累计有效结论。P07 §5只明示前者，未冻结后者的逐项追溯、互斥六计数或异常清单，故不能把本表换算成“80/80”或“67/67”的最终结论。

## 4. 本批 AUD 与最小闭合条件

### P07-PC010-20260913/AUD-001｜成本比较的执行环境口径互相矛盾

**证据。** P07 §4.1 将上游限定为“同一 VM、同一 venv、同一 mcp 2.x SDK”的 stdio 捕获，并断言双方 schema 都由“同一 SDK、同一主机”渲染；但当前侧唯一指定输入是 P06 差异化正式 HTTP 报告。P06 v7 §0/§0.1、总纲 ACC-021～023 都要求该报告由**虚拟机外**官方 SDK 客户端直连 VM 内 `28790/mcp`，不得把 stdio 或控制面当正式路径。P07 未定义当前客户端运行环境、SDK/解释器身份及与上游 VM stdio 的可换算差异，故“同一主机、唯一差异是工具集”的结论不可检验。

**影响。** REQ/RACC-024 与 ACC-024 的 schema、固定任务比较不能证明同口径；这不是允许把任务比较退化为静态注册计数或单边披露的理由。

**最小闭合条件。** 在 P07 方案中冻结双方的比较环境 manifest：执行主机身份的脱敏事实、解释器/MCP SDK 版本、传输模式、启动命令/垫片 SHA、当前 `tools/list` 原始字节及同一 fixture/Snapshot186 身份。要么让两侧在同一客户端运行时采集，要么逐维声明不可相同的 host/transport 并只比较已证明可换算的指标；后者不得把“同主机”写成事实。两侧 schema/固定任务的差异、不可比维度和阻断规则须由测量器机械验证；未满足即阻断受影响 ACC-024 维度并回流，不新增 fallback。

### P07-PC010-20260913/AUD-002｜P06 新 r3 交接未被 P07 机械绑定

**证据。** P06 v7 §0.2～0.3 将唯一将来证据根冻结为 `Logs/P06/wf-01a05d1d-p06-r3/`，要求 r3 的原始恢复/服务/响应头/fixture/tools-list、package manifest、接收账本、资格 selection、final-selection 与 aggregate 互相绑定，且拒绝旧根/跨 attempt 拼接。P07 §3只泛称等待“新资格、五场、严格聚合及独立证据核对”，§4.2.1只称“P06 final-selection”，§4.3只列五份 schema 与接收清单 hash；没有冻结 r3 根、selection/ledger/aggregate/package-manifest/资格选择的确切输入、SHA、拒绝逻辑或 P06-PC009 之后的实施证据身份。现有 `tests/p07_cost_measurement.py` 仍指向 r2 是待实施代码事实而非本 AUD 的依据，但它说明方案必须明确防止旧 epoch 被消费。

**影响。** P07 可在文字上满足“选择一条 run”，却误从 r2、未封包或未获新 P06 证据闭合的报告取数；这同时破坏 REQ/RACC-024 的 pair 身份和 REQ/RACC-025 的阶段门禁。

**最小闭合条件。** P07 必须把 P06 输入冻结为 r3 的固定相对对象及字节身份：历史根索引、资格 `qualification-payload-manifest`/ZIP/budget/selection、每个 selected run 的 `package-manifest`、接收账本、`final-selection`、`aggregate`、报告、原始 tools/list/schema、snapshot/服务/会话/fixture 原件；验证其相互 SHA、P06 v7 OID、r3 根和成功/失败状态。测量器必须拒绝 r1/r2、缺 manifest、跨 attempt、未选 run、没有 P06 新实施证据闭合或独立核验所需输入的 pair。P06-PC009 的方案通过不是替代物：实际 P06 证据闭合后才允许进入 P07。

### P07-PC010-20260913/AUD-003｜ACC-025 的失败闭环遗漏当前开放 CHK

**证据。** P07 §5要求逐条处理的历史失败闭环仅为 `CHK-001~018`，但方案标题/§9 本身来自 CHK-026/027；授权记录与独立核验 `-08` 都明确 CHK-024～027 未关闭。P07 §3的泛化 P06 等待条件不能替代终检中每个失败项的状态、证据、负责层、最小闭合和重新裁决记录。

**影响。** 即使七篇索引和 80 项链接存在，ACC-025 的“无失败遗留、失败闭环可查”仍不能机械验证；尤其 CHK-024/025 的 P06 证据前提和 CHK-026/027 的 P07 成本/最终声明可能被遗漏。

**最小闭合条件。** 将 §5 改为对当前规范链所有已知 CHK（至少 CHK-001～027，包含 019～023 的历史状态与 024～027 的当前开放状态）建立不可覆盖 ledger：ID、来源对象 OID、受影响 REQ/CON/NON/RACC/ACC、状态、原始证据、唯一负责层、最小闭合、重新裁决条件和后续独立核验引用。方案复审不关闭任何 CHK；最终记录只有在对应新原始证据及独立核验均成立时才可标 closed，否则必须保留 open/blocked。

### P07-PC010-20260913/AUD-004｜最终 80/67 追溯与六计数没有可机械消费的输出契约

**证据。** P07 §2只列五类概括承接，§5要求 80 正式项“最终证据链接”和可复算互斥计数，却没有固定 ID 集、每行字段、证据内容 OID/原始文件 hash、状态枚举、异常 ID 或 67 项累计有效结论的单独矩阵。总纲 §12.3要求 67 项沿“累计结论 → 正式条目 → OUT/ACC/Pxx → 契约”双向核对，并明确 80 计数不能替代 67；授权记录也明确作者的“五项技术处理”自评不替代 80/67。现行文字因而无法拒绝漏列 CON-014、NON-011/NON-013、旧接口物理删除或任何后续 CHK 影响的终检行。

**影响。** REQ/RACC-025 的最终计数可被人工摘要替代，不能重算“完整/未落点/语义弱化/不可验收/失效或复核条件缺失”，也不能辨别 P07 自身动作与前序 Pxx 证据。

**最小闭合条件。** 冻结最终追溯产物 schema，并要求测量/终检程序拒绝缺行、重复 ID、未知状态、未内容寻址的证据或两集合混计：

1. 80 正式项一行一个稳定 ID，包含 OUT、ACC、负责 Pxx、当前证据路径和 SHA/OID、状态、异常 ID、失败/重新裁决链接；
2. 67 累计结论一行一个序号，包含其正式条目映射、同样的最终证据与状态；
3. 两套各自的六项互斥计数与异常清单，且合计精确等于 80 或 67；
4. P07 直接责任 14 项另作可读切片，明确 REQ-015 的旧 wrapper/helper 无引用和物理删除、CON-010/013/014、NON-003/011/013 与 RACC-015/016/024/025 的证据。

## 5. 未发现新问题的方面

P07 v3 已把技术建议 10 的五个主要方向写成具体拒绝条件：上游 `ENABLE_DANGEROUS_TOOLS` 仅隔离子进程、双层 envelope 严格成功、Flow/uploads 绑定、五个 fixed pair、profile 只观察且不得从耗时猜重试、静态注册面不得成为通过型 fallback。`collect_artifact` 的立即返回与 `get_collection_results` 的状态循环也被正确区分；原生 uploads VQL 及 `_Components` 前缀检查保留了文件清单终点。P07 不新增工具、不改 130 面、双传输、单 Windows endpoint 或 P05/P06 安全边界，且将 P06 保持为进入 P07 的前置。

这些通过项不消除 AUD-001～004：它们分别约束比较运行环境、P06 r3 输入身份、失败闭环范围与全局最终追溯产物。

## 6. 最终裁决与额度

**不通过。** P07 v3 方向保真、上游任务映射和隔离/成功/配对规则具有可实施基础，但 4 项问题使 ACC-024/025 仍无法机械验收。作者应只回流 P07 方案文字和未来终检/测量的输入输出契约；不得借此改需求、总纲、P06 职责、工具面或关闭任何 CHK。修订后需要对新的内容 OID 另行取得授权复审；本批不自动授予第二次复审。

本批额度结算：全面复审已用 `1/1`；定向复审 `0/0`（未授权）；历史批次保持原状。复审在授权的 60 分钟等待上限内完成。
