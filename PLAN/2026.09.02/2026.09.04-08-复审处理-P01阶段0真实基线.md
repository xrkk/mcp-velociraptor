# P01 阶段0真实基线复审处理记录

> 记录路径：`/home/adminn/projects/mcp-velociraptor/PLAN/2026.09.02/2026.09.04-08-复审处理-P01阶段0真实基线.md`
>
> 记录属性：不可变；本文件创建后不得修改
>
> 会话角色：作者/执行方
>
> 审核性质：首次审核祖先后的后续改版复审结果引入
>
> workflow_id：`wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2`
>
> 审核批次：`P01-BATCH-3a988480-de5603cc-ee7c23e1-auth001-snapshot184`
>
> 原复审记录：`2026.09.04-07-复审-P01阶段0真实基线.md`；Git blob `cb105d721f97cfc7b01045fc8d9c78cb9af48a9c`；SHA-256 `958dc89f56840d1d7d09f654992a7626d0847bb53813460d80718569f3b9642d`
>
> 处理前方案：同目录 `2026.09.04-01-实施子方案-P01阶段0真实基线.md` v3；Git blob `3a988480ce8b28536c653ed2f0f5429cefeca70f`；SHA-256 `1046c3916dfec436b78fa858513a2c5a717b9b5c3d1662d7ba43ac122e2dcb90`
>
> 修改后待复评方案：v4；Git blob `3b28b532209fcbbee5bbc4a3746af2a21c07222a`；SHA-256 `53f73c5c6cf5cc9c0eae3dbc9c3379091d0bac653b94015a1e89e41913a6d84a`
>
> 最终方案：v4；状态 `已审核, 可以实施`；Git blob `778de584144e1c69239ffa33f4d94143f5cc2819`；SHA-256 `ce15a3c9e14da7c5caaafad8ea4718b3d650ee24b3d4b13ff1561f362301a9ea`

## 1. 身份、来源与漂移

- 复审记录绑定的需求 v0、总纲 v2、P01 v3、首次审核祖先和首次审核处理身份均已逐项回读，与复审记录一致。
- 本地审核基线 HEAD 仍为 `1f97a4f4e64d27faf07fe684d8cbfe270b57757d`；产品 tracked diff 为零，只有既有未跟踪 `PLAN/` 与 `Logs/`。
- 决策关键产品路径的内容身份沿用复审记录第3章；本轮未修改产品代码、依赖清单、README、VM、配置、凭据或快照，因此不存在影响复审结论的路径漂移。
- 原复审记录保持不可变。本记录只引入 `AUD-006`、`AUD-007`，不改写历史 `AUD-001`～`AUD-005`。

## 2. 逐项处理

### AUD-006｜恢复目标缺少唯一机器状态

- 接受度：同意。
- 风险：高；双快照说明字段可能使后续恢复误选没有测试环境的 Snapshot 183。
- 对应：AUTH-001、DEC-007、IMP/ACC-P01-001/009/010，第4.1～4.5节。
- 实际修改：P01 v4 冻结唯一 `快照恢复状态.json`、严格 epoch/phase/name/marker/单元素 allowlist schema，以及无 VMware 执行能力的 `快照恢复选择器.py`；有效参数仅供追溯。恢复入口只能消费选择器返回的 argv，非法、双 active、marker/name 不符或 `.next` 残留均在 `vmrun` 前退出。
- 激活闭环：epoch2 先写同目录 `.next`，特殊事务模式只在 canonical epoch1 合法且 next/证据绑定完整时产生 Snapshot184 候选 argv；完成唯一一次候选恢复后，复核同一 hash并原子 rename，随后由常规入口写后回读。激活后只做无副作用选择探测：184通过，183退出3且不产生 revert argv，也不追加第二次 revert。
- 最小闭合条件：已全部写入第4.1、4.2、4.5节和 ACC-P01-001/010；实施时必须产生 epoch1、next、候选恢复、原子替换、epoch2 回读和旧目标拒绝证据。
- 状态：当前有效；关闭 AUD-006。

### AUD-007｜Instant datastore 与高权限 API 身份缺少首次/重试闭环

- 接受度：同意。
- 风险：高；残留 datastore 或同名用户不能证明只有一份有效 credential，原地重签可能留下不可撤销旧证书。
- 对应：AUTH-001、CON-005/006、IMP/ACC-P01-003/009/010，第4.4节。
- 实际修改：P01 v4 冻结 `deployment_id=wf-01a05d1d-p01-deployment-001`、递增 attempt、首次空前态、固定 guest 路径和无秘密 manifest；冻结 official binary 版本/哈希、loopback-only Instant 命令、唯一 server config 发现和精确 `config api_client` 命令。
- 身份闭环：输出文件不得存在或覆盖；manifest计数必须为0；guest签发 sentinel与host `PREPARED` 先落盘；命令只执行一次；成功后仅输出身份、角色、大小和哈希，server侧证明用户/角色唯一，manifest计数变为1且host记录 `CONFIRMED`。
- 重试闭环：Snapshot184 激活前任何失败、未知或部分残留均禁止原地删除、复用或重签；仅导出非秘密证据，然后严格恢复 Snapshot183，并证明部署根、`.venv`、API output全部消失后，才以同一 deployment_id和递增 attempt重做。Snapshot184 激活后只复用快照中的 `READY_FOR_SNAPSHOT` manifest/config，永不重签。
- 最小闭合条件：已全部写入第4.4节和 ACC-P01-003/009/010；实施时必须产生空前态、manifest stage、PREPARED/CONFIRMED、单次调用、server用户角色、失败重入反例或“不适用”及激活后复用证据。
- 状态：当前有效；关闭 AUD-007。

## 3. 实际 diff、影响和继承

受影响并重新复评的项：AUTH-001/用户安装后快照要求、DEC-007、第4.1～4.5节、IMP/ACC-P01-001/003/009/010、秘密边界、恢复 fallback、P05快照交接和需求保真计数。

未受影响并继承的项：Snapshot183历史恢复事实、ACC-P01-001/002既有证据、AUD-001～005闭合结果、official v0.77.2 binary/hash、loopback-only方向、guest-only secret、当前requirements/P02锁版边界、Snapshot184创建前门、产品代码零修改、P02～P07单向依赖，以及除上述受影响切片外的需求/总纲映射。

全局不变量：单 Windows VM、root Org/唯一 Windows client、无生产或外部凭据、秘密不出 guest、无 VM 联网能力控制、无冷启动、Snapshot184验证前不激活、Snapshot183不删除、P01不修改产品代码或依赖清单。

## 4. 修改后作者完整复评

| 维度 | 结果 | 证据 |
| --- | --- | --- |
| 对象/来源/稳定ID | 通过 | P01仍为原路径，v4；IMP/ACC-P01-001～010稳定唯一；需求v0与总纲v2未变 |
| 双向追溯 | 通过 | 九个规范/授权切片均落到IMP/ACC，十个IMP/ACC均有来源和验收 |
| 可独立实施 | 通过 | 状态schema、选择器接口/退出码、部署manifest、签发命令、失败回退和激活事务均已冻结 |
| 权限与秘密 | 通过 | AUTH-001范围不扩张；single-sign、guest-only、loopback、整机回退消除残留 |
| Pxx依赖 | 通过 | P01不依赖P02～P07成功；P02～P05只消费P01事实 |
| ACC闭合 | 通过 | ACC-P01-001～010均包含前置、动作、预期、证据、通过/失败/阻断边界 |
| fallback | 通过 | checkpoint消耗异常与部署失败回退均有唯一触发、动作和退出门；不代替核心机制 |
| 范围/零越界 | 通过 | 产品代码/依赖/README仍零修改；只新增治理契约 |

需求保真确认：应承接数 `9`；完整数 `9`；未落点数 `0`；语义弱化数 `0`；不可验收数 `0`；失效或复核条件缺失数 `0`。异常 ID：无。结论：通过。

## 5. 再次第三方审核判定

本轮修改只用规范性授权、用户明确的安装后快照要求、复审最小闭合条件和已证实的 Velociraptor/VMware行为唯一确定，用于关闭本次复审已经指出的两个根因；没有另增架构方向或权限范围。

| 触发条件 | 判定 | 具体依据 |
| --- | --- | --- |
| 跨越两个独立项目或改变多业务共享契约 | 未命中 | 只修改本仓库P01治理与指定VM恢复状态，不改变外部项目契约 |
| 不可逆/高风险迁移、权限、安全、隐私或外部关键契约 | 未命中新的触发 | 原高权限身份风险被single-sign和整机回退收窄；未扩大AUTH-001；Snapshot183保留，状态切换失败不触碰epoch1 |
| 改变总体架构、核心数据流、并发一致性、恢复或切换机制 | 未命中新的触发 | 修改是对本轮已经全面复审的恢复切换机制按AUD-006最小条件补全，不另选新方向 |
| 核心路径难回滚或影响面无法限定 | 未命中 | 激活前canonical保持epoch1；失败next隔离；Snapshot183不删除；影响限于本workflow状态文件和指定VM |
| 修改后仍有多个有证据的可行方向 | 未命中 | 唯一状态、单次签发和失败整机回退均已冻结，没有待选方向 |
| 用户或项目约定明确要求再次审核 | 未命中 | 用户授权连续推进；项目约定未要求每次文字闭合都再次外审 |

审核循环熔断：未触发。AUD-006/007均为首次处理，未以同一根因复开。

结论：本记录同时构成首次审核后的作者自评闭环；无需再次第三方审核。P01 v4 状态已改为 `已审核, 可以实施`，子方案实施入口恢复。

## 6. 紧邻入口状态

- P01：v4，已审核，可以实施。
- 最早未完成实施子方案：P01。
- 长程授权：进行中。
- 下一动作：先建立/验证 epoch1恢复状态和选择器，再执行 AUTH-001允许的本地测试部署；安装、API、唯一client和依赖全部通过后拍摄并验证 Snapshot184，最后原子激活它为唯一后续恢复目标。
