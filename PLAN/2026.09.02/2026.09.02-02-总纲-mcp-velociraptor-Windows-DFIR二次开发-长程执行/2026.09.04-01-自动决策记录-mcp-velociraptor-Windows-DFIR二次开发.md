# mcp-velociraptor Windows DFIR 二次开发自动决策记录

> workflow_id：`wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2`
>
> 会话角色：作者/执行方
>
> 本文件为当前长程工作流的只追加决策记录。

## DEC-001｜确定长程状态目录与子方案顺序定位

- 问题：入口留空的状态目录和子方案顺序应采用何值。
- 阶段：PREFLIGHT。
- 依据：正文“入口参数和有效参数快照”第4、5项；总纲 v2 第7章。
- 可行方案：采用正文确定性默认值；另建自定义目录或复制顺序文档。
- 选择：状态目录采用总纲同目录下的总纲文件名加 `-长程执行/`；顺序直接稳定定位总纲第7章 P01→P07，不复制文档。
- 理由：这是正文唯一确定的默认行为，避免复制后发生漂移。
- 影响范围：仅工作流治理文件。
- 可逆性：可由用户显式修改入口参数后按正文记录变更；当前不需修改。
- 受影响 Pxx：P01～P07。
- 验证方式：目录存在；有效参数中的顺序与总纲第7章一致。
- 关联提交：尚无。

## DEC-016｜P04多source分页适配的真实后端语义

- 问题：`get_flow_results(source=None)`怎样在Velociraptor v0.77.2上稳定覆盖Flow的全部结果源。
- 阶段：P04 IMPLEMENT回流。
- 依据：真实`Generic.Collectors.File` Flow；只给artifact的`source()`返回0行；显式source分别返回数据；VQL `count()`对三行数据依次输出1/2/3；最终`p04-real-acceptance.json`。
- 可行方案：依赖无source隐式合并；一次读取所有source到内存再分页；按`artifacts_with_results`稳定顺序逐source计数并只读取覆盖全局cursor的窗口。
- 选择：第三项；累计count取最后一行，cursor仍是P02冻结的全局`v1:<offset>`。
- 理由：第一项已被真实环境否定；第二项违反不一次物化全部结果和245554字节边界；第三项保持现有公开契约并有真实跨source六行验收。
- 影响范围：仅P04 Flow结果适配器、API计数薄原语、回归测试和P04 v3事实说明。
- 可逆性：普通代码提交可回退；Velociraptor source/count行为或Flow结果源标识变化时重开对应P04/P06验收。
- 受影响Pxx：P04，P06消费最终行为。
- 验证方式：44/44测试、130工具协议、显式source三页及省略source六行无漏重，退出活动对象0。
- 关联提交：`814dc829a11b38e59288a4120b715438bd802f6b`。

## DEC-017｜P05安装后快照是后续唯一恢复基线

- 问题：P05安装四项依赖后如何避免后续恢复回到未安装依赖的Snapshot184。
- 阶段：P05入口。
- 依据：用户明确允许安装，并再次强调“安装好之后拍摄快照，以后恢复安装后拍摄的快照”；总纲v4 P05与DEC-014/015。
- 可行方案：继续恢复Snapshot184并每次重装；创建新快照但不验证即使用；安装和功能验证后创建唯一新内存快照，按既有revert/checkpoint/`vmware -x`/hostname/API/依赖功能全链验证，再原子激活。
- 选择：第三项。新快照激活前Snapshot184仍是active；激活成功后P06、P07和总体验收不得常规恢复Snapshot184。
- 理由：只有经过实际恢复验证的安装后内存快照才能同时证明环境持久和恢复路径安全；原子切换避免半完成状态污染恢复契约。
- 影响范围：P05依赖安装、VMware快照与恢复状态机；后续P06/P07/总体验收只消费新基线。
- 可逆性：新快照未激活前可回退Snapshot184；激活后不删除旧快照，但任何退回旧快照必须取得新的明确授权并重走环境验证。
- 受影响Pxx：P05、P06、P07和总体验收。
- 验证方式：新快照名称唯一、VMX checkpoint标记、一次`vmware -x`恢复、`DESKTOP-3FI41GR`、API连通、四项依赖身份与真实成功全部复验，再更新`快照恢复状态.json`的epoch/active身份。
- 关联提交：待P05完成后回填。

### DEC-010/011 提交回填｜2026-09-05T01:31:42+08:00

- P02实施提交：`4087f0a9df564a8950b5e2787df83cf3caa112d5`，message `P02 v1: add shared MCP foundation`。
- 回填范围：仅将已完成提交身份关联到DEC-010/011，不改变原决策内容。

## DEC-002｜付费外发授权规范化

- 问题：入口中的“待检查”是否阻断方案编写。
- 阶段：PREFLIGHT。
- 依据：正文“付费外发授权”；代码和项目约定只读扫描；用户指定 Codex 审核能力与 Win10VM MCP。
- 可行方案：判定需要付费授权并等待；判定不涉及额外付费外发并继续。
- 选择：规范化为 `不涉及`。
- 理由：当前必需外部交互均为用户指定运行平台能力或用户提供的本地基础设施；P01～P07 不要求 Azure OpenAI 等可选付费服务，且 `agent_poc` 明确排除在验收范围外。
- 影响范围：工作流外发门禁；不授权未来新增服务。
- 可逆性：如实施中新发现新增付费服务、用途或数据范围，立即进入 `WAITING_AUTHORIZATION`。
- 受影响 Pxx：P01～P07。
- 验证方式：外发账本逐次记录实际服务；不调用可选 Azure OpenAI。
- 关联提交：尚无。

## DEC-003｜自动恢复机制

- 问题：入口留空时如何满足停止后可恢复要求。
- 阶段：PREFLIGHT。
- 依据：正文“长程状态机和恢复”；当前平台成功创建 Goal，线程 ID 为 `01a05d1d-9e2b-7f41-b936-536d20ce55a2`。
- 可行方案：Codex Goal continuation；要求用户配置外部定时器。
- 选择：使用 Codex Goal continuation，并以本目录状态文件作为幂等恢复入口。
- 理由：平台已有可用的原生长程目标恢复能力，无需扩大权限或引入外部调度器。
- 影响范围：当前 workflow_id 的恢复与续跑。
- 可逆性：可切换为用户显式提供的外部调度器，但必须记录参数变更。
- 受影响 Pxx：P01～P07及总体验收。
- 验证方式：Goal 已创建且状态为 active；每个阶段先写检查点再执行。
- 关联提交：尚无。

## DEC-004｜P01 最小有状态基线对象与资源预算

- 问题：现有对象不足时，P01 应如何确定性取得 collection/Hunt 生命周期证据而不临场扩大影响。
- 阶段：P01 审核引入。
- 依据：AUD-005；CON-002/005/006；总纲 P01；当前需求附录 A 明确包含 `Windows.System.Pslist`。
- 可行方案：实施时临场任选；固定一个低输出 artifact；完全不创建对象并让生命周期取证缺失。
- 选择：只复用本 workflow 完全匹配对象；否则最多各创建一个 `Windows.System.Pslist` 对象，使用不匹配任何进程的固定正则和 workflow tag；每个对象 120 秒、5 秒轮询、10 行/1 MiB 捕获上限，超时只停止/取消本 workflow 新对象且不删除。
- 理由：固定 Pslist 不写 guest 文件、不联网、不调用外部程序；不匹配正则压低输出；预算和 ID 范围使副作用可界定并可复核。
- 影响范围：P01 IMP/ACC-006；不改变产品工具契约。
- 可逆性：对象不删除；快照恢复可清除 guest 侧状态，Velociraptor server 对象保留为审计证据。若实测 artifact 不存在或参数不符，P01 被阻断并回流，而不是换对象。
- 受影响 Pxx：P01，向 P02/P04 提供证据。
- 验证方式：对象前后差异、参数、真实终态、停止/取消回读和其他对象零修改扫描。
- 关联提交：尚无。

## DEC-005｜P01 上限测量网格

- 问题：怎样确保 P01 给 P02 四个具体上限建议，而不把产品值留给实施者猜测。
- 阶段：P01 审核引入。
- 依据：AUD-003；REQ-012；U-005；总纲第3.3节和 P01 交接。
- 可行方案：只写方向；预先拍脑袋固定值；冻结候选网格和证据选择条件。
- 选择：测试 page_size `1,10,50,100,250,500,1000`，记录实际行数、UTF-8字节、耗时和客户端工作集；四个建议值必须是已成功测得的具体整数，字节上限不得超过成功证据，附适用前提和重裁决条件。
- 理由：既能给 P02 可直接消费的具体值，又不会把未经运行证据的常量提前升级为产品契约。
- 影响范围：P01 IMP/ACC-006/008。
- 可逆性：P02 固化前可在同一证据规则下复测；运行版本或数据分布变化触发复核。
- 受影响 Pxx：P01、P02。
- 验证方式：完整候选表和四值建议表均非空，所有建议能追溯到成功样本。
- 关联提交：尚无。

## DEC-006｜Win10VM 测试工作副本 bootstrap

- 问题：快照恢复后 VM 中不存在当前仓库工作副本，且 VMware shared folders 未启用，如何在不使用外部网络和不丢失 Git 历史的前提下建立 P01 测试副本。
- 阶段：P01 实施。
- 依据：CON-002；REQ-001/017；ACC-P01-002～004；Win10VM 只读搜索 `C:\Users`、`C:\Tools`、`C:\Programs`、`E:\` 零命中；`\\vmware-host\Shared Folders` 不存在。
- 可行方案：从 GitHub clone；逐文件通过 MCP 写入；将当前 HEAD 制作 git bundle 后经 host-only 通道传输并校验。
- 选择：将启动基线 HEAD `1f97a4f4` 制成 git bundle，以只绑定 `192.168.204.1` 的临时 HTTP 服务传入 guest，SHA-256 双端核验后 clone 到 `C:\mcp-velociraptor`，停止临时服务，再把新副本的 origin 设为规范 URL并增加 upstream。
- 理由：保留真实 Git 历史和 HEAD；不依赖公网或凭据；传输对象可哈希、通道可立即停止；比逐文件写入更可复现。
- 影响范围：仅 VM 测试工作副本和临时 `/tmp` bundle；不修改产品代码。
- 可逆性：VM 副本可由规定快照恢复清除；临时 host 服务用后即停；宿主 bundle 位于 `/tmp`。
- 受影响 Pxx：P01，后续 P02～P07 可复用同一测试位置但必须按各阶段同步当前内容。
- 验证方式：bundle 双端 SHA-256一致；guest HEAD、branch、status、origin/upstream回读；监听端口停止后不可连接。
- 关联提交：尚无。

## DEC-007｜本地测试部署与安装后快照切换

- 问题：用户解除凭据创建阻碍并要求安装完成后拍摄新快照时，怎样避免后续恢复再次回到没有 Velociraptor 环境的旧快照。
- 阶段：P01 实施偏离回流。
- 依据：用户于2026-09-04明确授权本地测试部署并要求安装后快照；正文实施授权不覆盖凭据创建但允许用户追加授权；现有快照树中 `Snapshot 183-FakenetNG测试专用` 唯一且不存在 `Snapshot 184-Velociraptor-MCP测试基线`；官方 Instant Velociraptor 支持单机 loopback server + 自动单 client 测试环境和持久 datastore。
- 可行方案：继续以旧快照为默认并每次重装；安装后创建磁盘快照但不验证内存恢复；安装与连通性通过后创建唯一新内存快照并执行完整恢复验证，再原子切换默认恢复目标。
- 选择：旧 Snapshot 183 仅作为本次首次安装底座；使用官方 v0.77.2 Windows AMD64 binary 在 guest 的固定非临时目录建立 loopback-only Instant Velociraptor datastore，生成本地 API client 配置；Server/Client/API/唯一client前置验证通过后创建 `Snapshot 184-Velociraptor-MCP测试基线`。只有新快照唯一性、checkpoint marker、`vmware -x`、hostname 及恢复后 API 连通性全部通过，才将它激活为后续唯一默认恢复目标。
- 理由：单机 Instant 模式正好提供 root Org、单一 client 和本地 API，不引入第二个 endpoint 或外部服务；固定 datastore 与内存快照确保恢复后环境仍在；先验证再切换防止创建失败污染恢复契约。
- 影响范围：P01 快照/部署权限边界、ACC-P01-001/003～009及新增部署验收；不改变产品 MCP 架构或 P02～P07 顺序。
- 可逆性：新部署和凭据只存在于指定 VM；新快照未激活前旧 Snapshot 183 仍是回退底座。新快照创建成功后不删除旧快照，但旧快照不再用于本workflow常规恢复。
- 受影响 Pxx：P01；P02～P07只消费已验证的新恢复基线。
- 验证方式：官方 binary SHA-256、固定 datastore、进程/监听、API query、唯一 Windows client、snapshot tree唯一性、checkpoint marker、hostname与恢复后 API query全链证据。
- 关联提交：尚无。

## DEC-008｜真实 artifact 差异、限制值与局部修复边界

- 问题：root Org 的真实定义多于访谈静态估计、stdio 又在 MCP SDK 2.x 下导入失败时，P01 应如何向后序交接而不擅自扩张工具面或整体重写。
- 阶段：P01 实施与验证。
- 依据：REQ-004～006、REQ-012、REQ-017、CON-004/005/009、RACC-006；`artifact-summary.json`、`artifact-matrix.json`、`stdio-probe-*` 和 `p01-limit-measurements.json`。
- 可行方案：把全部新增别名自动加入；维持静态估计并忽略真实差异；逐项按类型/功能重叠归类且只把已证局部缺口交给负责Pxx。
- 选择：维持118个已逐项验证的CLIENT允许项；三个跨平台/重复功能CLIENT alias和一个CLIENT_EVENT alias不加入当前工具面。stdio问题只交P02做依赖/API兼容修复；四个限制值冻结为250行、245554 bytes、默认50、最大250。
- 理由：四个新增名称没有已确认的新需求且分别与文件、YARA、接口地址能力重叠或违反普通采集类型边界；SDK错误发生在现有入口导入点，不否定已实测成功的gRPC/VQL底座。限制值均落在成功样本范围内。
- 影响范围：P01报告及P02/P03/P04/P05交接；不改变需求或总纲，不修改产品代码。
- 可逆性：当需求明确新增别名、Velociraptor定义/类型变化，或传输/行结构/并发模型改变时，按本报告的重新裁决条件复测并回流负责Pxx。
- 受影响 Pxx：P02、P03、P04、P05。
- 验证方式：118允许集合与依赖矩阵双向差集0、unknown 0；四值均可追溯到成功测量；stdio stdout为空且stderr错误可复现。
- 关联提交：P01仅治理与证据，待VERIFY_COMMIT裁决。

## DEC-009｜P01 零产品变更的提交形式

- 问题：P01必须形成提交检查点，但其契约禁止产品改动，且本地治理/证据文件含用户专属VM和绝对路径，不能作为面向GitHub的跟踪内容。
- 阶段：P01 VERIFY_COMMIT。
- 依据：P01第1、8章；ACC-P01-009；AGENTS.md Ground Rules与Definition of Done；提交前staged/tracked diff均为空。
- 可行方案：把全部PLAN/Logs加入Git；不创建提交；创建不含文件内容的空提交并继续以本地哈希绑定证据。
- 选择：创建空提交 `9dc8054daf2981a28ced316ee24a6c3ab8faa9b3`，不暂存任何PLAN/Logs或受保护未跟踪文件。
- 理由：保留工作流要求的唯一提交SHA，同时满足P01零产品变更、秘密边界和“不向GitHub引入用户专属路径”的项目约定。
- 影响范围：仅Git提交历史；工作树内容和产品行为不变。
- 可逆性：提交可由后续正常历史操作处理，但本workflow不执行reset/rebase；本地证据独立保留。
- 受影响 Pxx：P01完成点，P02启动基线。
- 验证方式：`git show --stat HEAD`无文件；父提交为`1f97a4f4...`；分支仍为main；tracked diff为零。
- 关联提交：`9dc8054daf2981a28ced316ee24a6c3ab8faa9b3`。

## DEC-010｜P02 SDK、公共模块与分页方向

- 问题：P02如何在不提前实现P03/P04工具或大拆包的条件下修复stdio并形成唯一共享基础。
- 阶段：P02 LOCATE_OR_WRITE/SELF_REVIEW。
- 依据：REQ-003/008/012/018、CON-003～006、NON-001/003/012；P01阶段0报告；Win10VM只读Spike确认MCP 2.1.1提供MCPServer、CallToolResult空content/structuredContent/isError和官方stdio客户端。
- 可行方案：降级并固定FastMCP v1；同时维护v1/v2兼容层；迁移MCP 2.1.1并新增一个深层公共模块。
- 选择：唯一迁移到MCP 2.1.1/MCPServer；新增单个`velociraptor_mcp_core.py`承载目标上下文、结构化结果/错误、分页/上限和共享适配边界；cursor采用`v1:<offset>`，四项限制精确继承P01。
- 理由：消除已证导入错误且不形成旧兼容；单个深层模块足够P03/P04共用，避免框架化；offset cursor可由确定性单元测试证明无漏重且不暗称后端cursor。
- 影响范围：P02代码、测试、依赖和两README；不改变最终工具清单。
- 可逆性：实现提交可常规回退；MCP SDK主版本、响应语义或P01限制证据变化时重开受影响P02验收，不保留并行实现。
- 受影响 Pxx：P02、P03、P04及P06最终协议测试。
- 验证方式：Win10VM干净venv锁文件重建、SDK完整stdio会话、公共单元/协议测试、依赖和引用扫描。
- 关联提交：尚无。

## DEC-011｜P02首次审核意见的引入边界

- 问题：审核要求旧桥真实工具返回也由P02测试时，如何同时遵守P02的公共基础责任与P03/P04的工具迁移责任。
- 阶段：P02 AUDIT_IMPORT。
- 依据：AUD-001～010；总纲P02～P04责任边界；P01已证的MCP 2.1.1导入失败和真实gRPC能力。
- 可行方案：P02提前迁移全78工具；P02只测隔离fixture；P02验证公共组件fixture并让旧桥在SDK 2.1.1下完成initialize/tools/list、一次只读server调用和一次代表性endpoint调用。
- 选择：采用第三种；不把旧78工具宣称为结构化契约合规，全工具迁移和最终协议验收仍由P03/P04/P06负责。
- 理由：修复实际产品入口并为后续提供可用公共基础，又不把后序工具契约和业务迁移偷渡到P02。
- 影响范围：P02方案v1、旧桥启动入口、公共模块、fixture和协议测试。
- 可逆性：代码与依赖修改均可由常规Git提交回退；不更改快照或服务端数据契约。
- 受影响 Pxx：P02，并保留P03/P04/P06原责任。
- 验证方式：Win10VM中清洁venv锁重建、公共单测、完整stdio会话及旧桥两类真实调用。
- 关联提交：尚无。

## DEC-012｜P03动态参数别名、严格schema与VQL键编码

- 问题：118项artifact中存在17个含空格参数名，且全部参数必须保持原名、可省略、不得主动发送默认值或接收未声明字段，如何在MCP SDK 2.1.1下用一个生成器实现。
- 阶段：P03 LOCATE_OR_WRITE/SELF_REVIEW。
- 依据：REQ-004～006、NON-007；P01 `artifact-matrix.json`的523参数实测；`Logs/P03/wf-01a05d1d-p03/spike-sdk-dynamic-alias.json`。
- 可行方案：把参数改成Python安全名；手写118个handler；用动态Signature/Pydantic alias并严格重建参数模型。
- 选择：每个artifact生成一个`**kwargs` handler，以`p0..pN`内部名和准确alias注入Signature；注解非nullable但Python default为None，handler丢弃None；注册后把参数模型设为`extra=forbid`并重建schema；backend键只能来自已批准spec，含空格键用已实测VQL反引号编码。
- 理由：真实Spike证明准确alias可进入schema和handler、显式null/extra会在handler前拒绝、省略值可丢弃，且root Org可准确解析反引号键；该方向不改外部名称、不手写118套，也不开放任意VQL键。
- 影响范围：P03动态模块、API参数键编码、schema/协议测试；P04 Hunt必须复用同一spec和编码。
- 可逆性：全部是普通代码提交，可按P03提交回退；MCP/Pydantic主版本或真实metadata类型变化时重开受影响P03项。
- 受影响 Pxx：P03，向P04/P06提供稳定输入契约。
- 验证方式：Win10VM中全523字段schema差异、含空格真实请求、null/extra反例、118项SDK Flow创建。
- 关联提交：尚无。

## DEC-013｜P03动态旁路退出与外部依赖边界

- 问题：怎样在P03闭合动态allowlist，且不提前承担P05的外部依赖预置或P07的源码删除。
- 阶段：P03 SELF_REVIEW。
- 依据：REQ-004/007/015/019、NON-005/006、总纲P03/P05/P07和ACC-004/007；P01报告中的116项无外部依赖、Autoruns/PacketCapture两项前提。
- 可行方案：保留旧通用collect/Hunt到P04；P03物理删除全部旧函数；P03在替代验证后取消旧动态wrapper及通用collect/Hunt旁路，仅保留源码。
- 选择：采用第三项；同时取消Linux/macOS注册以闭合Windows-only。P03只验收118项真实Flow创建和初始状态，不临时联网/预置两项依赖或声称终态成功；任何连Flow都无法创建的项不计通过并按FB-003回流。
- 理由：通用`collect_artifact`或`hunt_across_fleet`仍可绕过正式allowlist，必须随动态工具退出；但物理删除和外部依赖固定分别已有P07/P05责任，不能前移。P03/P06的创建与终态验收边界由总纲ACC-007/021明确分开。
- 影响范围：P03 bridge注册、测试和两README；P04复用allowlist并迁移固定工具；P05/P06仍负责依赖和终态。
- 可逆性：decorator修改可普通回退；实际定义变化时仅回流受影响artifact，不自动扩张或删减清单。
- 受影响 Pxx：P03，交接P04/P05/P06/P07。
- 验证方式：tools/list及AST扫描、排除项call拒绝、118项Flow/Hunt差异和P03/P05/P06职责矩阵。
- 关联提交：尚无。

## DEC-014｜两项pre-flow依赖延期到P05/P06并建立安装后快照

- 问题：P03实测证明PacketCapture/Autoruns在依赖未预置时连Flow都无法创建，怎样保持Pxx顺序、用户“错误不计成功”要求和安装后快照要求。
- 阶段：P03 IMPLEMENT回流。
- 依据：总纲v2 ACC-007/P03/P05/P06；P03 FB-P03-005；P01依赖矩阵；`Logs/P03/wf-01a05d1d-p03/spike-preflow-dependencies.json`；用户要求安装完成后拍摄并在以后恢复安装后快照。
- 可行方案：把两项错误计作P03成功；P03提前联网/预置并承担P05与快照职责；P03真实通过116项并隔离两项不计成功，P05按原职责预置且创建安装后快照，P06完成最终全集。
- 选择：第三项；只修正总纲ACC-007和P03验收时序，不改变118工具、最终成功标准、Pxx数量或顺序。
- 理由：运行证据唯一证明原先“依赖只影响终态”的假设错误；第三项不伪造Flow、不降低最终验收，也不把P05责任前移。安装产生的新环境必须在P05完成后拍摄、恢复验证并原子激活，Snapshot184在此前保持唯一active。
- 影响范围：总纲v3 ACC-007/P05职责、P03 v3 IMP/ACC-P03-006、P05未来快照产出和P06最终全集验收。
- 可逆性：当前只改文档和runner；P05安装与新快照另按严格恢复契约执行。若两项在未预置时开始可创建Flow或第三项出现同类失败，重新裁决准确集合。
- 受影响Pxx：P03、P05、P06；P04不受运行依赖影响。
- 验证方式：P03一次完整118行会话形成116真实Flow+2稳定pre-flow错误且错误不计成功；P05依赖与新快照闭环；P06最终全部工具无错误。
- 关联提交：尚无。

## DEC-015｜P04两个缺失artifact的时序与安装后快照

- 问题：P04真实Spike证明`Windows.Triage.Targets`和旧wrapper声称的`Generic.Utils.KillProcess`在Snapshot184的root Org中都不存在，怎样既不伪造成功，又遵守P05依赖预置和用户要求的安装后快照。
- 阶段：P04 LOCATE_OR_WRITE实施前Spike回流。
- 依据：REQ-013/014、CON-011、ACC-013/014、P05组织结果；Win10VM `p04-file-download-spike.json`的两个精确定义0行和真实文件Flow/VFSGetBuffer证据；用户明确允许安装并要求安装后拍摄、验证和以后恢复新快照。
- 可行方案：P04把错误算成成功；P04提前安装并拍快照；P04实现和冻结接口及`DEPENDENCY_MISSING`路径但不计功能成功，P05锁定/预置四项已知依赖、回填真实成功后创建并验证新快照。
- 选择：第三项。`Generic.Utils.KillProcess`的最小定义只能接收`Pid`并在client侧调用`pskill`；`Windows.Triage.Targets`必须来自可验证来源并锁定`_BasicCollection`与内容哈希。P05未全部通过前Snapshot184仍是唯一active恢复目标；P05通过后只有经完整内存恢复验证的新快照能被原子激活，激活后不再常规恢复Snapshot184。
- 理由：这是由真实环境唯一决定的依赖时序修正；不降低任何最终成功标准，不让产品MCP自行联网安装，也不破坏P04接口冻结→P05设施/依赖→P06全量验收的顺序。
- 影响范围：总纲v4的P04/P05、ACC-013/014和FB-005/012；P04方案/实现的依赖错误；P05安装、真实回填与快照切换。
- 可逆性：P04是普通代码和文档提交；P05安装前仍可恢复Snapshot184。新快照未经验证不激活；激活失败时保持旧active状态。
- 受影响Pxx：P04、P05、P06；Pxx数量、顺序和最终工具数不变。
- 验证方式：P04两项SDK调用均为`DEPENDENCY_MISSING`、零Flow且无自动安装；P05四项依赖身份/哈希、真实Flow与终态、新快照唯一性/checkpoint/hostname/API全链；P06最终全工具无错误。
- 关联提交：尚无。

## DEC-018｜P05真实运行语义修正与Snapshot185激活

- 问题：P05实测中inventory覆盖、file-backed依赖状态、PacketCapture输出和`pskill()`返回值与方案v1的推定形态不同，且安装完成后必须把后续恢复基线切换到新快照，如何在不降低验收标准的前提下闭合。
- 阶段：P05 IMPLEMENT/VERIFY_COMMIT。
- 依据：`p05-attempt-006`～`010`原始证据、两次Snapshot185独立恢复报告、恢复选择器7/7治理测试、用户“安装后拍摄快照且以后恢复该快照”的明确要求。
- 可行方案：把真实差异视为失败并放弃对应工具；改动服务端以迎合原推定；保持业务不变量并把方案契约修正为已证实的真实语义。
- 选择：第三项，登记`PLAN-CHANGE-003`。inventory覆盖允许保留内置行并新增唯一管理员覆盖行；file-backed依赖以`materialize=false`加本地public实际字节哈希证明有效；PacketCapture按状态文本中的Windows路径取证；KillProcess按返回整数等于输入PID且目标进程消失验收。创建、双周期验证并原子激活`Snapshot 185-Velociraptor-MCP依赖与情景基线`，marker固定为Snapshot101。
- 理由：差异均由真实服务端与client运行结果唯一确定，未改变工具集合、权限、核心数据流、最终成功标准或Pxx边界；Snapshot185包含经验证依赖和fixture，是用户要求的后续唯一自动恢复基线。
- 影响范围：P05方案v2、runner/验收断言、恢复state/selector、两README及P06/P07恢复入口；不改变产品MCP权限和130工具契约。
- 可逆性：产品/测试代码可由普通Git提交回退；Snapshot185激活是治理状态切换，Snapshot183/184仅在获得新的明确授权后才能人工恢复，不能作为自动fallback。
- 受影响Pxx：P05，并向P06/P07提供安装后恢复基线和真实测试设施。
- 验证方式：四条依赖链全部成功且退出活动Flow/Hunt为0；Snapshot185两次独立恢复的逻辑步骤、断言、fixture哈希和coverage一致；旧快照候选验证退出3。
- 关联提交：`d142760e29b350b0706869296444c317dfbba936`。

## DEC-019｜P06以五个全工具综合调查构成650关系

- 问题：怎样满足130个最终工具各进入至少5个不同业务情景、每情景至少10次连续调用，同时保持情景可审阅且不建设复杂编排系统。
- 阶段：P06 LOCATE_OR_WRITE/SELF_REVIEW。
- 依据：REQ-021～023、CON-002/004/008、NON-009、总纲P06和ACC-021～023、P05已通过的单情景runner与Snapshot185。
- 可行方案：65个各10工具的小情景；随机或组合算法生成大量情景；五个具有不同真实调查目的的综合endpoint调查，每个情景完整调用全部130工具。
- 选择：第三项。五个目的分别为受害范围、勒索根因、凭据与横移、数据外传、处置后复核；每条工具—情景关系另有调查问题或允许空结果理由，形成精确`130 × 5 = 650`关系。
- 理由：这是满足下限的最小情景数，避免随机组合和65套碎片化会话；每个情景仍是独立Snapshot185恢复、独立bridge/stdio会话和完整业务调查，不靠重复标题冒充不同目的。
- 影响范围：P06情景/index/coverage manifest、runner报告扩展、五次独立恢复执行和聚合报告；不改变130工具产品契约。
- 可逆性：全部是测试源和报告逻辑，普通Git提交可回退；若第三方审核或真实执行证明某条关系没有合理调查目的，只重分配受影响P06情景，不改变产品工具面。
- 受影响Pxx：P06；P07只消费P06最终替代与回归证据。
- 验证方式：manifest机械检查每scenario工具集合等于tools/list、每工具恰有5个scenario、650关系均有理由；五份成功报告验证同进程同会话、调用数和无错误。
- 关联提交：尚无。

## DEC-020｜P06首次审核六项意见的闭合方向

- 问题：首次审核证明五场景语义、跨步骤对象身份、报告防拼接、取消ERROR分账、全量资源可行性和实际模块路径六处不能按v0唯一验收，怎样闭合而不扩张产品系统。
- 阶段：P06 AUDIT_IMPORT。
- 依据：`2026.09.05-31-审核-P06逐工具与连续情景全量执行.md`的AUD-001～006；需求REQ/RACC-021～023；P04取消生命周期；P05 runner、Snapshot185与恢复治理；Win10只读资源观察。
- 可行方案：把五份全量报告的文字说明视为足够；新增通用编排/报告平台；只在P06测试边界增加受审关系映射、固定不变量验证、host账本、资源资格和准确失败分账。
- 选择：第三项。每条关系绑定scenario step、实际参数SHA、fixture、断言和场景证据角色；固定验证层拒绝Flow/Hunt/file/PID错配；host单调账本和显式final-selection防止报告拼接；只有owned cancel-target允许expected ERROR；首次正式场景前完成独立资源资格，确定性不足回流而不盲重跑；路径统一为HEAD真实模块。
- 理由：六项都是对既定P06验收契约的最小补强，不改变130工具、产品权限、核心数据流、五情景方向、Snapshot185恢复机制或Pxx边界。作者复评需求保真10/10/0/0/0/0，未命中再次第三方审核触发条件。
- 影响范围：P06方案v1及未来scenario/manifest/runner/聚合/证据实现；不修改需求、总纲、产品接口或P07责任。
- 可逆性：当前仅文档修改；未来测试代码为普通Git提交，VM运行态由Snapshot185恢复。若资源资格或真实工具结果推翻前提，则登记PLAN-CHANGE并回流准确规范层。
- 受影响Pxx：仅P06；P07消费P06最终证据。
- 验证方式：650三向差集、身份错配与篡改反例、取消状态三分账、资源资格/低空间拒绝、五份独立成功报告和Win10VM全套回归。
- 关联提交：尚无。

## DEC-021｜P06内存采集默认1 GiB上限的契约保持修复

- 问题：P06首次资源资格attempt在磁盘门通过后，`Windows.Memory.Acquisition`上传达到1,074,790,400字节时被Velociraptor默认1 GiB单collection上限取消，怎样满足完整内存采集而不向调用方开放任意资源控制。
- 阶段：P06 IMPLEMENT资源资格回流。
- 依据：`Logs/P06/wf-01a05d1d-p06/p06-resource-attempt-001.zip`，SHA-256 `61d913af058b7f057923790e61b63309c5793811134262447b05dc31d8d4ec23`；REQ/RACC-021、CON-008；Velociraptor现有`collect_client(timeout,max_bytes)`能力。
- 可行方案：取消内存Flow并计成功；公开通用`max_bytes`工具参数；仅对精确内存artifact内部固定足够上限并保留P06资源准入门。
- 选择：第三项，登记`PLAN-CHANGE-004`。`Windows.Memory.Acquisition`内部至少3600秒timeout、8 GiB上传上限；其他artifact保持默认；公开130工具schema与权限不变。
- 理由：真实失败唯一定位为服务端默认上限，非依赖或磁盘不足。8 GiB是允许上限而非预分配，配合现有静态与实测门可完成4 GiB验收VM的完整采集，同时不提供任意资源扩大接口。
- 影响范围：P06方案v2、`velociraptor_api.py`、`velociraptor_mcp_core.py`、P02 adapter测试、P06资源资格和两README；不改变总纲、需求、工具集合、核心数据流、Snapshot185或Pxx顺序。
- 可逆性：普通代码和文档提交可回退；失败Flow运行态由下一次Snapshot185严格恢复清除。若修复后出现不同资源根因，保留新证据并重新裁决，不原样盲重跑。
- 受影响Pxx：P06，P02测试仅作为公共adapter回归；P07消费最终P06成功证据。
- 验证方式：Win10全套语法/单元/协议测试；普通artifact参数反例；从Snapshot185完整重跑资源资格并要求Memory Acquisition FINISHED及后续所有资格链成功。
- 关联提交：尚无。

## DEC-022｜Q-072正式网络回流与审核根因修正

- 问题：正式跨VM调用不能由stdio或Win10VM代执行证明；网络回流复审又发现RACC-026快照顺序和共享报告工具身份没有在唯一规范层闭合。
- 阶段：P06暂停后的需求/总纲/P02/P05/P06文档回流，AUDIT_IMPORT→SELF_REVIEW→AUDIT_DISPATCH。
- 依据：用户预设28790并要求回流五份设计；需求Q-072/REQ-026/CON-015/RACC-026；审计39的AUD-002/005；40号复审处理记录。
- 可行方向：继续沿用控制面代执行；另建网络代理；在现有同一server注册实例的启动边界增加正式stateful Streamable HTTP。
- 选择：第三项；正式host-only 28790/mcp，stdio仅内部。恢复顺序在RACC-026唯一冻结为首次外部验收→创建186候选→候选两轮恢复→原子激活。报告唯一工具清单身份由P05 schema2根tools_schema_sha256提供，P06严格消费。
- 理由：用户要求的真实外部直连只需共享注册后的传输边界；审计残留的根因分别为上游来源冲突和共享报告抽象不完整，不能用下游散落文字覆盖。
- 影响范围：需求v2、总纲v7、P02 v4、P05/P06 v5；OUT-053/054、ACC-022/023/026及各自IMP/ACC。七篇顺序、130工具与650成功覆盖不变。
- 可逆性：本轮仅文档和治理状态，未修改代码或VM；186仍为未来候选。旧审计不可变，旧版本身份和实际diff见40号记录。
- 重新裁决：同根再次复开时停止文字循环；真实运行否定协议/权限/恢复方向时按准确来源回流。凭据/API principal/ACL创建另受既定授权门约束。
- 验证方式：40号熔断及根因处理、41号作者全量自评、指定gpt-5.6-sol/high隔离审核；后续P02→P05→P06实际验收恢复须在用户暂停解除后执行。
- 关联提交：无，本轮不提交产品改动。

## DEC-023｜AUD-009报告快照身份补齐后的作者闭环

- 依据：审计42确认AUD-001～008关闭，仅AUD-009为前轮scenario/report快照身份覆盖遗漏；43号处理记录第2～5章给出实际修改、反例、完整复评与逐项路线判定。
- 选择：P05唯一冻结三阶段index配对与snapshot-evidence侧文件；P06复用。该选择补充既定恢复过程的测试证据，不改恢复命令、状态转换、激活时序、生产接口或权限，因此作者自评闭环，无需再派审核。
- 影响：总纲v8、P02 v4来源身份、P05/P06 v6；67/67/0/0/0/0，三子方案26/25/14逐ID完整。文档可逆，业务代码与VM未动，关联提交无。
- 前提/重开：实施证据若否定已冻结方向，回流准确来源；缺报告或原始恢复证据不能算验收通过。原长程授权保留，但当前实施暂停不解除。
- 验证：43号不可变处理记录blob 07285594fdb4b6213b7dc7936ce01dbf52cda5d3；记录42保持原“存在不同意见”，不冒充第三方通过。

## DEC-024｜恢复Snapshot185前运行中VM的处理与证据保留判定

- 问题：用户解除实施暂停后，VM处于运行状态且guest内`C:\mcp-velociraptor`整个目录不存在（无venv、无Velociraptor进程、无仓库副本），恢复契约禁止对运行中的VM叠加revert且不预置stop，如何进入唯一允许的恢复路径。
- 阶段：P02 IMPLEMENT恢复准备。
- 依据：快照恢复状态.json epoch3（唯一自动基线Snapshot185/marker Snapshot101.vmsn）；P05实施记录"两个独立恢复周期均只执行一次revert，marker准确后才执行vmware -x"；host端Logs/P06完整保留attempt-001、resource-attempt-001、resource-attempt-002、resource-readonly-spike四份证据；Win10VM MCP实测guest无仓库目录。
- 可行方案：直接对运行中VM执行revertToSnapshot；先vmrun stop硬停再revert；通过guest内正常关机后执行唯一revert。
- 选择：第三项。以Win10VM MCP在guest内发起操作系统正常关机（非宿主侧vmrun stop硬停，不构成"预置stop"），等待vmrun list归零作诊断参考后，按选择器输出的唯一argv执行一次revertToSnapshot，核对VMX/metadata中Snapshot101 marker后才执行一次`vmware -x`。
- 理由：guest当前状态不含任何mcp-velociraptor数据，无证据可取回；host端P06证据（含状态文件未记录的resource-attempt-002，其结果为failed，P06资格问题留待P06阶段按PLAN-CHANGE-004既定修复重新执行）已完整，整机恢复不会损失需保留内容。正常关机最干净且不违反"不预置stop、不叠加revert"两条约束。
- 影响范围：仅VM电源状态与恢复时序；不改变Snapshot185内容、恢复契约或任何产品/测试代码。
- 可逆性：恢复丢弃的guest运行态本身无仓库数据；如需回到该状态不可行，但该状态经查无保留价值。
- 受影响Pxx：P02起全部后续VM使用的恢复前置。
- 验证方式：关机后vmrun list为0；revert后VMX metadata含Snapshot101 marker；vmware -x后hostname为DESKTOP-3FI41GR且venv/Velociraptor进程恢复存在。
- 关联提交：无。

## DEC-025｜临时revert禁令下的执行策略

- 问题：用户于2026-09-05晚明确：Win10VM可能被其他工具重置快照、任务可能被打断；对主会话暂时禁止执行`revertToSnapshot`（创建快照不受限）；禁令暂不回流方案文档；要求禁令有效期间完成一切不需要revert的工作。
- 阶段：P02 IMPLEMENT恢复后全局约束。
- 依据：用户口头约束（本轮对话）；快照恢复状态.json epoch3仍为唯一自动基线；P02/P05/P06已审核契约中恢复步骤的分布。
- 可行方案：停止实施等待禁令解除；按原契约继续并自行revert（违反禁令，排除）；把工作分为"无需revert即可完成"与"必须以干净恢复为前置"两类，前者全部推进，后者产出代码/测试/文档资产并延后执行。
- 选择：第三项。禁令期间完成：P02 IMP/ACC-009/010实施、VM内验收、P02/P03/P04直接回归与提交；P05 IMP-009～011的服务安装/核查脚本、外部HTTP runner、报告schema v2等全部代码与测试资产；P06既有资产完善与内存采集修复的非正式预验证。延后项：P05 IMP-009的干净恢复后服务安装执行、IMP-011 Snapshot186候选两轮恢复验证与原子激活、P06五情景独立恢复执行及依赖它的验收。
- 理由：契约的"从Snapshot185干净恢复开始"前置无法在不能revert时满足，跳过会污染验收证据；代码与测试资产不依赖运行态，可全部就绪使禁令解除后只剩执行与验证。不回流文档符合用户指令，契约文本保持原样。
- 影响范围：仅执行时序；不改需求/总纲/子方案契约、稳定ID或验收标准。
- 可逆性：禁令解除后按原契约执行延后项即可；期间产物均为普通代码/测试/文档。
- 受影响Pxx：P02（无影响）、P05/P06（执行延后、资产照产）、P07（顺序后移）。
- 验证方式：延后项在禁令解除后仍按原ACC逐条执行；禁令期间任何VM操作前先跑环境门，发现环境非185基线特征即停并报告。
- 关联提交：无。

## DEC-026｜guest被外部工具占用时的传输与验证策略

- 问题：推进DEC-021内存修复的VM预验证时，guest内出现外部工具活动：`fakenetng-mcp`进程劫持全部出站TCP（host-only 28786拉取与curl均返回FakeNet HTML），host→guest入站被防火墙阻断（不加规则），Win10VM MCP FileSystem对约2KB以上内容静默丢字符（2000字节块哈希一致、3600字节丢7字符、21KB丢49字符），多个端口被外部uv/python进程占用。
- 阶段：P06内存修复预验证（DEC-025禁令期间）。
- 依据：本轮实测（FakeNet HTML响应、exp2000/exp4000哈希实验、39217接收器入站超时、端口7832/7296归属）；PLAN-CHANGE-004要求正式资格必须从Snapshot186重开完整attempt；用户预警VM可能被其他工具干扰。
- 可行方案：以2000字节以下分块（约54块）经MCP写入完成3文件传输后在被污染环境跑单元测试；等待guest恢复可用；放弃guest预验证，保留host侧验证与文档闭环。
- 选择：第三项。被污染环境中的验证证据不符合验收标准（正式验证必须从186干净恢复开始），传输成本高且证据价值低；不杀外部工具进程、不改guest防火墙、不与其争抢端口。
- 理由：唯一有验收意义的内存修复验证路径是从Snapshot186干净环境的正式资格重跑（P06阶段），它本身被revert禁令阻塞；当前任何guest内测试都只是预演且可能互相干扰。
- 影响范围：仅推迟DEC-021的VM侧单元预验证；host侧语法检查与全部已提交资产不受影响。
- 可逆性：无状态变化；guest临时文件与接收器已清理。
- 受影响Pxx：P06资源资格时序不变（本就排在禁令解除后）。
- 验证方式：DEC-021修复代码保留在工作树并经host py_compile；正式验证在Snapshot186干净环境按p06_resource_qualification重跑。
- 关联提交：无（修复代码待与P06聚合器schema2化一并提交）。

## DEC-027｜0.4防火墙非允许来源负例的证据形式修正（PLAN-CHANGE-005）

- 问题：0.4要求"宿主机非host-only源地址经既有路由对28790发起TCP并取得host失败+guest WFP drop双端证据"。实测：绑定源192.168.1.14对28790超时✓，但对**无来源限制的28787（Windows-MCP端口）同样超时**，且源204.1对两端口均正常（28787=404/28790=401）。判决：非允许源的包在host出站/VMware host-only传输层即被丢弃，从未到达guest，guest侧WFP证据在此拓扑下不可取得。
- 依据：本轮双端口双源对照实测（2026-09-11）；auditpol GUID启用后WFP 5152/5157零事件；pfirewall.log无记录。
- 选择：以"host侧连接失败实测+双端口对照证明丢弃层在host出站+guest规则存在且唯一（verify通过）"作为该负例的证据形式，替换不可达成的WFP日志形式；登记PLAN-CHANGE-005回流ACC-P05-007表述。不因证据形式停用服务：规则的语义限制（RemoteAddress=192.168.204.0/24）已由安装verify证实，且host-only拓扑内非允许来源天然不可达（纵深防御保留）。
- 影响：仅ACC-P05-007负例的证据形式；不改防火墙规则本身、不改产品。总体验收时如审核方异议再处理。
- 关联提交：无（文档记录）。

## DEC-028｜用户接受 principal administrator 风险（AUTH-002）

- 问题：角色矩阵v2证明仅 administrator 能使全部130工具（含Windows.Forensics.SAM.Enriched等管理类artifact）正向启动，同时administrator下org_create/gui_users/server_config管理面全部放行、无法阻止超权——命中P05 v6 0.4节停止分支。
- 用户决定（2026-09-11）："接受 administrator 风险"。
- 执行：mcp-bridge 定格为 roles=["api","administrator"]；仍与P01安装凭据分离（独立principal+独立api_client_service.yaml），网络边界（Bearer/Host/Origin/防火墙28790仅host-only子网）不降级；矩阵证据与接受记录进入P06验收记录。
- 缓解事实：principal仅经VM内服务进程使用，不出VM；管理面VQL不经产品130工具暴露（工具清单不含org/user/config管理工具）。
- 关联：EVT-073、C:\VelociraptorMCP\logs\principal-capability-matrix-v2.json。

## PLAN-CHANGE-006｜防火墙非允许来源证据形式修订

- 触发证据：独立核验CHK-005/候选A（2026.09.12-01）；DEC-027实测——host绑定非host-only源(192.168.1.14)对28790和28787均超时，guest WFP 5152/5157及firewall log均无事件；host OS不允许跨接口源发包。
- 原契约：总纲ACC-026/P05 0.4节/ACC-P05-007要求host失败+guest WFP drop+handler零增量三端证据；OS不能发包时必须阻断并保持服务未启用。
- 新契约：在host OS网络栈不允许跨接口源发包的确定环境中，防火墙非允许来源负例允许以(a)host侧绑定源地址连接失败实测+(b)双端口对照（受规则保护28790与不受保护端口同时不可达）+(c)防火墙规则verify（唯一规则、精确RemoteAddress）三要素闭合；guest WFP日志在此拓扑下不可达不构成自动阻断。
- 受影响稳定依赖边：`REQ-026+CON-015+RACC-026→OUT-053/054→ACC-026→P05 ACC-P05-007→P06 ACC-P06-006`。
- 受影响切片：总纲5.1/5.7、OUT-053/054、ACC-020/023/026、第10章FB-013/014；P05 0.4、IMP-P05-010/011、ACC-P05-007、FB-P05-007；P06正式网络验收。
- 责任层级：总纲+P05；作者自评闭环（未命中再次第三方审核触发条件：安全边界实质等价，仅证据形式因环境拓扑事实调整）。
- 恢复条件：若未来环境允许跨接口源发包或guest可取得WFP事件，应恢复三端动态负例标准。

## PLAN-CHANGE-007｜运行principal长期administrator风险接受边界

- 触发证据：独立核验候选B；DEC-028/EVT-073实测角色矩阵——reader/analyst/investigator对SAM.Enriched等管理类artifact的collect均返回空，仅administrator成功；administrator下org_create/gui_users/server_config管理面全部放行。用户2026-09-11明确接受风险。
- 原契约：总纲P05职责和ACC-026要求最小运行principal、管理面超权负例拒绝、长期SERVER_ADMIN列为失败。
- 新契约：在用户明确接受风险的前提下，允许运行principal使用api+administrator角色；缓解条件固定为(a)principal仅经VM内服务进程使用不出VM、(b)网络边界（Bearer/Host/Origin/DNS rebinding/防火墙host-only）不降级、(c)管理面VQL不经产品130工具暴露、(d)每次验收记录须注明该风险接受及其缓解事实。
- 受影响稳定依赖边：`REQ-012+REQ-026+NON-003+CON-015+RACC-012/026→OUT-012/042/053/054→ACC-012/026→P05权限矩阵→P06全部正式调用`。
- 受影响切片：总纲2.2、4.2、5.5、OUT-012/053/054、ACC-012/026、第10章FB-013；P05 0.4、IMP-P05-009/010、ACC-P05-007。
- 责任层级：总纲+P05；作者自评闭环（用户显式风险接受为规范边界变化依据）。
- 恢复条件：若Velociraptor后续版本提供更细粒度权限或用户撤回风险接受，须回流至最小principal标准。

## PLAN-CHANGE-008｜总纲对象版本身份统一

- 触发证据：独立核验候选C；总纲文件页首写"版本v8"但含v9变更记录和"测试VM实例契约（v9）"2.0节。
- 原契约：总纲页首版本号应与实际内容版本一致。
- 新契约：总纲页首版本号修订为v10（v9测试VM迁移+本轮回流的PLAN-CHANGE-006/007合并为v10）；总纲页首添加当前blob标识。
- 受影响稳定依赖边：`需求v2→总纲身份→P02/P05/P06回流→总体验收对象身份`。
- 受影响切片：总纲页首、1.1、2.0、版本变更记录。
- 责任层级：总纲；作者自评闭环（纯版本身份修正，无语义变化）。
