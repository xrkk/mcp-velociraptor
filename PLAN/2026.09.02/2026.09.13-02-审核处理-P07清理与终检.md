# 审核处理：P07 清理与终检 v2（AUD-001/002/003 处置 + 第4轮 CHK 引入）

> 会话角色：方案作者（主会话）
> 处理日期：2026-09-13（Asia/Shanghai）
> 对象：`2026.09.12-20-审核-P07清理与终检.md`（审核结论：有条件通过，AUD-001/002/003）
> 受审方案：`2026.09.12-18-实施子方案-P07清理与终检.md`（审核时 SHA-256 `4b4d5871…`；处理后 `6406c7d3…`）

## 1. AUD-001（实施前闭合）：全回归清单扩充

处置：方案第 6 节改写为"AUD-001 扩充"版——显式列入 `tests.test_p02_transport`（含 `test_stdio_and_http_share_identical_toolset` 同一性终检落点）及 P02/P03/P04 全部可运行模块，并加"任一模块不可运行时逐模块记录理由，不得静默省略"条款。
执行情况：第 4 轮核验（CHK-022）指出实施记录实际只列 9 个模块、遗漏 4 个且未给排除理由。**作者接受**：2026-09-13 已在 VM 以 `VELOCIRAPTOR_API_CONFIG` 逐模块重跑全部 13 个 test 模块，13/13 全绿（test_p06_aggregate 以其路径式调用运行 5/5；详见本记录第 4 节）；遗漏系实施记录书写遗漏而非未执行，本处理记录如实补正。

## 2. AUD-002（实施时闭合）：垫片装置留存与回退口径入正文

处置：`Logs/P07/upstream78-launcher.py`（FastMCP→MCPServer 别名垫片，SHA-256 `ccd19174…`）与 `upstream78-probe.py`（`bcc4edd3…`）内容寻址归档；方案 4.3 节将其列入输入清单并披露；垫片不可接受时的回退口径写入正文。
执行情况：已按处置执行（第 4 轮核验确认装置在档、上游源码与 git 基线 blob 逐字节一致）。

## 3. AUD-003（报告时闭合）：阻断边边界裁决入正文

处置：方案 4.2 节新增"边界裁决"段：schema 维度新建上游同口径运行捕获、任务维度如实披露无可比基线，论证该披露满足 REQ-024"如实比较"。
执行情况：已按处置执行。**但第 4 轮核验（CHK-021）裁定该裁决构成对总纲 ACC-024 明确阻断边（"被阻断为P01无可比运行基线"）的擅自改读**：诚实披露不能替代双边任务比较，也不能自行把阻断条件变为通过条件。作者接受该裁定，处置升级为：**实测取得可比的上游固定任务运行基线**（上游 78 桥在同 VM/同 SDK 下执行同一固定调查任务，实测调用/重试/输出量，与当前产品同口径比较），使阻断边不再命中，而不是改读阻断边。结果另行记录于 P07 实施记录增补与本处理记录后续修订。

## 4. 全回归 13 模块重跑结果（2026-09-13，VM venv，`VELOCIRAPTOR_API_CONFIG=api_client_service.yaml`）

| 模块 | 结果 |
| --- | --- |
| tests.test_p02_core | 14/14 OK |
| tests.test_p02_stdio | 2/2 OK |
| tests.test_p02_transport | 25/25 OK（含同一性终检） |
| tests.test_p03_dynamic | 6/6 OK |
| tests.test_p03_stdio | 4/4 OK |
| tests.test_p04_contract_golden | 1/1 OK |
| tests.test_p04_fixed_tools | 15/15 OK |
| tests.test_p04_registration | 3/3 OK |
| tests.test_p05_contracts | 5/5 OK |
| tests.test_p05_scenario_runner | 9/9 OK |
| tests.test_p05_service_script | 7/7 OK |
| tests.test_p06_contracts | 7/7 OK |
| tests.test_p06_aggregate（路径式调用） | 5/5 OK |

注：`tests.test_p06_aggregate` 在包式调用下因历史导入风格（`import p06_aggregate_reports`）失败，属调用方式差异而非模块缺陷，以路径式调用运行为准（VM 过程留痕 `Logs\P06\r4reg_*.txt`）。

## 5. 第 4 轮 CHK 一并引入本处理

CHK-019/020/021/022/023 的修复按各自最小闭合条件执行：CHK-022 由本记录+全回归+复审闭合；CHK-019 由聚合器强化（提交 `5c52f32`）+ 新证据根全 attempt 登记闭合；CHK-020 由 Snapshot186 资源资格重跑闭合；CHK-021 由上游固定任务基线实测闭合；CHK-023 由绑定新 HEAD 的最终验收闭合。各项证据以新实施/验收记录为准。

## 6. 复审请求

修改后的方案（当前 SHA 另计）连同本处理记录提交 sub-plan-reviewer 复审；复审结论回填后进入新实施/验收记录。
