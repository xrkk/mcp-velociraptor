# 全面复审：P06 PLAN-CHANGE-009 回流方案

> 复审批次：`P06-PC009-20260913`（本新批次全面复审 `1/1`，本记录写入即消耗；不继承、重置或扩充旧批次额度；未授权定向复审）
>
> 会话角色：第三方方案审核方；不是作者/实施方，也不是实施后独立核验方
>
> 复审性质：对固定 P06 v7 字节的全面正式方案复审。只读检查；不执行 VM、网络、业务或回归测试，不修改被审方案、需求、总纲、代码、既有证据或 CHK。
>
> 结论：**通过（仅方案层，可按既定门禁进入受控实施）**

## 1. 固定身份、授权和现场

| 对象 | 本次固定身份 |
| --- | --- |
| 被审方案 | `2026.09.05-29-实施子方案-P06逐工具与连续情景全量执行.md` v7；内容 OID `94e9c29890680bf7e3c8120a880012442707caa4` |
| 规范上限 | 需求 v2，内容 OID `4906941e0698ffe7476838c4479ce344ce30ef27`；总纲 v10，内容 OID `a2658be62622a373bf65bbebc50ae33e8a99654e` |
| 授权 | `2026.09.13-11-自评与复审授权-PLAN-CHANGE-009-010.md`；新批次只授予本次全面复审，等待上限 60 分钟 |
| Git 定位 | `main@ac1c58a9439aaa303d071a8a040e0f26db85bbdb` |
| 现场 | 写入前保留既有 README、P06 候选测试源码改动、`Logs/`、`PLAN/`、`tests/__pycache__/` 等 dirty/未跟踪内容；本复审不把它们当作已提交或实施通过证据 |

上述 OID 均由当前工作树字节以 `git hash-object` 复算。它们是**内容 OID**，不是本地对象库可由 `git cat-file` 读取的已提交 blob；本记录据此固定输入，未把该区别误写为已提交事实。

旧 `P06-BATCH-df4e0372-de5603cc-43784f06-d142760e` 的额度和历史 AUD 不属于本批。为避免编号混淆，本批新 AUD 命名空间预留为 `P06-PC009-20260913/AUD-*`；本次没有新 AUD，未复用、关闭或重新裁决任何历史 AUD。

## 2. 范围、方法和不作出的结论

本审完整阅读被审 P06、需求 v2 的 REQ-021/022/023/026、CON-002/004/008/015、NON-009/010、RACC-021/022/023/026，以及总纲 ACC-021/022/023/026、P05→P06→P07 责任边和恢复/网络边界。交叉读取 P03 的 118 动态 invocation（含九项 `resource_sensitive`）、P04 的 12 固定工具生命周期和 P05 的 schema v2、P06_ACTIVE、Snapshot186 与恢复原件契约；也静态检查了 runner、聚合器、资格与资源门候选源码/反例，判断方案可落到既有测试设施。

未运行任何测试或远端/VM 操作。当前候选源码仍可见旧 P06/r2 根、可配置入口或尚未生成的 r3 资产；这是 P06 将实施和验证的对象，不是把“待实现代码缺失”误判为方案缺陷。本审也不认证这些候选源码、任何 `Logs/`、正式资格、逐工具/五情景运行或最终聚合。

## 3. 方案完整性与需求保真

P06 v7 保持 118 动态 + 12 固定 = 130 工具、五个不同调查目的、至少 650 工具—情景关系、Snapshot186/schema3/epoch4、外部官方 SDK 直连 VM 内 `28790/mcp`、单场单 HTTP 会话/服务身份、失败覆盖清零和 P07 后置。它没有改工具面、权限、产品接口、P05 的恢复协议或正式入口安全边界，也未把 Win10VM MCP 变成产品调用代理。

本次将 RACC 镜像独立计数，P06 直接应承接 14 项：REQ-021/022/023/026，CON-002/004/008/015，NON-009/010，RACC-021/022/023/026。计数如下。

| 指标 | 数量 | 异常 ID |
| --- | ---: | --- |
| 应承接 | 14 | — |
| 完整 | 14 | — |
| 未落点 | 0 | — |
| 语义弱化 | 0 | — |
| 不可验收 | 0 | — |
| 失效或复核条件缺失 | 0 | — |

具体而言，逐工具真实 Flow/Hunt/file 成功链、错误独立分账和空结果条件满足 REQ/RACC-021 与 CON-008；五个固定业务情景、manifest→scenario step→report call/assertion 三向差集、同场去重与失败整场清零满足 REQ/RACC-022；受索引的声明式 runner、官方 SDK、同会话连续性和 stdio coverage 为空满足 REQ/RACC-023、CON-004 与 NON-009；外部 `28790/mcp`、Bearer/Host/Origin 回归、服务/会话观察 join 和控制面不得代理正式调用满足 REQ/RACC-026、CON-002/015 与 NON-010。

## 4. 对 PLAN-CHANGE-009 与既往建议的复核

`2026.09.13-10-技术复核-PLAN-CHANGE-009-010回流.md` 仅是技术建议而非本审上限。本次按当前 P06 内容 OID 独立复核后确认，该建议中 P06 的两个可选择解释已经由 v7 §0.3 固定，而不是留给实施者临场裁决：

1. 每个 attempt 的 `package-manifest.json` 固定 POSIX 相对名称、普通文件/链接拒绝、UTF-8 字节序排序、大小和 SHA-256、canonical JSON 和 manifest 自身/传输 ZIP 排除；账本与 final-selection 同时绑定 manifest 路径、manifest SHA 与 `package_sha256`。资格包再以 payload manifest → ZIP → budget → final manifest 的两层顺序消除自引用。缺成员、附加成员、路径不安全或哈希不符均 fail-closed，成功 selection 不可接受。
2. `p06_resource_policy.json` 将每个固定情景的每个工具步骤逐一列为 `admission`、`dependent` 或 `bounded`，并冻结 reason、qualification label、owner step 和索引 SHA。九项资源敏感动态工具中八项普通启动、PacketCapture start/stop、Triage、fixture collect、download 合为准确 13 个 admission；相关 status/results/files 为 explicit dependent，其余 bounded 也必须给出安全参数/有限输出理由。双向差集、漏分类、错 owner、缺读数、缺 deadline 或产品调用后才拒绝均为失败且 coverage 为空。

该封包和策略规则把 CHK-024/025 所指出的“只信摘要/顶层哈希”和“由实施者决定哪些步骤算大”的风险转为可机械拒绝的将来验收条件。资格仍必须以外部正式 HTTP 会话执行，Windows MCP 限于固定只读资源观察，故不与 CON-002、REQ-026 或 P05 的单 server 边界冲突。

§0.2 已明确把将来证据根唯一迁移到 `Logs/P06/wf-01a05d1d-p06-r3/`，并逐项保留旧根索引和开放 CHK；§4、§8 中遗留旧根文字仅作被该前向覆盖条款取代的历史/旧产物表述，不构成另一条可选择正式根。实施时必须实际把所有 producer/consumer 常量和无 CLI 覆盖的入口统一到 r3，不能沿用旧 r2 证据冒充新 epoch。

## 5. AUD、关闭条件和阶段边界

本批新 AUD：**0 项**。因此没有可列的 `P06-PC009-20260913/AUD-*` 证据或最小闭合条件；异常规范 ID 也为无。

这不是对实施结果的放行声明。CHK-024～027 保持开放，且本记录不关闭它们：

- CHK-024/025 的最小闭合仍须在 r3 中生成新的原始恢复/服务/会话/资源/包证据，外部 HTTP 资格和逐步骤资源门/时限实际生效后重跑受影响场景并严格聚合；
- CHK-026/027 仍属于 P07 的可比双边成本与最终全阶段证据/声明边界，不能由 P06 方案通过替代；
- 任何 P05 Snapshot186、认证、服务、恢复、资源资格或工具业务失败，仍依 P06 FB-P06-001～007 回至唯一负责层，不能用 stdio、控制面代理、旧快照、步骤级重跑或历史 r2 证据降级通过。

## 6. 最终裁决与额度

P06 v7 在固定需求 v2 和总纲 v10 上具备完整、可机械验收、可回流且范围受限的实施契约；**方案复审通过**。紧邻工作只能按 P06 固定顺序先落实 IMP-P06-008 及其反例，再进行正式资格、逐工具/五情景、聚合和最终回归。该裁决不把任何候选代码或运行证据认证为通过，不改变被审方案文本状态，也不授权关闭 CHK。

本批额度结算：全面复审已用 `1/1`；定向复审 `0/0`（未授权）；历史批次额度保持原状。复审在授权的 60 分钟等待上限内结束。
