# P04 固定工具与生命周期实施记录

> 会话角色：作者/执行方
>
> 记录性质：可追加实施记录；保留历史状态，不覆盖旧轮次
>
> workflow_id：`wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2`
>
> 子方案：`2026.09.05-15-实施子方案-P04固定工具与生命周期.md` v3；blob `0ec0b4bcc15d78760768d3dc16cfe6822b17c5f2`；状态`已审核, 可以实施`
>
> 总纲：`2026.09.02-02-总纲-mcp-velociraptor-Windows-DFIR二次开发.md` v4；blob `43784f06d8f253061579815b93ef3bc743555691`
>
> 首次审计：`2026.09.05-17-审核-P04固定工具与生命周期.md`；blob `9075e7a39b4ea8442843821d63d540db69e1738f`
>
> 审核处理：`2026.09.05-18-审核处理-P04固定工具与生命周期.md`；blob `c9399a3a995169290f83b6434e44fa248b0a14ee`
>
> 最终作者自评：`2026.09.05-20-自评-P04固定工具与生命周期-v3.md`；blob `189d910f5dc0be341e284194ae8aba12023205fc`
>
> 实施授权：当前workflow的P01～P07长程授权进行中

## 1. 实施基线与范围

- 分支：`main`
- 前置HEAD：`1640284ff42bb5d5265afd173d0837f82b5251bb`
- 完成提交：`814dc829a11b38e59288a4120b715438bd802f6b`
- 验收环境：Win10VM `DESKTOP-3FI41GR`，当前恢复基线仍为`Snapshot 184-Velociraptor-MCP测试基线`；P04未安装依赖、未创建或切换快照。
- 产品提交边界：固定工具服务、公共模型/API薄原语、单候选原子注册、P04直接测试和两份README；`PLAN/`、`Logs/`、凭据、VM fixture及下载物均未提交。
- 需求保真：应承接7组，完整7，未落点0，语义弱化0，不可验收0。

## 2. 实施结果

1. 新增12个固定工具：`run_vql/start_hunt/get_hunt_status/stop_hunt/get_flow_status/get_flow_results/list_flow_files/download_flow_file/cancel_flow/collect_file/collect_forensic_triage/kill_process`；生产工具面固定为118动态+12固定=130。
2. 全部成功和错误经MCP structuredContent返回；工具输入/输出schema、错误过滤、分页行数与245554字节上限均由独立golden和协议测试覆盖。
3. Flow结果支持显式source及省略source的全局cursor：按`artifacts_with_results`逐source计数和窗口拼接，不一次物化全量数据。真实验收发现VQL `count()`输出累计序列后修正为取最后一行，并新增回归测试。
4. 文件身份由完整components/accessor/upload_id确定；精确重复去重、冲突拒绝；下载目录按flow哈希隔离，拒绝reparse/symlink/junction和越界，临时文件写入、fsync、原子`os.link`发布且不覆盖已有文件。
5. Hunt只对唯一Windows client创建暂停Hunt、普通Flow并精确`hunt_add`；停止Hunt不隐式取消Flow，Flow取消保持独立生命周期。
6. `Windows.Triage.Targets`与`Generic.Utils.KillProcess`在Snapshot184缺失时稳定返回`DEPENDENCY_MISSING`且零Flow；真实成功责任按既有总纲留给P05。
7. 旧11项过渡注册退出生产面，旧函数体按总纲留到P07物理删除；联网限制/恢复及Quarantine/Sinkhole类工具不存在于最终工具面。

## 3. 失败、修正与重跑

- 生命周期Spike补充真实非终态`IN_PROGRESS`，并确认Hunt停止与Flow取消是两条独立操作。
- Snapshot184没有pytest，P04测试入口改用环境中实际可执行的`unittest`；依赖安装仍归P05。
- 真实多source验收首次发现只给artifact读取为空；显式按source拼接后，第二次又发现累计`count()`误取首行导致第二页为空。取最后累计值后，从单测、完整回归、stdio协议到真实Velociraptor验收全部重新执行并通过。
- 所有失败轮次均不计成功；失败运行由验收器finally精确回收自有Flow/Hunt，最终证据`active_at_exit`为空。

## 4. 最终验证与证据

| 验证 | 结果 | 证据 |
| --- | --- | --- |
| 完整测试 | 44/44通过，39.484秒 | `Logs/P04/wf-01a05d1d-p04/p04-final-tests.txt`，SHA-256 `85459cb68900e45f92a922fafb7ca6f3ef63e76700fe6238867f43dfbe92bbef` |
| stdio协议 | 130/130工具；两次清单摘要一致；移除项及联网操作0暴露 | `p04-protocol.json`，SHA-256 `bb474bd4b945033cf3e6138dff92519fcaabefd1e13dd24278d38a689554d0c6` |
| 真实生命周期 | Flow/Hunt/file/download/cancel、多source 6行、三页cursor及同名异目录全部通过；退出活动对象0 | `p04-real-acceptance.json`，SHA-256 `79d7fdd0cae790d3179d2c4484932489d32949b72e5b2657c1860e1358df581f` |
| 源码身份 | 15个提交文件host/guest SHA-256逐项相同 | Win10VM最终哈希回读与本地`sha256sum`转录 |
| 证据回传 | 只含5个最终证据文件；接收器单次请求后退出 | guest/host zip SHA-256 `ec20a041443cdadae276673193e7255a6ae6317cb6c48b2e67855c684e18ae30` |
| Git边界 | cached diff-check通过；提交只含15个产品/测试/文档文件 | 提交`814dc829a11b38e59288a4120b715438bd802f6b` |

## 5. 最终IMP/ACC状态

| 对象 | 最终状态 | 证据摘要 |
| --- | --- | --- |
| IMP-P04-001～004 | 通过 | 公共契约、API薄原语、12工具服务及118+12联合原子注册已落地并由独立golden/故障注入覆盖。 |
| IMP-P04-005 | 通过 | Win10VM完整测试、真实stdio和生命周期验收全部通过；最终无遗留对象。 |
| IMP-P04-006 | 通过 | 两README已同步130工具、固定工具语义、Windows-only及P05依赖边。 |
| ACC-P04-001～004 | 通过 | Flow/file/Hunt/VQL正常、分页、边界、取消和错误路径可观察且闭合。 |
| ACC-P04-005～006 | P04部分通过 | 两接口和`DEPENDENCY_MISSING`/零Flow行为通过；安装与真实成功按总纲由P05闭合。 |
| ACC-P04-007 | 通过 | tools/list严格118+12，旧注册与联网操作0暴露，启动故障无部分服务窗口。 |

P04当前没有未开始、进行中、不通过、被阻断或待复审项。P04实施授权状态：已完成；workflow长程授权继续覆盖P05～P07。P05必须在四项依赖安装和验证后创建、验证并原子激活新的安装后测试快照，后续恢复不得再使用Snapshot184。
