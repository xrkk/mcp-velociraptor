# 无人值守职责、传输授权与自评实施门

> 创建日：2026-09-13；不可变授权承接记录；Codex 主会话角色为规划/作者/修复/调度方。
> 起点：main HEAD `ac1c58a9439aaa303d071a8a040e0f26db85bbdb`；保留全部当前 dirty 改动及历史 PLAN/Logs。

## 当前用户指令

用户已授权解除18号记录的固定源码向既定验收VM传输阻断，并重新划分职责：子智能体负责实施与核验，主会话负责规划、修复、派遣等其他任务。最新指令为“按此职责划分, 完成所有未完成的任务. 开始无人值守, 需要决策时由你自己决策. 不需要三方评审, 方案你自评通过后即可实施”。

本记录对未来执行的治理优先级高于旧总纲/P05/P06/P07及17/18号记录中的“必须再次方案第三方复审”条款：方案由主会话完整自评通过后即可实施，不再派遣方案第三方复审，不产生或关闭新的AUD、不重置旧批次额度。既有方案审核及结论保持原件，不改成新版本的通过证据。该调整不取消实施后的独立全面核验、需求保真、阶段顺序、恢复门禁或真实成功标准。

## 分工与隔离

- 主会话：规范及PLAN-CHANGE、完整自评、调度和验收条件、问题归因与修复、边界裁决及最终汇总。修复变更同样交实施方重跑并接受独立核验。
- 实施子智能体：按已经自评的契约完成建设、测试、环境操作和原始证据归档；发现失败反馈主会话，不自行扩大产品接口、权限、来源、目标或降低验收。
- 独立核验子智能体：另起无上下文新智能体，未参与其核验对象的规划/实施/修复；只读核验及不可变CHK/结论，不整改、不代补证据、不自审。
- 每次派遣模型固定 gpt-5.6-terra / xhigh，fork_turns=none，任务说明提供必要路径/背景/限制，主会话每个任务等待上限60分钟。观察超时不等于执行结束，须复核同一活句柄，不重复启动。
- 单一VM独占调度：仅获当前执行窗口的实施/核验智能体操作 `192.168.204.232` / `DESKTOP-3FI41GR`；其余主会话和智能体不同时同步、恢复、重启或运行业务。不同文件任务才可并行；源冻结后测试不以新源冒充旧证据。

## 传输及受保护范围

用户明确授权经 `192.168.204.1:28786` 单请求host-only通道向 `192.168.204.232` 的 `C:\mcp-velociraptor` 同步本轮固定P05/P06测试源码、JSON输入及两README，双端SHA核对后关闭监听并执行Windows测试；不包含凭据、PLAN、Logs、二进制或任意新增目录。18号的具体授权缺失已解除；环境审批仍按正常流程申请，不绕过新拒绝。

无人值守裁决仍限定当前需求/总纲共同产品边界。保留用户Snapshot187、其他VM、既有快照与不可变证据；不得泄露secret、清理未知进程/trace/Hunt或把取消、错误、局部测试当作全量成功。若操作需要新的敏感权限或边界扩展，先保留证据并在既有授权内推进可独立完成项，不以无人值守指令消除安全限制。

## 完成门

目标不缩减：P05新可恢复基线初验及双恢复/原子激活，P06新资源资格/逐工具/五场650关系及严格封包聚合，P07成本/清理/文档与80正式条目、67累计结论双向追溯，实际提交与总体验收，最后对新HEAD独立全面核验。任一必要项缺证据不标记总目标完成。

本记录仅承接授权和职责，不是方案自评通过记录、实施完成或独立核验通过声明。
