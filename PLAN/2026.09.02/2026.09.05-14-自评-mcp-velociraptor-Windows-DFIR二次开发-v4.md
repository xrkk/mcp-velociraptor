# mcp-velociraptor Windows DFIR 二次开发总纲 v4 作者自评

> 会话角色：作者/执行方
>
> 对象：`2026.09.02-02-总纲-mcp-velociraptor-Windows-DFIR二次开发.md` v4
>
> 对象 Git blob：`43784f06d8f253061579815b93ef3bc743555691`
>
> 对象 SHA-256：`0e79f3ac01a7542167d7b1e14d0127b1efb967ff799d71de3ef505b9d80f8b62`
>
> 首次审核祖先：`2026.09.02-04-审核-mcp-velociraptor-Windows-DFIR二次开发.md`
>
> 首次审核引入：`2026.09.03-01-审核处理-mcp-velociraptor-Windows-DFIR二次开发.md`
>
> workflow_id：`wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2`

## 1. PLAN-CHANGE-002处理

P04在Win10VM的Snapshot184上完成最小受控Spike。`Windows.Triage.Targets`与`Generic.Utils.KillProcess`的root Org精确定义查询均为0行，而`Generic.Collectors.File`已以含空格路径的32字节fixture实证Flow、upload枚举、`_Components`、`VFSGetBuffer`分块下载及双端SHA-256一致。Flow取消和Hunt停止/主Flow分离也有真实状态证据。

v4仅修正已确证的依赖时序：P04实现全部12工具和两项明确`DEPENDENCY_MISSING`路径，不把错误计成成功；P05依用户明确授权锁定、预置四项已知依赖，回填真实成功后才创建和验证安装后快照；P06仍对最终全部工具要求无错成功。不改变Pxx数量、顺序、工具面或最终标准。

## 2. 需求保真与双向追溯

| 维度 | 结果 |
| --- | --- |
| 当前有效结论 | 应承接66，完整66，未落点0，语义弱化0，不可验收0，异常ID无。 |
| 正式契约 | 25 REQ、14 CON、13 NON、25 RACC的OUT/ACC/Pxx稳定映射保持；无ID重编、复用或删除。 |
| 方向前提 | G039/Q-061明确允许triage依赖缺失时报告安装要求；REQ-014的指定artifact缺失是P04新证事实，用户已授权安装且要求新快照。 |
| 验收语义 | P04缺失错误不计成功；P05必须真实Flow回填；P06必须全量无错。无降标或将报错折算为覆盖。 |

## 3. 可交接性、Pxx独立性与快照门禁

- P04仍冻结最终12个固定接口、schema、状态、错误和旧注册退出；P05不改产品接口。
- P05的原有“允许依赖预置/确定准备步骤”职责被精确化为四项已知依赖，没有新增阶段或倒置P04/P05。
- `Windows.Triage.Targets`不允许仿造，必须来源可验证且锁定`_BasicCollection`和内容哈希；`Generic.Utils.KillProcess`最小定义只允许`Pid -> pskill`，不扩展命令执行。
- Snapshot184在P05完成前仍是唯一active目标。新快照必须经唯一匹配、checkpoint marker、`vmware -x`、hostname和API连通性完整恢复验证才原子激活；激活后不再常规恢复Snapshot184。

## 4. 再次第三方审核触发判断

| 触发项 | 判定与证据 |
| --- | --- |
| 跨项目/多业务共享契约 | 未命中；只在当前小模块、单VM和原P04/P05范围内。 |
| 不可逆迁移、权限、安全、隐私或外部关键契约 | 未命中；新快照未验证不激活，安装前Snapshot184可恢复，无外部业务数据。 |
| 总体架构、核心数据流、并发/恢复/切换机制 | 未命中；仍为同一stdio bridge -> root Org -> 唯一client，快照算法精确沿用P01已验证机制。 |
| 核心路径难回滚或影响面无法限定 | 未命中；P04普通提交可回退，P05依赖只在指定VM/root Org，新快照原子切换。 |
| 多个有证据可行方向 | 未命中；已有精确缺失、既定artifact名、用户安装/快照指令，不存在两个并立产品方向。 |
| 用户或项目约定明确要求 | 未命中；用户要求安装后快照，未要求本次局部时序修正再次外审。 |

结论：`AI 自评闭环`。总纲保持`v4 / 已审核, 可以实施`，恢复P04子方案流程。

## 5. 机械预检

- 对象路径、v4、Git blob与SHA-256已绑定。
- REQ/CON/NON/RACC、OUT/ACC/Pxx稳定ID无重复或重编；双向映射不变。
- P04/P05/P06依赖无后序反向实施：P04只冻结接口，P05回填外部依赖，P06终验。
- ACC-013/014的通过、失败、被阻断与不计成功边界已明确；FB-005/012闭合。
- 占位符、未声明fallback和执行状态checkbox均无新增。
