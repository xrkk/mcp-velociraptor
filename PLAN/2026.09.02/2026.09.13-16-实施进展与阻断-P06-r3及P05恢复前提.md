# 实施进展与阻断：P06 r3 / P05 恢复前提

创建：2026-09-13，Asia/Shanghai。写入前扫描本目录当日 Markdown 最大流水号为 15，复查 16 不存在后独立分配。本记录不可变，后续更正另建记录。

会话角色：作者/执行方；平台 Codex。本记录不是独立核验或方案复审，不新增/关闭 CHK、AUD，不消耗、重置或扩充审核额度。用户要求“继续推进，遇到阻碍再停”，本轮在真实恢复前置失败后停止正式验收。

## 1. 对象与边界

分支 main，HEAD `ac1c58a9439aaa303d071a8a040e0f26db85bbdb`，未提交本轮候选修改。进入时工作树已有修改和未跟踪 Logs/、PLAN/，均保留。

下列为当前工作树内容 OID，不声称已经提交或写入 Git 对象库：

| 对象（同目录） | 内容 OID |
| --- | --- |
| 2026.09.02-01-需求提炼-mcp-velociraptor全阶段设计.md v2 | 4906941e0698ffe7476838c4479ce344ce30ef27 |
| 2026.09.02-02-总纲-mcp-velociraptor-Windows-DFIR二次开发.md v10 | a2658be62622a373bf65bbebc50ae33e8a99654e |
| 2026.09.05-22-实施子方案-P05测试数据与情景基础设施.md | 532844e4cc7892fdb9ee447b8b128b11503478ba |
| 2026.09.05-29-实施子方案-P06逐工具与连续情景全量执行.md v7 | 94e9c29890680bf7e3c8120a880012442707caa4 |
| 2026.09.12-18-实施子方案-P07清理与终检.md v4 | dcb51cf81165d05204870eae85540df8d05b1d7e |

本轮没有修改上述规范性对象。P06 方案复审记录 12、P07 定向复审记录 15 的通过仅为方案层；CHK-024～027 保持开放。没有派遣实施后独立核验方，因为尚未满足实施完成门。

## 2. 候选实施与已验证范围

在既有候选修改上落实：固定 r3 路径；为全部 P06 声明步骤生成并绑定资源分类，显式区分独立 PowerShell 资源资格与固定 owned cancel probe；新外部 HTTP 逐工具错误验收；真实 tools/list 原件与 schema 联结；成功/失败完整封包及只追加接收账本；资格预算消费逐项核对真实 Flow 开始/终态/结果/文件/下载调用与原始 MCP 返回；资源观察原件联结；PacketCapture 起始/停止后 inactive 检查。两份 README 同步说明固定入口与证据规则。

正确 Windows VM（192.168.204.232，仓库 C:\mcp-velociraptor，锁定 guest venv）在恢复前运行五模块回归，共 **54 tests，OK，TEST_EXIT_CODE=0**。其中合成证据/模拟资源校验的单元测试不构成真实资格或 650 关系覆盖。原始记录：

- `Logs/P06/resource-gate-regression-92f8c17e32c14113989c3e0fe2e4b57e/manifest.json`，SHA-256 `0f4db0a099b974310486634ae7b21e5df1350df6c6c1f42e561798f1930bdfb3`；含同步文件完整 SHA 集和实际测试命令。
- 同目录 `response.json`，SHA-256 `31aeaad20994f9a64e465078b9e170fb88375f023330b259d4787115756ccfc1`。
- host `py_compile` 和 `git diff --check` 通过，仅为静态检查，不替代 Windows 测试。

仍未完成：全部回归、KillProcess 紧邻身份/保护集门禁、trace 所有权与异常闭合、资格失败封包边界、聚合器完整步骤反向重放和历史完整性、真实资格/逐工具/五场/聚合、P07 成本及 80/67 终检、实施提交和独立核验。不能据 54 项单元测试宣布 P06 或项目可验收。

## 3. 保全与真实恢复

按已获用户授权，仅回传正确 VM 的 Logs/P06、Logs/P07，原件未编辑：

`Logs/pre-restore-preservation-35a29ed852f74a81ba82805561f0b9db/guest-p06-p07.zip`

实际 3,403,144 字节，host/guest SHA-256 同为 `c151dffee9e1801c323ef3ee552103105c1fddaaf771415b9130120180e437f8`；恢复前重算 SHA、ZIP CRC、路径首段与唯一名称通过。28786 单请求传输完成后关闭监听。更早保全及全部旧报告未覆盖。

新只读历史索引在 `Logs/P06/wf-01a05d1d-p06-r3/historical-roots.json`，引用 r1/r2，不改旧纪元。

恢复原件目录：`Logs/P06/wf-01a05d1d-p06-r3/restore-preflight/8dd2e3b7-c84b-48cc-98ec-77044fa74fef/`。

canonical selector 确认 schema3/epoch4/NETWORK_ACTIVE，唯一自动对象 Snapshot 186-Velociraptor-MCP网络部署基线；实际 VMware 树唯一匹配。对 `/home/adminn/vmware/Win10MalBox-Velo/Win10MalBox-Velo.vmx` 仅执行一次 revert（exit 0），marker 实值 `Win10MalBox-Velo-Snapshot2.vmsn`，随后首次且唯一 `vmware -x`。没有使用元数据中另外存在的 Snapshot187，没有冷启动、第二次 revert 或其他 VM 操作。

临时 host 检查器最初以 startswith 误把 `checkpoint.vmState.readOnly` 也选入，保守停止在启动前；原始 marker 本身正确。修正为精确键解析，保存独立 `pre-start-marker-recheck.json` 后在同一 attempt 执行第一次启动，未覆盖原误判证据。这是本轮临时检查器实现错误，不是 Snapshot marker 失效。

目录保留 selector、原始 VMware 元数据/返回、两次 marker 读数、启动记录、完整 canonical/activation 字节、控制面 hostname/进程原始返回及 fixture 原件。该对象只是恢复前置 attempt，不冒充完成的 schema2 正式运行，不进入成功 selection 或 coverage。

## 4. 确定性前置阻断与 PLAN-CHANGE 候选

恢复后首次只读查询和随后留档复核均发现以下事实；全程没有正式产品工具调用：

1. fixture 清单仍指定 PID 1304、creation_time `2026-09-04T23:42:32.923Z`、固定 workflow/attempt token，但 Win32_Process 全量查询中 PID 1304 无匹配。因此 P05 §2.3/§3 和 P06 §1 的恢复后专用进程起点不成立。
2. Velociraptor 进程 PID 7604/5760 的父 PID 分别为 5660/1580，父进程均不存在，不能提供 P05 要求的父 PID + start-time 保护集及前后不变证明。不得凭 PID 单独调用 kill，亦不得删除父进程约束。
3. host 留档窗口 `2026-09-13T13:00:36.743885+00:00`～`13:00:38.215569+00:00` 与 guest 观察时钟相差约 216,564 秒。此项只记录恢复时钟观察；没有改时钟，也不把采样实现的 120 秒校验阈值扩张为新规范。前两项已足以阻断。

hostname 为 DESKTOP-3FI41GR；正式服务 Running，PID 7988，NT SERVICE\mcp-velociraptor；trace 原始输出为 inactive。没有因为服务运行就忽略 fixture/保护集失败。

关键证据（相对于上述恢复目录）：

- `post-restore-hostname.json`，SHA-256 `7d76dbc065fbad07340b40cfad91ef873b403832e42a6e855b289715edfe7ab2`，含实际只读 PowerShell、host 时间、完整原始响应和 fixture 原字节 base64。
- `fixture-instance-original.json`，由原始 base64 解码后复算并匹配 guest Get-FileHash，未改写内容。
- `preflight-result.json`，SHA-256 `3b892e9a924442f5c1333f69c90f252f110bfaad78451467da23b709adf9a8b3`，formal_product_calls=0，coverage=[]。

**PLAN-CHANGE 候选：P05 恢复基线及受保护进程前提重新裁决。** 不能在 P06 内局部重建 fixture、重启服务、直接选择 Snapshot187、降级保护集或更改 canonical 来掩盖。P06 v7 §1/§0.2/FB-P06-004 已明确前置失效回 P05；现有证据不能归因为 P06 产品代码引入，因为失败是在恢复到固定 Snapshot186、候选测试同步和任何正式工具调用之前观察到的。

受影响稳定边：OUT-021/022/023/053/054 → P05 恢复/fixture/安全前提 → IMP-P06-002/003/005/008 → ACC-P06-001～006 → P07 入口 → ACC-024/025；主要切片为 REQ-021/022 及依赖这些运行证据的验收。此处仅登记候选和证据，不擅自分配新正式 PLAN-CHANGE ID 或修改负责方案。

最小恢复条件：授权回流 P05，按当前台账分配稳定 PLAN-CHANGE，裁决可恢复 fixture、保护集及时间前提；完成必要需求保真、自评/审核、真实基线验证和规范激活后，由新 HEAD/新证据 attempt 重新开始 P06。不得将本次前置报告改写为通过或沿用旧成功资格。

## 5. 当前结论

**实施被 P05 恢复前提阻断，P06/P07 未完成，项目尚无新的核查通过结论。** 保留恢复后的现场和全部 host 原件，不局部整改环境，不继续资源资格或 KillProcess，不进行 P07 清理。需要用户决定是否授权上述 P05 基线回流，之后才能继续正式验收。
