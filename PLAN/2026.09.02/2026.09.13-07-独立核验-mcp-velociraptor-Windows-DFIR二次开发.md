# 独立核验：mcp-velociraptor Windows DFIR 二次开发（第5轮全面重验）

> 会话角色：独立核验方  
> 执行平台：Codex  
> 核验模式：全面核查；当前 Codex 主会话亲自执行；未创建子智能体  
> 核验性质：实施后独立核验，只形成 CHK、PLAN-CHANGE 候选和核查结论；不整改，不产生或关闭 AUD，不消耗、重置或扩充审核批次  
> 创建日期：2026-09-13（Asia/Shanghai）  
> 结论：**核查不通过**

## 1. 对象身份、角色与规范边界

当前会话进入本轮前已绑定且保持 `会话角色：独立核验方`，不存在作者、执行方或第三方审核方角色冲突。本轮没有委派子智能体，也没有修改任何需求、总纲、Pxx、代码、测试、既有记录或既有证据。

| 对象 | 本轮固定身份 |
| --- | --- |
| 当前需求文档 | `/home/adminn/projects/mcp-velociraptor/PLAN/2026.09.02/2026.09.02-01-需求提炼-mcp-velociraptor全阶段设计.md`；v2；工作树 blob `4906941e0698ffe7476838c4479ce344ce30ef27`；SHA-256 `a05916e9e874699391191e213306df72a16a1fee8a3b08e9676a206e5134d081` |
| 当前总纲 | `/home/adminn/projects/mcp-velociraptor/PLAN/2026.09.02/2026.09.02-02-总纲-mcp-velociraptor-Windows-DFIR二次开发.md`；v10；工作树 blob `a2658be62622a373bf65bbebc50ae33e8a99654e`；SHA-256 `f51ccc0761edd3a2954348fc2d626ad045678dc2d3e84067a1b63119c2262ec6` |
| 分支与核查 HEAD | `main@ac1c58a9439aaa303d071a8a040e0f26db85bbdb`；提交时间 `2026-09-13T18:06:09+08:00` |
| P01～P06 子方案 blob | P01 `778de584144e1c69239ffa33f4d94143f5cc2819`；P02 `e7523ce8898f3cfd21296af9eb2a4dd3c3726ac8`；P03 `fc44d59b604fa6d1b105575195925565a10a353a`；P04 `0ec0b4bcc15d78760768d3dc16cfe6822b17c5f2`；P05 `532844e4cc7892fdb9ee447b8b128b11503478ba`；P06 `076b5deb2a4d014323a3f75352353b7a9331196f` |
| 当前 P07 子方案 | `2026.09.12-18-实施子方案-P07清理与终检.md`；工作树 blob `46341d612a95e54d9b2df8b5803589c4326ca5f4`；SHA-256 `08d4db0b14b3ade13b8030e9327d9a7951eb9c82d25f08510f2c669bbe8aea74`；旧 `-13` 仅作历史 |
| 当前整改/审核链 | `2026.09.13-02` SHA `3db7b649…`；`-03` SHA `a445493d…`；`-04` SHA `c6fa967e…` |
| 当前总体验收 | `2026.09.13-05-总体验收记录-第4轮CHK闭合.md`；blob `44bcac5807fe931cf3a5b1023b913d6e8bf6bb0d`；SHA-256 `98c5e99501f865791a0fc8db1c89e43474e47eb91902a0ecd446eadb8f8464aa` |
| 历史定向核验 | `2026.09.13-06-定向核验-第4轮CHK闭合-通过.md`；blob `966490ff7acaf5429c008f487d0124901155ef28`；SHA-256 `d16c854b…`；由子智能体形成，只作来源追溯，不替代本轮主会话全面核验 |

需求文档与总纲字节自第4轮未变，二者共同构成本轮规范上限。核验边界为 bridge、P01～P07、跨 Pxx 契约、当前代码/测试/提交/证据；排除 `agent_poc` 业务能力及仓库无关健康。受保护现场为 Windows VM、快照、服务、防火墙、secret、未跟踪 `Logs/`、`PLAN/` 及用户既有内容。本轮开始及写入前 tracked 工作树无修改，既有未跟踪项为 `Logs/`、`PLAN/`、`tests/__pycache__/`。

## 2. 对象定位、覆盖与双向映射

已定位 P01～P07 方案、自评、审核/处理、实施、总体验收、代码、13 个测试模块以及 P06/P07 当前证据。覆盖 26 REQ、15 CON、13 NON、26 RACC，共 80 项正式契约；另覆盖 67 项累计有效结论、OUT-001～054、ACC-001～026及其 Pxx 责任边。

| 当前需求条目 | 当前总纲项 / 负责 Pxx | 实际落点 / 提交 | 当前验收证据与本轮判定 |
| --- | --- | --- | --- |
| REQ-001～012、015～020 | OUT-001～020；P01～P07 | 产品代码、配置、清理、文档至 `91e1e0f` | 当前 diff 未触及既有通过语义；抽查零危险开关残留；完整 |
| REQ-013/014/021～023/026 | OUT-013/014/021～023/053；P04～P06 | 五场报告、`5c52f32` 聚合器及新证据根 | 选定报告内部聚合成功，但恢复原始记录、Snapshot 绑定、资源资格拓扑和逐步资源门不满足 P06 契约；不可验收，CHK-024/025 |
| REQ-024 | OUT-024/ACC-024；P07 | `ac1c58a`、`upstream78-fixed-task.json`、`cost-measurement.json` | schema 双边数字可复算；固定任务调用/重试/输出量并未按同一可比口径完整比较；不可验收，CHK-026及 PLAN-CHANGE 候选 A |
| REQ-025 | OUT-025/ACC-025；P07 | `-02→-03→-04→-05→-06` | 审核对象链存在，但最终验收的“全部闭合”和 13/13 回归证据声明与可定位事实不一致；不可验收，CHK-027 |
| CON-001～015 | OUT-026～039/054；P01～P07 | 产品、网络、快照和 P06 链 | 10 项完整；CON-002/004/008/011/015 随 P06 正式证据链不可验收 |
| NON-001～013 | OUT-040～052；P01～P07 | 当前代码、配置、README | 13 项完整；生产代码、配置、两 README 中危险开关零匹配 |
| RACC-001～026 | ACC-001～026；P01～P07 | 上述链反向映射 | 15 项完整；RACC-006/007/013/014/020～026 不可验收 |

反向扫描未发现当前生产工具面超出 118 动态 + 12 固定、单 server、双传输、单 Windows endpoint 的新功能，也未发现旧危险开关恢复。异常集中在 P06/P07 证据闭合、ACC-024 比较口径及最终记录真实性。

## 3. 需求保真计数

正式条目按互斥分类计数。“完整”要求参与者、输入输出、触发/顺序、状态/生命周期、不变量、异常/边界/结束、方向前提/重新裁决和可观察验收均能从实际落点与可定位证据恢复。

| 指标 | 数量 | 异常 ID |
| --- | ---: | --- |
| 应承接数 | 80 | 26 REQ + 15 CON + 13 NON + 26 RACC |
| 完整数 | 56 | 见第2节 |
| 未落点数 | 0 | — |
| 语义弱化数 | 0 | — |
| 不可验收数 | 24 | REQ-013、REQ-014、REQ-021～026；CON-002、CON-004、CON-008、CON-011、CON-015；RACC-006、RACC-007、RACC-013、RACC-014、RACC-020～026 |
| 失效或复核条件缺失数 | 0 | 规范条件可恢复；异常在于实现/证据未满足或记录作出相反声明 |

后四项中“不可验收数”非零，需求保真门禁不通过。

67 项累计有效结论独立重核：应承接 67、完整 48、未落点 0、语义弱化 0、不可验收 19（2、4、6、8、32、35、40、42、53、56、59～67）、失效或复核条件缺失 0。旧轮次、否决方向和未采纳建议未计入规范上限。

## 4. 独立验证

| 动作 | 独立结果 | 判定 |
| --- | --- | --- |
| 严格聚合只读重算 | `aggregate()` 与磁盘 JSON 全字段相等：status=success、5 场、130 工具、650 关系、0 unexpected error、0 未执行 | 只证明聚合器当前检查范围内部一致，不证明被遗漏的外部恢复证据存在 |
| 23-attempt 账本 | attempt 1～23 连续，7 failed、11 superseded、5 success；selection 对18个未选项有理由；报告和当前 `package_hash` 可复算 | 状态表进步成立，但账本/包契约仍不完整，见 CHK-024 |
| 五个选定场恢复证据 | 每份 `snapshot-evidence.json` 各引用6个原始恢复记录；30/30 在新证据根均无法定位；五份 `canonical_sha256` 均为64个字母 `a`，而资格根 `current-restore.json` 是真实 `b2182bf5…` | Snapshot186 原始恢复绑定不可验证，CHK-024 |
| 资源资格代码/证据 | `p06_resource_qualification.py` 明确使用 `stdio_client`；资格 JSON 没有正式 HTTP session、外部客户端、服务身份或 `snapshot_evidence_sha256`；调用前 4GiB 门仅包围9个动态 invocation，triage、fixture、download 前没有门 | 不满足 P06 正式拓扑及资源门，CHK-025 |
| 正式场景资源门扫描 | `MIN_FINAL_FREE`、`formal_scenario_minimum_free_bytes`、`c_free_bytes`、`step_deadlines_seconds` 仅存在于资格脚本；场景 runner/五报告没有逐大步骤前资源读数、阈值、deadline或拒绝理由 | CHK-025；第4轮 CHK-020 的最小闭合条件未被当前证据证明 |
| 上游固定任务 | 捕获文件可解析为 `collect_artifact` + `get_collection_results` 两次 wire 调用、647B、5.188s、真实结果行 | 捕获本身存在；没有内部 attempt 轨迹，不能从5.019s证明“一次内部重试” |
| 当前 RecycleBin 等价链复算 | 五场 wire 调用为10/7/5/4/4；实际 `attempt>1` 重试调用为6/3/1/0/0；成本报告却以 attempt 序号求和写成 `poll_attempts` 31/13/6/4/4；只有输出字节，没有当前固定任务 elapsed | 重试量口径错误且耗时维度缺失，CHK-026 |
| ACC-024 报告结构 | `per_scenario/totals` 是五个不同整场（4188 calls/1708 retries/2587611B/19649604ms），上游对象只是单个 RecycleBin 子任务；报告未给双方固定任务差异表，结论还写“task costs are reported for the current product only” | 不能构成总纲要求的固定任务双边完整比较，CHK-026 |
| 13模块回归证据 | `-02/-05` 声称 VM 原始留痕位于 `Logs/P06/r4reg_*.txt`，当前证据树中零文件匹配；仅有人工汇总表 | 声明无可定位原始证据，CHK-027 |
| 当前主机独立回归 | 9个关键模块 `py_compile` 通过；`test_p06_aggregate.py` 5/5通过；全 discover 因当前主机缺 `mcp/grpc/jsonschema`，10模块导入失败、7项跳过 | 仅记 ENV-001 环境观察，不冒充 Win10VM 全回归，也不把范围外环境缺依赖立为 CHK |
| 记录身份 | HEAD、`-05`及账本/selection/aggregate/资格/成本文件 SHA 均与记录所列吻合 | 内容寻址身份成立，但身份正确不消除内容反例 |

## 5. CHK

### CHK-024｜新 P06 账本和证据包仍未携带、绑定或验证选定场的原始恢复记录

- 需求条目：REQ-021～023、REQ-026、CON-004/008/015、RACC-020～023/026。
- 总纲项：OUT-021～023/053、ACC-020～023/026；P06第3.6节、Snapshot186证据与host接收账本契约。
- 负责 Pxx：P05/P06。
- 证据：五个选定 attempt 18/19/21/22/23 的 `snapshot-evidence.json` 共列30个 `*_snap/revert/marker/host/canon/act` 原始记录，当前新证据根30/30无法定位；五份记录的 `canonical_sha256` 都是64个 `a`。账本缺 P06 明文要求的 `snapshot_evidence_sha256`、server instance/pid/start-time/observation、runner pid、session lifecycle、received_at 等字段。当前 `package_hash` 只哈希 report 所在目录的3个直接文件，不递归也不覆盖外层恢复记录；聚合器只检查快照名称/阶段/epoch，不复算 `canonical_sha256`，也不解析、定位、验哈希 `restore_records`。
- 反例：聚合 success 在删除或从未回传30个恢复记录、保留占位 canonical SHA 时仍成立，因而不能证明从规定 Snapshot186 独立恢复或证据先回传再恢复。
- 影响：新账本虽列出23项，但“完整原始集合、内容寻址包、恢复绑定、防拼接”仍不可验收；`-05/-06` 对 CHK-019 的闭合声明不成立。
- 最小闭合条件：在新的不可变证据纪元回传每个相关 attempt 的完整原始恢复/服务/会话/runner证据，账本逐字段转录规范身份；包哈希覆盖这些字节；聚合器独立定位并复算 restore-record哈希、canonical SHA与snapshot evidence链后，再从规范起点重跑和聚合。

### CHK-025｜Snapshot186 资源资格使用 stdio，且正式场景未实施逐大步骤资源门与 deadline

- 需求条目：REQ-021～023、REQ-026、CON-002/004/008/011/015、RACC-020～023/026。
- 总纲项：ACC-020/021/023/026；P06第3.3、3.5、3.6节及 IMP-P06-003/008。
- 负责 Pxx：P05/P06。
- 证据：资格脚本启动 VM 内 stdio 子进程，而 P06 要求每个资格/逐工具/场景 attempt 均由虚拟机外官方 SDK 直连正式 `28790/mcp`；资格 JSON 不含正式HTTP/session/server/外部客户端和同run `snapshot_evidence_sha256`。4GiB调用前门只在9个动态项循环内，triage、fixture、download不在门内。场景 runner与五份正式报告完全没有资格冻结的 `formal_scenario_minimum_free_bytes`、每大步骤增量余量、step/scenario deadline和调用前拒绝证据。
- 反例：`-06` 自身已观察“triage/fixture/download无逐调用前门、场景runner无每个大步骤前增量门”，却把它降为环境观察；这正是第4轮 CHK-020 明确要求证明生效的规范条件。
- 影响：资格结果不能认证正式网络路径；磁盘满与大Flow失败的确定性防护仍未落到正式五场，资源失败可能再次在Flow启动后发生。
- 最小闭合条件：按 P06 现行契约从 Snapshot186 以外部HTTP正式入口执行资格，生成完整snapshot/session/server身份链；对资格规定的所有大调用以及五个正式场景的每个大步骤实施并记录调用前资源门和deadline，保留低空间拒绝反例，再重跑受影响场景与聚合。

### CHK-026｜ACC-024 固定任务双边比较口径不完整且重试计数错误

- 需求条目：REQ-024、RACC-024；累计有效结论4/6/40/65/66。
- 总纲项：OUT-024、ACC-024；P07第4.2节。
- 负责 Pxx：P07。
- 证据：上游是一个 RecycleBin 子任务的2次线调用；当前报告的主 `per_scenario/totals` 是五个不同整场，不是同一固定任务。新增 `current_equivalent_chains` 只有 calls/`poll_attempts`/bytes，没有 current elapsed、明确差异表或真正同口径的 retry 字段。其 `poll_attempts` 是 attempt序号总和31/13/6/4/4，而按脚本其他位置冻结的 `attempt>1` 口径应为6/3/1/0/0。上游JSON没有内部attempt轨迹，实施记录“一次内部重试”不可从现有证据恢复。报告结论仍明写“task costs are reported for the current product only”，与“双边实测”声明矛盾。
- 反例：同一JSON同时呈现单个上游子任务、五个当前整场和不完整的当前子链，无法回答同一个固定任务双方各自调用、重试、输出量及差异。
- 影响：ACC-024“比较完整、口径透明”失败，REQ/RACC-024不可验收；`-05/-06` 对 CHK-021 的闭合声明不成立。
- 最小闭合条件：在不改变规范时，以同一目标等价任务分别捕获双方完整开始/等待/结果（以及方案声称的文件清单）链，冻结相同或明确可换算的调用/重试/输出量/耗时口径，保留可观察的内部重试证据，输出逐指标双边比较与差异；不得把五个整场总量当作单个上游子任务的对侧。

### CHK-027｜当前总体验收和全回归记录作出超过可定位证据的闭合声明

- 需求条目：REQ-024/025、RACC-024/025及全部80项的最终记录真实性。
- 总纲项：ACC-024/025、第9/12章追溯与证据契约；P07第6/8节。
- 负责 Pxx：P07/总体验收。
- 证据：`-05` 声明 CHK-019～023全部闭合，但 CHK-024～026给出其关键最小条件仍未满足的直接反例；`-02/-05` 又声称完整13模块VM回归过程留痕在 `Logs/P06/r4reg_*.txt`，当前证据树不存在任何该匹配文件。`-03`曾明确这只是“声明性证据层面”，`-04`也明确不预支实施层认证；`-05`没有补入原始输出便升级为完成声明。
- 反例：人工13行结果表不能独立证明测试命令、环境、HEAD、逐模块输出和退出码；主机当前又无依赖，无法提供等价重跑证据。
- 影响：ACC-025要求的失败闭环、完整回归和最终互斥计数不可复算；定向子智能体的“通过”不能替代当前主会话全面核验。
- 最小闭合条件：在所有开放实现问题实质闭合后，保存绑定新HEAD的13模块原始命令/环境/逐模块输出/退出码和文档审计；重新建立80项双向映射及互斥计数，形成新的总体验收，再发起新的主会话独立全面核验。

## 6. PLAN-CHANGE 候选（核验方不登记稳定 ID）

### 候选 A｜P07“目标等价任务”映射的阻塞语义与终点需重新裁决

- 证据：P07第4.2节把上游 `collect_artifact` 描述为“内部阻塞至完成”，并将其与“启动→等待→结果→文件清单”目标等价；实际捕获显示 `collect_artifact` 0.168秒即返回 flow_id，后续 `get_collection_results` 才负责等待和结果，且上游链没有文件清单调用。
- 需改变的规范性对象：P07任务等价与映射契约；若坚持当前映射，还会影响总纲 ACC-024 的“比较完整”边界。本轮核验方不修改该对象。
- 受影响稳定依赖边：`REQ-024/RACC-024/累计结论4、6、40、65、66 → OUT-024 → ACC-024 → P07 → ACC-025/最终交付`。
- 受影响切片：P07第4.2/4.3/8节、成本脚本methodology与固定任务报告、最终追溯矩阵。
- 回流条件：作者须登记稳定 `PLAN-CHANGE-xxx`，以实测行为修正P07映射的启动/等待/结果/文件终点、适用边界、失效和重新裁决条件，完成需求保真、自评及必要审核后，对新HEAD重新验收。若无需改变P07契约而能严格按现行四阶段目标取得双边证据，可作为纯实现修复处理，不触发此候选。

## 7. 实施方资质评估

评估对象仅为本轮可观察的实施产物和连续整改质量，不推断个人背景。

| 维度 | 证据判断 |
| --- | --- |
| 代码与问题求解能力 | **胜任/中上**：能实现23-attempt选择校验、包哈希、复杂P06报告聚合、上游工具捕获和成本脚本；当前可运行的聚合单测5/5通过 |
| 规范保真 | **不合格**：在 CHK-020 已明确要求“每个大调用前门禁生效”后，仍以资格stdio和场前冒烟替代正式HTTP逐步门；把明确契约缺口降为“环境观察” |
| 证据工程与可追溯性 | **不合格**：五个最终选定场30/30恢复原始记录缺失，canonical SHA为明显占位值，包哈希不覆盖规范要求的原始证据；声称存在的全回归日志无法定位 |
| 测量与验收严谨性 | **不合格**：将attempt序号求和标作poll attempts，缺当前固定任务耗时与上游重试轨迹，仍声明任务维度双边实测；报告结论与此声明相反 |
| 闭环与自我纠错纪律 | **不合格**：连续多轮在关键最小条件未满足时宣布全部CHK闭合，并依赖定向核验把已识别的规范缺口排除在CHK之外 |

综合结论：**实施方目前不适合继续以“自主端到端实施负责人 + 自验收收口方”的资质继续整改。** 技术编码能力足以承担边界明确、逐项可机械验收的受控修复，但规范判读、证据真实性门禁和完成声明纪律未达到继续独立主导本项目的水平。若仍由同一实施方继续，适合的角色只能是受控执行者：不得自行裁决规范边界或宣布总体验收，产物须由独立负责人逐对象、逐原始证据门禁后才能进入下一项。这一评价不是整改动作，也不改变任何规范性对象。

## 8. 结论

**核查不通过。**

本轮确认的有效进展是：23个attempt已显式列账并分类，selection拒绝理由齐全，聚合器当前检查范围内可复算为5场/130工具/650关系/0错误，P07审核处理与复审对象已形成，双边tools/schema数字可复算。仍不通过的决定性原因是：选定场的30份恢复原始记录全部不可定位且snapshot canonical SHA为占位值；资源资格走stdio且正式场景没有逐大步骤资源门；固定任务成本没有形成同口径完整双边比较且重试量计算错误；13模块全回归只剩声明表、所称原始留痕不存在。

正式80项计数为完整56、未落点0、语义弱化0、不可验收24、失效或复核条件缺失0；累计67项为完整48、未落点0、语义弱化0、不可验收19、失效或复核条件缺失0。存在 CHK-024～027 及 PLAN-CHANGE 候选 A，故禁止核查通过。

本记录之外未修改任何被核验对象，也未转换会话角色。
