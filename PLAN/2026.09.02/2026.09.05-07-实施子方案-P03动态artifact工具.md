# P03 动态 artifact 工具实施子方案

> 子方案路径：`/home/adminn/projects/mcp-velociraptor/PLAN/2026.09.02/2026.09.05-07-实施子方案-P03动态artifact工具.md`
>
> 子方案版本：v3
>
> 状态：已审核, 可以实施
>
> 所属总纲：`2026.09.02-02-总纲-mcp-velociraptor-Windows-DFIR二次开发.md` v3，Git blob `345476052cc6fd11fd711e1daa1c470215547bb6`
>
> 需求文档：`2026.09.02-01-需求提炼-mcp-velociraptor全阶段设计.md` v0，Git blob `de5603cc9ca1c63f62feaba7ad397343d1513448`
>
> P01 交接：`2026.09.05-01-阶段0基线报告-mcp-velociraptor-Windows-DFIR.md`，Git blob `582d5148fb376057e534a5502f9fbac5c4e71b39`
>
> P02 交接：`2026.09.05-06-实施记录-P02公共基础与最小测试骨架.md`，Git blob `bcdaa887981ed4d5f9eb557e94b947d38cf04506`；完成提交 `4087f0a9df564a8950b5e2787df83cf3caa112d5`
>
> workflow_id：`wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2`
>
> 会话角色：作者/执行方

## 1. 目标、模块边界与排除

本方案只实施总纲 P03：以需求附录 A 和 P01 实测矩阵共同限定的 118 个 Windows `CLIENT` artifact 为唯一动态集合，在进程启动、进入 stdio 服务之前读取 root Org 的真实 metadata，原子完成集合、类型、名称冲突及 schema 校验，然后用生成器注册 118 个准确原名的独立 MCP 工具。每个工具内部复用 P02 的 root Org、唯一 Windows client、结构化结果和真实 Flow 启动原语，只传调用方明确提供的参数并立即返回真实 `flow_id/status`。

核心维护范围：

- 新增单个深层模块 `velociraptor_dynamic_artifacts.py`：正式 allowlist、metadata 模型、类型映射、schema 校验、handler 生成和动态注册；
- `velociraptor_api.py` 中仅新增 root Org artifact metadata 的只读薄查询；
- `mcp_velociraptor_bridge.py` 中只接入动态注册、启动前原子门禁，并取消 35 个旧 `windows_*`、两个旧 Windows YARA、旧通用 `collect_artifact`、旧通用 `hunt_across_fleet`、15 个 Linux、10 个 macOS及两个非 Windows artifact-list 工具的注册；这些旧函数源码暂留，等待 P07 按引用证据清理；
- `tests/` 中 P03 metadata/schema/注册/stdio/真实启动测试和脱敏证据生成；
- `README.md` 与 `agent_poc/README.md` 中 P03 已实现的动态工具、Windows-only 和过渡工具边界；
- P03 方案、自评、审计、处理、实施记录与 `Logs/P03/<workflow>/` 脱敏证据。

直接依赖范围：P01 的 118 项矩阵和定义哈希、实际 523 个参数及依赖分类；P02 的 `TargetContext`、`VelociraptorBackend.start_collection`、`FlowReferenceResult`、`success_result/error_result`、MCP 2.1.1 启动与测试骨架；Snapshot184 中的 root Org、唯一 Windows client、guest-only API config和锁定依赖。

明确排除：三个 Hunt、五个 Flow/file、`collect_file`、`collect_forensic_triage`、`kill_process` 和新版 `run_vql`；P05 fixture/依赖预置；P06终态、结果和五情景覆盖；P07旧函数物理删除和危险开关清理；通用 `collect_artifact`、自定义/备用 artifact、`CLIENT_EVENT`、upload/upload_file、YaraUrl、Linux实现/验收、macOS支持、agent_poc兼容、多Org/多endpoint、运行时profile及第二server。P03不把后序完整终态成功当作本阶段通过条件。

## 2. 规范性承接与稳定依赖边

| 需求/约束 | 总纲落点 | P03实施落点 | P03验收 |
| --- | --- | --- | --- |
| REQ-002、NON-002 | OUT-002、OUT-041；P03；总纲ACC-002 | IMP-P03-006、008 | ACC-P03-001、008 |
| REQ-004、NON-005/006 | OUT-004、OUT-044/045；P03；总纲ACC-004 | IMP-P03-001、003、006 | ACC-P03-002、003、006 |
| REQ-005、NON-007 | OUT-005、OUT-046；P03；总纲ACC-005 | IMP-P03-002、003、004 | ACC-P03-003、004 |
| REQ-006、CON-004 | OUT-006、OUT-029；P03；总纲ACC-006 | IMP-P03-003、005、007 | ACC-P03-005、007 |
| REQ-007、CON-005/008 | OUT-007、OUT-030；P03；总纲ACC-007 | IMP-P03-004、007 | ACC-P03-006、007 |
| REQ-015动态替代切片、CON-013 | OUT-015的P03切片；P03/P07 | IMP-P03-006 | ACC-P03-001、006、008 |
| REQ-016、CON-010/014 | OUT-016、OUT-035/039；P03/P07 | IMP-P03-008 | ACC-P03-008 |
| REQ-019动态部分、CON-002 | OUT-019、OUT-027的P03测试切片；P03输入/P04汇总 | IMP-P03-001～008 | ACC-P03-001～008 |

稳定依赖方向：`需求v0 -> 总纲v3/P03 -> P01真实矩阵 -> P02公共契约 -> IMP-P03-xxx -> ACC-P03-xxx -> P04 -> P05依赖与安装后快照 -> P06最终全集`。P03的受控schema/协议测试与116项真实Flow只消费已完成P01/P02；两项已证依赖延期不计成功，也不阻断P04实现独立固定工具。

## 3. 已证事实与冻结决定

### 3.1 P01/P02事实

- root Org 实际有202个 `Windows.*` 定义：167 `CLIENT`、33 `CLIENT_EVENT`、2 `SERVER`。附录A 118项全部存在、均为`CLIENT`，允许集合差集0；四个新增alias不自动扩张清单。
- 118项共523个参数：空类型124、bool 93、choices 11、csv 13、hidden 21、int 33、int64 12、multichoice 1、regex 155、string 1、timestamp 47、upload 3、yara 9；没有float或upload_file实例，也没有非空`validating_regex`实例。三个upload恰为三个YARA artifact的`YaraUrl`。
- 116项无声明外部依赖；`Windows.Sysinternals.Autoruns`缺预缓存工具，`Windows.Network.PacketCapture`声明运行时外部下载。实施中真实SDK调用进一步证明：Snapshot184里两项都在Flow创建前返回`BACKEND_ERROR/missing_flow_id`且Flow/Hunt增量为0；这不是终态失败。P03不把错误计成功，真实启动计数固定为116/116；P05按原职责预置两项并在安装后创建、验证、激活新快照，P06再把两项及最终全集真实成功作为通过条件。
- P02已证明MCP 2.1.1的纯结构化结果、进程内唯一client、真实Flow ID/state及锁定依赖可用。当前旧产品工具仍是过渡面。
- 2026-09-05实施前只读资源基线为C盘可用17883152384 bytes、物理内存可用2382844 KiB/总4193332 KiB；第6章阈值据此保留启动余量，版本/快照或资源分布变化时必须在调用前重测，不能沿用过期数值。

### 3.2 P03冻结决定

1. allowlist以产品源码中的准确118项`name -> approved_definition_sha256`映射承接需求附录A与P01批准身份；启动时对root Org实时metadata双向核对这118项，不从“所有Windows CLIENT”自动扩张。哈希算法严格为`sha256(raw_definition.encode("utf-8")).hexdigest()`：`raw_definition`是root API JSON解码后未经strip、换行转换、Unicode规范化或重序列化的`raw`字符串。任一同名定义哈希不同都在handler注册前按FB-P03-003原子失败并列出名称和expected/actual hash，不输出raw正文。
2. 每个artifact生成一个独立handler，但不手写118套函数。生成器为每个参数建立安全内部名`p0..pN`与准确外部alias，并注入`inspect.Signature`；MCP SDK按alias生成schema和回传参数。Win10VM Spike已证明含空格的`Boot execute`及数组`Tables`可注册、调用并得到结构化结果。
3. 所有字段在JSON Schema中都不是`required`。Python签名使用“非nullable注解 + default None”：调用省略时生成器丢弃None，显式null由SDK校验拒绝，从而只向Velociraptor发送调用方明确提供且非null的值。不得将backend default复制进请求。
4. 类型表固定为：空类型/`string`/`regex`/`yara`/`timestamp`/`csv`映射JSON string；`bool`映射boolean；`int`/`int64`映射integer；`float`映射number；`choices`映射单值enum string；`multichoice`映射enum string array。schema以`x-velociraptor-type`、`x-velociraptor-default`保留真实类型和后端默认，以`format`标注regex/yara/timestamp/csv；`description`、`friendly_name`、`choices`、`validating_regex`原样承接。`validating_regex`非空时先验证其自身可编译，再作为字符串`pattern`；坏表达式使启动原子失败。
5. `hidden`、`upload`、`upload_file`一律不进入schema；三个YARA artifact中的`YaraUrl`按名称再做独立拒绝断言，即使未来metadata类型漂移也不能暴露。`YaraRule`保持string并标注yara格式。未知类型、空/重复参数名、空/重复artifact名、类型非CLIENT、缺失允许项、工具名冲突或任一输入/输出schema非法均使进程在`mcp.run()`前失败。
6. 失败启动通过统一`main()`边界把一句不含秘密的诊断写stderr并返回非零；不得写stdout、不得进入部分服务。正常启动完成全部118项注册后才调用`mcp.run()`，该进程的清单不再改变；metadata变化只在重启时重新读取。
7. 动态调用复用唯一`TargetContext.run_with_client`和`VelociraptorBackend.start_collection`；`operation`使用不与已退出旧工具混淆的`start_artifact_collection`，warnings只承载可公开依赖提示，`flow_id/status`来自后端。handler不轮询终态、不取结果、不建Hunt。
8. P03分两步取消旧注册：先使动态118项及其针对性测试通过，再取消35个旧`windows_*`、两个旧YARA、`collect_artifact`、`hunt_across_fleet`和三个`list_*_artifacts` decorator并重跑；同时为Windows-only门禁取消15个Linux和10个macOS helper注册。函数源码仍保留；P04负责其余固定/collection/result迁移，P07才按无引用证据物理删除旧Windows/YARA实现。
9. 真实118项中17个Autoruns参数名包含空格。backend参数键只能来自已验证spec；普通标识符原样编码，含空格的已批准键用VQL反引号引用。metadata键含反引号、控制字符或空值时原子失败。Win10VM root Org Spike已证明`dict(`Boot execute`=FALSE)`得到准确键；不得放宽成调用方可提交任意VQL标识符。

## 4. 动态注册与数据契约

### 4.1 metadata快照

`read_root_artifact_definitions()`在root Org执行一次只读查询，返回每个定义的`name/type/description/parameters/raw`。每个参数只接受`name/type/default/description/friendly_name/choices/validating_regex`这组已证字段；多余metadata不进入外部schema。注册器先按第3.2节第1项对未经变换的raw字符串计算SHA-256并与源码批准映射逐项比较，再把真实定义规范化为不可变spec；当前哈希和完整矩阵摘要只进入脱敏测试证据，不在产品响应中回传原始定义。

启动双向比较的批准集合始终是源码118项name/hash映射；实际root Org可以有其他定义，但它们不注册。批准集合中缺失、重复、非CLIENT或同名raw hash不符都是失败；附录B、CLIENT_EVENT、其他CLIENT和自定义项没有通用旁路。P04的`start_hunt`必须复用本模块同一allowlist/spec，而不能复制第二份。

### 4.2 输入schema和编码

每个工具名严格等于artifact原名；description严格取实际metadata说明。输入properties严格等于“实际参数减hidden/upload/upload_file，再减三个YaraUrl”；properties名称保持原大小写和空格。字段全部可省略，`additionalProperties=false`。注册后把SDK生成的参数模型设为`extra=forbid`并强制重建，再发布同一模型生成的schema；Win10VM Spike已证明extra字段在handler前失败。choice/multichoice必须拒绝集合外值；bool/int/int64/float必须拒绝字符串冒充，且bool不得被integer接受；显式null和未声明字段由SDK拒绝。生成器在进入backend前再次按spec把值归一化，regex和非空`validating_regex`失败返回`INVALID_ARGUMENT`。

后端调用字典由接收到的alias中值不为None的条目构造；空字典传为`None`。不读取或填入`x-velociraptor-default`。三个YaraRule是普通文本；任何URL、文件上传、临时服务或清理生命周期都不存在。

### 4.3 原子启动与结构化输出

注册过程在stdio服务前依次执行：初始化API连接；读取metadata；验证集合/类型/参数；对全部118项构建handler；在隔离的候选server中检查工具名冲突、输入schema和`FlowReferenceResult`输出schema；全部成功后形成正式固定server并运行。实现可以在同一尚未运行的server上先构建后提交注册，但必须证明任何异常时从未进入stdio read loop，不能捕获错误后继续提供残缺清单。

动态成功结果唯一为`FlowReferenceResult(operation="start_artifact_collection", status=<真实初始state>, warnings=<零或公开依赖提示>, flow_id=<真实ID>)`。异常经P02 `error_result`转换；用户输入错误为`INVALID_ARGUMENT`，配置/连接/目标/后端错误沿P02稳定码。进程启动期错误不是工具调用结果，必须是非零退出和stderr诊断。

## 5. 实施项

### IMP-P03-001｜固化118项allowlist和root metadata薄查询

- 范围：新增动态模块常量/spec及`velociraptor_api.py`只读函数。
- 现状：批准集合只在需求/P01证据，旧bridge仅有宽泛artifact查询。
- 目标：源码承接准确118项name/hash映射；一次root查询返回真实定义/参数/raw；按未经转换raw UTF-8字节严格核对名称、类型和批准hash，不自动纳入其他定义或同名替换。
- 前置：P01/P02 CHILD_DONE。
- 依赖：需求附录A、P01 artifact-matrix SHA-256 `3293cc55d4d6a734cc9349a274a17f9e54a4b0ee72222e16b5179ab31a0e7544`和其中118个`definition_sha256`。
- 顺序：第一个实施。
- 实际落点：`velociraptor_dynamic_artifacts.py`、`velociraptor_api.py`、P03测试。
- 允许裁决：内部dataclass/Pydantic类名和查询投影排序；raw字段及其字节不得重建、strip或规范化，不得从实际全集推断授权。

### IMP-P03-002｜实现metadata-to-schema类型映射

- 范围：spec规范化、类型表、aliases、description/choice/default/format/validation扩展和禁用字段过滤。
- 现状：旧wrapper手写参数且改名/丢metadata。
- 目标：完整实现第3.2节第2～5项和第4.2节；523项逐项可对账，未知类型原子失败。
- 前置：IMP-P03-001。
- 依赖：P01实测类型统计及Win10VM动态alias Spike。
- 顺序：在注册器之前。
- 实际落点：动态模块与表驱动测试。
- 允许裁决：安全内部名和自定义schema扩展键的代码组织；外部名、类型表、禁用规则和省略语义不得改变。

### IMP-P03-003｜实现原子动态注册器

- 范围：handler签名生成、118项schema构建、名称冲突/JSON Schema/输出schema全量预检和server接线。
- 现状：当前server只有静态decorator工具。
- 目标：正常启动一次性注册118项；任何连接/metadata/type/conflict/schema故障都在stdio前失败，无部分服务。
- 前置：IMP-P03-001/002。
- 依赖：MCPServer 2.1.1 `add_tool`、P02结构化返回模型。
- 顺序：schema完成后，真实调用前。
- 实际落点：动态模块、bridge main边界及fixture。
- 允许裁决：候选server或预构建spec后一次注册；不得延迟到首次tools/list或运行中改变清单。

### IMP-P03-004｜接入唯一目标和普通Flow执行

- 范围：每个生成handler的调用路径、参数归一化、P02结果/错误工厂。
- 现状：旧wrapper暴露client/org并同步取结果。
- 目标：无目标参数；只传显式业务参数；安全编码17个含空格的已批准参数键；每次只创建一个普通collection并立即回读真实state，返回纯structuredContent，不建Hunt/不取结果。
- 前置：IMP-P03-003。
- 依赖：P02 `TargetContext`和`VelociraptorBackend.start_collection`。
- 顺序：注册器后、真实全量调用前。
- 实际落点：动态模块、公共backend必要的非重复小扩展和P03测试。
- 允许裁决：闭包/partial及operation内部标识；不得复制client缓存、错误或collection实现。

### IMP-P03-005｜建立五类启动故障和正常stdio测试

- 范围：连接失败、允许项缺失、类型错误、名称冲突、schema错误fixture以及正常SDK会话。
- 现状：P02只证明公共fixture与旧bridge可启动。
- 目标：每类失败均在读stdio前非零退出、stdout空、stderr含稳定不敏感原因；正常完整清单一次可见。
- 前置：IMP-P03-003/004。
- 依赖：官方SDK 2.1.1和隔离metadata fixture。
- 顺序：动态实现后，取消旧注册前。
- 实际落点：`tests/p03_*`。
- 允许裁决：fixture环境变量名和临时目录；不得修改真实root定义或用部分启动通过。

### IMP-P03-006｜取消已替代、旁路及非Windows旧注册

- 范围：bridge中37个旧Windows/YARA、`collect_artifact`、`hunt_across_fleet`、25个Linux/macOS helper和三个artifact-list decorator；保留函数体。
- 现状：这些工具仍在旧78项过渡面。
- 目标：动态针对性测试通过后取消37个已替代注册、两个动态/Hunt旁路及已被独立工具schema取代的三个artifact列表；最终P03 tools/list无Linux/macOS注册或旧artifact discovery，旧源码按P07时序暂留。
- 前置：ACC-P03-002～006的第一次动态验证通过。
- 依赖：动态替代矩阵。
- 顺序：不能早于替代测试；修改后重跑全部P03 ACC。
- 实际落点：`mcp_velociraptor_bridge.py`、注册扫描测试。
- 允许裁决：使用无副作用`@_unregistered_source`标记或删除decorator；不得物理删除旧函数或取消P04仍需迁移的Windows固定工具。

### IMP-P03-007｜以确定性资源门禁执行118项真实启动与协议验收

- 范围：Win10VM official SDK单会话逐工具调用、118行确定性调用表、Flow/Hunt前后差异、请求参数、取消确认和资源收敛。
- 现状：P01仅验证代表性底座，尚未逐项调用动态工具。
- 目标：先生成并审阅`tests/data/p03_invocations.json`的118行固定表，每行含artifact、准确parameters、risk_class、cancel_policy及理由；再使116个无外部依赖项经产品stdio工具调用并返回真实Flow ID/初始state且无Hunt新增。另对PacketCapture/Autoruns实调用并机械确认稳定`BACKEND_ERROR/missing_flow_id`、Flow/Hunt增量0、明确不计成功和责任转交P05/P06。默认参数规则为：有公开regex的artifact只显式传第一个regex=`^__mcp_p03_no_match__$`；无regex默认传空对象；例外覆盖为Memory.Acquisition传`Compression=Snappy`，Memory.PEDump传`Pid=0, BaseOffset=0, FilenameRegex=^__mcp_p03_no_match__$`，Memory.ProcessDump传两个no-match regex，PacketCapture传`StartTrace=false`，CmdShell/PowerShell传`Command=exit 0, Timeout=30, Stateful=false`，NTFS.Recover传`MFTId=9223372036854775807`。表按artifact排序且必须与118 allowlist双向差集0。
- 前置：IMP-P03-001～006。
- 依赖：Snapshot184、唯一client、真实root Org metadata。
- 顺序：最后一个代码验收组。
- 实际落点：P03集成runner与`Logs/P03/<workflow>/`证据。
- 允许裁决：每次调用间0～2秒sleep；批次固定每10项持久化一次证据。不得改变118行参数/风险/取消规则、跳项、并发轰炸、把错误计通过或用直接API启动替代SDK tools/call。

### IMP-P03-008｜同步文档并形成P04/P06交接

- 范围：两README、最终tools/list/schema矩阵、P03实施记录。
- 现状：README仍把全部旧跨平台工具列为当前注册面。
- 目标：说明动态118项、准确参数/省略、Windows-only、Linux源码TODO/macOS不支持、agent_poc不兼容及仍待P04迁移的固定过渡工具；不虚称P04/P06完成。两README分别在术语首次出现处用普通语言解释动态artifact工具、allowlist、root Org、Flow、Windows-only和stdio分别让调用方做什么、会对唯一Windows VM或进程通信产生什么影响。
- 前置：IMP-P03-001～007。
- 依赖：最终实际tools/list和测试结果。
- 顺序：最后同步并复验。
- 实际落点：`README.md`、`agent_poc/README.md`、实施记录。
- 允许裁决：附录链接和示例布局；不得恢复旧多目标/跨平台/JSON文本为动态工具说明。

## 6. 验收项

### ACC-P03-001｜Windows-only注册与文档边界

- 前置：IMP-P03-006/008完成。
- 输入：最终P03 source注册扫描、官方SDK tools/list和两README。
- 实际命令：Win10VM运行P03注册扫描/stdio测试并核对文档切片。
- 精确预期：tools/list无15个`linux_*`、10个`macos_*`或三个`list_*_artifacts`；动态集合仅Windows；Linux明确为未注册TODO，macOS明确不支持；暂留源码不冒充可调用工具。
- 证据：tools/list JSON、AST/decorator清单、README切片。
- 通过：代码、协议和文档三者一致。失败：任一Linux/macOS入口可调用或文档声称支持。被阻断：P01实际清单失效。

### ACC-P03-002｜批准allowlist与真实定义闭合

- 前置：IMP-P03-001/003完成。
- 输入：源码118常量、P01矩阵、实时root定义、tools/list。
- 实际命令：在Win10VM读取真实metadata并做四方双向差集；尝试附录B代表、CLIENT_EVENT、四个新增alias和未知自定义名称。
- 精确预期：name/hash映射=需求附录A+P01批准身份=118个动态工具，名称四方双向差集均0、批准/实时hash差异0；每项实际存在、类型CLIENT且raw哈希算法与P01一致。保持名称/类型但分别改变raw查询、默认、参数或description的fixture均在stdio前失败且诊断只列名称/哈希；所有排除项不在tools/list且直接call由SDK稳定拒绝；`collect_artifact`、`hunt_across_fleet`和`list_windows_artifacts`均不注册，P04未来`start_hunt`只能复用同一allowlist终验。
- 证据：四方矩阵、definition hash、tools/list和拒绝结果。
- 通过：118项一一对应且无越权。失败：漏项、额外项、非CLIENT或动态旁路。被阻断：真实定义较P01发生语义漂移，按总纲FB-003只回流受影响项。

### ACC-P03-003｜523项metadata与schema逐项一致

- 前置：IMP-P03-002/003完成。
- 输入：118项实时metadata及全部参数、生成tools/list schema。
- 实际命令：运行表驱动比较和官方SDK list/call负例；覆盖每种实际类型及float/upload_file合成反例。
- 精确预期：允许字段数量为523减21 hidden减3 upload=499；外部property准确原名且全部optional/non-null/additionalProperties=false；类型表、description、choices、默认扩展和format一致；choices/multichoice错误值、显式null、错误标量类型和额外字段在handler前拒绝；未知类型/坏validating_regex阻断启动。
- 证据：类型覆盖表、逐字段差异、schema快照和负例转录。
- 通过：逐字段差异0且所有正反例成立。失败：字符串降级、默认被主动发送、说明/枚举丢失或非法输入到达backend。被阻断：出现规范未裁决且无法可靠映射的新类型，按FB-004回流。

### ACC-P03-004｜hidden/upload/YaraUrl和显式参数编码

- 前置：ACC-P03-003通过；IMP-P03-004完成。
- 输入：21 hidden、3 upload/YaraUrl、9 yara参数及每种允许类型代表值。
- 实际命令：schema扫描；SDK调用合成fixture并读取注入backend收到的参数；真实低输出artifact执行至少一组显式参数和一组全省略调用。
- 精确预期：hidden/upload/upload_file/YaraUrl零暴露；YaraRule为string/yara；省略调用backend参数为空；显式调用只含原名和值，bool/int/int64/float/list不被字符串化；非法字段由SDK拒绝且backend调用计数不增。
- 证据：禁用字段差集、backend请求转录、真实Flow请求参数。
- 通过：全部编码与排除语义成立。失败：默认注入、改名、类型变化或上传旁路。被阻断：真实metadata变化触发ACC-P03-003。

### ACC-P03-005｜原子启动和stdio完整性

- 前置：IMP-P03-003/005完成。
- 输入：连接失败、缺项、类型错误、名称冲突、schema错误五个隔离fixture及正常root环境。
- 实际命令：逐个启动子进程捕获原始stdout/stderr/退出码；正常环境用官方SDK initialize/tools/list/close。
- 精确预期：五类故障均非零退出、stdout字节数0、stderr只含对应稳定诊断且无路径/凭据；未进入stdio、无残缺tools/list。正常启动退出0并一次列出全部118动态工具；同进程第二次tools/list清单/hash完全相同。
- 证据：五类进程转录、退出码、正常两次list hash。
- 通过：全部失败关闭且正常清单固定。失败：部分服务、零退出、stdout污染、秘密泄漏或运行时变更。被阻断：Snapshot184 API不可用且按已批准恢复runbook仍失败。

### ACC-P03-006｜116项真实普通Flow与两项依赖延期边界

- 前置：ACC-P03-002～005通过；IMP-P03-004/007完成。
- 输入：最终118动态工具、唯一Windows client、调用前Flow/Hunt集合。
- 实际命令：先验证118行调用表与allowlist差集0；记录C盘可用字节、空闲物理内存、本轮Flow活跃数及server累计上传字节基线；官方SDK在同一stdio会话按表逐项tools/call。每个call最多60秒，取得真实ID后立即以测试侧root API取消该Flow（若已终态则记录），每1秒回读、最多30秒，确认其不再处于运行态且本轮活跃数回到0后才允许下一个；每10项复查资源和Hunt差异。
- 精确预期：执行前C盘可用空间至少12GiB、空闲物理内存至少1GiB；任一时刻本workflow活跃Flow最多1。116个无外部依赖项各产生恰一个新普通Flow，返回非空真实`flow_id`和后端初始state，`isError=false/content=[]/structuredContent`符合输出schema；两项已知依赖实调用均得到`BACKEND_ERROR/missing_flow_id`且Flow/Hunt增量0、标记`not_counted_as_success=true`。无Hunt新增、不等待完整结果、不直接返回结果行；请求严格等于调用表。累计C盘下降不得超过4GiB、所有本轮Flow累计上传不得超过2GiB、单Flow上传不得超过512MiB、全轮墙钟不得超过4小时；每个本轮非终态Flow都有取消请求和30秒内非运行态回读。阈值或取消确认失败时立即停止后续创建、记录失败并先收敛当前Flow；不能把停止前部分计为通过。
- 证据：118行固定调用表、逐项时序、Flow详情/request/上传字节、取消与终止回读、ID唯一性、Hunt差集、每10项磁盘/内存/活跃数、SDK转录和总耗时。
- 通过：116/116真实Flow、2/2已知依赖延期边界及全部调用/取消/资源不变量成立；两项错误不计成功。失败：其余任何错误/漏项/伪ID/多Flow/Hunt/同步结果、资源阈值或收敛失败，或把两项错误计成功；失败后按规定起点修复重跑。被阻断：出现已知两项之外的artifact在真实后端连Flow都无法创建，按FB-003回流。

### ACC-P03-007｜故障、结构化结果与协议回归

- 前置：IMP-P03-005/007完成。
- 输入：P03全部单元/fixture/生产stdio测试及P02测试。
- 实际命令：Win10VM运行AGENTS语法检查、`python -m unittest discover -s tests -p "test_p03_*.py"`、P02回归；SDK实际调用成功、参数错误、目标错误和backend错误路径。
- 精确预期：发现数非零且全通过；动态成功与错误均纯结构化、准确outputSchema、无文本副本；目标参数零暴露；P02 15项保持通过；stdout仅JSON-RPC。
- 证据：完整测试输出、用例数、协议转录、schema和返回样本。
- 通过：全部通过且可重复。失败：skip/预期外错误/协议污染/P02回归。被阻断：Win10VM通道和独立官方SDK通道均不可用。

### ACC-P03-008｜替代退出、范围、README与交接

- 前置：ACC-P03-001～007通过。
- 输入：全diff、注册/引用扫描、两README及P04/P06消费接口。
- 实际命令：`git diff --check`、AST注册清单、旧37项与跨平台27项扫描、秘密/路径/第二实现扫描和README核对。
- 精确预期：旧35 Windows、2 YARA、`collect_artifact`、`hunt_across_fleet`和三个`list_*_artifacts`不在tools/list但函数体暂留；Linux/macOS helper不注册；P04范围内其他旧固定工具未被擅自重写/删除；动态模块是allowlist/schema唯一来源且可由P04 Hunt复用；两README准确反映P03阶段。两份文档各自对动态artifact工具、allowlist、root Org、Flow、Windows-only、stdio六个术语给出首次出现行、调用方动作和实际影响的检查表，六项均通过；无用户路径/秘密、通用旁路、第二server或后序实现。
- 证据：diff、AST/引用矩阵、最终tools/list、README切片和实施记录。
- 通过：边界清楚且P04/P06可直接消费。失败：旧注册残留、物理早删、重复实现、越界或文档失真。被阻断：前序ACC未通过。

## 7. 测试、资源、证据和提交契约

- 所有语法、单元、协议、真实集成与回归测试只在Win10VM执行；host只编辑、静态归因及维护治理文件。
- 若需要恢复，只使用epoch2已激活的`Snapshot 184-Velociraptor-MCP测试基线`；恢复前先把guest证据经只绑定`192.168.204.1`的host-only临时通道拉回并双端SHA-256核验，用后即停。
- 118行严格按第5章IMP-P03-007固定表和资源阈值执行；116项真实Flow任一时刻最多活跃1个，取得ID后必须完成取消/终态确认才可继续；两项依赖失败必须零Flow/Hunt增量且不计成功。测试侧root API只可取消本workflow本次新建Flow；不停止其他Flow、不删除服务端证据、不恢复快照冒充清理。超限或不收敛时先停止后续创建并保全证据，需恢复时严格走Snapshot184 runbook，恢复前先回传guest证据。
- 证据目录固定为`Logs/P03/wf-01a05d1d-p03/`，至少包含metadata/schema矩阵、五类启动故障、SDK list/call、116项Flow/Hunt/request矩阵、两项不计成功的依赖延期记录、测试输出、guest/host源码哈希和端口关闭检查。
- P03提交只包含可机械归属的产品代码、P03 tests和两README；PLAN/Logs及既有未跟踪现场不提交。提交前保护原有dirty状态并核对没有P04～P07实现。

## 8. fallback与重新裁决

- FB-P03-001：批准项缺失、类型非CLIENT、名称冲突或任何schema非法时原子失败，不用旧wrapper、通用collect、字符串降级或残缺清单继续。
- FB-P03-002：未知参数类型或无法解释的`validating_regex`按总纲FB-004原子失败，只重开受影响REQ-005/IMP/ACC；不能映射为自由string。
- FB-P03-003：真实定义/hash/参数相对P01发生语义变化时，按总纲FB-003冻结受影响artifact并回流正式清单/映射裁决；其他已证项不静默增删。
- FB-P03-004：真实Flow启动的普通连接/离线/超时错误沿P02稳定错误映射且不换目标；只有精确client不存在才触发P02已有一次重解析。任何失败调用都不计118项通过。
- FB-P03-005：两项已知外部依赖不由P03临时联网、下载或预置。Snapshot184已证明二者在创建Flow前拒绝；P03必须实调用、记录零Flow/Hunt增量和`not_counted_as_success=true`，不能把预期错误冒充成功。P05负责预置并在安装后拍摄、验证和激活新快照；P06必须真实成功，最终标准不降低。
- FB-P03-006：118项执行中若磁盘/内存/上传/墙钟阈值、活跃Flow上限或30秒取消确认任一失败，立即停止后续创建并把ACC-P03-006/007记为失败；不提高阈值或保留运行Flow继续。修复测试参数/取消路径后从Snapshot184规定起点重跑完整118项。
- Velociraptor版本、root定义、MCP SDK主版本、第二endpoint/Org或产品允许集合变化会使受影响矩阵/schema/目标契约失效并触发复核；不保留并行兼容模式。

## 9. 双向追溯

| IMP | 需求/OUT/Pxx | ACC | 产出 |
| --- | --- | --- | --- |
| IMP-P03-001 | REQ-004；OUT-004；P03 | ACC-P03-002/003 | 118常量、root metadata/spec |
| IMP-P03-002 | REQ-005、NON-007；OUT-005/046；P03 | ACC-P03-003/004 | 类型映射与禁用字段 |
| IMP-P03-003 | REQ-004～006；OUT-004～006；P03 | ACC-P03-002/003/005 | 原子注册器与schema预检 |
| IMP-P03-004 | REQ-007、CON-005；OUT-007/030；P03 | ACC-P03-004/006/007 | 普通Flow handler |
| IMP-P03-005 | REQ-006、CON-004；OUT-006/029；P03 | ACC-P03-005/007 | 故障fixture和stdio测试 |
| IMP-P03-006 | REQ-002/015/019、NON-002；OUT-002/015/019/041；P03 | ACC-P03-001/006/008 | 旧动态/跨平台注册退出 |
| IMP-P03-007 | REQ-007/019、CON-002/008；OUT-007/019；P03 | ACC-P03-006/007 | 118项真实启动证据 |
| IMP-P03-008 | REQ-016、CON-010/014；OUT-016/035/039；P03 | ACC-P03-001/008 | 两README与P04/P06交接 |

反向核对：ACC-P03-001由IMP-006/008；ACC-002由IMP-001/003；ACC-003由IMP-001～003；ACC-004由IMP-002/004；ACC-005由IMP-003/005；ACC-006由IMP-001～004/006/007；ACC-007由IMP-003～005/007；ACC-008由全部IMP形成最终范围证明。没有无主IMP/ACC或后序依赖。

## 10. 版本变更记录

- v0｜2026-09-05：依据需求v0、总纲v2、P01真实118项矩阵、P02公共契约和Win10VM MCP 2.1.1动态alias Spike首次写入；状态未审核。
- v1｜2026-09-05：作者自评前修正动态旁路退出边界，把旧`collect_artifact`和`hunt_across_fleet`纳入P03取消注册；补入17个空格参数的VQL反引号编码、extra=forbid参数模型及真实Spike证据。状态保持未审核。
- v2｜2026-09-05：引入首次全面审核AUD-001～004：冻结118项name/raw SHA-256产品门禁和精确UTF-8算法；将`list_windows_artifacts`一并取消注册；为118项真实调用冻结参数规则、敏感例外、单活跃Flow、强制取消确认和磁盘/内存/上传/墙钟阈值；把CON-014六术语动作/影响检查表纳入两README验收。作者引入后完整复评19/19/0/0/0/0通过，未命中再次审核条件，状态改为已审核。
- v3｜2026-09-05：实施中新事实触发PLAN-CHANGE-001：Snapshot184真实调用证明PacketCapture/Autoruns因P01已知外部工具缺失而在Flow创建前拒绝。P03不下载、不预置、不计错误成功，改为116项真实Flow加两项依赖延期边界；P05按原职责预置并创建安装后新快照，P06对最终全集真实成功。工具面、最终118项标准、Pxx数量和顺序不变；首次审核祖先有效，作者自评闭环通过且未命中再次第三方审核条件。

## 11. 审核重点

- 对象身份：P03；P01/P02均已完成，P04～P07尚未实施；没有调序、合并或重拆Pxx。
- 需求保真门禁：应承接19项规范切片，完整19，未落点0，语义弱化0，不可验收0，失效或复核条件缺失0；异常ID无。
- 方向闭合：批准集合固定118项name/raw hash且不从实际全集扩张；类型表、精确alias、optional/non-null省略语义、schema扩展、hidden/upload/YaraUrl、原子失败、结构化Flow返回、118项资源/取消门禁、六术语文档检查及旧注册退出时序均已冻结。
- 稳定依赖：P03的全部schema/stdio和116项真实Flow只依赖P01/P02；两项已证pre-flow依赖失败不计成功，由P05按原责任预置并创建安装后新快照，P06必须对最终全集真实成功。该边界不允许扩大到第三项。
- 协议反例：重点检查含空格参数名、显式null、bool/int混淆、choice/multichoice、未知类型、坏validating_regex、extra字段、outputSchema和失败前stdout是否真正可测。
- 范围风险：重点检查是否提前实现P04固定工具、P05 fixture、P06情景或P07物理删除，及是否错误保留Linux/macOS注册或删除其允许暂留源码。
- 资源风险：118项必须真实SDK启动但不并发轰炸；测试侧取消只能限于本轮Flow，不能成为产品接口或完整终态证据。
- fallback核对：只允许第8章六项及P02既有精确client恢复；无旧wrapper、通用collect、字符串降级、临时联网、忽略失败或部分启动fallback。
- 送审成熟度：方向阻断0、验收阻断0、占位决定0、无主依赖0；送审准入通过，审核路线固定为必须首次第三方全面审核。
