# 实施自评补充：P05 原件解析与 Windows 首轮纠偏

- 创建：2026-09-14，Asia/Shanghai。独立当日流水 11；创建前扫描同目录及子目录全部当日 Markdown，最大 10，写前复查无同名。
- 平台/角色：Codex；主会话为项目规划、修复与整合方。本记录为实施自评，不是第三方方案审核或实施后独立核验，不产生/关闭 AUD、CHK，不改变审核额度。
- 工作 HEAD：ac1c58a9439aaa303d071a8a040e0f26db85bbdb。当前工作树含既有未提交改动；本记录不声称 HEAD 包含下述实现。
- 上位：10号完整方案自评及 P05 v9 §0.8；需求 v2 blob 4906941e0698ffe7476838c4479ce344ce30ef27、总纲 v13 blob 038f1725f030e88124c800ea88d0daccb4b4fdbc、P05 v9 blob f2d03258ba8132134c1e821733f06e6801ad6fa7、P06 v10 blob a7229c5dda4067942a97c83e33b19edd79ea5eeb、P07 v7 blob 1f55a241904de8c716ac06cdd3f04113fdb9b62b。本轮未改这些规范对象。

## 1. 本轮实证，不冒充产品通过

Windows 隔离测试原件位于 `Logs/P05/windows-schema5-service-tests-6fbcd7cf-6f3a-4cc5-b417-9c916aa51a99/`：24项冻结源码包 SHA256 `21ff8d083f6af328759d006383ab16bdcdbd059eb4832dac70bd8cae03f2f702`，guest 逐文件 SHA/Git blob 校验；真实 Windows Python 3.13.7 测试 54 项，49通过、2失败、3错误、0跳过。`unit-mcp-transcript.json` 是原始消息；`guest-unit-result.json` 为从 id61 Response 原文解析的派生视图，其 SHA256 为 `17a4e87b3041e5dd3b5a56c69fa5af59a92fc3fc4202828d16304a0988768636`。源身份以该包 manifest 为准，不能套用于后续工作树。

失败已独立对照源码定位：Linux宿主 VMX 字符串被 Windows Path 当成本地路径；两处测试还要求已退役186；Windows fixture直接调用 POSIX 目录 open/fsync。前两类是本轮状态承接实现/测试遗漏；第三类是测试平台适配，不能据其推断 Linux 实际原子迁移失败。修复跨平台语义路径、187断言；Windows仍实测临时文件原子rename及字节保留，POSIX syscall顺序/异常传播独立mock测试；真实宿主迁移仍必须实际目录fsync，绝不移除或吞错。

该轮服务 ABI/配置/安全诊断/HTTP生命周期 mock 测试通过；安装器仅 PowerShell Parser.ParseFile 无语法错误。没有实际安装、服务启停或后端/业务网络调用。后续新增 ACL 门和原件解析不在旧通过范围。

只读部署原件 `Logs/P05/deployment-readonly-ZMHNtTfd/` 表明：目标 MAC/IP匹配；专用服务存在、Stopped/Auto；protected env缺下载根设置；repo/Python有AuthenticatedUsers Modify。前期错误抑制探针曾返回服务null，不能据此归因为用户改动或本轮引入。本次仅列修复前态，未写guest ACL/配置。

## 2. 实施范围及新增来源登记

下述仅实现既定§0.7/0.8可观察谓词和拒绝分支，不增加业务需求、工具、权限、fallback、快照代次或验收豁免：

- `tests/p05_snapshot_raw.py`、`tests/test_p05_snapshot_raw.py`：命令首末UTC、准确argv、退出码、完整stdout/stderr字节身份，实际vmrun总数、vmsd唯一displayName→UID/current→filename及guest真实MAC/IP/DHCP的解析。宿主路径只作身份文字，不在Windows解引用。
- `tests/p05_baseline_adoption.py`、`tests/test_p05_baseline_adoption.py`：接纳包完整Source/Ref/旧canonical字节及上述原始观测。禁止只搜名字/MAC/false字符串即通过；接纳包源集合新增实际接纳解析器和snapshot原件解析器。
- `tests/test_p05_recovery_namespace.py`：真实 selector CLI 限P05固定证据根；通用只读解析器仍可验证P06包内副本。canonical保留原始绝对位置，副本只按容器内记录/UUID尾部/SHA绑定，不回读旧宿主路径，不改写canonical原件。
- `tests/test_bridge_service_diagnostics.py`：仅固定公开错误分类和无secret解释/退出码，保留单一bridge/HTTP入口。
- `tests/p05_ready_evidence.py`、`tests/test_p05_ready_evidence.py`：完整十二类原始谓词；必须实际采集并在Windows正反例通过后接线，缺类拒绝。
- `tests/p05_activation_raw.py`、`tests/test_p05_activation_raw.py`：四链、依赖、完整server/client网络窗口、七入口正负例、PC006三要素、HTTP/stdio schema及实例join原件解析；完整网络窗口不能以首末两次连接列表替代。
- 既有服务安装器新增只读ACL检查：protected env先验ACL再读；API配置、下载根、日志目录及服务代码树分域检查，不自行修ACL、不改IP/WindowsMCP自启、不启停服务。既有手动/自动模式保留，新建仍手动。

上述新参与取证/验证的源码及对应测试必须纳入 activation 的 implementation_sources 和本次 Windows 冻结包；本记录纳入 source_inputs。后续采集器或验证器新增实际执行源码时同样先登记补充，不以列表非空充当来源闭合。冻结后如字节变更，重新打包、重新验证受影响项，不复用旧通过。

## 3. 保真自评与尚未开启的门

10号的25项P05/14项P06/14项P07逐项方案保真结论未因本次纯实现修正发生变化；这里只核对上述实现靶点，不重报80正式需求/67累计结论的完成数。服务参与者/权限、源码与原件输入输出、接纳→准备→初验→两候选周期→原子激活顺序、187物理保留、188零到一且不覆盖、失败原件保留、跨attempt拒绝、源漂移复测及独立核验条件全部继承，不弱化。

当前 canonical 仍是历史3/4/186，尚未执行实际187管理接纳；未恢复/创建/删除快照，未启用服务。新接纳实现Windows正反例、十二类真实ready、四链与网络完整原件、两周期、P06/P07、新HEAD独立核验均未完成。激活总验证器的缺原始语义终门继续关闭。不得以本记录或49项隔离单测替代任何上述门槛。

结论：本次纯实现纠偏和下一轮Windows隔离验证可继续；真实接纳、部署准备及产品验收分别以其前置条件实证闭合后推进，不预先授予通过，不关闭CHK。
