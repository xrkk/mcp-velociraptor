# 实施记录：P07 清理与终检 v2（2026.09.12-18 方案）

> 会话角色：实施执行方（主会话）
> 实施日期：2026-09-12（Asia/Shanghai）
> 方案：`2026.09.12-18-实施子方案-P07清理与终检.md`（审核有条件通过，AUD-001/002/003 三条件均已引入方案正文后实施）
> 前置：P06 差异化五场通过（`2026.09.12-21-总体验收记录-P06差异化五场重跑通过.md`）
> 本记录 HEAD：`bbc9849`

## 1. ACC-024 真实成本实测（CHK-014 闭合）

`tests/p07_cost_measurement.py` v2（提交 `bbc9849`），输出 `Logs/P07/cost-measurement.json`（SHA-256 `211eb44f7ec14997…`（初版 `966cc590…`，修正方法论文本中的提交哈希笔误后重生成，数字不变））：

| 维度 | 上游 78（同口径真实捕获） | 当前 130（正式会话捕获） | 差异 |
| --- | ---: | ---: | ---: |
| 工具数 | 78 | 130 | +52 |
| tools/list canonical JSON 字节 | 63,474 | 180,964 | +117,490 |
| token 估算（字符数÷4，口径已声明） | 15,868 | 45,237 | +29,369 |

- 上游捕获：git `9dc8054` 基线 blob 原样运行（FastMCP→MCPServer 垫片 `Logs/P07/upstream78-launcher.py`，垫片与探针已内容寻址归档并在方案 4.3 列入输入清单，AUD-002）；stdio 真实 `tools/list` 捕获 `Logs/P07/upstream78-tools-list.jsonl`（SHA-256 `78fa5a25…`）。
- 双方由同一 VM/venv/mcp-2.x SDK 渲染（P01 证据证明本环境从未有 mcp v1，上游代码只能以 2.x 渲染——同口径声明成立）。
- 固定调查任务成本（五场差异化正式报告真实数据）：总调用 4,357、逐场 1197/944/778/736/702、重试次数与逐调用结构化输出字节数在报告中逐场给出；上游任务维度无可比运行基线，按方案 4.2 边界裁决如实披露（AUD-003），不虚构。
- 结论不预设方向：当前产品在全部 schema 维度上大于上游（如实报告）。

## 2. 全回归（AUD-001 扩充清单，VM venv）

| 模块 | 结果 |
| --- | --- |
| tests.test_p02_core | 14/14 OK |
| tests.test_p02_transport | 25/25 OK（7 项无 API 配置时按设计跳过）；**HTTP/stdio 工具 schema 同一性终检**（`test_stdio_and_http_share_identical_toolset`）在 `VELOCIRAPTOR_API_CONFIG` 下单独执行 **OK** |
| tests.test_p03_dynamic | 6/6 OK |
| tests.test_p03_stdio | 4/4 OK（API 配置下） |
| tests.test_p04_fixed_tools | 15/15 OK |
| tests.test_p05_service_script | 7/7 OK |
| tests.test_p05_scenario_runner | 9/9 OK |
| tests.test_p06_contracts | 7/7 OK（含两两非同构门禁与非 ASCII 转义回归） |
| tests.test_p06_aggregate | 5/5 OK |
| p06_aggregate_reports（差异化证据） | status=success（2026.09.12-21） |

无静默省略；路径式/包式调用差异与 API 配置前提已在过程留痕（VM `Logs\P06\reg_*.txt`）。

## 3. 零残留与文档复验

- 危险开关标识跨生产代码/配置/两 README/example.env grep 零命中（审核方已独立复核；本轮再复验维持零命中）。
- 两 README 与实际启动/配置路径一致；P06 差异化重跑后无过时表述需要修正（快照/场景表述未因差异化改变口径）。

## 4. 提交链

| 提交 | 内容 |
| --- | --- |
| `bf3c3f4` | P06 五场景差异化（CHK-013） |
| `efe3144` | probe 非 ASCII 修复 + 回归测试 |
| `bbc9849` | ACC-024 真实成本测量 v2（CHK-014） |

## 5. 残余与边界

- P07 方案第 5 节（ACC-025 七篇索引/追溯矩阵/最终验收记录）以独立最终验收记录收口（`2026.09.12-23`）。
- 成本口径中 token 为文档化近似（chars/4），不冒充 tokenizer 实测。
- 上游 78 任务维度基线不存在为客观事实，按边界裁决披露。
