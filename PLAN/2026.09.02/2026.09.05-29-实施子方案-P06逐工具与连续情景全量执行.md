# P06 实施子方案：逐工具与连续情景全量执行

> 正式作者版本：v18（PC020；PC017/018/019与PLAN-CHANGE-014继续有效；待主方完整自评）
>
> **当前状态**：作者采纳已同步／待主方完整自评／实施门关闭；不把旧报告升级为当前覆盖。
>
> **当前九对象组**：`CURRENT-PC020-R01`；需求`v8`、总纲`v21`、P01`v4`、P02`v7`、P03`v3`、P04`v10`、P05`v17`、P06`v18`、P07`v15`。正式身份由`PLAN/2026.09.20/current-normative-inputs-pc020-r01.json`描述；`CURRENT-PC017-018-019-R02`保留为历史起点。

> **PLAN-CHANGE-014 覆盖块（2026-09-14，用户裁决）**：移除与 FakeNet-NG 撞车的网络域机制（评估与实施见 `2026.09.14-16-PLAN-CHANGE-014裁决-移除Velo方案中FakeNetNG撞车网络机制.md`）。要点：
> 1. PacketCapture netsh-trace 链整体移除：P05 四链→三链（Autoruns/Triage/KillProcess）；P06 资格链与覆盖集随之调整。
> 2. etl2pcapng 依赖移除（唯一用途为该链转换），依赖清单 3→2。
> 3. 完整网络窗口硬门降级为可选支撑证据；"零公网来源请求"保留为设计不变量（执行层=serve_locally 架构+已移除的上游探针），不再要求全窗口证明。
> 4. 注册面 130 不变；逐工具 129 实测 + 1 登记排除（Windows.Network.PacketCapture，排除原因=网络抓包职能划归 FakeNet-NG 域且本 VM netsh 机制异常）；覆盖关系 650→645；资源 admission 13→11。
> 5. 保留：localhost:8000/public 工具本地服务（Velociraptor 原生机制、零公网唯一执行层）、PC006 防火墙边界、全部安全/权限/回滚规则。
> 本块以下旧文本中与之冲突的"四链/650/13 admission/网络窗口门/etl2pcapng"表述均为被覆盖历史。

>
> 状态：新基线配对已承接，待本版完整自评与P05真实激活；20号记录不授权当前现场的恢复、资格或五场执行
>
> 会话角色：作者/执行方
>
> 来源类型：GrillMe 总纲实施子方案
>
> 历史输入（仅追溯，不作为当前执行引用）：总纲`2026.09.02-02-总纲-mcp-velociraptor-Windows-DFIR二次开发.md` v13，Git blob `038f1725f030e88124c800ea88d0daccb4b4fdbc`
>
> 历史输入（仅追溯，不作为当前执行引用）：需求`2026.09.02-01-需求提炼-mcp-velociraptor全阶段设计.md` v2，Git blob `4906941e0698ffe7476838c4479ce344ce30ef27`；原P06直接适用REQ-021/022/023/026、CON-002/004/008/015、NON-009/010、RACC-021/022/023/026
>
> 前置提交：P01 `9dc8054daf2981a28ced316ee24a6c3ab8faa9b3`，P02 `4087f0a9df564a8950b5e2787df83cf3caa112d5`，P03 `1640284ff42bb5d5265afd173d0837f82b5251bb`，P04 `814dc829a11b38e59288a4120b715438bd802f6b`，P05 `d142760e29b350b0706869296444c317dfbba936`
>
> workflow_id：`wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2`

## 0.PC020 P06消费契约（v18正式作者版本最高优先条款）

本节覆盖下方§0/§1.0、IMP-P06-002/003/005/006/008、ACC-P06-001～006和fallback中冲突的未来snapshot/schema/epoch、恢复kind、封包和选择表述；历史原件不改。PC014的130注册、129实测+1登记排除、645关系，PC017文件/分诊、PC018分页、PC019真实初始状态及全部安全/资源门保持。

### 入口和配对

P06唯一入口是P05已原子提交并回读的schema6/epoch8/`NETWORK_ACTIVE`，active及allowlist唯一为`Snapshot 189-Velociraptor-MCP可恢复验收基线`，migration/preparation Ref与epoch7逐字节相同且activation Ref有效。selector、逐工具资格、资源资格、五个scenario/index、receiver、final-selection和aggregate都必须绑定这一身份。schema5/epoch6/188仅是精确migration历史前驱；schema6/epoch7/187只是P05准备；旧186、旧4/5、旧报告/预算、未激活189均产生零正式覆盖。

上游stage配对必须分开验证实际恢复snapshot与canonical读回：`P05_REPAIR_INITIAL`恢复187且读回schema6/epoch7/`PREPARATION_BASELINE`/active snapshot 187；`P05_REPAIR_CANDIDATE`恢复189但仍读回schema6/epoch7/`PREPARATION_BASELINE`/active snapshot 187；只有`P06_ACTIVE`恢复189并读回schema6/epoch8/`NETWORK_ACTIVE`/active snapshot 189。candidate canonical若提前成为epoch8或active snapshot 189，receiver、selection和aggregate必须拒绝。

每个P06恢复的stage固定`P06_ACTIVE`，恢复record kind集合恰为`snapshot_metadata/revert_operation/pre_start_marker/post_restore_hostname/canonical_readback/pc020_migration/pc020_preparation/activation_evidence`。每项仍为exact `kind/path/sha256`，集合唯一无额外项；三份PC020图按P05固定相对路径递归带入并重算，不读取宿主旧绝对路径，不把旧baseline adoption或旧activation当当前kind。

其中新189 activation根必须含required `issued_at`，为合法UTC RFC3339 Z字符串并受根SHA覆盖；P06还须验证canonical activation Ref的`activated_at`与其逐字相等。缺失、null、非法、不等或从mtime/当前时钟/receipt补值均拒绝；不以时间戳大小新增跨机顺序门。

### 封包与manifest

P06接收每个selected run时验证完整P05 migration、preparation和activation图（含`issued_at`、根SHA和canonical `activated_at`等值）、当前九对象source identity以及本run的restore_attempt_id/run_id/session/server/process/Flow/raw/ready/report绑定。package manifest对其明确管辖子树`T`执行`members = all plain files(T) - E`；`E`仅为manifest自身及同层直接引用根record。外层必须列出嵌套manifest、admission/migration/root/report和所有普通原件；任意排除、链接/reparse、逃逸、缺件、重复或冲突身份失败。根record由外层Ref或selection绑定，不写自hash。

多个P06包可以复制相同的规范/产品source、冻结preparation/migration和189 creation等不可变上游；相同路径/hash本身不是跨attempt失败。每个P06 restore和每个P05 Phase的运行事实不可复用：restore_attempt_id、run_id、Flow、raw/ready/network/service/report、session/process及实际Triage/file必须用attempt/time/身份语义独立验证。

### 五场、选择与聚合

1. 五个受审scenario/index全部固定189+`P06_ACTIVE`及其实际source/index SHA；runner不得以CLI/environment替换snapshot、stage、证据根、参数或source。
2. 五场分别从189执行独立完整恢复，产生不同restore_attempt_id/run_id和各自HTTP session证据；每个scenario必须执行其受审步骤且至少发生10次真实`tools/call`。旧attempt、失败重跑片段或另一场运行事实不得拼入。
3. receiver只接收完整immutable package并逐成员反算；账本只追加。final-selection每scenario只选唯一完整成功run，显式拒绝旧188、epoch7、缺任一PC020图、旧source/budget、重复attempt或status非success的包。
4. aggregate重新运行与selection相同的图、stage、manifest、session和业务谓词，要求五个不同恢复attempt；全局覆盖集合由129个实际工具各自在5个不同scenario中出现一次构成，故恰有`129 × 5 = 645`个唯一tool×scenario关系。PacketCapture是唯一登记排除，不属于129个实际工具。645是五场聚合后的关系总量，不是每场调用目标；同一tool在同一scenario中的重复调用或等待不增加关系数。129实测+1排除及645关系差集均为0；不能以summary passed、隔离模型PASS或文稿升版升级旧证据。

### IMP/ACC与失败归属

- IMP-P06-002/003更新schema/index、资格与验证器，使其消费schema6/epoch8/189及八类恢复记录；IMP-P06-005按五次独立189恢复执行；IMP-P06-006只读聚合；IMP-P06-008维持虚拟机外官方SDK直连28790及同场单服务进程/单HTTP会话。
- ACC-P06-001要求129实际工具和12固定工具约定链在新代次闭合，PacketCapture仍为PC014唯一登记排除；ACC-P06-002要求每场受审步骤与至少10次真实`tools/call`，并在五场最终聚合时达到129个工具×5个不同scenario=645个唯一关系，重复调用不重复计数，绝不要求单场645；ACC-P06-003验证声明式runner；ACC-P06-004保留失败、修复、整场重跑；ACC-P06-005交P07包含签发时间绑定的不可变identity；ACC-P06-006持续验证Bearer/Host/Origin、防火墙、HTTP/stdio schema同一性及秘密零泄露。
- 任一P05图、canonical、恢复身份、source、manifest、资源、安全或场景失败均不运行/不选择/不聚合；环境或P05入口失效回P05，工具业务错误回其唯一负责P02/P03/P04。P06不得修改VM来修复入口，不得选择旧188或另一snapshot，不得自动重试提限或把旧预算/报告重新贴标。

P06全部六项ACC通过后才可生成P07 handoff；本正式作者版本不表示任何正式运行已发生。

## 0. 目标、范围与排除

历史v10/v9配对文字仅追溯其形成过程；当前执行引用只接受上述`CURRENT-PC017-018-019-R02`组及`PLAN/2026.09.20/current-normative-inputs-pc019-r02.json`，并消费当前总纲v20 §2.0.3/2.0.4/2.0.5和P05 v16 §0.8/§1.0.1。P06_ACTIVE必须为已激活188、canonical schema5/epoch6/NETWORK_ACTIVE；schema5/epoch5/187仅供P05准备，不能进入本阶段。所有资格/逐工具/五场/index/scenario/策略/预算/接收/选择/聚合同时冻结此身份并重算源hash，不允许CLI替换。恢复原六类原件新增baseline_adoption，完整接纳目录与activation目录按原字节递归封包、去重和校验，引用路径不改写。旧186、4/5、准备5/5、跨代次、缺接纳原件或只含passed摘要均拒绝。P05源或配对改变时旧资格预算失效，重新完整资格和受影响整场执行。P06仍不修环境、不补fixture、不改时钟或网络来满足恢复门；失败回P05。

交P07只固定当前`CURRENT-PC017-018-019-R02`九规范组、`PLAN/2026.09.20/current-normative-inputs-pc019-r02.json`及其未来完整自评/实施/原件核对身份；旧`CURRENT-PC017-018-019`、旧“当前需求、总纲v13、P05v9、本P06v10”、旧作者组及PC017候选组仅为历史输入，不再共同指导执行，也不能以20号或旧版14/14计数替代。字段形状只消费当前P05 §0.8，不再以旧“六类”文字漏掉第七类。PC017的有向入口明确为P05初验与两个候选恢复周期已完成真实Triage回填、随后P05原子激活；P06只从该已激活基线执行正式资格/逐工具/情景，完成后才交P07。r3证据根、130工具、PC014 129+1/645关系、正负安全回归与其他阶段顺序不变。

PLAN-CHANGE-012 最高优先门禁：用户已删除旧快照并恢复唯一 `Snapshot 187-固定IP+WindowsMCP开机自启`。当前总纲 v12 §2.0.2、P05 v8 及 `2026.09.14-01` 覆盖本文 §1.0 和所有旧未来执行指令。旧186/schema3/epoch4及旧186→188/schema4/epoch5迁移均不得执行，不能仅替换名称来伪造新激活；用户恢复不计入执行方恢复验收周期。必须先由P05完成新起点/候选/状态配对契约、主会话完整保真自评和真实激活证据，再同步P06所有消费者并重新自评。本文14/14旧计数不证明v9已通过。五情景650关系、真实成功链、资源资格、原始证据和失败整场重跑要求不减少。

`2026.09.14-08` 的明确文件互传授权解除传输许可阻断，不解除上述恢复/正式执行门；允许与基线无关的隔离源码测试，但不计正式覆盖。控制目标必须核对MAC `00:0c:29:83:b8:65` 和当前固定IP，不使用指向其他克隆的既有Win10VM连接器，不使用屏幕键盘。旧正文连接器表述仅可由经核实目标的控制通道承接，不能用控制通道代理产品调用。P06/P07仍不得自行发明P05状态配对。

P06只完成最终130工具的逐工具真实链和连续情景全量执行。v2已有测试资产和内存采集修复保留，但实现因Q-072暂停；v3必须等回流后的P02/P05通过并激活Snapshot186，随后由虚拟机外官方SDK客户端直连VM内正式`28790/mcp`执行。VM内stdio只作快速诊断，不计逐工具或650关系正式覆盖。P06不重新设计接口、不临时安装依赖、不清理旧源码、不做P07成本测量，也不把runner变成生产编排器。

### 0.1 v3对旧正文的规范性覆盖

- 旧正文中“Snapshot185”作为P06恢复起点的全部未来动作改为Snapshot186；Snapshot185只作为P05网络部署前 predecessor 和失败回退证据，不得在P06常规恢复。
- 旧正文中“同一bridge进程和同一stdio会话”的正式情景要求改为“同一VM内Windows服务进程和同一虚拟机外有状态Streamable HTTP会话”。130×5=650、每情景至少10次tools/call、失败整场清零和五情景业务目的均不变。
- Win10VM MCP继续只用于恢复后hostname、guest测试、服务/后端取证和证据取回，不得代为调用mcp-velociraptor。外部runner必须记录直接端点、HTTP会话ID和VM服务进程身份。
- 已进行但未满足正式网络拓扑的P06资格/逐工具/情景attempt不计最终通过；其资源证据和`Windows.Memory.Acquisition` 3600秒/8GiB修复仍可继承，须在Snapshot186及正式入口重跑。

核心提交范围固定为：

- `tests/scenario_runner.py`：只做P06全量执行所需的索引选择、报告归属和严格成功覆盖统计，不改变其测试专用性质；
- `tests/data/p06_scenario_index.json`、`tests/data/p06_coverage_manifest.json`：五个受审情景的哈希索引和650条工具—情景关系及调查理由；
- `tests/scenarios/full/*.json`：五个完整声明式情景；
- `tests/p06_individual_acceptance.py`：最终tools/list双向核对、逐工具完整成功链和12固定工具约定错误的独立验收；
- `tests/p06_aggregate_reports.py`：只读合并五份已完成报告，机械验证覆盖、会话连续性和无失败，不启动server、不恢复快照；
- `tests/test_p06_*.py`：manifest、场景、runner和聚合器的独立golden/反例测试；
- 两README中的P06测试入口、证据和Snapshot186边界说明。

直接依赖范围只读P03的118项实际invocation清单、P04的12固定工具schema/生命周期、P05的fixture/index/runner和本地恢复selector。只有P06真实失败证明现有实现违反已冻结契约时，才允许对`mcp_velociraptor_bridge.py`、`velociraptor_api.py`、`velociraptor_mcp_core.py`、`velociraptor_dynamic_artifacts.py`、`velociraptor_fixed_tools.py`或`velociraptor_env.py`作最小契约保持修复，并重跑负责Pxx测试；工具名、schema、权限、目标、数据流或验收边界变化必须登记PLAN-CHANGE并回流对应方案资格，不能藏在P06实现中。实际路径以HEAD tree为准，旧名称`velociraptor_core.py`和`velociraptor_dynamic_tools.py`必须被机械拒绝为不存在。

排除其他模块、agent_poc业务代码、P07旧源码删除、VM中无关进程/文件、Velociraptor无关Org/client/datastore对象和宿主其他虚拟机。P06不提交API凭据、guest运行态、Flow上传物、内存镜像、PCAP、官方依赖字节、PLAN或Logs。

### 0.2 PLAN-CHANGE-009｜不可变新证据纪元与资源观察承接

来源为 `2026.09.13-07` 的 CHK-024/025 及 `2026.09.13-08` 的持续未闭合证据。旧账本、摘要恢复文件和占位 canonical SHA 不得修改成看似合格的历史。负责层为 P06，P05 的 schema-v2/schema3/epoch4/恢复协议不变；需求 v2 与总纲 v10 不变。

- 本次以及未来动作的固定证据根改为 `Logs/P06/wf-01a05d1d-p06-r3/`，只在代码常量中冻结，不增加可任意选根的正式 CLI。正文中旧 `wf-01a05d1d-p06/` 的历史事实和旧记录不搬移、不重哈希；作为未来产出路径时由本段覆盖。r3 内按 `<scenario-id>/<run-id>` 分隔不可变 attempt；所有失败、资格、逐工具和成功都登记新的只追加接收清单。另保存包含旧根、旧清单/selection/aggregate 文件 SHA 和持续开放 CHK 的历史根索引，历史失败不可丢弃，也不得作为当前成功包混入。
- P05 的六类恢复原件在 r3 下按唯一 restore_attempt_id 保存，canonical 文件是读取当时的完整 JSON 原字节，activation 是 canonical 引用的原字节；snapshot-evidence 中的路径相对于 r3 根。每份报告包带原始服务观察、HTTP 响应头转录、fixture 原件副本及真实 tools/list 字节，原件全部纳入递归包哈希。反向核查拒绝引用未落地、哈希不等、旧 schema、跨 attempt/会话/实例拼接。
- 外部 runner 消费根内 `fixture-instance.json` 的完整原件副本，并按 P05 唯一字段形状和原 spec SHA 验证；不增加 fixture 内容、工具、参数或任意命令覆盖入口。宿主产生的正式报告直接在 r3 留存；guest 原始取证在下一次恢复前通过既定单请求 host-only 28786 通道回传并验 SHA。正式产品端口仍只为 28790。
- 资源资格仍必须在外部官方 SDK 的一个正式 HTTP 会话中执行全部9个资源敏感工具、PacketCapture两条完整链、Triage、fixture采集及下载。Windows MCP 仅执行固定只读资源观察（内存、C卷、datastore、下载根、报告目录），不得代理任何产品 tools/call；控制面原始转录及 hostname/服务 PID/run_id/观察时间独立保存在 `resources/`，读数放入报告既有 steps。地址由正式 endpoint 的实测 guest IP 派生；不把退役控制VM地址或宿主读数当作目标资源。观察失败、身份漂移、陈旧读数或目录不可读时 fail closed，不把不可读目录计零。
- 资格结束先冻结完整原始 `qualification-payload.zip`（含报告、服务/头部/资源及恢复原件），再以其实际字节数生成 `resource-budget.json`，避免报告哈希/包大小自引用。预算绑定报告和ZIP SHA；`resource-qualification-selection.json` 显式选择唯一成功资格，消费方重算原始读数、峰值、增量、原P05上限与两倍实测deadline，并核对资格实现源码集合。任何资格实现/场景预算语义改变均重新资格，不沿用旧预算。
- 正式场景开始和每个大Flow启动/下载之前使用当前读数与资格剩余增量重算门禁；不得仅检查固定4GiB。各步骤与整场有可执行超时；拒绝必须发生在产品调用前，报告保留阈值/读数/原因，coverage为空。源码及反例先在Win10验证，随后通过完整恢复和正式HTTP重新资格/五场/聚合。

受影响稳定边与切片：OUT-021/022/023/053/054 → P05恢复与schema契约 → IMP-P06-002/003/005/006/008 → ACC-P06-001～006 → P07入口。仅改变证据纪元组织及落实缺失的观察/预算承接，不改变130工具、650关系、五个目的、唯一Snapshot186、服务权限、安全边界或失败规则。重新裁决条件：控制面无法只读观测、实际容量不足、资格字节不可闭合、P05恢复条件失效；触发即停止并回负责层，不能降级或新增fallback。此登记不关闭CHK，也不消耗/重置独立核验额度；必要方案复审单独留档。

### 0.3 r3 可机械验收的封包与门控细则

本节补足第0.2节，不改变P05报告根字段。每个attempt封包前递归枚举run目录全部普通文件，拒绝任一路径分量的链接/reparse、特殊文件、绝对路径、空分量、`.`、`..`、反斜线和重复路径。包内名称为`run/<相对路径>`；恢复六类原件以`restore/<相对r3根路径>`加入，按完整名称去重，同名不同内容即失败。必须含report、snapshot-evidence、六类恢复原件（含完整canonical与activation）、fixture-instance、server-observation、tools-schema、原始tools/list、http-headers及报告引用的全部resources原件；失败attempt允许缺尚未产生的成员，但须在报告failure中逐项声明，不能被成功selection接受。

`package-manifest.json`固定形状为`{"schema_version":1,"members":[{"path":名称,"size":原字节长度,"sha256":小写SHA256}]}`。members按名称UTF-8字节升序；JSON使用UTF-8、ensure_ascii=false、sort_keys=true、separators=(",",":"), allow_nan=false，末尾恰一个LF。manifest自身和外层传输ZIP不属于members；package_sha256精确定义为此manifest原字节SHA256。接收账本与final-selection同时引用manifest相对路径、manifest_sha256和相同的package_sha256；若有传输ZIP另记archive_sha256，不混用两类hash。不得在封包后更改成员，新增证据须新attempt或独立补充记录。

资格封包分两层防自引用：先以同算法生成`qualification-payload-manifest.json`，其成员排除自身、最终package-manifest、qualification-payload.zip及resource-budget.json；ZIP仅含该payload manifest与其成员，预算绑定payload ZIP实际长度/SHA及report SHA。预算完成后最终package-manifest包含payload manifest、ZIP、预算与全部普通原件，但不包含自身。普通场景包不使用资格排除项。聚合器同时重算两层；缺成员或附加未声明成员均失败。r3根的历史根索引逐项保存旧根路径、旧ledger/selection/aggregate存在事实及存在文件SHA，缺失写明确reason；新账本不可用此索引代替新attempt原件。

正式执行前生成并冻结`tests/data/p06_resource_policy.json`，在P06 index绑定其SHA；每个固定scenario的每个tool step恰有一行`scenario_id/step_id/tool/class/reason/qualification_label/owner_step_id`。class只有`admission`、`dependent`、`bounded`：9项resource_sensitive中的8项各一个启动，加PacketCapture start/stop两个启动，再加Triage、fixture collect、download，共13个admission；各自status/results/files为dependent并显式绑定owner；其余为bounded且给出受审安全参数/有限输出理由。工具步骤与策略表双向差集必须为0，不得运行时自行将未分类步骤判为bounded。所有步骤有deadline；13个admission在产品调用前读取并判门，其dependent逐步保留资源读数及同Flow证据。策略、invocation、scenario/index或资格实现hash改变即重新资格；低空间、漏分类、缺读数、无deadline或错owner均失败且coverage为空。

## 1. 已证实入口事实和恢复门禁

### 1.0 PLAN-CHANGE-011 的未来执行优先规则

本版新自评已由20号记录完成；下文“另立自评/未完成前”仍为一般失效门，不表示已经登记的自评缺失。实现源码、策略或输入改变后仍按原重新资格规则执行，不能用方案自评替代运行证据。

依据总纲v11 §2.0.1、P05 v7 §0.6及同目录19号用户授权，本文其余章节中的Snapshot186/schema3/epoch4作为未来P06入口统一被已激活的 `Snapshot 188-Velociraptor-MCP可恢复验收基线` / schema4/epoch5/NETWORK_ACTIVE 替代；186仅为P05重建前驱和历史证据，不能用于新P06常规恢复或fallback。183/184/Snapshot1/186退役顺序与状态逐项消费P05新canonical，不接管用户187。旧版本变更、失败attempt和历史引用不重写。

进入P06前必须逐层验证P05新自评记录、实际实施提交/源hash、初验、188创建实值及两个独立恢复周期的完整ready门/四链/正式代表报告/安全边界原件、原子激活与canonical回读；不得仅凭activation摘要中的passed。ready门含静态fixture、专用进程和持续父链、时钟120秒前提。各次P06恢复只能只读验证这些前提，不得重建fixture、调整时钟配置或重启后端代替恢复起点；不成立则按FB-P06-004回P05。

五个固定scenario/index的required_snapshot必须一并固定188，阶段仍P06_ACTIVE；资源策略、coverage关系中的基线前提、逐工具/资格入口、六类恢复验证器与聚合器一起更新并重新计算实际输入hash。P05的共享schema2及26根report、current-restore和snapshot-evidence exact keys不变；新配对唯一消费P05 §0.6。明确拒绝186、未激活188、旧epoch/schema/retired、跨attempt原件、缺时钟/父链及缺完整激活证据。

固定r3根保持不变，已有restore-preflight与历史根索引原样保留。新资格、逐工具、五场、接收/selection/aggregate全部使用新run/restore身份，旧预算不得沿用；任何受绑定源/策略/基线改变均重新资格及受影响整场执行。130工具、五个目的、650关系、参数/断言/状态链、资源预算与副作用闭合标准不变。稳定边为P05恢复/fixture/共享报告→IMP-P06-001～008/ACC-P06-001～006→P07；不增加产品接口、权限或fallback。

本文未来第三方复审门依19号授权改为主会话完整自评通过；旧v7复审12号及“需求/总纲v11/P05v7/P06v8”绑定只保留历史。交P07时绑定页首当前九规范组、`PLAN/2026.09.20/current-normative-inputs-pc019-r02.json`及未来完整自评后的实际OID、新自评/实施/独立证据核对记录；不得预填尚未生成的OID。正文旧14/14自评只属旧版，本作者修订的新自评另立记录，未完成前不宣称自评通过。

1. P04冻结的最终产品面经SDK实测为118动态+12固定=130个唯一工具。P03的`tests/data/p03_invocations.json`是118项已审安全参数起点；P04的`tests/data/p04_fixed_tools_golden.json`冻结12项schema。
2. P05 v2的已证历史是Snapshot185/schema2/epoch3/`DEPENDENCIES_ACTIVE`及四条依赖链；它只是本轮P05网络部署的predecessor，不是P06入口。P06开始前必须实际读取P05回流完成记录及两轮证据，确认canonical精确为schema3/epoch4/`NETWORK_ACTIVE`、active与automatic allowlist唯一为`Snapshot 186-Velociraptor-MCP网络部署基线`、183/184/185均manual-only，并从metadata读回Snapshot186的实际非空`.vmsn` marker；这些值不得从旧证据或本文预填。
3. 每个资格、逐工具或情景attempt开始前，host必须从canonical selector取得上述active对象，确认`listSnapshots ... showTree`唯一匹配Snapshot186，只执行一次`revertToSnapshot`；回读VMX准确marker后只执行一次`vmware -x`，最后用Win10VM只读hostname=`DESKTOP-3FI41GR`并核对API、唯一client、正式服务、权限/ACL、28790监听/防火墙完成门。标记被`vmware -x`秒退消耗时按用户规则开始新的单次revert attempt；标记缺失时禁止直接`-x`，禁止冷启动。
4. Snapshot186内必须是P05回流通过时的代码、依赖、fixture、专用运行API principal和正式服务状态。每次恢复完成后，必须用仅绑定`192.168.204.1:28786`的单请求host-only通道把当前P06候选测试源同步到`C:\mcp-velociraptor`，双端SHA-256一致后立即停止监听；不得修改依赖、API身份、服务配置、防火墙或产品配置。每份guest证据也必须在下一次恢复前同法回传host。
5. Snapshot186中的fixture instance、依赖、root artifact、专用测试进程和28790服务是每个attempt的共同干净起点。每个情景结束后专用测试进程可以被本情景的`kill_process`结束；下一情景必须通过完整Snapshot186恢复取得原进程，不以局部重建或服务重启代替快照起点。

以上P06入口以P05回流完成记录、Snapshot186双周期激活证据和用户恢复规则为唯一规范来源；本节的Snapshot185文字仅陈述predecessor历史。若schema3/epoch4、selector、唯一匹配、marker、hostname、API、唯一client、权限、服务、防火墙、依赖或fixture任一不符，该attempt在任何正式调用前失败，不使用Snapshot183/184/185或冷启动fallback。

## 2. 五个完整业务情景与650关系

REQ-022要求每个最终工具至少进入5个不同真实调查情景。130个最终工具的最低关系数为`130 × 5 = 650`。本方案采用正好五个覆盖全工具面的综合endpoint调查，而不是构造65个小场景或随机组合；每个情景都在一个新Snapshot186恢复attempt中，由一个虚拟机外官方SDK客户端连接同一VM内服务进程并保持一个有状态Streamable HTTP会话运行全部130工具，因此关系矩阵恰为650，且每个情景远超10次tools/call。

五个稳定情景及其独立业务目的为：

| scenario_id | 业务目的 | 重点问题 |
| --- | --- | --- |
| `p06-compromise-scope` | 已确认恶意执行后的整机受害范围判定 | 哪些执行、身份、文件、内存、网络、注册表和持久化证据表明主机已受害，影响边界在哪里 |
| `p06-ransomware-root-cause` | 勒索事件的入口、执行链、持久化和影响根因重建 | 初始进入、脚本/二进制执行、服务/任务/注册表持久化、文件系统影响和网络活动怎样形成时间链 |
| `p06-credential-lateral-movement` | 凭据访问与横向移动调查 | 登录、远程访问、用户/权限、缓存凭据、进程/服务、网络连接和落地痕迹能否证明账户滥用与横移 |
| `p06-data-exfiltration` | 数据发现、暂存和外传调查 | 浏览器/近期访问、文件/NTFS、命令、进程、网络与时间线证据能否界定被访问和可能外传的数据 |
| `p06-remediation-validation` | 隔离与处置后的残留风险复核 | 已知持久化、异常执行、网络、账户、文件、内存和日志面是否仍有残留，取证包是否足以支持关闭事件 |

`p06_coverage_manifest.json`必须为每个scenario列出130个不同工具。每条关系的固定字段为`scenario_id/tool/step_ids/parameters_sha256/fixture_preconditions/assertion_ids/investigative_question/expected_evidence/allowed_empty_condition`：`step_ids`至少一个且必须双向指向scenario源中的实际tool step；参数规范化SHA必须与这些step的实际arguments一致；fixture前置和断言ID必须实际存在；`investigative_question`与`expected_evidence`必须针对该scenario，允许空结果时还须给出精确条件。五组工具集合都必须与同一实际`tools/list`完全相等；文件只允许人工确定的上述五个情景，不提供随机seed、组合数、工具筛选或CLI覆盖。

关系真实性不是靠非空文字自证。独立golden必须拒绝缺step、孤立step、参数/断言hash漂移、复制的调查问题/预期证据、没有fixture或前序输出关系、以及报告中没有对应成功call/assertion的条目。对零参数或必须使用相同安全参数的工具，五条关系仍须分别绑定不同scenario的前序证据角色和场景特定断言；例如同一浏览器历史空结果，在受害范围场景回答“是否存在已知下载入口”，在外传场景回答“是否存在云盘/上传站点访问”，二者断言和允许空结果条件必须落到不同step/assertion，而不能只改说明。最终聚合输出`manifest relation ↔ scenario step ↔ report call/assertion`三向差集，650条均为0才通过。

同一工具在同一情景调用多次仍只计一个关系。sleep、initialize和tools/list不计tools/call或关系。关系只有在整个情景最终`success`、该工具至少一次调用`isError=false`且对应断言通过时才有效；失败或未执行情景贡献0关系，不能保留“前半段已覆盖”。

## 3. 逐工具真实成功链

### 3.1 118个动态工具

每个情景按受审固定顺序调用全部118项。普通动态工具执行以下闭环：

1. 以`p03_invocations.json`的显式参数为安全基线；可把no-match标记替换为该scenario固定标记以证明参数送达，但不得新增上传、本地规则文件、任意路径扩大或外部网络参数；
2. 动态工具调用必须`isError=false`并返回真实非空`flow_id`和初始状态；
3. 同会话用`get_flow_status`轮询到`FINISHED`，`ERROR`、超时、未知ID或伪状态均使整个情景失败；
4. 同会话用`get_flow_results(flow_id,page_size=1)`取得结构正确的结果页。正常空结果只在coverage manifest为该工具/情景解释无命中合理性时允许；
5. 有上传语义的artifact还须`list_flow_files`成功，实际文件可为空但结构和Flow身份必须正确；文件内容不自动下载，内存镜像等二进制不进入报告或Git。

`Windows.Network.PacketCapture`是唯一双调用动态链：开始前外层只读确认trace inactive；同会话以`StartTrace=true`完成开始Flow，再用已由P05实证的测试部署trace路径完成`StartTrace=false`停止/转换Flow，要求两Flow均FINISHED、PCAPNG/ETL文件清单存在且末尾trace inactive。该工具在一个scenario内虽调用两次仍只计一个关系。

`Windows.Memory.Acquisition`必须真实完成，不允许“启动后立即取消”冒充成功。每个情景从独立Snapshot186开始，完成后只记录Flow/file元数据和大小/hash，不下载或回传内存镜像；下一情景恢复Snapshot186清除该运行态。运行前记录guest可用空间，空间不足时该attempt失败并回传证据，不降低为取消覆盖。

### 3.2 12个固定工具

每个情景均执行同一套职责完整、参数随scenario/fixture变化的固定链，确保12项各有5个不同情景关系：

- `run_vql`执行只读、有限行查询并断言结构；
- `collect_file`采集本情景指定的一个fixture文件，等待FINISHED，依次由`get_flow_results`、`list_flow_files`和`download_flow_file`完成结果、枚举与单文件下载。五个情景使用五个不同fixture文件；每次恢复使下载根回到干净起点；
- 启动一个额外的`Windows.System.PowerShell`长sleep Flow，在创建时登记为本scenario唯一`cancel-target`并保存flow_id，确认非终态后由`cancel_flow`成功取消。只有该已登记owned ID、`cancel_flow isError=false`、before/after ID与状态均匹配时，后端`ERROR`才分类为`expected_cancel_terminal`；它计`cancel_flow`成功关系但不计PowerShell动态成功，也不进入unexpected failure。未登记、错ID或任何普通动态Flow的ERROR一律使整场失败；PowerShell另有`exit 0`且FINISHED的正常链；
- `start_hunt`只对唯一client启动长sleep PowerShell，`get_hunt_status`取得同一hunt/flow，`stop_hunt`只停止调度，随后`cancel_flow`显式结束仍运行的主要Flow。每个固定工具调用本身均须`isError=false`；
- `collect_forensic_triage`保持无参数；P06只读验证P05已完成来源锁定、三次真实回填和激活，并在本次资格/正式执行前由验收工作流复核目标、实际存储同卷与资源筛查；该筛查不是产品调用错误接口。在同一正式会话确认内部请求仅此入口使用Targets JSON数组字符串、timeout=2400和max_upload_bytes=4294967296，记录真实flow ID，等待FINISHED并读完results/files；完整目标、glob、源采集及HashOnly默认不得删减；
- `kill_process`在紧邻调用前核对fixture PID、creation-time和token，等待FINISHED并验证`Killed == 输入PID`、目标消失和受保护进程不变。

固定工具约定错误另由逐工具验收脚本在不计覆盖的会话中验证准确`isError/code/retryable/details`，至少包括未知Flow/Hunt/file、终态cancel、非法pid、非allowlist Hunt、路径逃逸和错误VQL。预期错误只证明错误契约，不进入650关系；五个正式情景中的工具调用必须全部按预期成功。末尾owned对象分列`finished`、`expected_cancel_terminal`和`unexpected_error`；前两类ID/所有权/状态链必须闭合，第三类必须为0。

PC018/019新增正式矩阵：分页以两个合法Flow及同一Flow两个合法source分别复用`v1:1`并读取各自第二行，另测未知Flow、非法source、非法编码、超范围offset、多source省略和全页合并；分页输出另作三分断言：空源`offset=0`和`offset==len`的终页均要求`next_cursor`存在且为JSON `null`；正常非终页要求字段存在且为合法规范cursor字符串；无分页操作要求整个`pagination`不存在。调用方保持链上下文，产品不登记签发来源、不自动归零。四个固定启动分别覆盖WAITING、IN_PROGRESS、FINISHED、ERROR、其他非空新值、缺失、空值：collect_file/collect_forensic_triage/kill_process的required `state`和start_hunt的required `flow_state`必须逐字来自创建后立即回读，Hunt `state`保持Hunt语义。每次只创建一个Flow且不等终态；缺失/空值为BACKEND_ERROR，ERROR初值不贡献业务成功。准确output schema静态断言只允许PC019的三个`state`和一个`flow_state`四个新增输出字段；终页null是既有P04 `string|null`契约勘误，不是第五个新增字段；inputSchema、130注册面、动态工具outputSchema与PC014的129实测+1登记排除/645关系不得改变。Win10VM stdio诊断与正式HTTP必须得到同一新版tools/list hash；五场source/index/report绑定`CURRENT-PC017-018-019-R02`，旧hash或旧输出schema报告一律拒绝。

`PC017-TRIAGE-BUDGET/SUCCESS`另设不计coverage的正负回归：同一时刻最多一个新主动triage Flow，正向记录请求预算并要求真实FINISHED、结果与文件链共同闭合；缺失/错误依赖、错误Targets编码、预算误用于其他入口、资源preflight不足、ERROR、取消、超限和关键采集错误均须明确失败且coverage为空，即使已有rows或uploads也不能转成功。cap按压缩前`FileBuffer.DataLength`逐块累加并在严格`> cap`后取消，记录在途/并行可能使最终实计超阈值；不把压缩后网络/物理量、uploaded汇总、idx元数据、逻辑大小或磁盘占用硬等同上限。4 GiB不足或完整性不可证即停并重裁决，不自动重试提限或缩小目标。

### 3.2.1 PC017文件视图、身份与读取矩阵

P06从P05受审fixture与已激活基线执行而非重新解释契约。正向覆盖普通file_id不漂移、28data+1idx得到28个逻辑原文件并返回`sparse_indexes_internal:1`、天然普通`.idx`保留、完全重复稳定去重；负向覆盖孤儿idx、未知/非字符串Type、顶层/嵌套Type冲突、同身份异元数据、多义配对、非法整数/大小、client/flow作用域错误及天然`.idx`/Windows大小写selector碰撞，均须过滤为指定BACKEND_ERROR reason且零落盘。list调用轨迹必须证明只读uploads metadata、不调用VFS内容读取。

下载正向覆盖普通文件、空文件和逻辑16/紧凑7字节且首中尾有洞的稀疏文件：普通显式`Padding=false`；稀疏先`Padding=false`全程实计紧凑长度且不落盘，再`Padding=true`重建逻辑内容；每块<=1 MiB、0字节EOF，最终size/hash来自实际逻辑字节。安全矩阵逐项执行P04 §3.2步骤1～9，并以`os.link`成功为提交点分账。提交前读取/长度/fsync/metadata/路径/link故障须零新完成文件；正常清理无part，叠加清理失败时允许owned part残留且保留原错误并附记`download_temp_cleanup_failed`。link成功后复核OSError须`BACKEND_ERROR/download_post_publish_failed`、保留受控fixture中的正确完成字节并正常清part；link成功后part unlink OSError同样报该错误并允许正确完成文件与本次part同时保留。路径/所有权不可信时停止清理，外部哨兵和其他请求对象保持。合法并发fixture限定为两请求竞争同一不存在且其他前提有效的目标，必须恰一正确发布、另一`ALREADY_EXISTS`，败者不覆盖/删除赢家，最终正确目标恰一个且双方part清零；已有目标保持原字节。ERROR Flow中完整单文件下载须与Flow/triage成功分账。stdio与正式HTTP均回归同一schema和行为；所有异常残留进入原始证据并交P07清理核对，不自动删除用户文件或既有证据。任何失败清零受影响正式项并从规定起点重跑，不冒称后端原子快照、源内容未变或任意本地特权攻击防护。

### 3.3 全量资源资格Spike与运行预算

审核引入前的安全只读Spike已记录`DESKTOP-3FI41GR`物理内存4,293,971,968字节、共享C卷总量107,028,148,224字节、可用18,004,791,296字节；证据为`Logs/P06/wf-01a05d1d-p06/resource-readonly-spike.json`。这只证明方向上有候选空间，不冒充全量峰值验收。

首次正式情景前必须从Snapshot186另开一个不计coverage的`resource-qualification` attempt，按正式网络顺序执行9个`resource_sensitive`动态项、Triage、PacketCapture、文件下载及其直接结果链，并在每步前后记录RAM、C卷、datastore、下载根和报告目录字节。Memory Acquisition使用Snappy；预运行静态门为C卷可用量至少`2 × (物理RAM + 64 MiB) + 4 GiB`，当前只读值须在恢复后重测而不能沿用。这个式子覆盖同卷client临时像与server上传副本，并保留4 GiB给其他Flow、证据和OS；资格attempt仍须用实测取代该保守方向值。

首次资格attempt的历史结果触发`PLAN-CHANGE-004`：当时的predecessor Snapshot185在静态资源门通过后，`Windows.Memory.Acquisition` Flow `F.DADMNITGQ4150`上传达到1,074,790,400字节时被Velociraptor的默认1 GiB单collection上限取消；当时C卷仍有17,994,207,232字节可用，因此不是磁盘不足或依赖缺失。原始证据包固定为`Logs/P06/wf-01a05d1d-p06/p06-resource-attempt-001.zip`，SHA-256 `61d913af058b7f057923790e61b63309c5793811134262447b05dc31d8d4ec23`。依据Velociraptor `collect_client()`已存在的`timeout`和`max_bytes`参数，产品适配层只对精确artifact `Windows.Memory.Acquisition`内部固定请求3600秒timeout和8 GiB上传上限；该值不进入动态工具schema，其他117个动态工具保持默认资源参数。8 GiB是允许上限而非预分配空间，实际运行仍必须通过本节静态和实测空间门。修复后的正式资格必须从Snapshot186重开完整attempt；历史失败只证明参数修复依据，不计coverage，且P02 adapter单元测试和P06目标测试必须在Win10先通过。

资格结果冻结`observed_peak_bytes`、每个大Flow增量、`evidence_reserve_bytes`实际包大小和最长耗时。正式scenario启动门为：同口径可用空间不少于`observed_peak_bytes + max(4 GiB, 2 × evidence_reserve_bytes)`；每个大步骤deadline取资格实测耗时的2倍且不低于其P05已通过上限，整场deadline为各步骤deadline之和加10%调度余量。每个大步骤前还按资格剩余增量重新计算最低余量；不足时在调用前失败，不开始该Flow。阈值、公式、读数和拒绝理由进入报告。

若资格attempt完成不了、超过静态门、或Snapshot186固定容量低于正式门，判为确定性资源阻断：不得用相同参数盲目重跑、不得取消Memory Acquisition或删减五情景；登记PLAN-CHANGE并按REQ-021/Q-065～066回流受影响用例/环境前提，取得新证据或规范决定后再恢复。资格attempt的所有运行态由随后恢复Snapshot186清除。

## 4. P05 runner的最小P06扩展

P06不得另造GUI或生产编排器。现有`tests/scenario_runner.py`保持单scenario、固定index、无工具/参数/path覆盖的入口，只允许以下最小扩展：

1. 根据scenario_id前缀在固定的P05/P06两个index中唯一解析，两个index同时命中时fail closed；P06报告固定写入`Logs/P06/wf-01a05d1d-p06/<scenario-id>/<run-id>/report.json`；
2. 报告严格消费P05 0.3节schema v2根键：`transport=streamable-http`、脱敏endpoint、`authorization_configured=true`，`tools_schema_sha256`只从本会话真实tools/list及同run `tools-schema.json`按P05唯一算法生成，HTTP会话ID直接来自真实`Mcp-Session-Id`响应头，server instance直接来自`X-MCP-Server-Instance`并与guest只读服务观察SHA-256 join；P06所需连续call sequence、Flow/Hunt ID、资源读数和状态只放入既有`calls/steps/coverage/cleanup/failure`结构，不新增或重定义身份根键，不记录凭据或二进制正文；
3. 增加固定、非CLI可配置的跨步骤不变量验证层且不改变P05 schema v2身份语义：动态start→status→results/files的flow_id；collect→status/results/list/download的flow_id/file_id；hunt start→status→stop→cancel的hunt_id/flow_id；PacketCapture两次各自Flow及start结果→stop输入trace路径；kill输入PID→result Pid/Killed。验证器直接读取受审scenario step映射和原始calls，任一错配令scenario failed且coverage为空；加入F1→F2、错hunt/file/PID的独立反例；
4. coverage只由整个scenario成功且断言及跨步骤不变量均通过的`isError=false`调用生成。cleanup、轮询和同工具重复保留原始调用，但聚合关系去重；任何一步失败、未执行、cleanup失败、unexpected ERROR或末尾owned Flow/Hunt未闭合使scenario状态failed且coverage为空；`expected_cancel_terminal`只按第3.2节严格分账；
5. `repeat_until`继续容许异步到达导致轮询次数差异；最终状态、断言、工具集合、fixture SHA和source/index hash必须一致。P06不改变P05引用、等待、断言的schema语义；PacketCapture使用P05已证固定测试部署路径，不新增eval、shell、正则转换或任意读取能力。

每次guest回传后，host先计算report和证据包SHA-256，再向固定`Logs/P06/wf-01a05d1d-p06/接收清单.jsonl`只追加一行：`monotonic_attempt/scenario/report_relative_path/report_sha256/package_sha256/source/index/fixture/tools_schema_sha256/server_instance_id/server_pid/server_process_start_time_utc/server_observation_sha256/runner_pid/mcp_session_id/mcp_session_lifecycle/status/received_at`。`tools_schema_sha256`及这些身份值必须逐字段转录schema v2报告并由聚合器对本run的`tools-schema.json`和原始tools/list证据复算，不能由CLI或外部diff文件输入。单调attempt由host账本顺序分配，不信任guest时钟；旧失败和成功都必须登记。最终另写不可变`final-selection.json`，逐scenario显式选择唯一report SHA并列出全部较早attempt及拒绝原因；不得用“最新时间”自动选择。

聚合器只读取本方案固定P06证据根、接收清单和final-selection，拒绝reparse/symlink、重复scenario/report/package SHA、缺历史、未登记report、旧source hash、不同tools/list hash、不同fixture spec、失败选择、额外场景或客户端自报身份替代真实响应头/guest观察。它重新计算全部输入SHA，结合受审runner source hash、响应头、guest观察hash、单一ClientSession代码路径和连续call sequence证明会话/服务连续性。输出固定为一个canonical JSON，含精确输入SHA集合、650三向关系、每工具5个scenario、每scenario至少10个声明工具步骤和实际call数、总耗时、失败/修复/重跑历史、五个独立runner/HTTP session生命周期、每场服务身份join及差集；跨Snapshot186恢复重复同一服务实例身份允许且必须明确归因于同一内存基线。聚合器不得启动server、执行工具、控制VM或修改原报告。

### 4.1 消费P05快照阶段证据

五个固定scenario及P06 index的required_snapshot必须全部为`Snapshot 186-Velociraptor-MCP网络部署基线`，index新增的snapshot_stage精确为P06_ACTIVE；按实际修改后的字节重算source/index哈希，不允许保留185值、CLI覆盖或运行时改写。P06按P05第0.5节的固定路径读取外层恢复记录，逐工具、资源资格和五情景报告都生成同run `snapshot-evidence.json`，根`snapshot_evidence_sha256`绑定该文件及原始恢复证据。接收账本在tools_schema_sha256之后增加snapshot_evidence_sha256，值只从报告转录；聚合器逐层重算，并把required_snapshot/index阶段、schema3/epoch4/NETWORK_ACTIVE、186 active/allowlist及激活证据、实际marker/hostname、restore_attempt_id、run_id/session/服务观察完整联结。

五场必须有五个独立restore_attempt_id；candidate阶段、185报告、缺侧文件/错哈希、跨attempt marker拼接或重复恢复记录均失败且coverage空。ACC-P06-002/004/005/006必须包含这些反例及五场正向证据；任何失败不得进入P07。共享字段、原始证据规则和固定阶段配对由P05唯一负责，P06不增加另一套恢复或报告协议。

## 5. attempt、失败修复与副作用闭合

五个情景固定按第2章顺序执行，每个情景单独恢复Snapshot186。一个attempt的边界是：selector/恢复门→同步当前源→Win10VM语法/目标/stdio诊断测试→核对既有Windows服务与28790安全边界→虚拟机外客户端直连单情景同服务进程/同HTTP会话执行→末尾Flow/Hunt/trace/进程/下载根核对→证据打包和双端hash回传。下一情景只能在前一情景最终成功报告已回传后开始恢复；产品端口28790不得被临时传输通道复用或关闭。

任一未预期MCP错误、非`expected_cancel_terminal`的Flow ERROR、超时、断言或跨步骤不变量失败、工具缺失、报告不完整、活动Flow/Hunt、未知trace所有权、错杀/未杀、资源门不足或证据未回传都使本attempt失败且不贡献覆盖。先保留原始报告和stderr摘要；若能证明PacketCapture trace由本attempt创建，可执行P05固定stop清理后取证，否则不碰未知trace。随后判定原因：

- 情景/runner错误：只改P06测试范围，Win10VM目标测试和外部runner自测通过后从Snapshot186重跑整个scenario；
- 现有产品实现违反冻结契约：作最小契约保持修复，重跑负责P02/P03/P04测试、P06目标测试，再从Snapshot186通过正式入口重跑该scenario；
- artifact真实定义/依赖推翻P01/P05前提：登记PLAN-CHANGE，暂停受影响项，回流唯一规范层和审核路线，不换工具、不计失败、不临时联网安装；
- VM恢复或证据通道问题：按恢复规则从canonical Snapshot186开新attempt；不冷启动、不恢复183/184/185、不把对话输出当证据。

最终聚合只接受`final-selection.json`按host单调attempt账本显式选择的五份全部通过、source identity匹配报告；所有较早失败/成功、修复diff、拒绝原因和重跑对应关系保留在host `Logs/P06/wf-01a05d1d-p06/`，不得覆盖或按guest时间推断“最新”。

## 6. 实施项

### IMP-P06-001｜冻结130工具与五情景关系清单

- 范围/落点：`p06_coverage_manifest.json`、五个scenario源、P06 index和独立golden测试。
- 前置/依赖：P03 invocation、P04 golden、P05 report schema v2和已激活Snapshot186身份。
- 目标：五个不同业务目的各完整覆盖同一130工具，650条关系均有调查理由/空结果边界，工具/场景双向差集0且文件hash入index。
- 允许实现细节：JSON排版和step id命名；不得改变五个purpose、工具集合、关系计数或添加随机生成入口。

### IMP-P06-002｜扩展单情景报告与严格覆盖语义

- 范围/落点：`tests/scenario_runner.py`及其单元反例。
- 前置/依赖：P05 runner现有信任边界。
- 目标：P05代表情景不回归；P06固定index/report路由、完整identity和“情景失败则coverage为空”成立，仍只有受审JSON可驱动工具。
- 允许实现细节：内部数据类/纯函数拆分；不得新增路径、工具、参数、shell或VM控制CLI。

### IMP-P06-003｜资源资格与逐动态工具完整成功链

- 范围/落点：五个scenario的118项动态步骤和P06逐工具报告。
- 前置/依赖：IMP-001/002；先从Snapshot186完成第3.3节正式网络资格attempt，每个正式attempt再从Snapshot186恢复。
- 目标：资源预算/每步与整场deadline/低空间调用前拒绝成立；每项在五个scenario均真实调用且五条均FINISHED/status/result完整链。PacketCapture、Memory Acquisition和上传类按第3章专门闭合。
- 允许实现细节：在已有schema范围内调整安全参数、page_size和轮询间隔；不得取消代替成功、临时安装或扩大采集目标。

### IMP-P06-004｜固定工具五情景成功链与约定错误

- 范围/落点：五个scenario固定链、`p06_individual_acceptance.py`及错误报告。
- 前置/依赖：P04契约、P05 fixture/依赖。
- 目标：12项在五个scenario各有成功关系；成功、取消、Hunt、下载、triage、kill及独立约定错误均与P04冻结语义一致。
- 允许实现细节：五个fixture文件与只读VQL的对应顺序；不得让预期错误进入coverage或结束非专用进程。

### IMP-P06-005｜五个独立恢复attempt执行

- 范围/落点：VM运行态与host `Logs/P06/wf-01a05d1d-p06/`不可变证据。
- 前置/依赖：IMP-001～004及全部Win10目标测试通过。
- 目标：五个scenario逐一从Snapshot186，由虚拟机外一个官方SDK session直连同一VM服务进程完整成功；每轮证据先回传再恢复；所有失败有修复和整场重跑链。
- 允许实现细节：轮询次数和报告压缩格式；不得跨session拼接或只重跑失败步骤。

### IMP-P06-006｜聚合矩阵与逐工具报告

- 范围/落点：`p06_aggregate_reports.py`、聚合反例测试、最终aggregate JSON和人可审阅摘要。
- 前置/依赖：五个最终成功报告及其source/index/fixture identities。
- 目标：接收账本和final-selection防篡改/防拼接；实际tools/list=130，动态=118、固定=12；650/650三向有效关系；每工具5、每情景≥10，五个不同runner/HTTP session生命周期，且每场内服务实例恒定并与guest观察join。内存快照恢复可使五场的服务PID/start time/instance id重复，这种重复必须与同一Snapshot186基线一致，不得误判为拼接；服务在单场内重启或身份漂移则失败。失败调用0、未执行0、unexpected error 0、覆盖差集0。
- 允许实现细节：排序和摘要布局；不得忽略旧失败历史或从失败报告抽取覆盖。

### IMP-P06-007｜最终VM/外部网络回归、README与提交

- 范围/落点：全套语法/单元/协议/真实逐工具/五情景证据，两README，Git分层扫描和唯一P06提交。
- 前置/依赖：IMP-001～006全部通过。
- 目标：README与28790正式入口、Snapshot186、stdio仅诊断和“不计错误覆盖”一致；VM内测试及虚拟机外正式网络验收均完整，提交只含P06可归属文本源，证据/运行态/二进制/秘密不入Git；交P07的清理证据完整。
- 允许实现细节：文档表格与测试文件组织；不得提前删除旧源码或做P07成本结论。

### IMP-P06-008｜把全量执行绑定到正式28790会话

- 来源：REQ-022/023/026、CON-002/004/015、OUT-022/023/027/029/053/054、总纲ACC-022/023/026。
- 前置/依赖：回流后的P02/P05通过，Snapshot186 active，P05正式代表情景两次成功；IMP-001～004测试资产已按新端点模式复核。
- 目标：runner正式模式不创建stdio子进程，而携带部署者token直连既有VM服务；每个attempt固定一个endpoint、一个HTTP ClientSession和一个VM服务PID/create-time/instance id，initialize一次后连续执行全部tools/call。报告严格使用P05 schema v2，不记录token或其指纹，只记录脱敏端点、真实响应头会话/服务身份、guest观察hash和Authorization已配置布尔值。
- 完成门：资格attempt、逐工具验收和五个情景全部从虚拟机外直连；VM内stdio只产诊断报告且coverage固定为空；聚合器拒绝transport非streamable-http、endpoint非28790/mcp、authorization未配置、缺/变更`Mcp-Session-Id`或`X-MCP-Server-Instance`、guest观察hash不符、经Win10VM MCP代理或跨会话拼接的报告。

回流后的顺序固定为先完成IMP-P06-008及相关runner反例，再执行003/004资源资格与逐工具链、005五情景、006聚合、007最终回归。003与004可以在同一scenario内交错执行，但实施记录必须分开归档动态与固定结论。

## 7. 验收项

### ACC-P06-001｜最终工具逐项真实调用（总纲ACC-021）

- 前置：P05通过；运行时tools/list冻结且与130清单双向差集0。
- 输入/动作：五个完整情景及独立错误契约会话；逐项核对118动态和12固定的参数、真实ID、状态、结果/文件或固定生命周期。
- 精确预期：130/130均有至少一条真实完整成功链；本方案实际要求每项在五个正式情景均成功；动态资格Flow均FINISHED，只有严格owned cancel-target可为`expected_cancel_terminal`，固定成功链正确，允许空结果逐关系绑定step/参数/断言和场景条件；约定错误代码另行准确。
- 证据：逐工具报告、Flow/Hunt/file ID、状态链、后端摘要、tools/list和覆盖差集。
- 通过/失败/阻断：双向差集0且130项全部成功为通过；任一注册/schema替代调用、失败计通过、ERROR、缺项、伪ID或无理由空结果为失败；P05合法依赖失效且无法在原契约修复时被阻断并回流受影响artifact。

### ACC-P06-002｜650关系连续情景覆盖（总纲ACC-022）

- 前置：ACC-P06-001基本链和P05 runner/fixture有效。
- 输入/动作：从Snapshot186分别执行五个固定情景；每个情景由虚拟机外官方SDK直连VM内28790服务，聚合器机械核对关系、声明step、实际call、VM服务PID/HTTP session和最终状态。
- 精确预期：650/650三向关系；每个130工具恰有5个不同scenario；每条关系绑定实际step/参数/fixture/断言/call，每scenario至少130个不同工具关系且实际tools/call≥10；sleep=0关系；重复工具同场景只计1；五份报告status=success且失败调用、失败断言/不变量、未执行、unexpected ERROR、未闭合对象均为0；host账本完整且final-selection输入SHA可复算。
- 证据：五个声明文件/index、650行manifest、三向差集、五份原始报告、host接收账本/final-selection、aggregate JSON、失败/修复/整场重跑历史和五次恢复门记录。
- 通过/失败/阻断：正式网络上的全部计数、业务理由、同VM进程同HTTP会话、恢复边界和无错误同时成立才通过；stdio/Win10VM MCP代理计覆盖、任一不足、拼接、失败贡献覆盖或未整场重跑均失败；Snapshot186、服务或P05设施失效时阻断并回P05。

### ACC-P06-003｜声明式runner全量执行（总纲ACC-023完整部分）

- 前置：P05代表情景双周期通过，P06受审index/source identity固定。
- 输入/动作：在虚拟机外通过锁定MCP SDK连接`28790/mcp`逐情景执行initialize/tools/list/tools/call，核对引用、等待、断言、报告和快照外层边界；VM内stdio只运行诊断回归并强制coverage为空。
- 精确预期：P05代表与P06全量均由同一runner完成；固定验证层机械拒绝Flow/Hunt/file/PID错配；无GUI、随机组合、生产入口、情景内快照或任意命令代理；报告、接收账本和选择清单可由source/input hash重复验证。
- 证据：runner源码/测试、P05回归报告、P06五报告、PID/session/source/index/fixture hash。
- 通过/失败/阻断：全部机制和全量报告成立为通过；路径/hash漂移、参数覆盖、任意执行、缺报告或情景中恢复为失败；SDK或P04协议变化使入口失效时阻断回流。

### ACC-P06-004｜测试执行责任与失败闭环（CON-002/008）

- 前置：全部P06候选源已同步guest且SHA一致。
- 输入/动作：AI在Win10VM执行语法、单元、工具业务、Velociraptor集成和stdio诊断；AI从虚拟机外执行正式网络逐工具/情景。逐个保存失败、归因、修复、目标回归和整场重跑。
- 精确预期：host静态检查不冒充测试；guest业务证据来自`DESKTOP-3FI41GR`，网络边界证据来自直接host→guest28790请求；资源资格和deadline有实测依据；无未关闭失败、无错误计成功。
- 证据：guest命令/退出码、不可变attempt目录、修复diff、双端证据SHA和实施记录。
- 通过/失败/阻断：全部真实测试和失败链闭合为通过；缺原始失败、只重跑步骤、host结果代替或证据未回传为失败；VM/API/恢复不可用时阻断。

### ACC-P06-005｜边界、交接与提交

- 前置：前四项通过，需求保真14/14/0/0/0/0。
- 输入/动作：扫描diff/index/tree/新reachable blobs和工作树，核对README、无活动Flow/Hunt/trace、五个scenario及聚合证据，暂存并提交仅P06范围。
- 精确预期：无凭据、二进制、上传物、用户专属路径或运行证据进入Git；P07得到稳定650关系和逐工具替代证据；不提前清理旧源码或声明P07成本结论。
- 证据：Git分层扫描、README差异、活动对象0、提交SHA、P06实施记录。
- 通过/失败/阻断：边界和交接全部成立为通过；秘密/二进制/证据入库、未声明越界、README错误或P07职责前移为失败；无法安全分离现有dirty hunk时阻断。

阶段汇总：ACC-P06-001～005连同下述网络回归ACC-P06-006全部通过才可进入P07；P06不宣称总纲ACC-015/016/024/025完成。

### ACC-P06-006｜正式网络拓扑全量回归（总纲ACC-026）

- 前置：ACC-P05-007/008通过，Snapshot186为唯一自动恢复目标，IMP-P06-008完成。
- 输入/动作：每个资格、逐工具和场景attempt从虚拟机外官方SDK携带token直连`http://<guest-host-only-address>:28790/mcp`；核对真实`Mcp-Session-Id`、`X-MCP-Server-Instance`、guest服务观察SHA、VM服务PID/create-time、监听/防火墙和HTTP/stdio完整schema hash；每批开始执行缺失/错误Bearer、缺失/错误Host、显式未允许Origin拒绝冒烟，并执行无Origin官方SDK及显式允许Origin正向冒烟。
- 精确预期：全部正式成功证据均来自直接网络路径，同一情景一个有状态会话和同一VM服务进程；130工具/schema差异0；安全正负例持续符合P02/P05冻结语义；报告、日志、命令行和Git中无token或token指纹。
- 证据：五份正式场景报告、逐工具/资格报告、外部客户端转录、真实响应头、guest服务观察JSON及SHA、VM服务身份、监听/防火墙、schema diff、正负例handler计数、secret扫描。
- 通过/失败/阻断：所有正式报告及安全回归通过才通过；stdio或Win10VM MCP代理计覆盖、跨会话/进程、schema漂移、认证旁路或secret泄露均失败并清零受影响scenario；Snapshot186/服务/认证失效时阻断并回P05，工具业务错误回唯一负责P02/P03/P04。

汇总（v4）：P06既有ACC-001～005和新增ACC-006全部通过才可进入P07；旧stdio或Snapshot185 attempt只保留历史依据，不计正式完成。

## 8. 当前实际入口与未来产出契约

当前已存在、只允许在Win10VM运行的基础命令为：

```powershell
Set-Location C:\mcp-velociraptor
$env:VELOCIRAPTOR_API_CONFIG='C:\VelociraptorMCP\secrets\api_client.yaml'
.\.venv\Scripts\python.exe -m py_compile mcp_velociraptor_bridge.py velociraptor_api.py velociraptor_mcp_core.py velociraptor_dynamic_artifacts.py velociraptor_fixed_tools.py velociraptor_env.py tests\scenario_runner.py
.\.venv\Scripts\python.exe -m unittest discover -s tests -p 'test_*.py' -v
.\.venv\Scripts\python.exe -m tests.scenario_runner --scenario-id p05-flow-triage
```

P06 scenario文件、逐工具脚本和聚合器尚未创建，因此本方案不虚构其当前可用命令。冻结建立后的工作目录为`C:\mcp-velociraptor`；单情景runner仍只接受已索引scenario ID，聚合器只接受固定P06证据根且不接受工具、参数或快照覆盖。输出分别位于guest `Logs/P06/wf-01a05d1d-p06/<scenario-id>/<run-id>/report.json`与host同构证据根；退出码0为完整通过，1为业务/断言失败，2为输入/schema/identity/环境门禁失败。实现后在实施记录补实际命令。

## 9. fallback与停止条件

| ID | 触发 | 行为 | 退出条件 |
| --- | --- | --- | --- |
| FB-P06-001 | scenario/schema/index/runner错误 | 失败报告coverage清零；只修P06测试范围并跑Win10目标回归；从Snapshot186整场重跑 | 同scenario新attempt完整success，旧失败和修复映射保留 |
| FB-P06-002 | 产品实现违反已冻结P02/P03/P04契约 | 只做最小契约保持修复并重跑负责Pxx测试及P06整场；若契约会变则登记PLAN-CHANGE回流 | 负责测试和失败scenario整场均通过，或规范层重新闭环 |
| FB-P06-003 | artifact/依赖运行事实或确定性资源资格推翻P01/P05前提 | 不换artifact/版本、不临时联网、不原样重跑、不计成功；持久化资源/行为证据、登记PLAN-CHANGE并回流唯一负责层或环境前提 | 原身份在闭合后的方案和有证据容量中成功，受影响scenario整场重跑 |
| FB-P06-004 | Snapshot186恢复、schema3/epoch4、marker、VMX、hostname/API/client/service/permission/firewall/fixture失败 | 本attempt不执行scenario；回P05修复其负责边界或按用户runbook从Snapshot186开新attempt，不冷启动、不恢复183/184/185 | selector schema3/epoch4/`NETWORK_ACTIVE`及完整恢复/正式网络门重新通过 |
| FB-P06-005 | 情景副作用或证据回传失败 | 只清理可证明本attempt拥有的trace/Flow/Hunt；先回传原始证据，hash不符不恢复 | 活动对象0、trace inactive、host/guest SHA一致、监听停止 |
| FB-P06-006 | 聚合差集、关系数、会话或identity不符 | 聚合失败，不手改报告；回到精确缺失scenario并整场重跑 | 五份最终报告identity一致且650关系全部机械通过 |
| FB-P06-007 | Git扫描发现秘密、二进制、运行证据、用户路径或越界文件 | 停止提交，从P06 staged范围移除并重做Win10验证和扫描，不删除受保护PLAN/Logs | index/tree/new blobs/tracked范围零命中且提交边界准确 |

禁止fallback：错误调用或失败scenario计覆盖、sleep计调用、跨进程/跨session拼接、只重跑失败step、情景内恢复快照、临时安装依赖、恢复Snapshot183/184/185、取消Memory Acquisition代替成功、随机生成情景、用host测试代替Win10、以stdio或Win10VM MCP代理正式调用、提前执行P07清理。

## 10. 双向追溯和需求保真

OUT-021→IMP-003/004/005/006/008→ACC-P06-001/004/006；OUT-022→IMP-001/002/005/006/008→ACC-P06-002/004/006；OUT-023→IMP-001/002/005/006/008→ACC-P06-003/006；OUT-027/029/053/054→IMP-005～008→ACC-P06-002/003/004/006；OUT-033→全部IMP/ACC及fallback。NON-009/OUT-048继续保证runner只执行受审情景，NON-010/OUT-049保证不引入第二server或Win10VM MCP代理。

| 规范ID（共14，RACC镜像独立计数） | 完整实施落点 | 验收落点 |
| --- | --- | --- |
| REQ-021 | 第3、5章，IMP-003/004/005/006/008 | ACC-P06-001/004/006 |
| REQ-022 | 第2、4、5章，IMP-001/002/005/006/008 | ACC-P06-002/004/006 |
| REQ-023 | 第4、8章，IMP-001/002/005/006/008 | ACC-P06-003/006 |
| REQ-026 | 第0、1、4～9章，IMP-005～008 | ACC-P06-002～006 |
| CON-002 | 第1、5、8章，IMP-003～008 | ACC-P06-004/005/006 |
| CON-004 | 第2、4章，IMP-002/005/006 | ACC-P06-002/003 |
| CON-008 | 第2、3、5、9章，全部IMP | ACC-P06-001/002/004 |
| CON-015 | 第0、1、4～9章，IMP-005/007/008 | ACC-P06-002/003/004/006 |
| NON-009 | 第0、2、4章，IMP-002/006 | ACC-P06-003/005 |
| NON-010 | 第0、1、4～9章，IMP-002/005～008 | ACC-P06-002～006 |
| RACC-021 | 第3、5章，IMP-003/004/005/006/008 | ACC-P06-001/004/006 |
| RACC-022 | 第2、4、5章，IMP-001/002/005/006/008 | ACC-P06-002/004/006 |
| RACC-023 | 第4、8章，IMP-001/002/005/006/008 | ACC-P06-003/006 |
| RACC-026 | 第0、1、4～9章，IMP-005～008 | ACC-P06-002～006 |

反向核对IMP-P06-001～008、ACC-P06-001～006和FB-P06-001～007均有上述规范来源或P03～P05稳定交接依据；没有依赖P07完成的通过条件。真实ID/状态、唯一client、无秘密路径、文件生命周期、正式网络入口和无联网工具面继续继承P02/P04/P05契约，P06以逐工具报告复核但不重复计为新增规范ID。

需求保真计数：应承接14、完整14、未落点0、语义弱化0、不可验收0、失效或复核条件缺失0；异常ID无。计数以稳定ID为单位，RACC镜像独立计数。

## 11. 版本、审核重点与实施门

- v12-candidate-r02：PC017 R02候选明确只消费P05“三次真实回填→激活”后的基线，执行前资源筛查归验收工作流而非产品接口；保留编码/4 GiB范围正反例、唯一主动Flow和真实成功门。待主方采纳/自评。
- v13-candidate-combined：合并PC017文件交付矩阵，覆盖逻辑原文件视图、idx内部元数据、身份/selector fail-closed、普通/稀疏读取、metadata漂移、ERROR分账及stdio/HTTP回归；按05号勘误纠正4 GiB计量口径。待主方采纳与完整自评，不表示矩阵已经运行。
- v14-candidate-combined-r02：当前执行引用统一到六文档R02组；安全矩阵改为逐项消费P04 §3.2完整算法，并分离合法并发的一成功一ALREADY_EXISTS与零发布错误分支。R01其余契约不变，仍待主方采纳与完整自评。
- v14（作者修订）：机械采纳已验收v14候选正文，当前组切换为`PC017-AUTHOR-REVISION`；待主会话完整自评，实施门关闭，代码、部署和验收未随文稿通过。
- v15（作者修订）：按08号裁决把文件安全矩阵拆为提交前、提交后复核、提交后清理与不可信所有权分支，并要求残留原件交P07披露；当前组切换为`PC017-AUTHOR-REVISION-2`，其余执行门和证据纪元不变。
- v16（作者修订）：承接PC018双Flow/双source分页正反例与PC019四固定启动状态矩阵，要求stdio/HTTP同一新schema及五场绑定九规范来源；PC014的129+1/645覆盖、PC017与阶段门均不变。
- v17（作者修订）：承接PC018终页勘误，增加终页`next_cursor=null`、非终页规范字符串、无分页时整个`pagination`不存在的三分矩阵；不改变PC019四字段差异、PC014/017、两周期激活或后验门。

- v0：首次送审版本。冻结五个全工具综合调查情景、650关系、动态FINISHED链、12固定成功/错误分账、五次Snapshot185独立恢复、失败整场重跑和P06最小runner扩展。
- v1：引入首次全面审核AUD-001～006。650关系升级为manifest↔step↔report三向绑定并要求场景特定断言；增加固定跨步骤ID不变量验证层；增加host单调attempt接收账本、显式final-selection和输入SHA防拼接；把cancel-target的`expected_cancel_terminal`与unexpected ERROR精确分账；加入基于只读VM事实的资源资格attempt、预算/deadline/低空间拒绝和确定性阻断回流；修正HEAD实际模块名及`velociraptor_env.py`边界。工具面、产品接口、五情景、650关系、Snapshot185恢复方向和Pxx顺序不变。
- v2：实施中的首次资源资格按预设停止条件保留失败证据并登记`PLAN-CHANGE-004`。真实Flow证明Velociraptor默认1 GiB上传上限会取消完整4 GiB内存采集；冻结仅对`Windows.Memory.Acquisition`内部传入3600秒/8 GiB的最小契约保持修复，公开130工具、schema、权限、五情景、Snapshot185和Pxx边界不变。修复后必须从Snapshot185重跑完整资格attempt，不接受失败步骤续跑。
- v3：Q-072回流。正式执行从VM内stdio子进程改为虚拟机外官方SDK直连VM内有状态Streamable HTTP `28790/mcp`；stdio只作coverage为空的诊断。P06恢复起点改为P05验证并激活后的Snapshot186，新增IMP-P06-008和ACC-P06-006，要求每情景同VM服务进程/同HTTP会话、认证负例和双传输schema同一性。五情景、650关系、每工具五情景、失败整场清零及v2内存资源修复不变；状态退回未审核。
- v4：引入网络回流全面复审AUD-001/003/005/008。逐项清除P06未来执行、资格、IMP和fallback中的Snapshot185目标，只允许其出现在P05 predecessor或历史版本/证据叙述；入口唯一为schema3/epoch4/`NETWORK_ACTIVE`的Snapshot186。正式报告严格消费P05 schema v2及真实`Mcp-Session-Id`、`X-MCP-Server-Instance`和guest观察hash，不再自定义服务身份；纠正内存快照可重复同一服务实例、但每场会话/runner必须独立的判据；补全14个稳定ID逐项追溯及Origin正反语义。状态保持未审核。
- v5：审核循环根因修正。P06接收账本和聚合器不再从未冻结的嵌套位置读取工具哈希，唯一消费P05 schema v2根`tools_schema_sha256`，并对同run `tools-schema.json`和原始tools/list证据复算；禁止由CLI、外部diff或另一会话填入。其余14项需求、650关系、Snapshot186和身份join契约不变；状态保持未审核。
- v6：引入AUD-009。固定五场scenario/index为186与P06_ACTIVE；逐工具、资源资格、场次报告及账本/聚合器消费P05第0.5节snapshot_evidence_sha256，拒绝185/候选/跨attempt证据。不改变既定186恢复机制、五场目的和650关系。
- v7：PLAN-CHANGE-009，按第0.2节前向创建r3证据纪元并明确只读资源观察与实际预算字节承接；旧错误证据原样保留，状态退回待复审，不预支正式运行或核查通过。

审核重点：

1. 五个全工具情景是否分别具有真实、可区分的业务目的，650条关系是否逐条有调查问题或允许空结果理由，而非只改标题；
2. `130 × 5 = 650`、同场重复去重、sleep不计和失败scenario覆盖清零是否可机械验证；
3. 118动态工具是否均要求真实FINISHED/status/result链，尤其PacketCapture、Memory Acquisition和外部依赖项没有用取消或错误代替成功；
4. 12固定工具在五个情景中是否都有成功关系，预期错误是否严格分账且不进入coverage；
5. 每个scenario是否从Snapshot186独立恢复、由虚拟机外客户端在同一VM服务进程/同一HTTP会话连续执行，证据先回传后重置且无冷启动/旧快照fallback；
6. P06对runner和产品代码的边界是否足够小，未重新发明生产编排器、未提前做P07清理。

送审成熟度：应承接14、完整14、未落点0、语义弱化0、不可验收0、失效或复核条件缺失0；方向阻断0、验收阻断0、占位决定0、无主依赖0。
