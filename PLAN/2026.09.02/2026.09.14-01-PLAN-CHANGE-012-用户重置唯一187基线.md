# PLAN-CHANGE-012：用户重置唯一 Snapshot187 后的基线回流

创建日：2026-09-14（Asia/Shanghai）。角色：主会话规划/作者/修复方。状态：新现场已确认；回流登记，完整规范、自评及实施尚未闭合。本记录不可变，不是核验通过或 AUD/CHK 关闭记录。

## 用户事实和实测对象

用户明确告知：“我已将其恢复快照 Snapshot 187-固定IP+WindowsMCP开机自启……我已删除其他快照只剩这一个。你继续。”继续授权适用于该现场上的既定项目工作，不推定允许改动其他 VM、覆盖用户快照、放宽网络保护或降低验收。

主会话在 2026-09-13 23:59 +08 附近以只读 vmrun/metadata 复核：

- VMX：`/home/adminn/vmware/Win10MalBox-Velo/Win10MalBox-Velo.vmx`。
- `listSnapshots ... showTree` 返回 Total snapshots: 1，唯一名称为 `Snapshot 187-固定IP+WindowsMCP开机自启`。
- vmsd：snapshot.current=3，snapshot0.uid=3，snapshot0.filename=`Win10MalBox-Velo-Snapshot3.vmsn`，snapshot.numSnapshots=1。旧 mru 项不是可用快照证明。
- VMware Tools 返回 IPv4 `192.168.204.232`；VMX MAC `00:0c:29:83:b8:65`。
- 直接请求 `http://192.168.204.232:28787/mcp` 的 PowerShell 只读查询成功，guest MAC `00-0C-29-83-B8-65`，`C:\mcp-velociraptor` 存在。混合表格输出没有完整显示 IP/DHCP/监听字段，不能将空白误报为验证通过；实施库存将重新以 JSON 留存原件。

以上宿主查询目前为工具返回证据，尚不是完整恢复 attempt 原件。用户完成的恢复不冒充执行方发起并完整记录的恢复周期，不计入候选双恢复次数。

## 控制通道和既有说明纠正

现成 `mcp__Win10VM__PowerShell` 连接器的只读身份查询返回 `.149`、MAC `00-0C-29-4C-FD-C0`，并非上述目标。该查询仅为身份/状态读取，未写入；今后本项目禁止使用该连接器冒充目标，必须直连已核实的 `.232` 并核对 MAC。hostname 被克隆，单独相同不构成对象身份。

主会话此前确实恢复过旧 Snapshot186。旧恢复可将 DHCP 配置带回，因此“未直接运行 DHCP 修改命令”不等于“本轮操作未改变运行现场的网络状态”。用户此次修复是新的权威现场变化。旧 DHCP 漂移诊断保持历史，不能替代新187的实测；不继续使用屏幕键盘输入、不擅改固定 IP。

## 对旧契约的影响与立即门禁

冻结旧来源内容身份：需求 v2 blob `4906941e0698ffe7476838c4479ce344ce30ef27`；总纲 v11 blob `863ab4038e81023ef917c8344c177798bd613d8b`；P05 当前 blob `beb3a3dfbef7df43cc742ebc4847f5e18726ece1`；HEAD `ac1c58a9439aaa303d071a8a040e0f26db85bbdb`。全部 dirty、历史规范/证据保留。

1. 现存 canonical 的 schema3/epoch4/186 与真实快照树不匹配；它只是历史状态原件，不能再授权恢复、准备或 P06 正式执行。不得悄悄将其 active_snapshot 字符串改为187来伪造激活。
2. PLAN-CHANGE-011 以186为前驱的初验和188激活迁移前提已失效；20号自评不能覆盖新拓扑。刚完成的186/188输入和联动代码只能作为未验证实现材料，禁止执行旧恢复 helper。已通知实施子智能体停止旧拓扑假设。
3. 用户187为当前唯一保留起点；不删除、改名、覆盖或假定它已通过P05/P06。当前仅授权非破坏性库存。新的自动恢复/候选创建须在主会话完成新契约与自评后实施，不能先动作后补规范。
4. 不减少四依赖、fixture/保护父链、真实四链、HTTP正负例、固定地址/服务自动启动、两次候选恢复、递归原件验证和原子激活门。P06资源资格、130逐工具、五场650关系、P07成本/清理/80正式项与67累计结论及新HEAD独立核验继续完整保留。

稳定受影响边：需求 REQ-020/023/026 与相关累计结论 → 总纲 OUT-020/023/053/054、ACC-020/022/023/026 → P05恢复起点/准备/索引/报告/activation/selector → P06资格/逐工具/五场/封包聚合 → P07成本起点/交接/终检。业务工具契约不因快照删除而改变。

## 后续闭合顺序及职责

1. 实施子智能体独占目标 VM 只读窗口，记录MAC/IP/DHCP/UTC/服务/监听/源码hash/fixture与依赖证据库存，列出用户恢复造成的差集；不能修环境或调用业务来补齐库存。
2. 主会话基于差集重订总纲/P05及P06/P07衔接、旧canonical归档及新状态迁移，形成可执行恢复契约和逐项保真自评。新增正式方案第三方复审仍按用户指令取消，不重置旧 AUD。
3. 自评通过后由实施子智能体执行、主会话修复；另一未参与对象实施的无上下文子智能体在固定新HEAD上独立全面核验。所有派遣 gpt-5.6-terra/xhigh、fork_turns=none，上限60分钟，VM单一操作者。

若新187缺少固定依赖、身份或数据，按准确差集回到负责P05的准备步骤；不靠猜测复用旧证据，不将普通控制通道可达宣称产品可用。当前目标仍未完成。
