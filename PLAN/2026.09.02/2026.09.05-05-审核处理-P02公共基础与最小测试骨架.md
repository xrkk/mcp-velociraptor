# P02 公共基础与最小测试骨架首次审核处理

> 记录日期：2026-09-05
>
> 记录性质：首次审核处理；写入后不可变；同时作为首次审核后的作者自评闭环
>
> 会话角色：作者/执行方
>
> workflow_id：`wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2`
>
> 当前Pxx：P02
>
> 审核批次：`P02-BATCH-1b3586e8-de5603cc-ee7c23e1-582d5148-9dc8054d`
>
> 全面审核额度：已用1/1；定向复审额度：已用0/1
>
> 原审计：`2026.09.05-04-审核-P02公共基础与最小测试骨架.md`；Git blob `dde818cb8b8630e80d20baf7822c58d2ac6147e4`；SHA-256 `8d5a0e8047163c4f003baf64b5eeec8dc05257ec3aeb364c217264d59d0768f2`；结论“存在不同意见”
>
> 处理前方案：v0；Git blob `1b3586e86628f5ab4b180ba5677407d03196cc56`；SHA-256 `a49772f80044937ca4add2ba1b28b2d2579f4dad9e037ffc113bcf0b445ebaa6`
>
> 处理后方案：v1；Git blob `733d56f1d9e2dafcd506eb486208816fb8bba28f`；SHA-256 `fd2c7320cca67af832a3787c4b0699369dee039bf53c567b4ba232ac8cce07c8`；状态`已审核, 可以实施`
>
> 本地基线：分支`main`，HEAD `9dc8054daf2981a28ced316ee24a6c3ab8faa9b3`；审核期间决策关键产品路径无漂移

## 1. 批次、范围与继承

本记录引入P02首次第三方全面审核。核心维护范围、直接依赖和明确排除均继承原审核第2章；修改只发生于P02方案和新增的脱敏只读Spike证据，尚未修改产品代码、配置、测试或README。P01全部通过事实、总纲Pxx顺序、P03～P07职责、Windows-only/单root Org/单client、无profile/第二server/联网控制和所有全局不变量继续继承。

新增证据：`Logs/P02/wf-01a05d1d-p02/spike-client-id-existence.json`，Git blob `6e8992a11fb730f38cdeea2d18a4dfd47e40ed20`，SHA-256 `4331f5d63caf93abcd0acff70e194bef84fc6ca540d07177fd6f98727931ab5d`。它记录Win10VM root Org只读精确client_id查询：现有ID返回1行，不存在ID返回0行；API配置正文未输出。

## 2. AUD逐项处理

### AUD-001｜部分同意，已闭合

- 同意：fixture不能复制一套假公共实现，也不能宣称旧78工具已经满足最终schema。
- 不同意：P02必须在替换前迁移全部旧78工具。总纲P02职责是公共基础，P03/P04负责把动态/固定产品工具迁移到该基础，总纲ACC-008还明确“全部工具由P06终检”；在P02抢跑全部迁移会直接违反Pxx边界。
- 修改：方案第3.2节第10项、ACC-P02-002/003/007/008明确fixture必须直接注册生产公共构件；P02子验收只裁决公共构件和真实注册路径，不把旧工具冒充完成；真实旧bridge另做迁移兼容call，P06对最终全部工具做零差集终检。
- 最小闭合：生产构件fixture的tools/list/outputSchema/tools/call通过；P03/P04只能复用该构件；P02 README明确阶段状态。无需修改总纲职责。

### AUD-002｜同意，已闭合

- 修改：IMP/ACC-P02-001/007增加真实bridge一个只读server查询和一个代表性endpoint tools/call；保存旧`->str` handler在SDK2下的实际schema、content/structuredContent/isError、stderr/stdout和退出码。
- 边界：该转录只证明SDK迁移未破坏过渡bridge，不冒充新公共结果契约。

### AUD-003｜同意，已闭合

- 修改与证据：先在Win10VM完成安全只读Spike。生产谓词冻结为：endpoint操作失败后，同root Org精确client_id存在性查询成功且0行才产生专用失效信号；查询失败/超时/非零行及普通失败都不清缓存。方案第3.2、4.2、IMP/ACC-003和fallback已同步。
- 结束：命中后最多一次重解析/重试；第二次失败为`CLIENT_ID_NOT_FOUND`。

### AUD-004｜同意，已闭合

- 修改：冻结`ensure_ascii=False, allow_nan=False, sort_keys=True, separators=(",", ":")`的紧凑UTF-8口径；helper接收完整base result和rows，构造最终structuredContent后计数。
- 测试：覆盖中文、转义、长ID/warning、pagination元数据、恰好边界和单行超限。

### AUD-005｜同意，已闭合

- 修改：cursor固定`v1:<offset>`规范编码、13字符/2147483647上限、前导零规则，以及空源、offset等于/大于长度、源增长/缩短和跨source责任。
- 结束：空源及offset==len为空终页；offset>len/跨source/源缩短越界为`INVALID_ARGUMENT`。

### AUD-006｜同意，已闭合

- 修改：删除合成`SUBMITTED`决定。collection取得真实flow_id后立即回读Flow详情，必须返回真实state；缺ID或state均`BACKEND_ERROR`。
- 影响：P03/P04只接收一个真实状态真值，不引入状态来源第二字段或假枚举。

### AUD-007｜同意，已闭合

- 修改：新增第4.4节逐码决策表，冻结13个code的owning layer、排他触发、retryable、details白名单和原异常不透传规则；补入实际需要的`AUTHENTICATION_FAILED`。
- 后序边界：P04拥有的`NOT_FOUND/NOT_CANCELLABLE`在P02只冻结公共模型/映射测试，真实生命周期由P04验收；新真实语义必须回流P02而非错用相邻code。

### AUD-008｜同意，已闭合

- 修改：全字段必需模型改为公共基类加Data/Flow/Hunt操作模型；只保留实际适用字段，序列化省略不适用字段而不是输出null。正常空数据保留空数组/对象且isError=false。

### AUD-009｜同意，已闭合

- 修改：从IMP-P02-006落点移除`example.env`；方案所有范围/ACC/提交契约明确该文件零diff。其现有Org/危险开关项仍由P07按既定物理清理时序处理。

### AUD-010｜同意，已闭合

- 修改：bootstrap政策明确只排除venv自带`pip==25.2`；P01其余45个distribution精确进入lock。要求wheel文件逐项SHA-256 manifest、lock到wheel缺失0、干净venv安装后排除pip的名称+版本集合与lock双向差集0。
- fallback：只能补齐与lock一致且校验哈希的wheel，不能改版本绕过。

## 3. 实际diff、失效与继承

处理后方案v1的实际语义diff：

1. 加入真实bridge两类tools/call和生产构件fixture边界；
2. 加入真实client存在性Spike与生产失效映射；
3. 将结果模型改为按操作组合并省略不适用字段；
4. 完整冻结结构化包络字节算法、cursor规范及边界表；
5. 改为collection创建后回读真实Flow state；
6. 新增13项错误决策表；
7. 明确45项lock、pip bootstrap排除、wheel hash和集合等价门；
8. `example.env`改为P02零diff；
9. ACC输入/动作/预期/证据和fallback随上述决定同步。

受影响并已重新打开/复查的旧结论：作者自评的输入输出、触发顺序、状态生命周期、不变量、异常边界、复核条件和可观察验收七个维度；IMP/ACC-P02-001～008全部重新复查。未受影响继承项：Pxx顺序、核心模块范围、P01事实、SDK 2.1.1方向、四个P01数值、单root/单client总模型、P03/P04/P06职责和全部排除项。

## 4. 修改后完整复评与需求保真

| 检查 | 结论 |
| --- | --- |
| 身份/版本/状态 | v1身份唯一；稳定IMP/ACC未重编号；状态已审核可实施 |
| 来源边界 | 只用需求v0、总纲v2、P01事实和新增安全只读Spike；未重访谈或扩需求 |
| Pxx边界 | 公共构件归P02；动态/固定迁移仍归P03/P04；最终全工具终检归P06 |
| 双向追溯 | 8个IMP、8个ACC、8项规范切片均有双向落点，无悬空 |
| 可独立实施 | 只依赖P01；生产构件fixture、真实bridge迁移call和至多一个Pslist collection可独立验收 |
| 状态/数据一致性 | 只返回真实flow_id/state；cursor、截断和client缓存只有一个真值 |
| 错误/秘密 | 逐码映射与白名单已冻结；原异常、路径、配置/证书不透传 |
| 依赖重建 | 45项lock、唯一bootstrap排除、wheel哈希和双向集合等价可机械验收 |
| ACC闭合 | 所有ACC均有精确输入、动作、预期、证据和三分边界 |
| fallback | 仅四项已声明fallback；无降级SDK、文本兼容、猜目标或提高限制 |

需求保真六项计数：应承接8、完整8、未落点0、语义弱化0、不可验收0、失效或复核条件缺失0。异常ID：无。结论：通过。

## 5. 再次审核逐项判定

- 跨两个以上独立项目或多业务共享契约：未命中。只修改当前仓库P02方案。
- 不可逆/高风险迁移、权限、安全、隐私或外部关键契约：未命中。只读Spike无状态变更；后续实现可由单提交回退。
- 改变总体架构、核心数据流、并发一致性、恢复或切换机制：未命中。仍为总纲规定的单公共层、gRPC/VQL、root Org和唯一client；修改只补精确契约。
- 核心路径难回滚或影响面无法限定：未命中。影响严格限于P02公共模块、启动hunk、测试/lock/README。
- 多个均有证据的可行方向：未命中。SDK、缓存、cursor、状态、错误与lock方向均已唯一化。
- 用户或项目约定明确要求再次审核：未命中。项目只要求首次独立审核和按触发规则处理。

判定：首次审核已引入；修改默认由作者自评闭环，无需再次第三方审核，不输出复审模式。

## 6. 最终结论

AUD-001部分同意并以Pxx责任边界闭合；AUD-002～010全部同意并完成修改。没有未解决中/高风险意见、待复审项或审核循环熔断。P02 v1状态为`已审核, 可以实施`，长程实施授权进行中，进入IMPLEMENT。
