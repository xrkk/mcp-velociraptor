# 自评：P07 清理与终检 v2 方案（2026.09.12-18）

> 会话角色：方案作者（主会话）
> 自评日期：2026-09-12（Asia/Shanghai）
> 对象：`2026.09.12-18-实施子方案-P07清理与终检.md`

## 1. 需求保真自检

| 需求条目 | 方案承接点 | 保真判定 |
| --- | --- | --- |
| REQ-015 | 第 2/6 节：零残留复验 + 全回归清单 | 完整：清理已落地，本方案补跨面复验与证据 |
| REQ-016 | 第 2/6 节：README 终检复验并按重跑结果同步 | 完整 |
| REQ-024 | 第 4 节：同口径实测（当前真实 tools/list + 上游真实 stdio 捕获 + 真实任务运行数据 + 诚实边界） | 完整：四要素（工具数/schema 字节与 token/调用重试输出量/与上游 78 比较）均有落点；"不预设更小"写入结论格式 |
| REQ-025 | 第 5 节：七篇索引 + 反向矩阵 + 失败闭环 + 可复算计数 | 完整 |
| NON-003 | 第 2/6 节零匹配复验 | 完整 |
| CON-015/RACC-026 | 不新开范围，安全边界复验入终检清单 | 完整（依赖既有 PLAN-CHANGE-006/007 已回流文本，不重复改动） |

不把核验方意见、旧轮次否决方向或仓库无关健康扩入需求。

## 2. 机制与可行性自检

- 上游 78 基线可行性：**已预验证**——上游源码（git `9dc8054`）经 FastMCP→MCPServer 垫片在同一 VM/venv 导入成功并注册 78 工具；stdio `tools/list` 已真实捕获并回传（SHA-256 `78fa5a25…`）。垫片与 SDK 版本差异在方案第 4.1 节显式披露，同口径声明成立（双方同一 SDK 渲染）。
- 当前产品 schema 数据：五份正式报告 `tools-schema.json` 一致（`2f115581…`，130 工具），已存在。
- 任务成本数据：差异化重跑五份报告（进行中）提供调用/重试/输出字节真实量；上游无任务运行基线的事实以 P01 证据（`stdio-probe-stderr.txt` 记录同一 ModuleNotFoundError）为据，如实披露而非虚构。
- 全回归：VM venv 当前 41 项测试全绿为基线，方案不引入会破坏它们的新机制。

## 3. 顺序与门禁自检

- 前置声明诚实：P06 通过基于 2026.09.12-16 记录；差异化重跑通过后以新记录为最终前置，失败则本方案暂停——与 REQ-025"失败停在本阶段"一致。
- 本方案自身前向：先审核后实施，实施记录晚于方案，消除旧 P07 对象的事后补写缺陷（CHK-016）。

## 4. 风险与边界

- 差异化重跑失败风险：由 P06 流程修复重跑，本方案暂停，不吸收 P06 失败。
- 上游垫片保真风险：已限定用途为 schema 渲染比较，不用于行为比较；如审核方认为垫片不可接受，回退口径为"双方均以静态注册面计数 + 当前产品以真实 tools/list 为准"并在报告标注差异。
- token 口径为文档化近似（chars/4），不冒充实测 tokenizer 值。

## 5. 自评结论

方案与需求 v2/总纲 v10 的 P07 职责一致，核心新工作（ACC-024 同口径实测）已有预验证的可行路径，顺序门禁与诚实边界成立。**自评通过，提交独立审核。**
