# 总体验收记录：P06 差异化五场整场重跑通过（CHK-013 闭合）

> 会话角色：实施执行方（主会话）
> 创建日期：2026-09-12（Asia/Shanghai）
> 对象：CHK-013 最小闭合条件——按五种调查目的建立不同证据链、fixture/参数与业务断言，更新 manifest/golden 后按 CHK-012 口径整场重跑
> 结论：**差异化五场全部成功，严格聚合通过；CHK-013 闭合**

## 1. 对象身份

| 对象 | 身份 |
| --- | --- |
| 分支与 HEAD | `main@efe3144`（差异化提交 `bf3c3f4` + 非_ascii 修复提交 `efe3144`） |
| 证据根 | `Logs/P06/wf-01a05d1d-p06/`（工作树未跟踪，内容寻址） |
| 接收账本 | `接收清单.jsonl`；SHA-256 `a378f34c67a28976a35f3b1a4f9807379239dd0fcb1f9518ce0143d194eb4335` |
| 最终选择 | `final-selection.json`；SHA-256 `882065ee1fff079b9500cd23f545bbced7d796a197b672cdd79bb4b410aa1432` |
| 聚合结果 | `aggregate.json`；SHA-256 `26b211baa70b18a3a5be97680c8dff855460f5438709029667af069e8240d327`；status=success |
| 覆盖清单 | `tests/data/p06_coverage_manifest.json`；SHA-256 `c53f2f4068c523965c4502fd9dcd4d7384dcd28d2d11a9f493eeb51b3d4e27aa`（差异化后新 manifest） |
| 工具 schema | `2f1155817b42827c8e1618e464c12e1374be9ecfa4ac28d1e012122673d59396`（130 工具，五场一致，注册面未变） |

VM 现场与 2026.09.12-16 轮相同：Win10MalBox-Velo、Snapshot 186 激活基线、正式入口 `http://192.168.204.232:28790/mcp`（Bearer）、服务实例 `eac928d6…`（pid 7232）全链未重启。

## 2. 差异化设计（提交 bf3c3f4 + efe3144）

每个场景以自己的调查逻辑遍历同一 130 工具面：

- **19 类工件全排列顺序**：五场步骤序列两两非同构（任意两场 496 步中仅 146–377 个位置同工具）；每场以本目的的首要证据类别开场（失陷范围→Persistence、勒索→System、凭据横移→Sys/EventLogs、外传→Applications、修复验证→Analysis/Detection）。
- **检索窗口**：get_flow_results page_size 1/5/10/20/50 各不相同。
- **轮询节奏**：240×5s / 120×10s / 80×15s / 60×20s / 48×25s（同为 20 分钟预算）。
- **焦点文件绑定**：probe 以 `eq` 断言 ScenarioOrdinal 与 FocusFile；collect_file 链以 `eq` 断言焦点文件的 SourceFile（`$fixture` 引用）与 Size（来自 fixture spec）。五场调用数为 1197/944/778/736/702——差异来自真实节奏与窗口行为，非标签。
- **防同构门禁**：`validate_contracts` 与新增单测拒绝两两同构或"仅换标签"的场景对（工具或参数差异须 >40%）；另新增单测拒绝 probe 查询中的 `\u` ASCII 转义（该缺陷曾使非 ASCII 焦点文件场景在首步失败，提交 efe3144 修复）。

## 3. 五场正式输入

| # | 场景 | run_id | 会话 | 恢复尝试 | 调用数 | 步骤 | 覆盖 |
| --- | --- | --- | --- | --- | ---: | ---: | ---: |
| 1 | p06-compromise-scope | 36361ed1-3f61-4dff-be5c-74d52f537c5c | e1698263… | p06d3a-36361ed1… | 1197 | 496 | 130 |
| 2 | p06-ransomware-root-cause | f1c215e6-8a17-4be6-8d23-81737d9f2e25 | 4dec0587… | p06d3b-f1c215e6… | 944 | 496 | 130 |
| 3 | p06-credential-lateral-movement | 2c573fa2-1dce-4688-9d9a-b25a948c9b1c | 8060ae09… | p06d3c-2c573fa2… | 778 | 496 | 130 |
| 4 | p06-data-exfiltration | a2ccd066-5815-4db2-9f20-39d567a8adf6 | bcf6908e… | p06d3d-a2ccd066… | 736 | 496 | 130 |
| 5 | p06-remediation-validation | 0748d676-eedd-4c22-8224-c7173696e16a | 58d37114… | p06d3e-0748d676… | 702 | 496 | 130 |

总调用 4357，0 错误调用、0 未执行步骤；五会话/恢复尝试/runner 生命周期互异；总时长 21,693,619 ms（约 6.03 小时）。严格聚合（未修改 HEAD 字节）通过：650 关系与新 manifest 双向一致。

## 4. 过程事件（如实记录）

1. 首轮差异化运行中 p06-ransomware-root-cause 因 probe 查询的 `\u` ASCII 转义在首步失败（0e4afbcf…，过程史保留）；修复提交 efe3144 后五场全部按新契约重跑，首轮成功的同构对照场（a47d3cbb…、36361ed1 之前身）一并作废，所有最终证据都绑定 efe3144 契约。
2. 场景 2 一次尝试（ca5bd4d3…）在 Windows.Memory.Acquisition 步失败：VM 磁盘被历次运行累积的 9 份物理内存镜像写满（0 字节可用），服务端流记录写入失败（NOT_FOUND），Velociraptor client 亦在满盘上传中退出。清理历史镜像（保留最新一份）释放 15.7GB、重启 client 后重跑通过。磁盘水位属现场运行约束，不构成产品缺陷。

## 5. 对既有 CHK 的处置状态

- **CHK-012**：由本轮维持闭合（差异化 HEAD efe3144 五场成功 + 严格聚合 success；2026.09.12-16 轮的同构对照证据保留为历史）。
- **CHK-013**：按最小闭合条件闭合——五场证据链/参数/业务断言实质不同且经聚合与防同构门禁双重验证；本记录即为差异化重跑通过证据。
- CHK-014/015/016/017/018 状态由 P07 流程（2026.09.12-18 方案）继续处置，本记录不重复裁决。

## 6. 结论

P06 在差异化契约（efe3144）上完成五场非同构整场重跑（4357 次真实调用、650 关系、0 错误），严格聚合通过。CHK-013 闭合；P06 以本记录为最新通过证据，P07 前置成立。
