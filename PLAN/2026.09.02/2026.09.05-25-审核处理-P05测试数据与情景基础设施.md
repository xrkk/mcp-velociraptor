# P05 测试数据与情景基础设施首次审核处理

> 会话角色：作者/执行方
>
> 审核性质：首次全面审核结果引入
>
> 审核批次：`P05-BATCH-c7ec47f7-de5603cc-43784f06-814dc829`
>
> 已用额度：全面审核1/1、定向复审0/1；当前唯一有效第三方结论为“存在不同意见”
>
> 原审计：`2026.09.05-24-审核-P05测试数据与情景基础设施.md`，Git blob `19077de6235b2055eeedac6bc7836a670e390194`，SHA-256 `17a79fdadcf546bd60d0aeddd30db4aa07becea4d7637ec648ce74177fbb638f`
>
> 处理前方案：`2026.09.05-22-实施子方案-P05测试数据与情景基础设施.md` v0，Git blob `c7ec47f78e3a10d0a69fb4472d4e7fa88b24a8b7`，SHA-256 `f32c9b3146b058ce37d9c7847108d93a0cdd1a3a8b358137069cfc40c4ae5172`
>
> 处理后方案：同路径 v1，Git blob `09ee718ada269f4f5849532bcbb2e60a3cb5c397`，SHA-256 `05fe84cb801f4a45ce66e4de8b101cc29ba8724b9fea48e1418ac9ab16aadb4a`
>
> 本地审核与处理基线：`814dc829a11b38e59288a4120b715438bd802f6b`
>
> 处理结论：十项意见全部同意并闭合；首次审核已引入，修改后由作者全面自评闭环，无需再次第三方审核

## 1. 身份、漂移和处理边界

审计记录的对象、版本、blob、SHA-256、审核批次、HEAD和来源身份均与派遣包一致；记录已实际持久化，审核方未改既有文件。审核期间总纲v4、需求v0、P01～P04提交、P05 source Spike和决策关键路径没有语义漂移；唯一新增文件是该不可变审计记录。处理只修改P05方案，不修改需求、总纲、原审计、源码、测试、VM、inventory、artifact、Flow、快照或恢复状态。

受影响切片为P05第0～11章、IMP-P05-001～008、ACC-P05-001～006和FB-P05-002/003/004/006。P05的四个依赖固定identity、130工具生产契约、P05/P06/P07阶段边界、两轮Snapshot185恢复目标和“旧快照只允许新授权人工使用”是继承项；总纲v4及20个规范稳定ID的含义不变。

## 2. 逐项处理

### AUD-001｜同意

- 原因：P01证据确实显示三项inventory已有不同于目标的合法前态，v0的“不同identity拒绝”会阻断首次转换；v0也未把当前运行态先恢复为Snapshot184作为安装硬前置。
- 修改：方案新增1.1节，逐字段冻结三项唯一predecessor，规定Snapshot184一次完整恢复后才开attempt，只允许predecessor到locked identity的一次转换、同目标no-op及其他identity拒绝。任何部分失败先回传差集，再从Snapshot184开新attempt，禁止删除/卸载模拟回滚。
- 最小闭合：已满足；IMP-004/ACC-001/FB-002同步承接。

### AUD-002｜同意

- 原因：v0只列操作符，无法唯一判断路径、类型、轮询、错误与cleanup。
- 修改：第4.1节冻结完整tagged union、RFC 6901路径、安全拒绝项、每个操作符语义、AND规则、repeat状态、scenario-level cleanup、canonical report schema/序列化和独立golden反例。
- 最小闭合：已满足；IMP-003和ACC-005不得从实现反向生成期望。

### AUD-003｜同意

- 原因：只验证tools/list不能阻止任意JSON借`run_vql`或命令artifact成为通用代理。
- 修改：第4.2节改为CLI只收scenario ID，scenario必须位于固定树、是普通文件且SHA命中tracked index；fixture路径固定推导；bridge argv/cwd/environment allowlist和`shell=False`冻结；VQL/命令参数只能来自已索引tracked字节，禁止覆盖或插值，并增加入口拒绝测试。
- 最小闭合：已满足；runner保持测试基础设施而不是产品编排器。

### AUD-004｜同意

- 原因：v0把tracked模板和含PID/绝对路径的运行态manifest混成同一对象，且任务、事件和进程副作用不够确定。
- 修改：第3章拆分不可变`fixture-spec-v1`和批准guest根内`fixture-instance-v1`，冻结文件、注册表、事件、禁用无trigger任务、Python路径/argv/sleep以及ownership、幂等和冲突状态机。
- 最小闭合：已满足；tracked spec不再被运行态改写。

### AUD-005｜同意

- 原因：v0保持`.next` fail closed，却没定义当前attempt生成的无效`.next`如何安全退出；v2键和交叉不变量不足。
- 修改：第5章冻结v2 exact keys、retired顺序和全部交叉不变量，补齐排他create、flush/fsync、成功原子replace/目录fsync以及仅隔离当前attempt失败`.next`后恢复Snapshot184选择的算法和反例。
- 最小闭合：已满足；来源不明`.next`仍阻断，绝不删除VMware snapshot。

### AUD-006｜同意

- 原因：PacketCapture操作系统全局trace，不能仅凭finally主张本workflow所有权。
- 修改：第2.3节要求事前只读inactive，start后以状态/时间/ETL路径取得attempt所有权；正常stop/convert及MCP失联时唯一外层紧急stop均冻结。无所有权不stop，只回传后恢复Snapshot184。
- 最小闭合：已满足；ACC-001和FB-003同步要求范围差集及最终inactive。

### AUD-007｜同意

- 原因：官方事实要求`EXECVE`，且仅保存PID不足以防止复用。
- 修改：第2.2节冻结KillProcess完整golden、Windows precondition、`required_permissions: [EXECVE]`、唯一source/query/result；第2.3节冻结调用紧邻前PID/start-time/token复核和受保护PID集合，结果必须`Killed=true`。
- 最小闭合：已满足；这是显式化既有`pskill`必需权限，不新增产品权限或能力。

### AUD-008｜同意

- 原因：localhost serve URL不能证明工具已经物化，且v0没有server+client网络观察窗口。
- 修改：第2.1节冻结inventory完整readback、probe时点、filestore独立hash，并把host-only传输和产品调用分账；产品窗口覆盖server/client DNS、TCP和服务日志，原始域名/URL必须零请求。
- 最小闭合：已满足；观察面不能取得时是阻断，不以推断通过。

### AUD-009｜同意

- 原因：现场`Logs/P05/source-spike/`已有受保护未跟踪EXE/ZIP，整个worktree不可能二进制零命中。
- 修改：第0、6～9章改为index、提交树、新reachable blobs、tracked worktree四层零命中；完整worktree逐项报告既有PLAN/Logs排除，新增排除失败。修正“只在宿主临时区”的错误陈述。
- 最小闭合：已满足；不删除审计证据，也不让它进入Git。

### AUD-010｜同意

- 原因：v0身份和自评按9个自然语言分组计数，漏了P05受委托回填与交叉适用的稳定ID。
- 修改：身份行和第10章建立20项规范ID、两层OUT、全部IMP/ACC/fallback的双向矩阵；CON-001单列为P01继承前提。
- 最小闭合：已满足；计数统一为稳定ID，不混用分组行。

## 3. 实际diff、传播与继承

实际契约diff共十组：准备状态机；inventory/readback/网络观察；KillProcess golden；PacketCapture ownership；fixture spec/instance；scenario schema；runner信任边界；恢复state v2及失败`.next`；Git扫描universe；20项追溯。没有改工具名称/schema、依赖版本/hash、P05职责、Pxx顺序或总纲验收分工。

受影响既有通过项是v0对依赖来源、runner设施、固定数据和六项ACC“可机械验收”的主张；这些项全部按v1从头重评。继承项为审计第3章核实的对象身份、P01～P04链、Snapshot184当前canonical、MCP SDK 2.1.1、130工具、四依赖字节identity、官方root VQL可行性和P05/P06边界。全局不变量仍是：所有测试由作者在Win10VM执行；失败不计成功；产品MCP不联网安装；不提供联网控制工具；不冷启动、不删除snapshot；PLAN/Logs不提交。

## 4. 修改后完整复评与需求保真

- 范围/非目标：只扩充P05测试和本地恢复治理契约，没有新增产品能力或跨项目接口。
- 后序依赖：runner/fixture/安装/snapshot均可在P05独立实现验收；P06只消费已完成设施。
- 方向与异常：所有方向唯一；每个失败分支有停止、清理或恢复边界；没有占位决定或无主依赖。
- ACC：六项均含前置、动作、精确预期、证据和通过/失败/阻断边界。
- 需求保真：应承接20、完整20、未落点0、语义弱化0、不可验收0、失效或复核条件缺失0；异常ID无。

## 5. 再次审核逐项判定

1. 跨两个以上项目或多业务共享契约：未命中；仅本仓库P05测试设施和本地VM治理。
2. 不可逆/高风险迁移、权限、安全、隐私、外部关键契约：未命中；`EXECVE`是官方`pskill`既有必需权限的显式golden，未扩大产品授权；所有写入可由Snapshot184恢复，依赖身份不变。
3. 改变总体架构、核心数据流、并发一致性、恢复或切换策略：未命中；仍是Snapshot184干净准备、Snapshot185候选双恢复、`.next`两阶段原子激活。新增失败`.next`隔离与fsync是该既定策略缺失的失败闭合，不改变active选择或切换方向。
4. 核心路径难回滚或影响面不可限定：未命中；产品代码尚未实施，方案修改可回滚，运行态写入失败统一恢复Snapshot184。
5. 多个有证据可行方向：未命中；每个AUD采用唯一最小闭合方案。
6. 用户或项目约定明确要求再次审核：未命中；要求是首次独立审核，已完成并引入。

因此不消耗定向复审额度，不输出复审模式。首次审核祖先和本处理记录共同建立后续作者自评闭环资格。

## 6. 结论

P05 v1状态更新为`已审核，可以实施`。首次全面审核的十项意见已全部引入，修改后需求保真六项为`20/20/0/0/0/0`，无需再次第三方审核；当前长程授权继续有效，恢复实施流程。
