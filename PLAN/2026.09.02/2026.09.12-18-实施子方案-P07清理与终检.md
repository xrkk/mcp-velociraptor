# 实施子方案：P07 清理与终检（v18 PC022正式作者版本；待主方完整保真自评）

> **当前状态**：作者稿已同步／待主方完整保真自评／实施门关闭；成本、终检及AUD/CHK均未由本文关闭。

> **当前九对象组**：`CURRENT-PC022-R01`；需求`v10`、总纲`v23`、P01`v4`、P02`v7`、P03`v3`、P04`v11`、P05`v19`、P06`v20`、P07`v18`。正式身份由`PLAN/2026.09.22/current-normative-inputs-pc022-r01.json`描述；`CURRENT-PC021-R04`保留为被本轮取代的作者稿前态，`CURRENT-PC020-R01`仍为正式父来源。
>
> **PC020父来源（历史身份）**：`CURRENT-PC020-R01`；需求`v8`、总纲`v21`、P01`v4`、P02`v7`、P03`v3`、P04`v10`、P05`v17`、P06`v18`、P07`v15`。其历史身份由`PLAN/2026.09.20/current-normative-inputs-pc020-r01.json`描述；`CURRENT-PC017-018-019-R02`保留为更早历史起点。

> 会话角色：规划/作者/修复/调度方（主会话）；子智能体实施
> 创建日期：2026-09-12（Asia/Shanghai）
> 状态：新基线交接配对已承接，待完整自评和P06全量交接；20号记录不授权当前现场的成本实测及终验

> **PLAN-CHANGE-014 覆盖块（2026-09-14，用户裁决）**：移除与 FakeNet-NG 撞车的网络域机制（详见 `2026.09.14-16-PLAN-CHANGE-014裁决-移除Velo方案中FakeNetNG撞车网络机制.md`）。对本方案：注册面 130 不变（tools-schema/清理仍以 130 为准）；逐工具实测口径改为 129+1 登记排除（Windows.Network.PacketCapture，网络抓包职能划归 FakeNet-NG 域）；覆盖关系 650→645；P05 侧四链→三链、etl2pcapng 依赖移除、完整网络窗口门降级为可选支撑证据。本块以下旧文本中与之冲突的表述均为被覆盖历史。

> 历史前置声明：旧版曾引用 `2026.09.12-16/-17` 作为P06通过依据；后续 `2026.09.13-07/-08` 已给出反例，本版不再消费该通过声明。新入口以第3节为准。
> 与旧对象的关系：`2026.09.12-13-实施子方案-P07清理与终检.md`（身份错误、事后补写）与 `2026.09.12-02-实施记录-P07清理与终检.md` 保留为历史，不作为本方案依据；本文件是身份一致、顺序正确的新 P07 方案。

## 0. PC021 P07交接契约（v18当前条款）

P07 只消费 P06 已接纳并归档的 PC022 内容图：逐字节复算 activation root、13键 v2 `snapshot189-activation-issuance-receipt-v2`、10键 epoch8 v2 intent、15键 `pc021-epoch8-activation-transition-receipt-v3` `COMMITTED` receipt、13键 `pc020-epoch7-migration-transition-receipt-v2` receipt、三个 Phase 的 5/4键 manifest 和全部成员，并核 current PC022 source/implementation identity、canonical H8 及 outer manifest 覆盖。下载成员只从归档 Phase 根的 `downloads/<flow_key>/<file_id>/content.bin` 读取；不可解引用报告中的旧 `local_path`，不可从进程内一次性能力推断已提交。每 Phase 真实 Triage 结果/list 和两个 fixture 实际下载链须完整，仍不下载 Triage upload。事件/时间字段是来源绑定的 controller 操作事实，不是跨进程能力、历史 API 的独立重放证明或硬件掉电保证。旧 receipt、旧 source、非 COMMITTED、缺件、extra、链接/reparse、hash/identity 不符均阻断 handoff；只回 P05/P06 或主方，不重签发、重试、刷新、回滚或选择旧 188。

缺失、extra、链接/reparse、hash/size/identity不符、报告改写、非COMMITTED或legacy形状均阻断handoff；回到P05/P06或作者负责层，不重签发、重试或回退旧bundle/188。80/67、成本pair、CHK owner、最终独立核验顺序和P07职责不变。本版仅同步作者稿，当前manifest及待完成的保真自评不能冒充正式handoff。

## 0.1 PC020 P07交接基线（v15历史父条款；由上节精确细化）

本节覆盖下方§1/§3.1/§4中冲突的旧snapshot/schema/epoch和handoff来源；成本方法、五个等价pair、80项正式/67项累计双矩阵、14项P07直接责任、PC014/017/018/019及CHK只追加关闭规则不变。

### 唯一入口与递归验证

P07只能消费P06基于schema6/epoch8/`NETWORK_ACTIVE`/`Snapshot 189-Velociraptor-MCP可恢复验收基线`生成并严格聚合的final-selection与handoff。每个selected run必须是独立189恢复、stage=`P06_ACTIVE`，含八类exact恢复records，并递归包含P05的PC020 migration、preparation和activation完整图；activation根必须含合法UTC RFC3339 Z `issued_at`、根SHA覆盖该字段，且canonical `activated_at`逐字等值。缺失、null、非法、不等、从mtime/当前时钟/receipt补值或不能以事件证据证明cycle2_done→root_issue→epoch8_next均拒绝；不以跨机时间戳大小替代顺序证据。P07逐字节重算所有Ref、manifest和普通成员；外层manifest必须覆盖嵌套manifest/root，固定`T/E`不得任意排除。旧188、epoch7、未激活189、旧source/budget/report、跨attempt拼接或缺任一图均拒绝。

P07入口绑定已由作者采纳并冻结的当前九对象manifest及对应正式OID/SHA（本次六份同步作者稿，另三份沿用冻结身份）、主方完整自评、P05实际migration/activation（含cycle2验证后、epoch8 `.next`前的`issued_at`签发绑定）、P06实现/五场以及已经成立的selection/aggregate，并消费当时已经存在的必需阶段原件；若前序阶段另有独立核查，也必须保留和验证。尚未生成的身份不得预填；当前正式manifest及尚未完成的主方自评不是正式handoff。P06产物或规范identity变化时旧handoff失效并重新验证，P07不修改P06原件。

“P07之后针对最终新HEAD的全面独立核验”不是P07入口前提。固定顺序为：P06验收成立 → P07入口 → P07完成成本、终检与追溯 → 冻结最终HEAD → 由未参与编写/修复者全面独立核验 → 将核验结果纳入终态交付记录与CHK账本。把最终独立核验反向依赖到P07入口会形成循环，必须拒绝。作者或方案修复者不得自行追加CHK closed事件。

### 成本pair与终检

- current侧五个固定pair分别只能选择P06 final-selection中对应scenario的唯一189完整子链；upstream侧每个pair也从189独立恢复并绑定相同fixture、artifact、参数、终点和原样上游。不得选择最好一次、跨pair合并或用五场总量代替单pair。
- 五个pair继续比较schema、wire calls/retry、内部status retry、完整CallToolResult canonical字节与elapsed_ms；拓扑差异照实披露，不预设当前更小。189只替换恢复来源，不改变可比性与阻断判据。
- `final-traceability.json`仍精确包含80个formal_items和67个cumulative_items；REQ/RACC-013/020/023/026、累计35/56/59/62/64/65/67必须落到PC020新代次实际证据。旧报告不得因文稿或hash相同自动升级；隔离模型PASS不计正式覆盖。
- P07只在P06全量和handoff成立后执行ACC-024/025、README/危险开关/旧接口终检与全回归。任一missing/weakened/unverifiable/review_condition_missing非零均不通过；开放CHK只能由针对新原始证据的独立核验追加closed事件。

失败时回到唯一负责层：P05图、激活签发时间绑定或恢复入口问题回P05，P06 selection/aggregate/场景问题回P06，正式identity或作者语义问题回主方。禁止选择旧188 fallback、重标旧证据、降低80/67或绕过安全与资源门。本正式作者版本不表示成本、终检或CHK已完成。

## 1. 参与者与职责

历史v7/v6配对与需求v2、总纲v13 blob、P05v9/P06v10只追溯形成过程；该历史条款中的执行身份已由页首`CURRENT-PC022-R01`组、`PLAN/2026.09.22/current-normative-inputs-pc022-r01.json`及§0/§0.1精确细化。成本current只取当前P06最终selection，上游五组均按当前P05唯一协议从schema6/epoch8/NETWORK_ACTIVE的189各自完整恢复；拒绝187准备状态、旧186、旧4/5、旧188及未激活189。交接包必须按§0/§0.1递归包含PC020 migration、preparation及PC021/PC022精确细化后的activation/issuance及v3 transition/三Phase完整原件图，P07逐字复算，不修改P06原件。hand-off实际绑定页首当前九规范组及新自评/实施/独立证据核对，不沿用旧20号许可。五组同目标/同fixture等价比较、80/67和CHK独立关闭规则不变；文件互传授权不解除P06前置门。

PLAN-CHANGE-012门禁继续有效；其中“当前需求v2、总纲v12（blob `222dad75f9e5e5a52d102ce867c8b8363e294ad1`）”现只作历史输入，当前规范边界由页首`CURRENT-PC022-R01`组及`PLAN/2026.09.22/current-normative-inputs-pc022-r01.json`承接。用户已保留唯一187，旧§3.1的186→188/schema4/epoch5配对不得执行；此前自评与交接身份仅留历史，不能充当当前通过依据。先由当前P05完成新起点/候选配对及实际激活，当前P06完成新版全量及严格交接验证，再固定本版输入OID并完整自评。当前成本pair、恢复、终验均暂停，不以用户187恢复或新文件互传授权代替P05/P06门禁。五组同口径成本、80正式项/67累计结论、14项责任切片、CHK只追加账本及全量回归边界不变。

优先规则：依同目录19号用户授权，本版取消未来方案第三方复审，由主会话完整自评通过后交子智能体实施；主会话负责修复，实施后另起未参与编写/修复的子智能体独立核验。本文旧角色表和审核顺序的未来含义由此覆盖，旧AUD不改写或重置。

| 角色 | 对象 | 职责 |
| --- | --- | --- |
| 方案作者/修复/调度方 | 主会话 | 本方案、完整自评、归因修复、边界裁决和调度 |
| 实施方 | terra/xhigh实施子智能体 | 按自评通过方案实施、全回归、原始记录 |
| 独立核验方 | 全新terra/xhigh子智能体 | 未参与被核验实现，实施后独立全面核验证据 |
| 决策方 | 主会话（用户无人值守授权内） | 既定边界内自行决策，新增敏感权限仍遵守安全门 |

## 2. 需求承接（历史需求v2/总纲v12、旧作者组及PC017候选组仅追溯；当前引用为CURRENT-PC022-R01）

| 需求 | 总纲项 | 当前状态与本方案动作 |
| --- | --- | --- |
| REQ-015（危险开关零残留） | OUT-015、ACC-015 | 代码/配置/文档清理已在 `a60a9f7`、`91e1e0f` 完成；本方案实施时做跨生产代码/配置/两 README/example.env 的零匹配复验并留证 |
| REQ-016（两 README 终检） | OUT-016、ACC-016 | 旧版曾更新至 Snapshot186/P06 现状；本方案实施时须按§0/§0.1的Snapshot189/P06当前交接复验实际启动/配置路径，并按差异化重跑结果同步快照/场景表述 |
| REQ-024（成本实测） | OUT-024、ACC-024 | **本方案核心新工作**，见第 4 节口径 |
| REQ-025（阶段门禁） | OUT-025、ACC-025 | 七篇顺序索引、失败闭环、双向追溯矩阵与最终验收记录，见第 5 节 |
| NON-003、CON-015 等关联 | OUT-042/053/054 | 零残留与安全边界复验纳入终检清单，不新开范围 |

### 2.1 PC017分诊切片的P07责任候选

`PC017-TRIAGE-SOURCE/BUDGET/SUCCESS`在P07只承担两README、成本披露和最终追溯，不重新定义P04～P06行为。有向交接唯一为P05初验+两个候选周期真实Triage回填→P05激活→P06正式验收→P07；不得把激活设成P05回填的前置。两README须一致说明：公开入口无参数；完整`_BasicCollection`、2400秒；仅该入口请求4294967296字节且其他入口/全局默认不变；预算计量为压缩前`FileBuffer.DataLength`逐块累加并严格`> cap`后取消，在途/并行可超出，不是压缩后网络/物理字节；当前部署1 GiB在正式采纳、自评和实施前不切换；P05受控派生来源/安装/激活门；13 GiB同卷与1 GiB内存只属验收工作流执行前筛查，不是产品阈值错误接口；ERROR/取消/超限/关键错误及仅有rows/uploads均非成功；无联网安装、自动提限、删目标、改HashOnly或运行期替换fallback。

ACC-024成本记录把4 GiB标为请求上限而非预分配、物理上界或产品下载配额，并分别报告压缩前`FileBuffer.DataLength`累计/严格取消口径、实际Flow上传统计、datastore/filestore卷读数、结果/文件链与证据体积，不把这些量互相替代。ACC-025最终矩阵以REQ-013/CON-011/RACC-013和累计结论35、56为来源，双向关联总纲OUT-013/ACC-013、P04 ACC-P04-005、P05 ACC-P05-002、P06 ACC-P06-001/004及本节；累计35保留“一键基础证据换取耗时/存储”的取舍及“只允许定向调查或基础集合不兼容”重裁决条件，累计56保留外部依赖前提及“不再允许外部依赖或无法取得兼容版本时取消工具或另设计复合采集”的后果。保留PC016搜索暂停、P05激活和实施后独立核验。任何4 GiB不足、身份/目标图/版本/存储拓扑变化或未知叶子使完整性不可证均记异常并回主方重裁决，不得用本候选宣称PC017整体闭合。

`PC017-FILE-VIEW/IDENTITY/READ`在两README中须一致说明：清单对象是全部采集逻辑原文件；idx范围表是内部重建metadata，以`sparse_indexes_internal:<数量>`披露而不可独立下载，天然普通`.idx`保留；file_size为逻辑长度、uploaded_size为data非洞字节；普通文件`Padding=false`，稀疏文件先紧凑预读再逻辑重建，最终size/SHA针对实际逻辑字节；未知Type、孤儿/冲突/selector碰撞和metadata漂移fail-closed；ERROR单文件下载不改变Flow/triage成功状态。必须披露不提供原始紧凑副本/独立范围表导出的能力代价，以及每个稀疏下载额外一次紧凑预读的时间/网络成本。若需要保全存储层对象、依赖端语义变化或重建不可证，回主方重裁决导出接口，不能静默fallback。

`PC017-PUBLISH-FAILURE`在两README中须用一致的用户语言披露：`os.link`成功是发布提交点；提交前失败不由本请求创建新完成文件，但清理失败可遗留本请求part；提交后复核或part清理失败返回`BACKEND_ERROR/download_post_publish_failed`而不报告成功，完成文件或本请求part可能保留，用户不得自动重试或覆盖。产品不会为回滚按路径删除完成文件；路径或所有权不可信时停止清理，绝不追删目录、赢家、外部哨兵或其他请求对象。只有发布后复核通过且owned part清理完成才报告成功。文档同时说明size/SHA只证明实际交付逻辑字节，不能冒称产品拥有预期源hash或抵御任意本地特权攻击。

`PLAN-CHANGE-018/019`在两README中须同迭代一致说明：cursor是本次显式flow_id/source上下文内的纯offset，相同文本可在另一合法上下文使用，调用方须保持分页链上下文；它不证明签发来源或后端快照，也不会自动归零。适用分页时`next_cursor`是required nullable字段：非终页为规范cursor字符串，终页为JSON `null`；无分页操作则省略整个`pagination`。固定启动的`status="success"`只表示控制调用完成；collect_file、collect_forensic_triage、kill_process的`state`是创建Flow后立即回读的真实初始状态，start_hunt的`flow_state`是主要Flow初始状态而原`state`是Hunt状态。初始状态不保证返回时仍相同、ERROR不代表业务成功，缺失/空值为错误。README必须披露output schema兼容变化，不暗示动态工具status改变。

ACC-024 schema成本与ACC-025终检只接受`CURRENT-PC022-R01`九规范组、P04新golden及该当前组之后实际生成的stdio/HTTP同一tools schema hash；旧报告不得因文稿升版被重标为新版证据。终检静态差异断言四个固定启动outputSchema只新增三个`state`和一个`flow_state`字段；终页`next_cursor=null`是对既有nullable字段的勘误，不是第五个新字段。inputSchema、130工具、动态输出以及PC014的129+1/645保持不变，并逐项关联P05启动/终态分账及P06分页/状态正反例。

ACC-024成本/运行记录须分别统计正常成功、已有目标、合法并发、提交前故障、提交前清理失败、提交后复核失败、提交后清理失败及不可信路径/所有权分支的调用结果、残留完成文件数、owned part数与清理诊断；残留是可观察代价，不能归零、自动删除或从证据包剔除。ACC-025终检必须把每个异常残留与原attempt/错误reason/所有权核对关联，确认没有删除完成目标、用户文件、赢家、哨兵或其他请求对象；清理由用户在MCP外决定，产品和P07都不新增删除工具。

## 3. 入口与顺序门禁

1. 入口前提：P06 全量通过。`2026.09.13-07/-08` 已证实当前正式证据未闭合，旧通过声明不能作为本版入口；PLAN-CHANGE-009资格只作形成过程，必须等待§0/§0.1规定的当前P06五场、严格聚合及独立证据核对成立，才执行本版成本实测与总体验收。本版只允许先回流方案及准备契约保持代码，不提前消费P06通过结论。
2. 顺序：本方案审核通过 → 实施第 4/5 节 → 全回归 → 实施记录 → 总体验收记录 → 定向核验。
3. 失败处理：任一回归或复验失败即停在本阶段修复重验，不进入下一节。

### 3.1 P06 r3 唯一交接输入

PLAN-CHANGE-011下游覆盖：本节旧总纲v10/P06v7/12号复审以及“当前需求v2、总纲v11、P05v7、P06v8”固定身份只用于历史追溯；新交接必须绑定页首`CURRENT-PC022-R01`九规范组及其manifest、正式采纳后的实际OID，以及新主会话自评、P05激活、P06实施提交/全量原件和独立证据核对记录。当前未形成的身份不得伪填或以旧审核代替。本文成本pair与恢复动作按§0.1使用P05验证激活后的 `Snapshot 189-Velociraptor-MCP可恢复验收基线`，按当前P05/P06契约完整恢复；旧186/188报告只为历史。current侧必须来自189的P06最终selection，upstream五条各从189独立恢复，双方fixture身份/参数/任务终点和原样上游规则不变。schema6/epoch8/NETWORK_ACTIVE与完整激活原件必须可复算；未激活189、旧186/188或旧资格均拒绝。正式外部HTTP与隔离VM内stdio差异继续如实披露，不新增同环境虚假承诺。其余第4～9节成本、80/67矩阵、CHK账本和完整回归门不变。

测量器只消费固定`Logs/P06/wf-01a05d1d-p06-r3/`，不接受任意证据根CLI。目录名不构成旧成功资格；其下只有绑定当前身份、按不可变attempt与显式selection生成的新证据才可接收，旧内容不移动、不重哈希、不当当前成功。生成只读`p06-handoff.json`，逐项列出相对路径、字节长度、SHA256：历史根索引、接收清单、resource-qualification-selection.json、资格report/qualification-payload-manifest.json/qualification-payload.zip/resource-budget.json、final-selection.json、aggregate.json，以及每个selected run的package-manifest.json与其中全部成员。成员须覆盖报告、P06_ACTIVE八类exact恢复records及§0/§0.1完整递归图、snapshot-evidence、服务观察、响应头、fixture、原始tools/list/schema和资源转录。拒绝路径逃逸、链接、重复、缺失、哈希不等、旧r1/r2、跨attempt或未选run；严格重跑P06聚合器，复算结果必须与被交接aggregate字节相符。

旧§3.1交接曾绑定需求v2/总纲v10 OID、P06 v7 OID `94e9c29890680bf7e3c8120a880012442707caa4`、正式方案复审记录12 OID `51d67463e4061a9cc861d929c0a69982c253a915`；当前handoff身份按§0/§0.1及页首manifest绑定，并继续要求P06实际实现提交和输入源码SHA、P06新实施记录及独立证据核对记录的路径/OID。记录12只证明方案获准，不是实施完成；缺新实施证据或核对结果不通过即拒绝交接。hand-off本身不进其所绑定P06包，避免自引用；P07每份输出绑定hand-off SHA。只读验证失败不修改P06原件，而回流P06；P06产物/提交改变时旧hand-off失效，重新完整验证。

## 4. ACC-024 成本实测口径（同口径、不预设结论）

### 4.1 工具面与 schema 体积

- **当前产品**：取差异化重跑正式报告中五份一致的 `tools-schema.json`（130 工具，正式 HTTP 入口本会话真实 `tools/list`），统计工具数、canonical JSON 字节数、token 估算。
- **上游 78 基线**：上游 `mcp_velociraptor_bridge.py`（git `9dc8054`，P01已验证基线blob）原样在指定Windows VM的既有venv中运行，经FastMCP→MCPServer别名垫片由该VM内SDK stdio客户端采集；原有`Logs/P07/upstream78-tools-list.jsonl`仅作历史线索，新实测另存不可变attempt。当前侧仍由VM外正式HTTP客户端采集，不虚称双方客户端为同一主机或解释器。
- **可比性门**：每侧保存`comparison-environment.json`，固定side、run_id、脱敏client/server主机身份、OS/Python/MCP SDK/Pydantic版本、解释器SHA、锁文件及依赖清单SHA、transport、启动命令（秘密只记已配置布尔值）、launcher/profile/canonicalizer源码SHA、原始tools/list及fixture/snapshot引用。MCP SDK/Pydantic精确版本和canonicalizer必须相同；主机/解释器/传输差异逐项明确列出，任何未知或未证明可换算的差异阻断受影响维度。
- **逐维比较边界**：schema只比较双方真实tools/list解码后name/inputSchema/outputSchema按同一规范化程序得到的canonical内容，不比较HTTP/stdio封装字节；完整CallToolResult按第4.2.1节同一程序规范化，并保存解析前原始MCP消息验证未丢字段。wire调用数与内部重试分别比较，不混合不同层；elapsed_ms均为各自客户端单调钟的首调用至末调用实测差值，只能称“所披露拓扑的观测耗时”，不能归因为工具集优化，也不估算扣除网络/profile开销。测量器逐指标输出输入SHA、转换程序SHA、比较前提、左右量/delta、差异事实及passed/blocked；不能完成双边原始输出、语义等价或可比性验证时ACC-024不通过，不以披露代替缺失实测。
- token 口径：canonical JSON 字符数 ÷ 4（文档化近似，不引入 tokenizer 依赖）。

### 4.2 固定调查任务成本

- 取差异化重跑五份正式报告的真实数据：每场工具调用数、重试次数（attempt>1 的调用）、逐调用结构化输出 JSON 字节数与总量、场景时长；汇总给出原始量。
- **上游阻断边处置（复审后定稿）**：总纲 ACC-024 将"P01无可比运行基线"列为阻断边；本方案不改读该边，而是**实测取得可比的上游固定任务运行基线使阻断边不命中**：在同一 VM、同一 venv、同一 mcp 2.x SDK 下，以上游 78 桥（git `9dc8054` 原样源码 + 垫片）执行同一调查目标的固定任务，实测其调用次数、重试行为与逐调用结构化输出字节数，与当前产品同口径比较。
- **任务等价与映射口径（PLAN-CHANGE-010，修正AUD-004旧映射）**：保持原调查目标与终点——同一客户端、同一fixture spec/原件哈希及§0.1规定的Snapshot189基线，完成 `Windows.Forensics.RecycleBin` 的启动 → FINISHED → 结果行 → 文件清单。上游 `9dc8054` 源码中 `collect_artifact` 是立即返回Flow元数据的启动工具，不是阻塞采集；`get_collection_results` 内部循环调用 `get_flow_status`，到FINISHED后读结果。文件清单通过上游原生 `run_vql` 执行仅含该client_id/flow_id的 `SELECT * FROM uploads(client_id=...,flow_id=...)` 查询取得，空清单也必须实际取得且与Flow绑定。当前产品使用动态启动工具 + `get_flow_status` + `get_flow_results` + `list_flow_files`。双方公开工具/上游源码均不改，增加的是上游任务映射中的原生文件查询，不降低“文件清单”终点。
- 同口径逐指标比较以固定子链为单位，不把五个不同整场 totals 放到单个上游子链的对侧。每侧记录 wire tools/call 数、首次以后轮询/重试数、逐调用完整 `CallToolResult`（同SDK以 `model_dump(mode='json',by_alias=True,exclude_none=True)` 后做UTF-8 canonical JSON）的客户端可见输出字节、从首调用开始至文件清单返回的实际耗时；给出每项左右值及差值，不以attempt序号求和。整场总量另列为补充，不能替代子链比较。
- 上游内部重试必须有真实观测：对未改动源码使用仅观察的Python profile事件，记录原 `get_flow_status` 的调用/返回状态与计时，并关联本client/flow；首次检查之后的调用才计内部重试。不改原函数、参数、返回值、sleep或调度；观察装置源码/SHA和开销边界随原件保留，0次重试也照实记录。若观察改变行为或不能取得可信内部轨迹，本项仍阻断，不根据约5秒耗时猜测重试次数。当前侧从实际匹配子链的 `attempt>1` 调用逐条计数，两侧的wire与内部探测维度分列而不混加。
- 若上游固定任务实测因客观原因不可取得，则按总纲阻断边处置：ACC-024 记为被阻断并回流规范，不以单边披露替代。
- 结论格式：原始量 + 口径 + 差异方向（允许当前产品更大，不预设更小）。

### 4.2.1 上游隔离、成功判据与固定配对

上游历史进程仅在隔离目录和专属子进程环境中设置`ENABLE_DANGEROUS_TOOLS=true`，只为既定uploads只读查询启用历史原生run_vql；不得写入当前产品、服务、全局环境、example.env或两README的配置示例。保存启动环境白名单事实（不保存秘密），结束时销毁该子进程环境。不能证明隔离即阻断ACC-024，不得修改历史源码绕过开关。

每次上游调用保存完整CallToolResult原件，并解析其文本JSON envelope，要求外层无isError且内层ok严格为true；缺失、歧义、多份冲突文本或ok=false均失败。启动所得唯一flow_id用于后续结果和uploads查询；profile原始事件须证明对同client/flow观察到FINISHED，结果不得关联另一Flow。查询仅为`SELECT * FROM uploads(client_id=<literal>,flow_id=<literal>)`，literal对实际ID按VQL字符串规则编码，执行前保存完整UTF-8查询/SHA与原始参数。非空上传项的可见_Components必须具有`clients/<client>/collections/<flow>/uploads`前缀；空清单也须有成功原始envelope。无法证明Flow绑定或观察不改变函数/参数/返回/sleep时阻断。

唯一比较集冻结为第4.2节五个P06场景各一组pair，顺序与P06第2章一致。current一侧只能选择P06 final-selection中该scenario唯一run的RecycleBin完整子链；每条upstream一侧在P06交接成立后从独立Snapshot189恢复执行一次同目标子链，绑定对应current的fixture spec及原件hash、client、artifact与等价参数。两侧分别记录restore_attempt_id/run_id/flow_id、源与输入hash、首末调用时间及全部原始输出。不得选择最好一次、跨pair合并、以五场总量对单次上游。失败尝试全部只追加登记；显式selection说明每次拒绝原因，修复后该pair完整重跑。

每条pair分别输出wire calls、wire retry、internal status retry、完整CallToolResult canonical UTF-8字节、elapsed_ms及逐指标左右值/delta。canonical采用sort_keys=true、ensure_ascii=false、separators=(",",":"), allow_nan=false、不加尾换行；token估算统一为canonical字符数/4并标明非实测token。profile装置保存源码/SHA、监听code object的来源文件/函数名/首行、事件顺序/时间/client/flow和观察开销说明，不可从耗时推断内部重试。整场数据只能作为另列附录。

### 4.3 产出

`tests/p07_cost_measurement.py`（v2，输入为真实捕获与正式报告，输出 `Logs/P07/cost-measurement.json` + 比较表）；原始输入内容寻址留存：
- `Logs/P07/upstream78-tools-list.jsonl`（上游 stdio 真实捕获，SHA-256 `78fa5a25…`）；
- `Logs/P07/upstream78-src/`（上游源码三文件，与 git `9dc8054` blob 逐字节一致）；
- `Logs/P07/upstream78-launcher.py` 与 `upstream78-probe.py`（历史AUD-002所要求的别名垫片与捕获探针原样留存并列入输入清单，显式披露SDK与传输差异；垫片/SDK可比性无法验证时，schema及受影响任务维度阻断并回流。静态注册计数仅能作诊断附录，绝不能替代双方实际tools/list/schema或作为通过型fallback）；
- 差异化重跑五份报告的 tools-schema 与接收清单哈希。

## 5. ACC-025 终检与追溯

1. 七篇子方案顺序索引：P01→P07 逐篇核对入口、产出、ACC、失败修复/重跑、交接，含本方案自身。
2. 反向追溯：全部 26 REQ + 15 CON + 13 NON + 26 RACC → OUT/ACC/Pxx 的最终证据链接，更新到差异化重跑后的证据身份。
3. 失败闭环：遍历当前需求/总纲规范链所引用的全部核查记录，至少包含CHK-001～027及后续新增ID。以来源记录OID与CHK ID组成稳定键，建立只追加事件账本，字段固定为source_path/source_oid/chk_id/affected_ids/acc/owner_pxx/state/evidence/minimum_closure/reconsideration/independent_check_ref/previous_event_sha256；state仅open/blocked/closed。旧结论不覆盖；019～023也要逐项承接，024～027当前保持open。作者修正或方案复审不能产生closed事件，只有针对新原始证据的独立核验明确成立才可追加closed；所有开放/阻断项列入最终异常清单。
4. 最终验收记录：以第5.1节两套完整矩阵重算六计数，不用摘要、旧通过记录或80项计数替代67项累计结论。

### 5.1 最终追溯机器契约

固定输出`final-traceability.json`，根字段为schema_version=1、normative_inputs、implementation_head、formal_items、cumulative_items、p07_direct_ids、chk_ledger_ref、counts、anomalies。normative_inputs保存当前需求/总纲路径、版本、内容OID与SHA；必须与本版规范边界相等。formal_items精确包含REQ-001～026、CON-001～015、NON-001～013、RACC-001～026，共80个唯一ID；cumulative_items精确包含需求“累计有效结论”序号1～67，不把历史被替代方向重新计入。

两类每行共同字段为id/source_anchor/source_text_sha256/formal_ids/out_ids/acc_ids/owner_pxx/contract_refs/actual_landing/commits/evidence/status/anomaly_ids/failure_refs/reconsideration_refs。contract_refs逐项恢复参与者、输入输出、触发顺序、状态生命周期、不变量、异常边界结束条件、采纳理由/适用前提/失效与重新裁决及可观察验收；引用必须有当前路径、定位及SHA/OID，不可只留术语或摘要。actual_landing是代码/测试/记录实际定位，commits必须可解析；evidence逐项保存原件相对路径、长度、SHA和所属run/attempt。累计行formal_ids必须等于需求附录当前映射，正式行反向可找累计来源及Pxx实际落点。

status只允许complete/missing/weakened/unverifiable/review_condition_missing。先独立记录所有异常，再为互斥计数按missing→weakened→unverifiable→review_condition_missing优先分配唯一状态；异常不能因优先级丢弃。counts分别为formal/cumulative两组expected/complete/missing/weakened/unverifiable/review_condition_missing，expected须为80/67，后五类之和须等于expected；anomalies分别列异常稳定ID及全部原因。程序拒绝缺行、重复、额外ID、未知状态、无内容寻址证据、错误映射或计数不等；后四类任一非零不得最终通过。源条目hash或实现HEAD变化时旧矩阵失效，须重新保真与核验，不自动套用旧结论。

P07直接责任切片精确为14项：REQ-015/016/024/025、CON-010/013/014、NON-003/011/013、RACC-015/016/024/025。REQ/RACC-015须有旧wrapper/helper无引用、物理删除和替代全回归原件；CON-010验证行为变更同迭代两README；CON-013/NON-011证明无旧工具/schema/同步响应兼容层；CON-014逐项定位术语首次解释；NON-003证明当前产品无危险开关/profile；NON-013证明agent_poc明确不支持且不进入新版验收。切片不是额外14份需求，也不替代80/67；它单独展示相同六计数和异常ID。

## 6. 全回归清单（AUD-001 扩充，复审后修正枚举）

仓库全部可运行测试模块逐一执行（VM venv，`VELOCIRAPTOR_API_CONFIG` 提供时含正式验收项）；以下为历史13模块基础清单，项数以本次发现及原始日志为准，新增模块必须追加，不能以旧计数漏测：

1. `tests.test_p02_core`（14 项）
2. `tests.test_p02_stdio`（2 项，stdio 适配）
3. `tests.test_p02_transport`（25 项，含 `test_stdio_and_http_share_identical_toolset`——HTTP/stdio 工具 schema 同一性终检落点，总纲 v5 变更记录指派 P07）
4. `tests.test_p03_dynamic`（6 项）
5. `tests.test_p03_stdio`（4 项，API 配置下）
6. `tests.test_p04_contract_golden`（1 项）
7. `tests.test_p04_fixed_tools`（15 项）
8. `tests.test_p04_registration`（3 项）
9. `tests.test_p05_contracts`（5 项）
10. `tests.test_p05_scenario_runner`（9 项）
11. `tests.test_p05_service_script`（7 项）
12. `tests.test_p06_contracts`（7 项，含两两非同构门禁）
13. `tests.test_p06_aggregate`（5 项；历史路径式导入风格，以 tests/ 目录为工作目录调用）

14. `tests.test_p06_resource_gate`（本次新增；资源门、预算重算、调用前拒绝、deadline与身份反例）。最终扫描全部`test_*.py`，逐项说明执行/归属/排除理由，不能把上述枚举当作扫描上限。

外加：差异化重跑严格聚合 success；文档审计（两 README + example.env 零漂移、危险开关零匹配）。任一模块不可运行时逐模块记录理由与影响，不得静默省略。2026-09-05 历史基线（P02 40/40、P03 10/10、P04 19/19）仅作追溯旁注，不替代本轮枚举。

## 7. 边界与不做

- 不新增工具、不改 130 注册面、不动双传输与单 Windows endpoint。
- 不修改 P01~P06 历史记录；差异化重跑以新记录叠加。
- 上游 78 基线只读（git 历史提取 + 独立目录运行），不回写仓库生产代码。
- 不推断通用模型成本；token 估算口径显式声明。

## 8. 验收标准

- ACC-024：全部实测口径透明、双边 schema 比较完整、任务成本为真实运行数据、报告不预设结论。
- ACC-025：七篇顺序证据完整、追溯矩阵闭合、失败闭环可查、最终记录计数可复算。
- ACC-015/016 复验通过；全回归全绿；实施记录与验收记录落盘并通过定向核验。

## 9. PLAN-CHANGE-010 登记与重新裁决

来源：CHK-026及 `2026.09.13-07` 候选A。实际证据为上游 `9dc8054:mcp_velociraptor_bridge.py` 的 `collect_artifact/get_collection_results/run_vql` 与当前 `velociraptor_api.list_flow_uploads` 的只读uploads查询；旧捕获只含两次调用且缺内部轨迹，不能补造原始文件清单。责任层P07，需求REQ-024/RACC-024、总纲OUT-024/ACC-024和P01→P07来源边不变。

影响切片：P01上游只读来源 → P07第4.2节等价任务映射/观察装置 → ACC-024双边比较 → ACC-025最终记录；并等待P06新纪元交接。采纳理由是恢复原任务终点和可观察口径，拒绝“删文件清单”“单边披露即可通过”“按耗时猜重试”三个方向。适用前提为同SDK、相同fixture身份、原样上游和未改变行为的观察。失效/重新裁决：uploads原生查询不可用、profile影响语义、fixture不可比、原始输出或耗时缺失时，按总纲阻断边回流；不得自设替代终点或补猜数据。

本版先完成需求保真/自评及必要复审，不沿用旧版审核状态，不关闭任何CHK，不把方案复审当作实施后的独立核验。

版本记录：v11-candidate-combined-r02（2026-09-20）统一当前PC017候选组并把旧需求/总纲/P05/P06引用明确降为历史；保留v10的README、稀疏额外预读成本、重裁决披露、正确triage预算和阶段顺序。仍只承担README、成本和终检，待主方采纳及完整自评，不改变旧证据或前序通过状态。

版本记录：v11（作者修订，2026-09-20）机械采纳已验收v11候选正文，当前组切换为`PC017-AUTHOR-REVISION`；待主会话完整自评，实施门关闭，代码、部署和验收未随文稿通过。

版本记录：v12（作者修订，2026-09-20）落实08号发布提交点与故障语义裁决，在两README、成本及终检中披露提交后错误可残留完成文件/owned part及禁止自动回滚删除；当前组切换为`PC017-AUTHOR-REVISION-2`，实施门仍关闭。

版本记录：v13（作者修订，2026-09-20）承接PC018/019，要求两README同步解释cursor请求上下文及status/state/flow_state，成本和终检只接受九规范组的新schema hash；旧运行报告不升级为新版证据，实施门关闭。

版本记录：v14（作者修订，2026-09-20）承接PC018终页勘误，要求两README和终检明确required nullable `next_cursor`的终页null/非终页字符串/无分页无`pagination`三分；不改变PC019四字段差异或实施门。

版本记录：v17（作者勘误，2026-09-21）按04号作者裁决冻结`CURRENT-PC021-R04`并消除旧身份/阶段歧义；固定r3证据根仍仅接收绑定当前身份的新证据。成本、清理、14职责、80/67、129+1/645、安全/残留规则与最终HEAD独立核验顺序不变；待主方完整保真自评，实施门关闭。

版本记录：v18（作者采纳，2026-09-22）按05号PLAN-CHANGE-022登记与用户采纳授权冻结`CURRENT-PC022-R01`：Windows刷新边界（三具名句柄有限生命周期、`MoveFileExW` flags 9、issuance v2/migration v2/activation v3 receipt版本门、FAILED_BEFORE_REPLACE按replace_attempted判定、source_inputs/implementation_sources分层）进入五规范；成本、清理、14职责、80/67、129+1/645、安全/残留规则与最终HEAD独立核验顺序不变；待主方完整保真自评，实施门关闭。
