# 定向核验记录：P06 五场景正式整场重跑证据链（通过）

> 会话角色：独立核验子智能体（acceptance-feature-verifier）
> 核验模式：定向核验（修复+核验循环第1轮）
> 核验日期：2026-09-12（Asia/Shanghai）
> 结论：**定向核验通过**（无任何不符项）

## 核验范围与方法

对 `main@91e1e0ffa471c9d03437a3e787eac3c968150a82` 工作树证据 `Logs/P06/wf-01a05d1d-p06/` 与
`PLAN/2026.09.02/2026.09.12-16-总体验收记录-P06正式整场重跑通过.md` 做只读核验：
聚合 import 重算（不落盘）、三文件 SHA-256 复算、五报告逐份抽查、650 覆盖关系与 manifest 双向差集、
验收记录声明核对、HEAD/工作树状态。

## 核验结果摘要

1. **聚合链**：接收清单 5 行 attempt 连续、报告/包哈希互异；import 重算输出 `success 650 5 5 5 0` 且与
   `aggregate.json` 全字段深度相等；三文件哈希与验收记录声明一致（0402ca9c…/9a8eb127…/1233b538…）；
   聚合内部已复核 tools-schema 字节+canonical 重算+130 工具、snapshot-evidence 身份 join、Snapshot 186
   P06_ACTIVE schema3 绑定。
2. **五份报告**：全部 status=success、failure 空、unexecuted 空、496 步、130 覆盖、调用数
   1223/1169/1173/1176/1184 与声明一致；零 is_error 调用、sequence 连续；会话/run/恢复尝试/runner(pid,start)
   五元互异；tools_schema_sha256 五场同值 2f115581…；fixture_spec 同值 bba83db3…；transport/endpoint/
   authorization/schema_version 全部符合 schema2 正式口径；报告文件哈希与账本逐一相符。
3. **覆盖关系**：五场并集 650 个互异 (scenario_id, tool) 对，与 `tests/data/p06_coverage_manifest.json`
   relations 双向差集为 0。
4. **验收记录**：第 2 节表格全部字段与实测一致；total_duration_ms=18131409 与五份报告求和相等；
   诚实边界属实（CHK-013/014 维持开放、P07 前向待实施、不声称 80 项全量复算）。
5. **现场**：HEAD 91e1e0f，tracked 零修改，未跟踪目录仅 Logs/、PLAN/、tests/__pycache__/（既定现场）。

## 总结论

**定向核验通过。** 无实测值与声明值偏离，无聚合字段不一致，无覆盖关系缺口，无身份复用，
验收记录无夸大或失实声明。CHK-012 的最小闭合条件（固定 HEAD 五场成功 + 内容寻址原始集合 + 严格聚合成功）
由本证据链满足。

完整逐项核验证据见核验子智能体输出（2026-09-12，本记录为其持久化摘要）。
