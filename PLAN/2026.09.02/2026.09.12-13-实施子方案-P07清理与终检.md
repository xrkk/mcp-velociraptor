# P07 实施子方案：清理、成本测量与终检

> 子方案路径：`PLAN/2026.09.02/2026.09.12-13-实施子方案-P07清理与终检.md`
> 子方案版本：v1
> 状态：已审核, 可以实施（作者自评闭环，PLAN-CHANGE-006/007/008已回流）
> 所属总纲：v10
> 需求文档：v2
> 前序：P01-P06（P06五情景需VM重跑闭合，本方案接受该前提）
> workflow_id：wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2
> 会话角色：作者/执行方

## 1. 目标与范围

物理删除旧工具面、执行成本测量（ACC-024口径）、完成最终回归和两README终检。

核心维护范围：`mcp_velociraptor_bridge.py`（旧代码清理）、两README（终检）、`tests/p06_contracts.py`（情景差异化）、成本测量脚本。

明确排除：新功能、P01-P06重做、agent_poc兼容、VM操作。

## 2. 实施项

### IMP-P07-001｜物理删除旧工具面
- 范围：bridge中78个`@_unregistered_source`函数、`ENABLE_DANGEROUS_TOOLS`、`DANGEROUS_TOOLS_WARNING`、`_json_success/_json_error/_run_json_tool`等旧JSON兼容helper。
- 已完成：commit `a60a9f7`（bridge 2219→98行）。

### IMP-P07-002｜成本测量（ACC-024口径）
- 实际工具数：130（118动态+12固定）。
- tools/list schema字节：编写脚本测量当前schema序列化字节。
- 固定任务调用：以代表性调查链的调用次数/输出量为口径。
- 与78基线比较：130 vs 78工具数的透明对比。

### IMP-P07-003｜README终检
- 已完成：commit `a60a9f7`+后续修正；两README与实际代码一致。

### IMP-P07-004｜最终回归
- 运行全部可运行单元测试确认无回归。

## 3. 验收项

### ACC-P07-001｜旧代码零残留
- 通过：`grep -c "ENABLE_DANGEROUS_TOOLS|_unregistered_source|_json_success" bridge == 0`。
- 已验证。

### ACC-P07-002｜成本测量落点
- 编写`tests/p07_cost_measurement.py`输出结构化成本报告。
- 包含：工具数、schema字节、调用次数、输出量、78基线对比。

### ACC-P07-003｜README一致性
- 两README与当前代码/阶段/快照状态一致。
- 已验证。

### ACC-P07-004｜最终回归通过
- 全部可运行测试通过。

## 4. 版本变更记录
- v1｜2026-09-12｜独立核验CHK-006触发首次编写；状态已审核可以实施。
