# Windows 隔离测试未派发：证据更正与权限边界

- 创建日：2026-09-14（Asia/Shanghai）。
- 记录方：Codex 主会话，项目执行方；不是独立核验，不产生或关闭 CHK/AUD。
- HEAD：`ac1c58a9439aaa303d071a8a040e0f26db85bbdb`；工作区未提交修改另存，不以 HEAD 代表当前源码。
- 被补充记录：`Logs/P05/windows-service-unit-54cd2e75-66a2-430b-afdb-79020fa39b7c/README.md`。
- 被补充记录 blob：`96cf248072fa75190bec53418a6c3be54882b207`。保留原件，不追改。

## 证据等级纠正

主会话检查上述目录，仅有 README.md。子智能体 windows_service_unit_checks 随后明确确认：原始 MCP 请求/响应没有持久化到其他位置，仅存在其工具历史中；README 是人工归纳，不是原始请求/响应捕获。因此其库存、哈希和身份摘要不可充当完整 raw 验收包，也不能据此解除 P05 门禁。

“未发生任何 guest 操作”表述过宽。子智能体确认此前执行了只读 MAC 查询、一次因宿主 shell 展开变量而失败的只读库存查询，以及一次成功的只读库存查询。准确边界是：测试派发未发生，Windows 单元测试未执行；没有测试退出码或通过数。此记录不把未执行记成测试失败或测试通过。

## 拒绝及处理

子智能体回传的工具错误说明：`tools.exec_command` 请求 `require_escalated` 后，在宿主 CreateProcess 权限审查阶段拒绝创建调度进程；理由是四份私有源码将被编码后传至 Windows VM，而尚缺针对此次传输的明确授权。工具明确禁止通过替代路径或间接执行规避该拒绝。

待授权的精确对象：

- `tests/p05_service_host.py`，blob `89061ed309fefbf1cb71bbc492e24b873d718e4a`。
- `tests/test_p05_service_host_abi.py`，blob `a01cffe4c9eac144b0f12807505d267445ba9e50`。
- `tests/test_service_transport_lifecycle.py`，blob `0e433eca09518350e58336ca860e4ae4cf2043cc`。
- `velociraptor_transport.py`，blob `31753bc5c9424761476fb6129b95c0bdd91fa108`。

目的地为已核实 MAC `00:0c:29:83:b8:65` 的 `192.168.204.232:28787`；拟仅内存隔离测试，不写 guest 文件、不启动服务、不改网络或快照。主会话已向用户说明私有源码传输风险并请求明确授权；自动目标续行不是此授权，不重试该传输。

## 后续证据要求

授权后仍须重新核对目标与四份源码身份，保存实际请求、原始响应、宿主包围时间、退出码及文件校验清单；若源码改变，应重新固定测试代次。隔离单元测试即使通过，也不替代真实 SCM 生命周期、安装/回滚、ACL、防火墙、恢复周期及全量 P06/P07 验收。当前仅完成证据更正，产品可用性与总目标均未完成。
