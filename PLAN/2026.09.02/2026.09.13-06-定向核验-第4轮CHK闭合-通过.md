# 定向核验记录：第 4 轮 CHK-019~023 闭合（通过）

> 会话角色：独立核验子智能体（acceptance-feature-verifier）
> 核验模式：定向复验（修复+核验循环第 5 轮）
> 核验日期：2026-09-13（Asia/Shanghai）
> 结论：**定向核验通过**（COMPLETE）

## 核验范围

第 4 轮独立核验（2026.09.13-01）五项 CHK 的最小闭合条件，对象绑定 HEAD `ac1c58a`。全程只读；聚合以 import 方式重算；包哈希独立重算不复用聚合器实现。

## 结果摘要

1. **CHK-019 闭合成立**：23 行账本（attempt 1..23 连续、状态合法、选定 18/19/21/22/23）；selection v2 对 18 个未选 attempt 逐项非空拒绝理由；强化聚合器（`5c52f32`）对全部 23 行逐行重算报告字节+包字节并强制裁决完整；import 重算与磁盘 aggregate 全字段相等（650 关系/五异会话/恢复/runner/0 错误）；抽验 3 个 attempt 包哈希全部 MATCH；旧证据根完整保留。
2. **CHK-020 闭合成立**：资格证据 12 步/99 调用、Snapshot186 恢复绑定、逐步七项水位、static_required=2×(4GiB+64MiB)+4GiB 精确吻合、逐调用前 4GiB 门禁在代码且 success 证明未触发；五场选定 runner 启动全部晚于资格结束；资格前 5 场按"资格先行"降 superseded。
3. **CHK-021 闭合成立**：上游固定任务基线实测（RecycleBin 2 调用/647B/5.019s，SHA `008e4921…`）；成本报告（`2abff0d0…`）含 AUD-004 映射与当前侧五场全量数字（独立复算精确吻合：4188 调用/1708 重试/2,587,611B/19,649,604ms）；旧"none"披露文本零命中；方案正文为"实测取得可比基线"口径且旧读法零命中。
4. **CHK-022 闭合成立**：审核处理（-02）→复审（-03，有条件通过）→复审闭合（-04，审计通过）链完整，指纹互引吻合；13 模块表与仓库一一对应零差集。
5. **CHK-023 闭合成立**：HEAD=ac1c58a、六个对象 SHA 全部实测吻合、tracked 零修改。
6. **非同构维持**：五场调用数互异、十对序列零相同、抽样对 734 位置仅 263 同工具。

## 不符项（叙述层，已由声明方更正）

两处记录叙述计数错误（账本状态分类 5/13→实际 7/11/5；选定五场调用数 1197/944→实际 1168/855，系沿用被取代纪元数字）——核验方裁决不构成闭合失败（闭合条件不依赖这些计数值，SHA 绑定证据完整自洽）；**声明方已在 -05 中更正**（更正后 SHA-256 前缀 `98c5e995`）。

## 环境观察（不立新 CHK，留待后续）

资格 runner 的 triage/fixture/download 三步无逐调用前门（有 before/after 水位与结尾储备检查）；P06 方案 3.3"场景每个大步骤前增量重算余量"未见场景 runner 代码实现（本轮以场前定向冒烟纪律替代）。属既有设计观察，非本轮整改引入。

## 总结论

**定向核验通过。** 修复+定向核验循环（最多 5 轮）以本轮通过收口：第 4 轮全部 5 项 CHK 实质闭合且证据可独立复算。
