# P05 实施子方案：测试数据、依赖与情景基础设施

> 正式作者版本：v18（PC021；PC020、PLAN-CHANGE-014与PC017/018/019继续有效；待主方完整保真自评）
>
> **当前状态**：作者稿已同步／待主方完整保真自评／实施门关闭；现场、canonical、VM及审核门保持关闭。
>
> **当前九对象组**：`CURRENT-PC021-R04`；需求`v9`、总纲`v22`、P01`v4`、P02`v7`、P03`v3`、P04`v11`、P05`v18`、P06`v19`、P07`v17`。正式身份由`PLAN/2026.09.21/current-normative-inputs-pc021-r04.json`描述；`CURRENT-PC020-R01`保留为历史父组。

## 0.PC021 activation签发、下载原件与epoch8契约（v18当前条款）

本节逐条承接已由作者采纳的PC021-CONTRACT R03有限契约；只把候选身份和旧插入锚点改写为正式作者稿身份，不改变既定机制。下文PC020父条款继续提供不冲突的恢复/阶段规则。

## 0.PC021.1 术语与边界

术语：

- `C7`：进入激活前实际读取并验证通过的 epoch 7 canonical 原件字节；`H7 = SHA-256(C7)`。
- `A`：`activation-189/<activation_uuid>/`；`R = A/activation-evidence.json`；`S = A/issuance-receipt.json`。
- `HR/HS`：`R/S` 实际字节 SHA-256。
- `N8`：从已验证 `C7` 和激活输出确定性构造的 epoch 8 canonical；`H8 = SHA-256(N8)`。
- “内容图有效”：当前字节、路径、哈希与 schema 闭合；不证明过去某次 fsync 成功。
- “操作可继续”：内容图有效，且同一进程、同一 controller 持有未消费的私有一次性完成资格。

本文只规定普通进程正确性、崩溃接续和可审计原件；不声称抵抗恶意同进程伪造、内核/介质欺骗、PKI 攻击或可信计算基攻击。

### 0.PC021.2 activation issuer、固定 receipt 与一次性完成资格

#### 0.PC021.2.1 保持既有 activation root，增加同层 receipt

`R` 保持 P05 v17 的 exact keys：

`schema_version/kind/workflow_id/candidate/checkpoint_marker/migration_evidence/preparation_evidence/creation_metadata/initial/candidate_cycles/source_inputs/implementation_sources/issued_at`

本节不替换其 schema，不给 root 添加 receipt、future canonical 或 future transition 引用。

`S` 必须且只能有下列 **13 个根键**：

`schema_version/kind/workflow_id/issuance_id/operation/source/epoch7/cycle2/activation_root/issued_at/events/status/error`

精确嵌套和值：

- `schema_version=1`；`kind="snapshot189-activation-issuance-receipt-v1"`；`workflow_id` 等于固定 P05 workflow；`issuance_id` 是等于 `<activation_uuid>` 的 canonical lowercase UUID；
- `operation="P05_ACTIVATION_ISSUE"`；`status="ROOT_VALIDATED"`；`error=null`；
- `source` exact keys 为 `source_inputs_sha256/implementation_sources_sha256`，分别是 `R` 中完整对应数组的 P05 canonical JSON SHA-256；摘要不替代逐个 Source 原件验证；
- `epoch7` exact keys 为 `canonical_sha256/schema_version/epoch/phase/active_snapshot`；值分别为 `H7`、整数 6、整数 7、`PREPARATION_BASELINE`、`Snapshot 187-固定IP+WindowsMCP开机自启`；
- `cycle2` exact keys 为 `restore_attempt_id/run_id/report/package_manifest`；两 ID 等于 `R.candidate_cycles[1]`，两个 Ref 与该对象对应 Ref 字节等同；
- `activation_root` 是 P05 三键 Ref，path 固定 `activation-evidence.json`，size/SHA 绑定 `R` 实际字节；
- `issued_at` 与 `R.issued_at` 和 events 第 2 项 `at` 字节等同；
- `events` 恰有六项，每项 exact keys `sequence/event/at`；sequence 依次为 1–6，event 依次为 `PRE_ISSUE_GRAPH_VALIDATED`、`ISSUED_AT_SAMPLED`、`ROOT_WRITTEN`、`ROOT_FILE_FSYNCED`、`ROOT_DIRECTORY_FSYNCED`、`ROOT_READBACK_VALIDATED`；`at` 是同一 issuer 在对应动作成功后取得的 UTC RFC3339 `Z` 观察值。

所有 receipt Ref 从 `A` 解析。绝对/盘符/空/点/dotdot/反斜杠/逃逸/重复/链接或 reparse/特殊文件/size-hash 不符均失败。sequence 与同一 controller 的控制流证明顺序，不比较 guest、host、VMware 或 writer 的墙钟数值。

`ROOT_VALIDATED` 只陈述该调用观察到的内容图和步骤，不是可由磁盘重建的 writer 授权，也不能单凭 receipt 当前字节证明历史 fsync 成功。

#### 0.PC021.2.2 非循环 issuer 算法

issuer 是一个生产操作，不提供公开 `skip_receipt/prevalidated/fixture/unsafe` 或等效 gate：

1. 要求 `A` 及其祖先为批准的普通非 reparse 目录；以 no-follow/lexists 证明 `R/S` 均不存在；生产平台若不能提供文件 flush 与父目录持久性（含 Windows 目录持久性）则在创建 root 前 fail closed，测试 seam 不得把 mock 当成生产成功。
2. 一次读取 `C7`，验证 schema6/epoch7 固定状态并保存 `H7`；冻结除 `issued_at` 外的 root 输入；私有 pre-issue predicate 完整验证 preparation/migration/creation、initial/cycle1/cycle2、三 Phase manifest、runtime/business predicate 和所有 Source；不接受 caller “已验证”断言且不写文件。
3. 步骤 2 成功后记录 event 1；实际取 issuer UTC 一次，event 2 与 `R.issued_at` 共用该值；禁止使用预选值、mtime、validator/writer/future 时间或 caller 值。
4. create-if-absent 创建 `R`，完整写、flush、file-fsync、close、parent-directory-fsync；按动作成功记录 events 3–5；no-follow 重开，逐字节等同并对保留的 `C7` 重跑完整 immutable-root verifier，成功后记录 event 6。
5. 用六事件和最终 root Ref 构造 `S`；create-if-absent 完整写、flush、file-fsync、close、parent-directory-fsync、no-follow 重开并逐字节等同；运行 public complete-bundle verifier，重新验证所有原件及绑定。
6. 只有步骤 1–5 全部成功后，同一 controller 才铸造 §2.5 私有一次性能力并直接调用 §4 writer；这才是“操作可继续”。

private pre-issue predicate 解决 receipt 尚不存在的 bootstrap；public complete verifier 从不把该中间态判为 issued success。root 先于向后绑定它的 receipt，但二者之间不存在成功窗口。

#### 0.PC021.2.3 publication、并发与失败保全

只有赢得 `R` exclusive-create 的进程可尝试 `S`。并发 loser 不写 receipt、不改变 winner。任一固定目标已经存在（即使字节相同）、临时别名、意外普通成员、链接/reparse 或类型不确定都在新发布前阻断，禁止自动复用、覆盖、改名隔离、修补或重试。

输入在 root 前漂移：不创建 final。root 任一步失败：原位保全已有 root，receipt 缺失，禁止 epoch8。receipt 任一写入/持久化/读回/终验失败：原位保全 root 及可能存在的 receipt，报告失败并禁止 epoch8。完整可解析 receipt 仍可能来自 fsync 返回失败的现场，public content verifier 不得反推历史返回值。

`implementation_sources` 必须在 pre-issue 前纳入实际 issuer、complete verifier、download preserver 及其直接 helper/tests；root、receipt、live report、下载字节、输出 manifest/result、future transition 都是输出，不得作为 implementation source 形成循环。PC021 作者决定在正式执行冻结时进入 `source_inputs`；正式采纳必须生成新的 current-manifest identity，历史 `CURRENT-PC020-R01` 原字节不改。本节是待主方完整保真自评的正式作者稿；历史 candidate、自检和 result 仅为过程证据，不替代本节正式正文。

#### 0.PC021.2.4 public 内容 verifier

一次调用必须从 `A` 验证固定同层 `R/S`、既有 root/Phase/Source/epoch7 predicate、receipt exact keys/事件和全部 cross-binding，并执行 §3 的 package 规则。它可返回 `content_graph_valid=true`，但不得返回 `operationally_issuable=true`、caller bool 授权、可序列化 token 或从磁盘恢复的许可；它明确不能判断历史 receipt fsync 是否曾失败。

#### 0.PC021.2.5 私有一次性签发完成能力（PC21-001）

能力是控制器私有、不可序列化、不可复制、不可由调用参数/环境变量/配置/文件/receipt 重建的进程内对象。概念字段固定为：

- `operation = "P05_ACTIVATION_ISSUE"`；
- `workflow_id`；
- `issuance_id`；
- `epoch7_sha256 = H7`；
- `activation_root_sha256 = HR`；
- `issuance_receipt_sha256 = HS`；
- 生命周期状态 `UNSPENT` 或 `SPENT`。

同一 controller 在调用 writer 前必须重新读取 `C7`、`R`、`S` 的实际字节，分别计算 `H7`、`HR`、`HS` 并与能力逐项相等。能力消费与“是否已消费”的判定必须在该 controller 的单一临界区内原子完成，且发生在创建 `.next`、intent、receipt 或其他 writer 副作用之前。

失败、进程崩溃、重启、能力丢失或能力已消费时：

- 不得根据磁盘上的完整 `R`/`S` 自动继续；
- 不得回退到旧 activation bundle 或旧 receipt；
- 不得重新铸造能力；
- 保留当前现场，停止并提交作者裁决；不得由执行方自动重签发、另启或更换固定 workflow/issuance。

能力已被某一 writer 调用消费后，即使该调用在首个文件副作用前失败，也不得复用。两个 writer 并发争用同一能力时，必须恰有一个成功消费；另一个在任何 writer 副作用前失败。

#### 0.PC021.2.6 必须具备的静态失败反例

后续实现至少应以可控 I/O seam 验证：

1. `S` 字节完整可解析，但 receipt fsync 注入失败：内容 verifier 可通过，控制器不铸造能力，writer 零副作用；
2. receipt 完整终验后，能力已经铸造但在 writer 开始/签发操作最终返回前崩溃：重启后内容仍可能有效，但进程内能力丢失，禁止自动接续；
3. 一个 writer 已消费能力后再次使用：第二次在副作用前拒绝；
4. 两个 writer 同时使用同一能力：仅一个消费成功，另一个在副作用前拒绝。

这些反例也必须断言：内容 verifier 不谎称能够判断历史 receipt fsync 成败；失败现场不接纳旧 bundle fallback。已 `COMMITTED` 的 epoch8 下游依据 transition 原件，不要求跨进程保留能力。

### 0.PC021.3 下载原件与 Phase manifest

#### 0.PC021.3.1 固定成员身份和编码

每个 Phase 中每次成功 `download_flow_file` 的包成员固定为：

`downloads/<flow_key>/<file_id>/content.bin`

其中 `flow_key = lowercase_hex(SHA-256(flow_id 严格 UTF-8 编码一次))`。`flow_id` 必须是非空 Unicode scalar-value 字符串，不做 normalize、case-fold、strip 或 path-decode；无法编码（含 unpaired surrogate）即失败。`file_id` 必须恰为 64 个小写 ASCII hex，并等于紧邻的完整 `list_flow_files` 返回的 P04 canonical logical-file identity。raw Flow ID、backend path、`original_path`、local filename 和 caller text 均不得形成路径段。

member 相对该 Phase 根而不是 activation 根。不同 Phase 即使得到相同派生文本也属于不同根；§5 要求九个 runtime Flow ID 跨 Phase 全部不同，不能靠目录隔离掩盖复用。

#### 0.PC021.3.2 真实产品字段与不可变 SDK/report 绑定（PC21-004）

最终 `report.json` 原字节包含每次调用的 `arguments`、解码后的 `structured` 以及原 MCP `mcp_result.structuredContent`。不得虚构第二份 raw-message、列表行字段或产品字段。

成功 `list_flow_files` 的真实结构必须按 `velociraptor_mcp_core.py` 解释：

- 顶层 exact keys 为 `operation/status/warnings/data/truncated`；列表是 `data`，不是 `files`；`operation="list_flow_files"`、`status="success"`，且完整列表要求 `truncated=false`；
- 每个 `data` row exact keys 为 `file_id/original_path/file_size/uploaded_size/accessor`；row **没有** `flow_id` 或 `sha256`；
- Flow 身份来自该 report call 的 `arguments.flow_id`。list call 的 `structured` 与 `mcp_result.structuredContent` 必须字节语义等同，且唯一选中的 row 由 `file_id` 确定。

成功 `download_flow_file` 的真实 `DownloadResult` exact keys 为 `operation/status/warnings/flow_id/file_id/local_path/size/sha256/original_path`。其 call `arguments` 必须精确含相同的 `flow_id/file_id`，且 `structured` 与 `mcp_result.structuredContent` 必须相等。list/download/package 映射固定为：

| 身份/度量 | list call / row | download call / result | 包内 member |
|---|---|---|---|
| Flow | `list.arguments.flow_id` | `download.arguments.flow_id == DownloadResult.flow_id` | 其严格 UTF-8 SHA-256 构成 `flow_key` |
| file | row `file_id` | `download.arguments.file_id == DownloadResult.file_id` | 形成 `<file_id>` 路径段 |
| 原路径 | row `original_path` | `DownloadResult.original_path`，两者相等 | 只作 source-time assertion，不作包路径 |
| 逻辑长度 | row `file_size` | `DownloadResult.size`，两者相等 | 实际 `content.bin` 字节数相等 |
| 存储长度 | row `uploaded_size` | 无对应 DownloadResult 字段；非负、保留原值，稀疏文件可与逻辑长度不同 | 不作为 member size，不强制等于 `size` |
| accessor | row `accessor` | 无对应 DownloadResult 字段 | 保留在不可变 list 原件，不发明副本字段 |
| 内容哈希 | row 无此字段 | `DownloadResult.sha256` | 实际 `content.bin` SHA-256 相等 |
| 产品落点 | row 无此字段 | `DownloadResult.local_path` | 仅在保全复制阶段按 §3.3 核源；搬运后不解引用 |

所有 size 字段均为非负整数且不接受 bool；哈希为 64 小写 hex；`file_id` 使用 §3.1 规则。`data` 缺失/非数组/为空、`truncated=true`、身份重复、字段非法或上述映射不一致均失败。禁止按目录扫描、mtime、glob、大小或“最新文件”猜测。

### 3.2.1 产品下载、报告冻结与包复制的因果顺序（PC21-005）

最终报告必须包含真实 download 响应，因此顺序只能是：

1. 在产品入口执行 `list_flow_files`，再执行实际 `download_flow_file`；产品先把下载结果发布到其可信 download root，并返回 `DownloadResult`；
2. scenario/report producer 把 list/download 的真实 `arguments`、`structured`、`mcp_result` 和 call 顺序纳入同一最终报告；所有本 Phase 调用及既有 cross-step 检查结束后，以现有 `report.json` 契约一次冻结并持久化最终原字节；
3. package preserver 只读该最终 `report.json`，按本节唯一定位 list row 和 download result，再从 §3.3 的可信产品落点执行包内保全复制；不得修改报告或重新调用产品来改选成员；
4. 两个 fixture 的包内复制均完成并读回验证后，才生成 Phase manifest；manifest 随后进入 activation root/receipt 图。

本顺序不新增 pre-download log、sidecar、临时报告或第二套日志契约。报告持久化失败则不开始包内复制；产品下载失败则不存在成功 download response，不能冻结为成功报告；报告冻结后 source 漂移或复制失败按 §3.3 保全并阻止 manifest/root/receipt。

#### 0.PC021.3.3 可信源准入与独占复制

package preserver 只在 §3.2.1 的最终报告冻结后开始。它从该 Phase 已接纳的同一 server-instance evidence/configuration 取得可信 P04 `VELOCIRAPTOR_DOWNLOAD_ROOT`；它不是 caller input，也不从 `local_path` 推断。唯一合法源是 `<trusted_root>/<flow_key>/<file_id>/content.bin`。response `local_path` 必须在 Windows native absolute-path 语义下精确指向它；alternate spelling、dot/dotdot、device/UNC、alias、short-name、大小写歧义或 root 外路径全部拒绝。

trusted root、每个祖先及子目录都必须为普通非 reparse 目录，source 是普通非 reparse 文件。no-follow 打开 source 后记录同一 handle 的 stable volume/file identity、logical size 和 change metadata，从该 handle 有界分块读取并计算 count/SHA，之后再核同一 handle 和 path-to-handle identity。identity、metadata、path binding 或内容偏差均为 source drift。零字节仅在首读 EOF、count=0、SHA 为 empty SHA-256 时合法。

destination Phase root 及 `downloads/flow_key/file_id` 每段均在 Phase 内且为普通非 reparse 目录。package preserver 在同目录 exclusive-create 唯一自有 `.part`，流式复制、flush/file-fsync/close，要求 count/SHA 与不可变 DownloadResult 的 `size/sha256` 相同；重开逐字节验证后以 create-if-absent 发布 `content.bin`，绝不覆盖或接受既有 target（即使字节相同）。发布后重验目标、以生产支持的 primitive 同步包含目录，再安全清理自有 `.part`。

不支持 durability、target collision、source drift、short/extra read、size/hash mismatch、I/O fault、unsafe path 或 cleanup 不确定均 fail closed。失败保留已发布成员和任何不确定自有残留，阻止 Phase sealing；只在重验安全 parent 与自有 identity 后清理精确 owned part，绝不删除 completed member、其他 attempt、外部 sentinel 或目录。

#### 0.PC021.3.4 搬运后验证

validator 仅从不可变 report 的 Flow/file identity 重建 member，相对当前 Phase 根读取普通非 reparse 文件，并核验两个 DownloadResult view 的 `size/sha256`、list row 的 `file_size/file_id/original_path` 及调用参数；不得从 list row 读取不存在的 SHA。原 `local_path` 只作为不可变 source-time assertion 保留；搬运后不解析、不打开、不改写，也不要求旧路径存在。报告、Ref 和成员原字节整体搬运仍保持哈希稳定。

不增加 copy sidecar 或产品 response 字段。不可变 response/list identity、确定性 member、实际 member 字节和 exact Phase manifest 共同构成证据，任何一个都不替代实际读取。

#### 0.PC021.3.5 Phase manifest 的唯一坐标系（PC21-003）

对每个 Phase：

- Phase 根目录记为 `T`；
- Phase manifest 固定为 `M = T/package-manifest.json`；
- Phase 对象中的 `package_manifest.path` 是**相对 activation 根 `A`** 的规范 POSIX 路径，且必须精确指向 `M`；
- `M.members[*].path` 是**相对 Phase 根 `T`** 的规范 POSIX 路径；解析成员时只能使用 `T / member.path`，不得使用 `A` 或进程 cwd；
- `phase_prefix` 是 `T` 相对 `A` 的规范 POSIX 路径，即 `package_manifest.path` 去掉末尾 `/package-manifest.json` 后的值。

上述两种坐标不得混用。解析后路径必须仍位于批准的根内，并按组件拒绝链接、junction/reparse point 和其他特殊文件。

#### 0.PC021.3.6 Phase manifest 精确 schema

`M` 使用确定性 UTF-8 JSON 编码。根对象必须且只能有 P05 §2 的 **5 键**：

1. `schema_version`：整数 `1`；
2. `kind`：固定值 `"pc021-p05-phase-manifest-v1"`；
3. `workflow_id`：与 activation root 相同；
4. `created_at`：manifest 实际签发时取得的有效 UTC `Z` 时间；
5. `members`：成员数组。

`created_at` 必须在所有成员写入、关闭并逐字节读回后、activation root 签发前取得；该要求是同一控制器的因果步骤要求，不要求与别的主机墙钟作数值比较。

每个 member 必须且只能有 P05 §2 的 **4 键**：

1. `path`：相对 `T` 的规范 POSIX 路径；
2. `size`：实际普通文件字节数；
3. `sha256`：实际普通文件字节 SHA-256；
4. `origin`：如下确定的批准证据相对来源选择器。

`origin` 的构造规则只有两种：

- 对确定性下载的最终内容成员：`<report_ref_path>#/calls/<i>/structured`。`report_ref_path` 是冻结 SDK 报告相对 `A` 的规范 POSIX 路径；`i` 是该报告 `calls` 数组中唯一产生此逻辑文件记录的十进制零基索引；该 selector 必须反向验证到 §3.2 的唯一 file ID、size 和 sha256；
- 对其余普通成员：`<phase_prefix>/<member.path>`，即该原件相对 `A` 的规范 POSIX 路径。

`origin` 不替代 `path/size/sha256` 的身份验证，也不得包含主机绝对路径、URI、反斜杠、`..` 或未规范化片段。

#### 0.PC021.3.7 完整成员算法

manifest 生成器必须：

1. 在停止所有 Phase producer 后递归枚举 `T`；
2. 拒绝任何链接、junction/reparse point、特殊文件、未关闭临时文件或 `.part`；
3. 令 `F` 为 `T` 下全部普通文件的集合；
4. 令 `E = { M }`，且 **只有** `M` 可以排除；activation root 位于 `T` 外，因此不在 `F`，也不得被额外列入排除表；
5. 对 `F - E` 的每个文件从实际字节计算 `path/size/sha256/origin`；
6. 拒绝重复路径、大小写折叠碰撞、Unicode 规范化碰撞和未知额外文件；
7. 按 `path` 的 UTF-8 字节升序输出 members；
8. 写入并持久化 `M`，随后读回，重新枚举并验证 `F - E` 与 members 为 exact set。

集合判定必须同时检查数量、唯一性和 exact set；不得依赖目录枚举顺序，也不得只验证 manifest 所列子集。

#### 0.PC021.3.8 历史兼容边界

`kind = "pc021-p05-phase-manifest-v1"` 是 **PC021 当前作者稿定义的新类型**，不是当前实现已有类型。当前 R03 合成 fixture 的“两键根（`schema_version/members`）+ 三键 member（`path/size/sha256`）”是 legacy 形状。

- 历史包和旧 fixture 字节不改写；
- legacy 形状可以按既有历史迁移规则作为历史原件保全，但不得被新 PC021 success verifier 当作本节 manifest；
- 只有PC021后续实现门开启且按本作者稿新创建的包才使用本节schema；
- 合成 fixture、live producer、P05/P06/P07 consumer 必须配套更新，不能靠 verifier 双义接受两种形状来伪造新成功。

### 0.PC021.4 epoch 8 writer、transition v2 原件与 P06/P07 接续

#### 0.PC021.4.1 writer 准入

epoch 8 writer 的唯一自动准入是 §2.5 的未消费能力。writer 必须在单一临界区内完成：

1. 核验能力类型、状态和全部绑定；
2. 从实际 `C7`、`R`、`S` 字节复算 `H7/HR/HS`；
3. 核验当前 canonical 仍精确等于 `C7`；
4. 原子地把能力从 `UNSPENT` 置为 `SPENT`；
5. 然后才允许任何 writer 文件副作用。

调用方布尔、fixture 标志、磁盘 token、完整 `R/S` 或旧 bundle 均不能替代能力。

#### 0.PC021.4.2 transition v2 固定位置与键集合（PC21-002）

本次 writer 的原件固定在 `A`：

- intent：`A/epoch8-transition-intent.json`；
- receipt：`A/epoch8-transition-receipt.json`。

两者均是 `R` 签发之后的 transition 输出，不属于 `R` 的 exact schema 或任何预签发 Phase manifest，不得反向改写 `R`。P06 继续使用既有 `activation_evidence` restore record：若其 restore attempt 为 `<restore_attempt_id>`，归档落点必须分别为 `restore/<restore_attempt_id>/activation/epoch8-transition-intent.json` 和 `restore/<restore_attempt_id>/activation/epoch8-transition-receipt.json`，并由既有 outer manifest 逐字节覆盖；无需、也禁止增加第九种 restore record kind。

v2 intent 根对象必须且只能有 **10 键**：

1. `schema_version`：整数 `2`；
2. `kind`：`"pc020-canonical-transition-intent-v2"`；
3. `workflow_id`；
4. `transition_id`：UUID；
5. `from_sha256`：`H7`；
6. `to_sha256`：`H8`；
7. `next_sha256`：`.next` 完整持久化并读回后的实际哈希，必须等于 `H8`；
8. `activation_root_sha256`：`HR`；
9. `issuance_receipt_sha256`：`HS`；
10. `created_at`：intent 实际创建时取得的 UTC `Z` 时间。

v2 receipt 根对象必须且只能有 **15 键**：

1. `schema_version`：整数 `2`；
2. `kind`：`"pc020-canonical-transition-receipt-v2"`；
3. `workflow_id`；
4. `transition_id`；
5. `from_sha256`；
6. `to_sha256`；
7. `next_sha256`；
8. `activation_root_sha256`；
9. `issuance_receipt_sha256`；
10. `started_at`；
11. `replaced_at`；
12. `directory_fsynced_at`；
13. `readback_at`；
14. `status`；
15. `error`。

所有非 null 哈希均为 64 位小写十六进制。receipt 的身份/哈希字段必须与 intent（若 intent 已创建）及实际原件相等。

receipt 状态与 nullability：

| status | `to_sha256` | `next_sha256` | `replaced_at` | `directory_fsynced_at` | `readback_at` | `error` |
|---|---|---|---|---|---|---|
| `COMMITTED` | `H8` | `H8` | UTC Z | UTC Z | UTC Z | `null` |
| `FAILED_BEFORE_REPLACE` | `H8` | 在 `.next` 验证前失败为 `null`，否则 `H8` | `null` | `null` | `null` | 非空字符串 |
| `TRANSITION_INDETERMINATE` | `H8` | `H8` | 仅实际观察到 replace 成功时为 UTC Z，否则 `null` | 仅实际观察到目录 fsync 成功时为 UTC Z，否则 `null` | 仅实际完成 canonical 读回时为 UTC Z，否则 `null` | 非空字符串 |

未知 status、成功状态含 error、失败状态无 error、事件未观察却有时间、已观察却缺时间，均失败。`COMMITTED` 还要求实际 canonical 字节哈希为 `H8`。

#### 0.PC021.4.3 不得改变的 writer 顺序

PC021 v2 **不改变**现行 P05 §5 的 `.next → intent → compare → replace` 顺序：

1. 读取并验证 current canonical 为 `C7/H7`；
2. 证明目标 `.next` 不存在；
3. 验证 preparation/migration/activation 内容与能力绑定；
4. 确定性构造 `N8/H8`；
5. exclusive-create `.next`，完整写入、flush/fsync/close，对父目录 fsync；重新打开逐字节读回并验证 `next_sha256 = H8`；
6. **在 `.next` 完整持久化和读回之后**，以 exclusive-create 写入并持久化 v2 intent，读回验证其绑定；
7. intent 成功后立即重新读取 canonical；只有实际字节仍精确等于 `C7/H7` 才可继续；
8. 原子 `replace(.next, canonical)`；
9. 对 canonical 父目录 fsync，重新打开 canonical 逐字节读回并验证 `H8`；
10. 写入、持久化并读回 v2 receipt；只有 `COMMITTED` receipt 完整验证后调用才报告成功。

canonical 在步骤 7 漂移时不得 replace，也不得自动移动/隔离 `.next`；失败现场原位保全。任何失败不自动重试，不复用已消费能力。

现有 epoch 6→7 writer 的 v1 intent/receipt schema 和行为不因本节被解释为 v2；它仍服务原代次。v2 仅适用于 PC021 epoch 7→8 transition。

#### 0.PC021.4.4 P06/P07 接续

一旦 epoch 8 已经 `COMMITTED`，进程内能力不再是 P06/P07 的输入。下游必须读取并验证：

- current canonical 实际字节与 v2 `to_sha256`；
- v2 intent 和 `COMMITTED` v2 receipt 的 exact schema、相互绑定以及 `H7/H8/HR/HS`；
- `R`、`S` 和完整 activation/Phase 内容图；
- restore 包 outer manifest 对这些普通文件的逐字节 size/hash 覆盖。

P06 使用既有 `activation_evidence` 递归携带 `A`，因此 transition v2 两原件在 `restore/<restore_attempt_id>/activation/` 固定路径进入既有 restore 包；不得增加第九种 restore record kind。P07 按同一归档路径及哈希验证。若 v2 receipt 缺失、非 `COMMITTED`、字段不一致或 canonical 不匹配，下游停止并保全，不以进程内 token、旧 bundle 或推测补齐。

### 0.PC021.5 每 Phase 的精确三 Flow 业务范围

`P05_REPAIR_INITIAL`、candidate cycle 1、candidate cycle 2 各自拥有恰好三个互异 collection Flow，且三个 Phase 的九个 Flow ID 全部互异：

1. 一个真实 `collect_forensic_triage` Flow，使用完整 `_BasicCollection`、2400 秒和已采纳的 4 GiB 单请求预算；
2. 一个固定 ASCII fixture `collect_file` Flow；
3. 一个固定 UTF-8 fixture `collect_file` Flow。

Triage Flow 必须真实到达 `FINISHED`；原 start response 与后续 terminal status 分开保留；枚举实际 declared result sources，并把每条 result chain 读到 terminal pagination；执行一次完整 `list_flow_files` 并保留列表。某个 declared source 可依现行契约为空，但 source set 缺失/未知、result/list 未执行、pagination 截断/未读完、ERROR/cancel/timeout/over-budget/critical error、存在 rows/uploads 却无闭合终页或 aggregate 无解释地全空均非成功。只有已评审场景明确允许正常空，且 source/list 已穷尽，zero-row/zero-file 才可通过。

两个 fixture Flow 各自真实到达 `FINISHED`，完成 file list，按 P04 file identity 唯一选择冻结的逻辑原件（不得猜 filename），实际调用一次 `download_flow_file` 并通过 §3。ASCII/UTF-8 payload 的 size/SHA 必须等于冻结 fixture 规范。list-only、fabricated call、改写 SDK result、缺包内 member 或共享 Flow 均失败。

Triage result 和完整 file list 保留，但本节不下载任何 Triage upload；两个 fixture download 是每 Phase 的 exact download-original set。不得增加 bulk-Triage copy、选择策略、数量、empty-list fallback 或下载预算。若后续发现正式来源明确要求下载 Triage-produced file，本节失效并返回作者对 target/count/empty/resource 重新裁决，不得擅自取消或扩张。

正常即时完成状态只能来自冻结报告内产品结构化终态并绑定 flow/artifact/client/result；HTTP 成功、快速返回或空列表不能自行解释为完成。等待、取消、终止、超时及失败均保留原请求/响应和最后受支持状态，不伪造即时完成；后续动作必须是显式新步骤。不同主机墙钟不用于推导因果。

### 0.PC021.6 producer / consumer 同步清单

PC021实施至少必须同步以下全部位置；漏一项不得宣称完成：

1. live Phase/activation packager：生成 5/4 键 manifest、fixed kind、created_at、origin 和 exact-set；
2. 合成 fixture producer：`tests/pc020_activation_fixture.py`，停止生成 legacy 2/3 键形状；
3. activation verifier：`tests/p05_pc020_activation.py`，验证坐标、exact keys、kind、created_at、origin 和 all-files-minus-self；
4. P05 transition writer：`tests/p05_pc020_transition.py` 的未来 epoch 7→8 路径；保留现有 epoch 6→7 v1，新增私有能力准入和 v2 记录；
5. P06 evidence verifier：`tests/p06_evidence.py`，识别新 manifest、固定 v2 transition 路径与绑定；
6. P06 packager：`tests/p06_package.py`，继续用既有 `activation_evidence` 递归携带，不新增 restore kind，并断言 v2 原件被 outer manifest 覆盖；
7. P07 handoff/restore verifier：按归档路径验证新 manifest 与 v2 transition 原件；
8. SDK/MCP report producer 与 Phase 下载 packager：先完成真实 product download，再冻结含 `arguments/structured/mcp_result` 的最终 report；按 `data` row 的 `file_id/original_path/file_size/uploaded_size/accessor` 和 DownloadResult 的 `flow_id/file_id/local_path/size/sha256/original_path` 做精确映射，之后复制并生成 manifest；
9. PC021 fixtures、goldens、正反例和 Windows 隔离测试：成套更新，历史 fixture 原字节另存而不覆写。

### 0.PC021.7 明确不作的推断

- 当前完整 receipt 字节不证明历史 receipt fsync 成功；操作可继续只来自同一控制器的一次性能力。
- `issued_at`、`created_at` 和业务报告时间不跨时钟域建立先后关系；因果顺序由控制器步骤和哈希引用证明。
- 新 Phase kind 不是当前实现已有类型；旧 R03 fixture 不是新 success fixture。
- 本节的作者稿同步不表示产品代码、共享测试、部署、VM、canonical 或快照已经修改或通过。
> **PLAN-CHANGE-014 覆盖块（2026-09-14，用户裁决）**：移除与 FakeNet-NG 撞车的网络域机制（评估与实施见 `2026.09.14-16-PLAN-CHANGE-014裁决-移除Velo方案中FakeNetNG撞车网络机制.md`）。要点：
> 1. PacketCapture netsh-trace 链整体移除：P05 四链→三链（Autoruns/Triage/KillProcess）；P06 资格链与覆盖集随之调整。
> 2. etl2pcapng 依赖移除（唯一用途为该链转换），依赖清单 3→2。
> 3. 完整网络窗口硬门降级为可选支撑证据；"零公网来源请求"保留为设计不变量（执行层=serve_locally 架构+已移除的上游探针），不再要求全窗口证明。
> 4. 注册面 130 不变；逐工具 129 实测 + 1 登记排除（Windows.Network.PacketCapture，排除原因=网络抓包职能划归 FakeNet-NG 域且本 VM netsh 机制异常）；覆盖关系 650→645；资源 admission 13→11。
> 5. 保留：localhost:8000/public 工具本地服务（Velociraptor 原生机制、零公网唯一执行层）、PC006 防火墙边界、全部安全/权限/回滚规则。
> 本块以下旧文本中与之冲突的"四链/650/13 admission/网络窗口门/etl2pcapng"表述均为被覆盖历史。

>
> 状态：§0.8固定新契约；待本版完整自评，不沿用20号记录；实现及真实激活尚未通过
>
> 会话角色：作者/执行方
>
> 来源类型：GrillMe 总纲实施子方案
>
> 总纲：`2026.09.02-02-总纲-mcp-velociraptor-Windows-DFIR二次开发.md` v13，Git blob `038f1725f030e88124c800ea88d0daccb4b4fdbc`；旧v11/v12/20号及更早身份只追溯历史，不构成本轮通过结论。
>
> 需求：`2026.09.02-01-需求提炼-mcp-velociraptor全阶段设计.md` v2，Git blob `4906941e0698ffe7476838c4479ce344ce30ef27`；P05直接适用REQ-007/013/014/020/023/026、CON-002/004/005/006/007/008/011/012/015、NON-003/004/009/010、RACC-007/013/014/020/023/026
>
> 前置提交：P01 `9dc8054daf2981a28ced316ee24a6c3ab8faa9b3`，P02 `4087f0a9df564a8950b5e2787df83cf3caa112d5`，P03 `1640284ff42bb5d5265afd173d0837f82b5251bb`，P04 `814dc829a11b38e59288a4120b715438bd802f6b`
>
> workflow_id：`wf-01a05d1d-9e2b-7f41-b936-536d20ce55a2`

## 0.PC020 恢复代次重建（v17历史父条款；由PC021精确细化）

本节完整纳入主方已验收的P05草案R02实质语义，覆盖下方§0～0.8中冲突的未来恢复身份、旧188激活、stage配对、证据kind和fallback；下方历史全文及不冲突的PC014/017/018/019、fixture、ready、安全、资源、网络、Triage/file和报告契约继续有效。本正式作者版本已完成作者采纳同步，仍待主方完整自评；不授权实现或现场动作。

### 1. Scope, participants and fixed objects

This supplement handles one exact predecessor only: the current canonical
bytes whose SHA-256 is
`2d5f378d20c163401b1c4ce7087db102a4719b394fc7a9408baa7d63d7ca4385`,
parsed as schema 5 / epoch 6 / `NETWORK_ACTIVE`, active and sole automatic
allowlist member `Snapshot 188-Velociraptor-MCP可恢复验收基线`. Schema 1–5
remain readable for history. Schema5 epoch6/188 is never a normal recovery
selection and enters this path only when its bytes match the fixed SHA exactly.
Any drift returns to the main session for a new decision; this is not a generic
legacy-state or arbitrary-epoch migration command.

The only new candidate name is
`Snapshot 189-Velociraptor-MCP可恢复验收基线`. The retained objects 187 and
188 are never deleted, renamed or overwritten. A collision with 189 stops the
attempt; the implementation must not choose 190 or another name. A marker is
never predicted from a UID and is accepted only from post-creation metadata.

Participants and responsibilities are:

- **main-session author** freezes the final PC020 normative group and self-review;
- **P05 recovery controller** reads and transitions canonical state, executes
  only the already authorized external recovery protocol, and owns `.next`;
- **evidence builders/verifiers** freeze source bytes and validate immutable
  packages; they do not select snapshots or write canonical state;
- **guest preparation and runners** operate only after epoch7 authorizes one
  187 recovery and produce post-recovery evidence;
- **P06/P07 consumers** have no entry until epoch8 and only read the resulting
  complete graph.

Inputs are the exact predecessor bytes, the current physical tree/metadata and
VM identity, preservation evidence, the adopted PC020 source set, and reviewed
implementation bytes. Outputs are one preparation-admission package, one
migration package, an epoch7 canonical plus transition receipt, three complete
P05 phase records and candidate-creation evidence, one activation package, and
an epoch8 canonical plus transition receipt.

### 2. Common immutable-object rules

All evidence is UTF-8 JSON unless a Ref explicitly preserves an original text
file. JSON uses sorted object keys, compact separators, `ensure_ascii=false`,
rejects NaN and ends with one LF. SHA-256 is lowercase 64-hex. UUID directory
names are canonical lowercase. Times are actual UTC RFC3339 values ending `Z`;
they describe evidence issuance or observed events, never a future state write.

An internal `Ref` has exact keys `path/size/sha256`: `path` is a non-empty
POSIX relative path from the containing bundle root, `size` is a non-negative
JSON integer (not bool), and `sha256` identifies the exact bytes. Absolute,
drive-qualified, empty, dot/dotdot, backslash, link/reparse, special-file,
escaping, duplicate-path/conflicting-identity and size/hash mismatch cases fail.

A `Source` has exact keys `repo_path/blob/content`; `repo_path` is the approved
repository-relative path, `blob` is the Git blob OID recomputed from the copied
bytes, and `content` is a Ref under `source/<repo_path>`. Source arrays are exact
sets with unique repo paths; direct dependencies added after freezing invalidate
the set and require a new pre-business freeze.

A recursive package manifest has exact root keys
`schema_version/kind/workflow_id/created_at/members`; schema is 1, kind is the
package-specific fixed value, and members is sorted by UTF-8 path bytes. Each
member has exact keys `path/size/sha256/origin`; `origin` is the approved
repository/evidence-relative provenance path and never a secret-bearing
absolute path.

For every package manifest, let `T` be its declared governed subtree and `E`
its exact exclusion set. `members.path` is exactly the set of all plain files
recursively present in `T`, relative to `T`, minus `E`; each path occurs once
and its size and SHA-256 identify those bytes. `E` may contain only that
manifest itself and, when it is inside `T` at the same package level, the root
record that directly references the manifest. The referencing root is bound by
an outer Ref or canonical reference and is therefore not self-hashed. A nested
manifest or nested root record is never excluded merely because it performs
that role in its own subtree: every outer manifest whose `T` contains it must
list it. There is no caller-selected exclusion list.

The exact governed trees and exclusions in this supplement are:

| manifest | governed subtree `T` | exact `E` |
| --- | --- | --- |
| preparation package manifest | entire `pc020-preparation/<uuid>/` | its `package-manifest.json` and same-level `admission.json` |
| historical manifest | the migration package's complete `historical/` copy | that historical manifest itself; `migration.json` is outside `T` |
| preparation-copy manifest | the migration package's complete `preparation/` copy | that copy manifest itself; copied `admission.json` and copied nested `package-manifest.json` are members |
| migration package manifest | entire `pc020-migration/<uuid>/` | its `package-manifest.json` and same-level `migration.json` |
| each source-set manifest | its complete frozen source-copy subtree | that source-set manifest itself; its referring package root is outside `T` |
| each Phase package manifest | the complete Phase evidence subtree named by its Ref | that package manifest itself; the referring activation root is outside `T`, and every root record inside `T` is a member |

Thus neither a manifest nor its same-level referring root contains the other's
future hash, while outer manifests cover all frozen subordinate manifests and
root records. Missing ordinary or nested files, duplicate/conflicting paths,
extra exclusions, links/reparse points or identity mismatches fail validation.

### 3. Schema6 canonical state

The exact canonical root keys are:

`schema_version/workflow_id/epoch/phase/active_snapshot/automatic_restore_allowlist/retired_snapshots/migration_evidence/preparation_evidence/activation_evidence`

`active_snapshot` retains exact keys `name/checkpoint_marker/purpose`.
`retired_snapshots` items retain exact keys `name/status`, with status exactly
`MANUAL_ONLY_REQUIRES_NEW_USER_AUTHORIZATION`. Let `a` be the unique active
name, `L` the automatic-restore allowlist names and `R` the retired names.
Exactly `L = {a}`, `L ∩ R = ∅`, and `R` has no duplicates. Active is therefore
deliberately in the allowlist and is not in retired; active and allowlist are
not required to be disjoint. Retired order remains the transition order below.
`workflow_id` remains the existing P05 workflow ID.

The migration reference exact keys and values are:

- `kind/source/evidence_path/evidence_sha256/predecessor_sha256/migrated_at`;
- kind `pc020-requalification-migration-v1`;
- source `pc020-migration`;
- predecessor SHA exactly the fixed SHA above;
- evidence path under `pc020-migration/<uuid>/migration.json` in the fixed P05
  evidence root; `migrated_at` equals the immutable record's issuance time.

The preparation reference exact keys and values are:

- `kind/source/evidence_path/evidence_sha256/qualified_at`;
- kind `pc020-snapshot187-preparation-v1`;
- source `pc020-preparation-admission`;
- evidence path under `pc020-preparation/<uuid>/admission.json`;
- `qualified_at` equals the immutable record's issuance time.

The activation reference retains exact keys
`source/evidence_path/evidence_sha256/activated_at`; source is
`snapshot189-activation`, evidence path is under
`activation-189/<uuid>/activation-evidence.json`, and `activated_at` equals that
record's required `issued_at` string byte-for-byte. The root SHA covers
`issued_at`. Missing, null, non-string, invalid UTC RFC3339 Z, unequal, or
defaulted-from-mtime/current-clock/receipt values fail. It is null at epoch7 and
non-null at epoch8; the issuance time is not the later replace time and creates
no cross-machine clock-comparison or extra-precision gate.

#### 3.1 Epoch7 preparation state

- schema/epoch/phase: `6 / 7 / PREPARATION_BASELINE`;
- active and sole allowlist: `Snapshot 187-固定IP+WindowsMCP开机自启`, with the
  pre-admission marker actually read from metadata;
- migration and preparation references: non-null and valid;
- activation reference: null;
- retired list: copy the predecessor list exactly, remove only 187, preserve
  every other item and relative order, then append 188. For the accepted
  predecessor this is `183, 184, Snapshot 1, 186, 188`.

Epoch7 grants only the right to execute the existing P05 one-revert/marker/
one-start recovery method on 187 and then prepare/test it. It does not state
that 187 was restored, that product readiness passed, or that P06 may run.

#### 3.2 Epoch8 active state

- schema/epoch/phase: `6 / 8 / NETWORK_ACTIVE`;
- active and sole allowlist: fixed Snapshot189 with its post-creation marker;
- migration and preparation references: byte-for-byte unchanged from epoch7;
- activation reference: valid and points to the completed new graph;
- retired list: copy epoch7 retired entries unchanged and append 187, yielding
  `183, 184, Snapshot 1, 186, 188, 187` for this predecessor.

The append order records transition history and is deliberate. It is not a
claim that retained physical snapshots were deleted. Old188 remains isolated,
manual-only and ineligible for formal coverage.

### 4. Recovery-before-admission separation

#### 4.1 Preparation-admission package

The directory is exactly `pc020-preparation/<uuid>/`. The signed root is
`admission.json`, with exact keys:

`schema_version/kind/workflow_id/admission_id/vmx/snapshot_name/checkpoint_marker/snapshot_tree/snapshot_metadata/vm_identity/preservation_manifest/retired_requalification/source_inputs/implementation_sources/package_manifest/qualified_at`

Allowed values and types:

- schema is integer 1; kind is `pc020-snapshot187-preparation-v1`;
- workflow ID is fixed; admission ID equals the directory UUID;
- `vmx`, `snapshot_name`, `checkpoint_marker`, `qualified_at` are non-empty
  strings; snapshot is exactly 187 and marker equals the raw metadata result;
- `vmx` identifies the one existing P05-governed VMX and is not a caller input;
- the next five evidence fields and `package_manifest` are Refs; source fields
  are exact Source arrays. `package_manifest` points to
  `package-manifest.json`, kind `pc020-preparation-manifest-v1`, which inventories
  every pre-existing bundle file except itself and `admission.json`.

The referenced objects are all produced before any PC020 recovery:

- `snapshot_tree`: attempt-bound raw read-only command showing exactly one 187
  and one retained 188 in the accepted 187→188 tree, current UID4, with no 189;
- `snapshot_metadata`: raw metadata bytes proving 187's unique marker and the
  tree relation; it does not infer a future running state;
- `vm_identity`: exact-key object
  `schema_version/kind/workflow_id/vmx/vmx_sha256/mac_address/current_snapshot_uid/observed_at/observation`,
  with kind `pc020-vm-identity-v1`, MAC `00:0c:29:83:b8:65`, current UID4 and an
  observation Ref to raw host/VMX identity bytes. It proves the target VM, not
  the contents that will exist after restoring 187;
- `preservation_manifest`: exact-key object
  `schema_version/kind/workflow_id/preservation_id/scope/created_at/transfer_receipt/receiver_stopped/included/excluded`;
  kind is `pc020-pre-recovery-preservation-v1`, scope is
  `current-project-nonsecret-evidence-before-pc020-recovery`, both receipt fields
  are Refs, `included` is a sorted array of exact `path/size/sha256` objects, and
  `excluded` is a sorted array of exact `category/reason` objects. It proves
  permitted evidence arrived with matching bytes and the temporary receiver is
  no longer reachable. Secrets and secret-bearing historical environment logs
  are excluded by category/reason, not copied or fingerprinted;
- `retired_requalification`: exact-key object
  `schema_version/kind/workflow_id/predecessor_sha256/snapshot_name/previous_status/new_scope/reason/decision_source/issued_at`;
  kind is `pc020-retired187-requalification-v1`, name is 187, old status is the
  manual-only value, new scope is `P05_PREPARATION_ONLY`, predecessor SHA is
  fixed, and `decision_source` is a Ref to the copied 16号裁决. The reason states
  that this is an explicit one-generation exception; it grants neither product
  readiness nor P06 eligibility.

Critically, admission contains no revert operation, post-restore hostname,
ready object, deployment result, Flow, report, Triage/file result or claim that
187's running contents have passed. Requiring any such future object is a
causal error and fails admission validation.

#### 4.2 Migration package

The directory is exactly `pc020-migration/<uuid>/`. The signed root is
`migration.json`, with exact keys:

`schema_version/kind/workflow_id/migration_id/predecessor_sha256/predecessor_canonical/predecessor_baseline/predecessor_activation/historical_manifest/preparation_admission/preparation_manifest/source_inputs/implementation_sources/package_manifest/migrated_at`

Schema is integer 1, kind is `pc020-requalification-migration-v1`, migration ID
equals the directory UUID, predecessor SHA is fixed, Ref fields use the common
shape, Source arrays are exact sets, and `migrated_at` is the record issuance
time. The migration record does not contain or reference epoch7 canonical
bytes, a transition receipt or any future recovery/ready result.

Before signing, the bundle recursively copies as plain immutable bytes:

1. the exact predecessor canonical to `historical/canonical.json`;
2. the complete old baseline/adoption directory named by its predecessor
   reference, under `historical/baseline-adoption/`;
3. the complete old activation directory named by its predecessor reference,
   under `historical/activation-188/`;
4. the complete already-signed preparation-admission bundle, under
   `preparation/`, without changing internal relative paths;
5. all frozen source and implementation bytes.

`historical_manifest` covers items 1–3 and `preparation_manifest` covers item 4.
The root Refs identify the copied canonical, old baseline root, old activation
root and copied preparation `admission.json`. Validators recompute every member,
verify the predecessor canonical byte hash and reconcile its historical
evidence paths/hashes with the copy provenance. They do **not** require the old
baseline/activation package to pass current predicates: those bytes are
preserved historical inputs, and the known old activation failure cannot be
converted into current success.

`package_manifest` points to `package-manifest.json`, kind
`pc020-migration-manifest-v1`, and inventories every file already present in the
migration bundle—historical copies, preparation copy, sources and subordinate
manifests—except itself and `migration.json`. It is completed before the root is
signed and cannot contain the root's future hash.

The migration package must verify that the separately referenced original
preparation package and the recursively copied preparation package have the same
manifest and bytes. Migration signing happens only after preparation signing;
preparation never references migration.

#### 4.3 Exact source sets and freeze boundary

`source_inputs` is the exact adopted set present before live execution:

- all nine post-PC020 formal normative inputs and their one current manifest;
- PLAN-CHANGE-020 documents 15, 16, corrective decision 17, activation issuance-time decision 18, and author publication decision 19 (publication governance only);
- the completed PC020 fidelity/self-review record;
- the existing PLAN-CHANGE-012 and file-transfer authorization records that
  govern 187 and evidence movement;
- PC017/018/019 source decisions only through the adopted current manifest and
  the explicit current decision files it lists, never an obsolete manifest as
  current authority.

`implementation_sources` is the exact byte set actually used by migration and
P05 evidence production: selector; migration/preparation validators;
`p05_snapshot_raw`; candidate creation; ready, dependency and external
acceptance validators; service lifecycle/ABI; scenario runner; package and
activation verifier; relevant selector/evidence tests; and the product bridge,
API, transport, core, dynamic/fixed/environment modules. The accepted PC017
file delivery, state/paging and 4 GiB triage-budget packages are represented by
their actual participating source files and reviewed identities, not by a claim
that old execution evidence applies to the new generation.

The source-set manifest and all copied source bytes are frozen before admission
and migration roots are signed. This isolated candidate, its result, future
live evidence packages, transition receipts and their own hashes are not source
inputs. A source may point only to bytes already present; no placeholder OID or
future self-reference is allowed.

### 5. Single-use migration and transition protocol

The P05 controller performs, in order:

1. read canonical as bytes, require the fixed SHA and exact special predecessor;
2. require no `.next`; freeze and validate preparation admission;
3. freeze and validate migration, including recursive history/preparation copies;
4. derive the one exact epoch7 state and its canonical bytes;
5. create `.next` with O_EXCL, write all bytes, flush/fsync/close, fsync the
   directory, reread and validate the candidate;
6. record ownership only after this process proves it obtained O_EXCL and writes
   an immutable intent that binds attempt ID, predecessor SHA and `.next` SHA;
7. immediately reopen canonical and compare byte-for-byte with step 1;
8. atomically replace, fsync the parent directory, reread and validate epoch7;
9. write a separate transition receipt. Reference timestamps remain evidence
   issuance times and do not pretend the replace already succeeded.

The transition receipt is outside canonical and has exact keys
`schema_version/kind/workflow_id/transition_id/from_sha256/to_sha256/next_sha256/started_at/replaced_at/directory_fsynced_at/readback_at/status/error`.
Kind is `pc020-canonical-transition-receipt-v1`; timestamps after an unobserved
step are null; status is `COMMITTED`, `FAILED_BEFORE_REPLACE`, or
`TRANSITION_INDETERMINATE`; error is null only for committed.

An unknown `.next` is never moved or deleted. Before replace, any failure keeps
canonical unchanged and may isolate `.next` only when the immutable intent and
this process's O_EXCL ownership prove it belongs to this attempt. If ownership
proof was not durably completed, it is unknown and execution stops. Failure of
replace-result determination, parent fsync or readback after replace yields
`TRANSITION_INDETERMINATE`: preserve all bytes, stop every recovery/business
action and return to the main session for read-only location assessment. There
is no automatic rollback, fallback, recovery selection or invented canonical
phase.

Repetition fails because a committed canonical no longer has the exact fixed
predecessor bytes. Concurrent attempts can have at most one O_EXCL `.next`
owner; all losers perform no canonical or VM mutation.

### 6. Post-epoch7 P05 execution and activation

Only after a committed and verified epoch7 transition may P05 perform:

1. one new actual 187 restore attempt: unique-name/tree read, exactly one revert,
   exact pre-start marker, exactly one start, then post-restore identity;
2. existing deployment/dependency/fixture preparation, preserving fixed IP and
   the control-plane autostart boundary and changing no unrelated network,
   service, credentials or VM object;
3. complete ready/security/resource checks and the initial formal HTTP report,
   including the first independent real Triage and file chain;
4. immediately recheck ready and prove 189 has zero exact matches;
5. issue exactly one live create command, prove exactly one 189 afterward and
   read its marker from metadata; historical reconstruction is forbidden;
6. restore the same 189 twice in two new attempts. Each attempt independently
   completes ready, security/resource, representative report, Triage and file
   chains and returns evidence before the next attempt;
7. after cycle2 is complete and verified, validate and sign the activation graph
   with required `issued_at`, verify canonical `activated_at` will equal it
   byte-for-byte, then atomically transition the exact epoch7 bytes to epoch8
   using the same `.next` protocol.

The activation directory is exactly `activation-189/<uuid>/`; root file is
`activation-evidence.json`, with exact keys:

`schema_version/kind/workflow_id/candidate/checkpoint_marker/migration_evidence/preparation_evidence/creation_metadata/initial/candidate_cycles/source_inputs/implementation_sources/issued_at`

Schema is 1, kind is `snapshot189-activation-evidence-v1`, candidate is the
fixed 189 name, marker is the actual new marker, migration/preparation fields
are Refs to recursively copied immutable packages, creation metadata is a Ref,
initial is one Phase, candidate cycles is exactly two Phases, and source arrays
are the exact frozen sets. `issued_at` is a required string containing the actual
UTC RFC3339 Z issuance time after cycle2 validation and before epoch8 `.next`;
it is hash-bound as part of this exact root. The bundle recursively includes the referenced
migration and preparation packages; their bytes must equal the epoch7
references. Nothing rewrites their pre-recovery facts.

`creation_metadata` retains the §0.7 exact structure and live raw sequence
`tree_before → create_operation → tree_after → metadata_readback`, updated only
for 189. `tree_before` must show zero 189; `tree_after` exactly one; both retain
187 and188 unchanged.

Each Phase retains exact keys
`restore_attempt_id/run_id/report/snapshot_evidence/ready/dependency_acceptance/entry_gate/network_evidence/package_manifest`, with seven Refs after the two IDs.
The initial Phase is `P05_REPAIR_INITIAL`, restoring
`Snapshot 187-固定IP+WindowsMCP开机自启`, while canonical reads schema6 /
epoch7 / `PREPARATION_BASELINE` / active snapshot 187. The two candidate
Phases are `P05_REPAIR_CANDIDATE`, restoring
`Snapshot 189-Velociraptor-MCP可恢复验收基线`, while canonical still reads
schema6 / epoch7 / `PREPARATION_BASELINE` / active snapshot 187 and the same
creation marker. All three attempt IDs
and report run IDs are non-empty and mutually distinct. Each Phase must contain
its own real Triage plus list/download file chain; startup response state and
later terminal state remain separate under PC018/019.

Phases may share immutable upstream inputs at the same verified identity:
normative and product source bytes and the frozen preparation-admission and
migration packages. The two candidate cycles may also share the single
immutable 189 creation record; the causally earlier initial Phase does not
consume it. Recursive copies of eligible inputs may therefore have equal hashes
in multiple Phase packages. Path equality or hash equality alone neither proves
a cross-round splice nor makes correct upstream sharing invalid.

Runtime facts are independently bound per Phase. Each Phase owns a distinct
`restore_attempt_id` and `run_id`, restore raw observations and ready record,
network and service observations, report, session/process observations, Flow
IDs, real Triage result, list/download file chain, and Phase package manifest.
Their decoded attempt/time/session/process/Flow bindings must identify that
round. Reusing the same Flow, raw restore observation or ready record as a
second Phase, relabelling another round's runtime fact, or mismatching any of
those semantic bindings fails even if a copied file has a different path.
Every one of the initial and two candidate Phases still independently satisfies
all Triage/file, ready, security and resource gates.

Activation signing occurs after cycle2 completes and is verified and before
epoch8 `.next`. Event evidence must prove `cycle2_done → root_issue →
epoch8_next`; timestamp magnitude is not used to invent cross-machine order. The
activation record never references epoch8 canonical or the activation transition
receipt. It never derives `issued_at` from mtime, a validation-time clock, a
receipt, or any later replace event and never writes a future event time.

### 7. Stage pairing and restore record kinds

Report schema2's business root, pagination/state semantics and
`snapshot-evidence.json` root remain unchanged. Index hashes are recomputed from
the reviewed 187/189 scenarios; CLI/environment cannot override the stage,
snapshot, evidence root or arguments.

| stage | required snapshot | required canonical | exact restore-record kinds |
| --- | --- | --- | --- |
| `P05_REPAIR_INITIAL` | `Snapshot 187-固定IP+WindowsMCP开机自启` | schema6 / epoch7 / `PREPARATION_BASELINE` / active snapshot 187 | `snapshot_metadata`, `revert_operation`, `pre_start_marker`, `post_restore_hostname`, `canonical_readback`, `pc020_migration`, `pc020_preparation` |
| `P05_REPAIR_CANDIDATE` | `Snapshot 189-Velociraptor-MCP可恢复验收基线` | schema6 / epoch7 / `PREPARATION_BASELINE` / active snapshot 187 | same seven kinds |
| `P06_ACTIVE` | `Snapshot 189-Velociraptor-MCP可恢复验收基线` | schema6 / epoch8 / `NETWORK_ACTIVE` / active snapshot 189 | the same seven plus `activation_evidence` |

Each restore record retains exact keys `kind/path/sha256`; kinds are unique and
the set must equal the applicable row—not merely have a minimum count. The
`pc020_migration` and `pc020_preparation` records recursively package their
complete reviewed directories. `activation_evidence` recursively packages the
complete activation graph. The old `baseline_adoption` and old activation are
inside migration history and are not current success record kinds.

P06 cannot consume epoch7, old188, an unactivated 189, a missing/extra kind,
an old seven-kind assumption, a stale package or a reused attempt. P06 retains
129 actual tools + 1 registered exclusion and 645 relations; no 650 revival.

### 8. Observable acceptance and failure matrix

| Case | Required observation | Required result |
| --- | --- | --- |
| Exact predecessor | Canonical bytes equal fixed SHA and exact 5/6/188 shape | Migration path may proceed; ordinary selection still rejects 188. |
| Drift | Any byte, root key, state or hash differs | Stop before evidence/state/VM write and request a new decision. |
| Existing `.next` | Any pre-existing file, regardless of content | Treat as unknown; do not move/delete/overwrite or touch VM. |
| Duplicate migration | Canonical is no longer the fixed predecessor | Stable refusal; no new package, state or VM write. |
| Concurrent migration | Two attempts race | One O_EXCL winner at most; loser performs no canonical/VM mutation. |
| Write/flush/fsync/validation fault before replace | Canonical rereads as exact predecessor | `FAILED_BEFORE_REPLACE`; preserve evidence; isolate only durably proved owned `.next`. |
| Canonical drift before replace | Byte comparison differs | No replace; preserve both byte sets and stop. |
| Replace/fsync/readback uncertainty | Replace may have occurred or durability/readback is unproved | `TRANSITION_INDETERMINATE`; preserve all bytes; no recovery/business/fallback pending main read-only assessment. |
| 187 retired exception | Admission proves current unique 187/marker/VM/preservation and explicit P05-only decision | Epoch7 may name 187 active; no product-ready/P06 claim. |
| 188 isolation | Physical 188 remains and appears retired; old bundle is history only | No select, no coverage, no deletion/rename/overwrite. |
| 187 actual restore | One fresh attempt after committed epoch7, exact marker and identity | Proceed to deployment/ready only after successful readback. |
| 189 collision | Fresh pre-create tree has one or more exact matches, ambiguous identity or drift | Stop; do not create, rename, overwrite or choose another number. |
| 189 creation | Zero before, one create, exactly one after, marker parsed from metadata | Creation evidence may be issued; retained187/188 unchanged. |
| Three business phases | Initial + two candidate phases have distinct attempts/runs and each has complete Triage/file, ready/security/resource/report evidence | Activation graph may be signed only after all three pass. |
| Activation issuance time | Cycle2 is complete and verified; root has required legal UTC RFC3339 Z `issued_at`; canonical `activated_at` is byte-for-byte equal and root SHA covers the field | Sign root after cycle2 and before epoch8 `.next`; reject missing/null/invalid/unequal/defaulted/future values without adding a clock-sync gate. |
| Legal immutable upstream reuse | Two or more Phases reference equal verified source, preparation, migration or single creation identities | Accept that sharing; continue only if every Phase's runtime facts are independently bound. |
| Cross-attempt/generation splice | Reused restore-attempt/run/Flow/raw/ready or mismatched attempt/time/session/process binding; path/hash equality alone is not the predicate | Reject graph; keep epoch7; no activation. |
| Epoch8 activation | Exact epoch7 bytes, complete new graph, O_EXCL and readback succeed | Active/allowlist solely 189; 187 appended retired; P06 entry opens. |
| Premature P06 | Epoch7, old188, unactivated189, old package/budget/report | Reject with zero formal coverage. |

Every case retains PC017 logical-file identity/read/publish-failure semantics,
PC018/019 real initial state and terminal nullable-cursor semantics, complete
`_BasicCollection`, 2400 seconds and 4 GiB request budget, the 13 GiB/1 GiB
workflow preflight, safe file download, three independent Triage/file chains,
ready/raw command predicates, fixed IP, existing autostart responsibilities,
Bearer/Host/Origin/ACL/firewall/security and resource gates. No new product
tool, permission, network path, secret handling, retry, cleanup or auto-start
requirement is introduced.

### 9. Fallback and stop rules

For PC020, this section supersedes only P05 clauses that would return to an old
snapshot or describe old 185/186/188 future execution. Evidence/source failure,
identity drift, preservation failure, unavailable 187, name collision, raw
predicate failure, phase failure or graph failure stops at the current safe
state and preserves all originals. Epoch7 remains preparation-only after any
post-migration P05 failure. No fallback selects old188 or another snapshot.

Only an owned `.next` may be isolated before replace. No automatic rollback is
allowed after replace uncertainty. P05 must not delete snapshots, fabricate a
marker/report, repair old activation evidence, reclassify an old failure, reuse
a phase's runtime facts, continue to P06, or change fixed
IP/autostart/network/service scope. Reuse of the explicitly shared immutable
upstream categories in §6 is not runtime reuse.

### 10. Causal dependency list (normative DAG)

Each arrow means the left object must be immutable and hashable before the
right object is issued:

1. source bytes → source-set manifests;
2. raw tree/metadata/VM observations → VM identity;
3. transferred evidence + closed-receiver observation → preservation manifest;
4. decision 16 copy + fixed predecessor SHA → retired requalification;
5. source manifests + VM/tree/metadata + preservation + retired requalification
   → preparation package manifest → preparation admission;
6. old canonical/baseline/activation copies → historical manifest;
7. preparation admission copy → preparation-copy manifest;
8. historical manifest + preparation admission/copy manifest + source manifests
   → migration package manifest → migration record;
9. preparation + migration records → epoch7 `.next`;
10. committed epoch7 → actual 187 restore;
11. actual restore → deployment/fixture/dependency preparation → ready/security/
    resource + initial report/Triage/file;
12. successful initial phase + fresh zero-match tree → one 189 creation → marker;
13. creation marker → cycle1 → returned cycle1 evidence → cycle2 → verified
    cycle2 completion;
14. migration/preparation copies + creation + initial + cycle1 + verified cycle2 +
    source manifests → activation root issue with required `issued_at`;
15. activation root hash + canonical `activated_at` byte-equality → exact epoch7
    bytes + activation record → epoch8 `.next`;
16. committed epoch8 → P06; successful new P06 selection → P07.

No edge points backward. Admission cannot consume restore/ready/initial facts;
migration cannot consume epoch7 or a transition receipt; activation cannot
consume epoch8; neither state nor a bundle contains its own hash. Any future
reference, self-hash, mutual Ref, or changed causal order fails validation.

### 11. Status

已作者采纳，仍待主方完整自评和后续实现/现场门。This formal author version does not authorize implementation or live execution. There are no
unresolved semantic questions inside the five choices fixed by decision 16 as
corrected by decision 17 and the activation issuance-time decision 18; decision
19 governs publication only. Final implementation source identities and the
implementation source set can only be frozen after the separate implementation
gate opens; they are execution inputs, not values invented by this formal author
version.

## 0. 目标、范围与非目标

**当前优先门禁（PLAN-CHANGE-012）。** 用户已恢复唯一 `Snapshot 187-固定IP+WindowsMCP开机自启` 并删除其他快照；以总纲v12 §2.0.2和同目录 `2026.09.14-01-PLAN-CHANGE-012-用户重置唯一187基线.md` 为准。本文下方185/186/188前驱、候选配对及状态迁移的未来执行全部暂停，保留其历史原件及可复用的业务/证据要求，不恢复已删除对象，不把187直接标为P05通过。当前仅由单一实施方经精确 `.232:28787` 控制面/MAC核对做只读库存；不使用屏幕输入或旧`.149`连接器，不改固定IP和WindowsMCP自启，不覆盖/删除/改名187。主会话完成新起点、状态迁移、索引和下游契约及逐项自评后另行解除；下文25/25历史计数不表示v8完整或可执行。所有四链、固定依赖/fixture、保护父链、报告原件、两轮恢复与正式网络成功门不降低。

P05 v1/v2已建立并验证Snapshot185、四项依赖、固定测试状态和stdio runner，完成提交为`d142760e29b350b0706869296444c317dfbba936`。v3因Q-072重新打开，只增加正式Windows网络部署和外部runner验收：消费回流后P02的同一server Streamable HTTP入口，配置VM host-only地址上的`28790/mcp`、Windows单进程服务、只允许当前宿主机host-only来源的防火墙，并在全部外部正负验收通过后创建和激活Snapshot186。它不改P04工具业务契约、不执行650关系、不创建token、不把Win10VM MCP变成正式代理。

### 0.1 v3对旧正文的规范性覆盖

- 旧正文关于Snapshot184→Snapshot185的内容是已完成历史证据，保持不改写；v3的新转换唯一为当前active Snapshot185→候选Snapshot186，名称固定为`Snapshot 186-Velociraptor-MCP网络部署基线`，实际vmsn只接受创建后读回值。
- 旧runner的stdio模式保留为VM内快速回归；正式代表情景和P06交接改为虚拟机外官方SDK客户端直连VM内`28790/mcp`。任何“stdio代表正式拓扑”表述由本节和IMP/ACC-P05-009～011/007～008替代。
- 除外部网络边界验收外，所有安装、语法、单元、服务状态、Velociraptor集成和fixture验证仍由AI在Win10VM中执行。外部客户端不得调用Win10VM MCP转发MCP请求。
- token必须由部署者提供，至少32个随机字节等价强度，只通过受保护的Windows服务环境/凭据配置传入；P05不得生成、选择、回显、复制到URL或持久证据。未提供时P05停在网络部署前。
- 第1～5章中Snapshot184→185、report schema v1和stdio子进程是已完成P05 v2的历史契约，不得作为v4未来执行命令。v4未来执行只以本节下方0.2～0.4、IMP-009～011和ACC-007/008为规范；出现冲突时以后者为准。

### 0.2 Snapshot185→186唯一状态迁移契约

当前唯一合法predecessor是canonical schema2/epoch3/phase=`DEPENDENCIES_ACTIVE`，active和automatic allowlist均精确为`Snapshot 185-Velociraptor-MCP依赖与情景基线`，retired按编号升序精确含183/184且状态均为`MANUAL_ONLY_REQUIRES_NEW_USER_AUTHORIZATION`。任何偏差在创建服务或候选前阻断。

新candidate固定为schema3/epoch4/phase=`NETWORK_ACTIVE`，根exact keys继续为`schema_version/workflow_id/epoch/phase/active_snapshot/automatic_restore_allowlist/retired_snapshots/activation_evidence`；`active_snapshot` exact keys继续为`name/checkpoint_marker/purpose`，值为Snapshot186名称、从VMware metadata实际读回的非空新`.vmsn`、以及“已验证依赖/fixture/专用服务/28790安全边界与外部情景的唯一自动恢复基线”；allowlist恰含Snapshot186。retired精确按183/184/185升序，状态均为`MANUAL_ONLY_REQUIRES_NEW_USER_AUTHORIZATION`。`activation_evidence` exact keys继续为`source/evidence_path/evidence_sha256/activated_at`，证据必须存在且hash一致。active、allowlist、retired名称互斥，禁止未知/缺失键、旧marker、跳epoch/降级、错误phase或重复/乱序retired。

唯一允许迁移是有效schema2/epoch3→有效schema3/epoch4。Snapshot186候选创建和两轮恢复期间canonical保持schema2/epoch3且`--emit-active`只输出Snapshot185；只有两轮全部通过才排他创建`.next`。`.next`采用UTF-8完整写、flush+fsync、同目录不覆盖显现及目录fsync；存在时普通emit fail-closed。`--emit-next`验证上述全部不变量和证据hash后，以同目录原子replace canonical并fsync目录，再回读证明active唯一为186且183/184/185 candidate验证均退出3。

`.next`失败只允许在证明它由本workflow/current attempt排他创建后，先保存完整字节SHA和错误，再证明canonical仍为有效schema2/epoch3/Snapshot185；随后把`.next`原子rename为带attempt和SHA前缀的隔离证据并fsync目录，最后证明`--emit-active`恢复输出Snapshot185。来源不明`.next`不得移动/删；任何创建、恢复、网络或状态验证失败均不激活、不删除snapshot、不冷启动，Snapshot185保持唯一active。

### 0.3 共享runner report schema v2

v6将canonical report保持为schema2并补齐其唯一工具清单绑定，根exact keys固定为：`schema_version/scenario/source_sha256/index_sha256/fixture_spec_sha256/fixture_instance_sha256/run_id/transport/endpoint/authorization_configured/tools_schema_sha256/snapshot_evidence_sha256/mcp_session/server_identity/server_observation_sha256/runner/started_at/ended_at/duration_ms/status/calls/steps/cleanup/failure/unexecuted_step_ids/coverage`，`additionalProperties=false`。原schema1只允许读取既有历史报告，不得产出新的正式证据。

- `transport`只允许`streamable-http`或`stdio`。HTTP的endpoint必须是脱敏的`http://<实测guest-host-only-IP>:28790/mcp`且不得含userinfo/query/token；stdio的endpoint固定为null且coverage必须空。
- `authorization_configured`只记录布尔值，正式HTTP必须为true；不得记录Authorization header、token本体、token指纹或任何可用于关联/恢复secret的材料。
- `tools_schema_sha256`为必需的64位小写十六进制SHA-256，只能由本会话真实`tools/list`结果计算。规范化对象是按工具`name`的Unicode码点升序排列的数组，每项exact keys为`name/inputSchema/outputSchema`；JSON序列化唯一使用`ensure_ascii=false`、拒绝NaN、对象键升序、分隔符`,`/`:`且无额外空白，再对UTF-8字节计算SHA-256。规范化完整数组同时写入同一run目录固定文件`tools-schema.json`，报告hash必须与其字节和本会话原始`tools/list`重算值三者一致；不得从外部diff文件、另一会话或客户端参数填入。
- `mcp_session` exact keys为`id/initialized_at/closed_at`；HTTP id必须直接来自真实`Mcp-Session-Id`响应头且同一情景不变，stdio使用SDK会话本地ID但不计正式覆盖。
- `server_identity` exact keys为`computer_name/service_name/pid/process_start_time_utc/instance_id/executable_sha256`。HTTP `instance_id`必须来自P02进程启动时生成的非秘密随机实例ID，并由每个HTTP响应的`X-MCP-Server-Instance`头返回；guest只读服务观察JSON记录同一ID/PID/start time/binary hash。`server_observation_sha256`绑定该canonical guest JSON，外部报告中的header ID与guest观察不等或服务重启均使情景失败。
- `runner` exact keys为`pid/process_start_time_utc/executable_sha256`。其余calls/steps/cleanup/failure/coverage沿用schema1语义。两种分支共用场景解析、引用和断言核心；HTTP不创建child bridge，stdio可记录子进程详情在calls证据中但不能新增根键。

schema/golden必须拒绝缺transport/endpoint/tools hash/session/server identity、`tools_schema_sha256`与本会话实际清单或`tools-schema.json`不等、HTTP/stdio代表报告hash不等、HTTP session变化、服务重启、观察hash不匹配、stdio产生coverage、token出现在任意字符串、外部文件冒充本报告绑定证据，以及schema1冒充正式报告。

### 0.4 专用运行身份、权限与可执行防火墙负例

Windows服务名固定`mcp-velociraptor`，运行身份固定为内置虚拟账户`NT SERVICE\mcp-velociraptor`，不使用LocalSystem、管理员用户或安装者账户。服务二进制/代码只读，下载根仅授予该服务身份创建/写入及必要管理员管理权限；Bearer和运行API配置ACL只允许该服务身份与必要管理员读取，其他普通/服务身份拒绝。安装脚本使用一次性管理员身份，安装结束后该身份不保存在服务配置。

现有安装期SERVER_ADMIN API配置不得被正式服务复用。取得用户对“创建API principal和调整上述ACL”的单独授权后，P05先建立能力矩阵Spike：从最低角色开始验证130工具所需的artifact/client/collection/Flow/Hunt/file/VQL动作，并对用户/ACL/配置/inventory管理等产品非目标VQL/API做超权负例；只保留使全部正向通过且超权集合最小的principal。若任意`run_vql`语义导致必须长期SERVER_ADMIN或无法阻止管理面动作，P05停止在Spike，记录证据并要求用户明确接受该风险或回流REQ-012；不得默认沿用高权凭据。

防火墙非允许来源负例按PLAN-CHANGE-006修订：实测证明host OS网络栈不允许跨接口源发包（绑定非host-only源对28790和28787均超时），guest WFP 5152/5157事件不可达。在此确定环境中以(a)host侧绑定源地址连接失败+(b)双端口对照（受规则保护28790与不受保护28787同时不可达）+(c)防火墙规则verify（唯一规则、精确RemoteAddress）三要素闭合。若未来环境允许跨接口源发包或guest可取得WFP事件，应恢复动态三端负例标准。

核心提交范围固定为：

- `tests/scenario_runner.py`：测试专用官方MCP Python SDK runner；
- 新增或扩展的Windows服务安装/卸载核查脚本、28790专用防火墙规则脚本及其单元测试；脚本必须目标精确、幂等且不触碰其他端口/规则；
- 外部runner入口与网络验收脚本：只接受端点、token环境变量名和证据目录，不接受任意命令；
- `tests/scenarios/schema-v1.json`、`tests/scenarios/representative/*.json`：声明式格式和代表情景；
- `tests/data/p05_dependency_manifest.json`、`tests/data/p05_fixture_spec.json`、`tests/data/p05_scenario_index.json`：无秘密的来源、fixture规范和受审scenario哈希索引；
- `tests/fixtures/artifacts/Generic.Utils.KillProcess.yaml`：非二进制、最小且可审阅的测试部署artifact；
- `tests/p05_prepare_fixtures.ps1`、`tests/p05_dependency_acceptance.py`及P05单元/协议测试；
- 顶层README与`agent_poc/README.md`的P05部署/测试边界更新。

直接相关共享接口只含P02的结构化结果/错误/cursor/限制和P04最终130工具协议、`TargetContext`与Flow/file生命周期。`PLAN/...-长程执行/快照恢复状态.json`和`快照恢复选择器.py`属于本地恢复治理范围，不进入产品提交。其他模块、生产接口、Velociraptor datastore中无关对象、VM中无关文件/进程和宿主其他虚拟机均排除。

恶意或敏感二进制不得进入Git。P05本身不需要恶意样本；官方Microsoft工具保存在验收VM，现有source Spike字节还作为受保护、未跟踪证据保存在`Logs/P05/source-spike/`，其准确清单和哈希须报告但不得进入index、P05提交树或新增reachable Git object。Velociraptor API凭据仍只在`C:\VelociraptorMCP\secrets\api_client.yaml`；新增HTTP Bearer token是独立的部署者提供secret，只在受保护服务配置中引用。任何报告只记凭据来源类别、`authorization_configured`布尔值和脱敏结果，不记录secret本体或token指纹。

### 0.5 场次快照身份：scenario/index到正式报告的唯一关联

本节由RACC-026既定恢复顺序直接导出，仅补充测试输入与证据契约，不改变0.2节恢复操作、状态迁移或激活条件；覆盖第4章仍把所有新情景锁在Snapshot185的旧约束。P05负责共享schema、index/runner及本节全部正反例，P06只消费。

- `tests/scenarios/schema-v1.json`的`required_snapshot`改为仅允许Snapshot185、Snapshot186两个完整名称的枚举，不能仅把旧const换成186。scenario其余顶层/步骤语义不变。固定index每行新增必需`snapshot_stage`，只允许下表组合，并与scenario的required_snapshot、source SHA、fixture spec逐字段匹配；拒绝其他组合或缺字段。历史schema/index只可读旧报告，不可产生正式证据。
- P05 index保留`p05-flow-triage`（`representative/p05-flow-triage.json`）作为首次验收；新增受审固定变体`p05-flow-triage-network-candidate`（`representative/p05-flow-triage-network-candidate.json`）供两轮候选恢复复用，只有scenario_id/required_snapshot与初验版不同，调查步骤、参数、断言和fixture完全相同。index分别锁定两份实际字节SHA，不能运行时替换snapshot或重写source hash。
- P06的五个既定scenario和`tests/data/p06_scenario_index.json`全部改为完整Snapshot186名称及下表P06_ACTIVE；共享runner不再持有全局Snapshot185常量。P06独立逐工具/资源资格报告也必须使用本节P06_ACTIVE证据绑定，不得只在五情景聚合时检查。

| index.snapshot_stage | scenario/index.required_snapshot | 外层恢复记录及canonical必须满足 |
| --- | --- | --- |
| P05_INITIAL | Snapshot 185-Velociraptor-MCP依赖与情景基线 | 185实际恢复完成；canonical为schema2/epoch3/DEPENDENCIES_ACTIVE且active唯一185。该轮允许随后按P05安装网络环境。 |
| P05_CANDIDATE | Snapshot 186-Velociraptor-MCP网络部署基线 | 已创建的同一186候选实际恢复完成，marker与候选metadata相等；canonical仍为schema2/epoch3/DEPENDENCIES_ACTIVE且active唯一185，不能要求提前激活。两轮restore_attempt_id不同。 |
| P06_ACTIVE | Snapshot 186-Velociraptor-MCP网络部署基线 | 186实际恢复完成；canonical为schema3/epoch4/NETWORK_ACTIVE且active/allowlist唯一186，激活证据hash有效。 |

恢复仍由外层工作流执行。外层在当前Pxx固定证据根（P05或P06的既有workflow Logs根）写入仅本次有效的`current-restore.json`，根exact keys为`workflow_id/run_id/restore_attempt_id/snapshot_stage/snapshot_name/checkpoint_marker/canonical_schema_version/canonical_epoch/canonical_phase/canonical_sha256/restore_records`。`run_id`在恢复前由外层新生成并排他预留，runner仅从此固定文件读取，不接收CLI/environment覆盖；重复run_id/restore_attempt_id正式执行、未知键或旧记录均拒绝。一个restore_attempt可在同一轮内产生HTTP与stdio诊断报告，但两者用不同run_id；候选两个周期和P06五场必须不同restore_attempt_id。

`restore_records`是非空数组，每项exact keys为`kind/path/sha256`，path只能为本Pxx证据根内普通文件的相对路径，禁止逃逸或链接。必须有snapshot metadata、revert操作、启动前VMX marker、恢复后hostname、canonical readback五类原始证据；P06_ACTIVE另需activation证据。各证据由外层在同一restore_attempt_id下实际采集并带该ID；runner复算字节SHA和内容，确认唯一snapshot名称、checkpoint marker、DESKTOP-3FI41GR以及上表状态。marker是恢复前实际VMX读回，不要求内存恢复运行后VMX仍保留marker。恢复记录绑定预留run_id；对同一恢复轮的stdio诊断，外层生成引用同组原始证据的独立run_id记录，不伪造新的恢复周期。

runner初始化正式会话、取得guest服务观察后，在同run目录写`snapshot-evidence.json`，根exact keys为`restore/scenario_id/source_sha256/index_sha256/mcp_session_id/server_instance_id/server_observation_sha256`；restore为上述完整current-restore对象的副本，其run_id必须等于报告run_id。后三项分别等于报告中的真实会话ID、实例ID与观察hash。文件按0.3节canonical JSON规则写入，report根必需`snapshot_evidence_sha256`为该文件字节SHA-256。原始restore_records随同run证据包携带，路径仍相对本Pxx固定证据根；接收方同时复算所有引用，不只信任摘要。snapshot-evidence不含report hash，避免循环hash。stdio采用同样证据结构但coverage空，不贡献恢复正式报告或P06覆盖。

初验、两轮候选和P06验收均须复算`scenario/index → required_snapshot/stage → restore记录及canonical/marker → run_id/session/server观察 → report`。P05 schema/golden与候选激活验证器必须拒绝：错snapshot/stage组合、缺snapshot_evidence_sha256、缺侧文件/原始恢复记录、185报告冒充186、跨attempt marker/会话/服务观察拼接、两轮复用同一恢复记录、候选未激活即提交P06_ACTIVE。P06聚合器实施同一检查并要求五场独立恢复ID；任一不符使对应报告失败且coverage空，不能激活候选或进入P07。此门禁归IMP-P05-003/010/011与ACC-P05-005/007/008，P06归其报告/聚合IMP及ACC-P06-002/004/005/006；缺恢复证据是验收阻断，不允许以外层文字说明替代。


### 0.6 PLAN-CHANGE-011：恢复基线重建与新交接

执行治理更新：同目录19号用户授权取消未来方案第三方复审，本文所有新候选“审核/复审”前置改为主会话完整自评通过；子智能体负责实施，主会话负责修复，实施后由另一独立子智能体核验。旧审核计数/记录不重置、不作为新通过证据。

本版方案完整自评及身份见20号记录。本节保留的草案阶段“未完成自评/时钟修复未确认”等描述仅追溯收敛过程，当前时钟修复入口以下文实测条款为准；下游已由P06 v8 §1.0及P07 v5 §3.1承接。方案通过不代表实现、时钟恢复稳定性、保护链或两周期运行已经通过。代表输入/索引的实际字节SHA在实施生成后、首次业务执行前须由主会话再核对其与本节固定语义一致并冻结，不要求在代码尚未建设时伪填SHA。

跨主机进程身份澄清：正式HTTP runner位于宿主，目标/后端/准备器位于guest，PID只在所属主机内有意义。guest的同一次紧邻进程快照核对目标及guest保护根/父链，宿主另以本地真实PID/create-time核对runner及其父，二者均绑定同一run/请求和主机身份；禁止把宿主PID当guest PID查找，也禁止用同号PID拼接。guest观察结束后下一正式产品操作即为被保护的kill调用，之前取得的宿主身份不得由任意CLI输入；完成后分别复核两侧身份。控制面只做固定只读观察，不能代理正式kill。该澄清落实既定保护集合与外部HTTP拓扑，不增加生产门控或声称跨主机原子性。

来源为同目录 `2026.09.13-17-PLAN-CHANGE-011-P05恢复基线回流登记.md`、总纲 v11 §2.0.1，以及 `Logs/P05/plan-change-011-fixture-82ba2e15-86fd-47a4-aea8-c1930e2317ce/` 的前后原始证据。Snapshot186 的旧 fixture PID 已不存在，后端父进程亦缺失；当前基线不能满足既定进程保护与可恢复验收，不以旧通过记录代替本轮证据。本节覆盖未来执行的旧快照名称、阶段及迁移值；第0.2/0.5和第1～11章中的旧迁移保持历史可读，不再作为新执行入口。四依赖版本、130工具业务契约、fixture 静态规范、权限风险接受和网络边界不变。

**唯一状态迁移与排除。** predecessor 必须精确为现存有效 schema3/epoch4/NETWORK_ACTIVE、active/allowlist 唯一 Snapshot186；新候选完整名称固定 `Snapshot 188-Velociraptor-MCP可恢复验收基线`。Snapshot187 属用户现场，不读取为测试来源、不恢复、不改名、不删除；186 只作本轮 P05 重建前驱，不允许新 P06 正式验收。新状态为 schema4/epoch5/NETWORK_ACTIVE，根及嵌套 exact keys 沿用0.2，active/allowlist 唯一188且 marker 来自其创建后 metadata 实测；retired 必须逐字保留前驱的183/184/Snapshot1各项及顺序并追加186，状态均 manual-only，不把187加入治理集合。任何前驱差异先阻断，禁止自动修正 canonical。

**准备顺序与保护。** 先把当前 P05/P06/P07 证据按原字节、长度、SHA 回传宿主并确认临时接收器退出，再依既定 runbook 完整恢复186。逐项复核四依赖、静态文件/注册表/事件/disabled任务及 ownership/spec/instance；只有第3章允许的“静态全部相等、专用进程不存在”才可重建该进程，不得为满足条件先杀掉存活 fixture。准备器和其父进程必须持续存活至验收后复核结束；后端亦必须有可观察的活父进程。测试专用 `tests/p05_process_parent.py` 只允许固定 frontend/client/fixture 三角色，无任意命令、PID、配置覆盖，无自动重启或产品入口。后端调整前先记录并紧邻复核既有进程 PID/creation/full argv/executable 与本方案固定版本和配置相等，确认无活动 Flow、无本轮活动 Hunt、无 trace；不得终止身份不明进程或历史 Hunt。旧 PAUSED/STOPPED Hunt 单独列为保留现场，不能伪称全部 Hunt 数量为零。只可在精确前驱退出后启动一次对应角色；角色并发锁、启动失败与观测失败均不得导致重复启动或遗失子进程管理。未知锁不可删除绕过。

**创建和每次恢复后的 ready 门。** 实测 hostname、唯一 Windows client、API、专用 MCP 服务身份/监听/ACL/防火墙与既定缓解条件；四依赖身份与本地字节；全部静态 fixture；专用进程 PID/creation/full argv/token 且创建前剩余寿命至少7200秒；后端、准备器、runner 及其父进程身份；无活动本轮资源和 trace；无临时传输监听。kill 调用紧邻前用同一进程快照核对目标与完整保护集合，紧接实际调用，任何缺项、复用或时间不相容均不调用；返回后核对目标消失、Killed 等于输入PID、保护集合 PID/start-time 不变。该检查不声称把仅接受 PID 的后端 API 变成原子身份操作。宿主与 guest UTC 必须以当轮原始读数证明满足 P06 已定120秒门；不能改写证据时间或放宽门槛。当前 VMware Tools timesync Disabled、guest 落后约两天仅为故障证据，时钟修复方式尚待确认，未闭合不得通过 ready 门或创建候选。

**时钟修复的确定入口。** 上段的时钟待确认项已由 `Logs/P05/plan-change-011-clock-8d342270-79c1-4398-ba11-de2e674a2efe/` 的隔离实测收敛：既有 `VMwareToolboxCmd.exe timesync enable` 一次执行后读回 Enabled，guest UTC 与宿主回读相差不足1秒。此证据不等于新候选恢复验证。P05 从186重建时，在准备新进程和正式验收之前，先记录原始时间及 status，仅在明确 Disabled 前态时启用既有 VMware Tools 宿主同步一次；已经 Enabled 只读复核。不得改变 NTP 源、时区、联网配置，不能按对话时间 Set-Date。同步后重新采集 guest UTC 和包围该请求的宿主 UTC，须满足120秒既定门；失败不循环切换、不扩大改动。候选中应保存 Enabled，候选及P06每次恢复必须自动满足该时间门，不允许P06修配置。时间跳变前创建的本轮临时进程不能用旧 creation time 冒充新快照剩余寿命合格；下一轮依完整恢复和第3章允许的准备重新建立。

**初验、两轮和原子激活。** 先从186恢复完成上述准备、ACC-P05-001～007全链与外部正式 HTTP 代表情景。候选创建紧邻前再跑 ready 门，snapshot tree 中188必须零匹配，运行 VM 只创建一次，创建后唯一匹配并记录实际 marker。两次独立恢复188各一次 revert、marker 存在才一次启动，分别完整执行 ready 门、四链、代表情景与服务/网络正负验收；每轮证据回传后才下一轮。杀掉 fixture 后仅依第3章合法状态重建并再次验证，禁止用重建替换该轮恢复初始状态证明。两轮不得复用 restore_attempt_id 或报告；只读检查及单元通过不是完整恢复周期。

两轮全部通过且原始证据可递归重算后，才按0.2既定排他 `.next`、完整写入/fsync、不覆盖显现、验证、原子 replace、目录 fsync、回读流程迁移到schema4/epoch5/188；此前 canonical 始终保持3/4/186。失败只可隔离能证明属于本 attempt 的 `.next`，保留原字节/SHA并证明 canonical 仍有效前驱；未知 `.next` 阻断。禁止删除或重命名任何快照、跳过第二轮、冷启动、把未激活188交给P06。激活证据必须引用初验及两轮完整报告、ready 原始记录、受审输入/实现来源和创建 metadata，接收方逐项重算，不只读取 `passed` 摘要。

**共享报告阶段。** report schema2 的26根键、current-restore exact keys、snapshot-evidence 及六类原始恢复证据沿用0.3/0.5，不增加可由 CLI 覆盖的入口。新增两个固定 P05 代表变体，ID 为 `p05-flow-triage-repair-initial`、`p05-flow-triage-repair-candidate`，业务步骤/参数/断言与原代表情景相同，只差ID和required_snapshot；先固化实际字节/index SHA再审核。五个 P06 输入及独立逐工具/资源资格入口必须同时更新，禁止临时替换字符串。

| index.snapshot_stage | required_snapshot | 同轮原始恢复/canonical 要求 |
| --- | --- | --- |
| P05_REPAIR_INITIAL | Snapshot 186-Velociraptor-MCP网络部署基线 | 实际恢复186；schema3/epoch4/NETWORK_ACTIVE、唯一active186；本轮准备另有差集 |
| P05_REPAIR_CANDIDATE | Snapshot 188-Velociraptor-MCP可恢复验收基线 | 实际恢复同一188候选且marker匹配创建记录；canonical仍3/4/NETWORK_ACTIVE/186 |
| P06_ACTIVE | Snapshot 188-Velociraptor-MCP可恢复验收基线 | 实际恢复188；canonical4/5/NETWORK_ACTIVE、active/allowlist唯一188，完整激活证据有效 |

旧 INITIAL/CANDIDATE/ACTIVE 配对仅供历史报告追溯。新验证器必须拒绝186冒充P06、未激活188、错误schema/epoch/retired/marker、跨attempt拼接、旧报告替代新周期、缺保护父进程、静态漂移、时钟不新鲜和只凭摘要激活。正向连同每类拒绝分支在 Windows 验证。

**归属、失败与完成。** 修复仍归 IMP-P05-002/003/005/008～011 和 ACC-P05-003～008；恢复/证据失败归 FB-P05-003～005/008，未来恢复前驱及状态值均以上述新迁移为准。稳定边为 P05 fixture/保护状态→P06 KillProcess/情景；P05 schema/index/restore/activation→P06逐工具/资源资格/五场/聚合；P06新合格r3交接→P07成本/终检；总纲ACC-020/022/023/026。P05不替代P06全量或P07清理，不关闭任何CHK/AUD。当前草案未完成25项逐条自评，原第10/11章25/25计数只属v6；时钟修复及下游绑定未闭合，v7不具备送审成熟度。完成新规范自评及正式复审后才能创建候选，实施后另开新HEAD独立核验。

### 0.7 Snapshot188 激活原件包的机械边界

本节补齐§0.6递归激活证明的文件接口。20号自评未冻结该接口，不能据其旧计数跳过本节实施、源核对和Windows正反例；真实候选创建/激活门持续关闭，补充自评另立记录。以下文件都是验收记录，不是生产接口或任意输入入口。

**自包含目录及引用。** 在P05固定证据根下排他创建 `activation-188/<uuid>/activation-evidence.json`；目录内全部为无秘密普通文本原件。引用 `Ref` exact keys为 `path/size/sha256`：path是相对该activation父目录的POSIX路径，size为非负整数，sha256为小写64位十六进制；逐分量拒绝绝对路径、空段、dot/dotdot、反斜线、链接/reparse/特殊文件。先解析完整文件图，拒绝循环、重复身份冲突、缺件或字节不等。原P05运行文件按原相对P05根路径完整复制到 `p05-originals/`；报告内部的restore_records等原相对路径只能从此固定子根解析，不修改报告原件。源码副本只放 `source/<repo-relative>`，不复制secret/二进制。P06把整个目录按原字节带入 `restore/<restore_attempt_id>/activation/`，内部相对路径不改；不得只带最外层JSON或回退读取宿主旧绝对路径。

activation根exact keys为 `schema_version/kind/workflow_id/candidate/checkpoint_marker/creation_metadata/initial/candidate_cycles/source_inputs/implementation_sources`；schema_version=1，kind=`snapshot188-activation-evidence-v1`，candidate完整188名称，workflow固定，marker为创建实值。creation_metadata为Ref；initial为一个Phase，candidate_cycles恰两个Phase；三个restore_attempt_id/run_id均非空且彼此不同。

Phase exact keys为 `restore_attempt_id/run_id/report/snapshot_evidence/ready/dependency_acceptance/entry_gate/network_evidence/package_manifest`，后七项均Ref。report为P05共享26根schema2正式HTTP代表报告，status=success、无failure/未执行/失败断言/cleanup，tools/list=130；snapshot-evidence必须按§0.5及§0.6完整join同report、run、session、服务观察、源码/index与原始恢复记录。initial必须是P05_REPAIR_INITIAL/186/canonical3/4/186；两cycle必须P05_REPAIR_CANDIDATE/188/canonical3/4/186，并绑定同一创建marker。canonical在所有阶段仍为前驱，不能循环引用本次activation。package_manifest采用P06已定schema1 members(path/size/sha256)形状，覆盖阶段全部原件且排除manifest自身，逐项枚举和复算；不能以manifest正确替代业务验证。

**来源集合。** Source exact keys为 `repo_path/blob/content`，content为Ref；以原字节复算SHA及Git `blob <length>\0<bytes>` OID，repo_path必须是规范仓库相对路径。source_inputs必须包含当前需求/总纲/P05/P06/P07、19/20及后续补充自评、p05_dependency_manifest/p05_fixture_spec/p05_scenario_index、scenario schema-v1及两个repair代表输入；只接受这些指定文本。implementation_sources必须包含本次实际使用的scenario_runner、p05_dependency_acceptance/p05_external_acceptance/p05_prepare_fixtures/p05_process_parent/p05_real_acceptance/p05_service_host/p05_service_install、p06_evidence、恢复selector、新激活验证器（如有），以及产品bridge/API/core/dynamic/fixed/env模块。集合缺项拒绝；实现新增直接参与取证/验证的源码必须先登记并补入，不能以列表非空代完整。源副本必须等于本次执行冻结身份，不以随后dirty内容替换。

**creation_metadata。** JSON exact keys为 `schema_version/workflow_id/candidate/checkpoint_marker/vmx/tree_before/create_operation/tree_after/metadata_readback`，schema_version=1；后四项Ref。tree_before/tree_after是实际vmrun snapshot tree命令/退出码/stdout/stderr/宿主首末UTC记录，188分别精确零/一匹配；create_operation只含一次实际snapshot命令且成功，禁止前置stop；metadata_readback为实际vmsd相关原字节和解析定位，marker必须从该候选项唯一解析，不接受仅另写一个marker值。vmx固定本实例，真实动作顺序before→create→after/readback可复核，不能以entry_gate代创建证明。

**ready。** JSON exact keys为 `schema_version/workflow_id/run_id/restore_attempt_id/snapshot_stage/snapshot_name/checkpoint_marker/observations`，schema_version=1；observations恰含 `host_clock/guest_identity/fixture_static/fixture_instance/guest_processes/host_processes/parent_bindings/dependencies/service/acl/firewall/resources` 十二个Ref。每份原件保存实际请求/命令、开始结束时间、退出状态和未丢字段的响应；validator解析实际返回数据，不接受只有passed/ok/摘要或脱离原始返回的手写expected值。

逐项谓词固定：host_clock为包围guest_identity读请求的真实UTC区间，guest UTC在区间扩展±120秒内；hostname精确匹配且API唯一Windows client为既定身份；fixture_static逐项对spec和instance复算文件哈希/大小、registry键值、事件source/id/message唯一性、任务disabled/no-trigger/fixed-action与ownership/无reparse；fixture_instance保持§3原形状；guest_processes为目标和保护成员的实际PID/ParentPID/UTC creation/full argv（仅批准对象完整命令行），host_processes为宿主runner/父对应原件，parent_bindings将fixture→本次PowerShell→persistent Python和后端→persistent Python精确join启动原件，来源hash/时间和PID复用拒绝，寿命门仍7200秒；dependencies逐字段重算§1/2已锁inventory/artifact/本地字节；service/acl/firewall分别按§0.4及PC006/007核对实际服务账户、单实例/端口、权限ACL及唯一规则/精确RemoteAddress，不保存secret正文或指纹；resources保存真实owned Flow/Hunt状态清单和trace状态/临时监听，活动owned资源为零、trace inactive、临时监听为零，历史PAUSED/STOPPED对象单列保留。任一原件不能解析到这些事实即拒绝ready，不退化为存在性检查。

**dependency_acceptance。** JSON exact keys为 `schema_version/workflow_id/run_id/restore_attempt_id/inventory_artifacts/four_chains/network_observations`，schema_version=1，后三项Ref。inventory_artifacts继承既有 `p05-dependency-acceptance-v1` 的完整before/after/selected override/probe/local_public_bytes/locked artifact记录，复算§2谓词，inspect/仅ok不可通过；four_chains继承 `p05-real-acceptance-v1` 实际calls/arguments/result/flow_states/kill_identity/trace/owned资源原形状并保留原始SDK消息，不允许删除stderr或覆盖失败attempt。四链必须分别闭合PacketCapture start/stop各真实FINISHED、start结果ETL→stop参数→ETL/PCAPNG清单且前后trace/所有权成立；Autoruns FINISHED及results；Triage FINISHED/results/files；Kill输入PID→真实Flow/结果Pid/Killed→目标消失且两侧保护身份不变。network_observations必须含调用窗口内server和client的DNS/TCP/日志或等价完整网络取工具观察原件、PID/time/source；只允许loopback/既有server-client/本地public，禁止原始公网来源请求。只有开始结束两次连接列表或URL外观不构成完整网络窗口证据。

**entry_gate与network_evidence。** entry_gate是HTTP入口正负例，不是snapshot创建。其exact keys为 `schema_version/workflow_id/run_id/restore_attempt_id/cases`，cases恰含 `no_origin/allowed_origin/missing_bearer/wrong_bearer/missing_host/wrong_host/wrong_origin`，每项Ref指向原始HTTP请求脱敏元数据、实际status/headers/body及handler调用计数前后原件。成功两项必须真实initialize成功且有session/instance头；Bearer缺/错401、错误Host421、错误Origin403，缺Host按HTTP解析层拒绝（400或入口421）且不进入handler；负例handler增量零。token只记已配置布尔值，不保存Authorization内容或指纹。

network_evidence exact keys为 `schema_version/workflow_id/run_id/restore_attempt_id/non_allowed_source/schema_identity/service_observation`，后三项Ref。non_allowed_source完整保存PC006三要素（绑定非允许源连接失败、28790/28787双端口对照、精确防火墙规则）同轮原件，不以单一超时替代；schema_identity保存双方真实HTTP/stdio tools/list原件、同算法规范化130工具name/inputSchema/outputSchema和各自hash，复算两者相等；service_observation保存正式报告全部实例头与guest PID/start/instance/executable hash的join，单场漂移拒绝。采集器/validator的独立正反例必须逐一证明上述拒绝分支，不接受客户端自报true。

任何尚无原始格式解析器、未取得完整窗口、缺少文件或只可读历史summary的类别一律不能激活；实施方保留失败原件并交主会话修复，不补造success。此规则不会把本节尚未实施的强校验描述成已有能力。

### 0.8 PLAN-CHANGE-012：187准备基线的显式接纳与188激活

本节优先于§0开头的暂行只读门及§0.2/0.5/0.6/0.7中旧状态配对；解除实施门仍以本版完整自评和实现正反例为前提。用户187作为准备起点并非产品验收通过。旧状态原件、失败记录、旧审核均保留，不追改或借用旧通过结论。四链、十二类ready原件、网络正负例、两个候选完整周期及源集合闭合要求全部继承§0.7，除下面明确列出的状态/来源变化外不变。

#### 0.8.1 对象与状态

目标只为本方案既定VMX、MAC `00:0c:29:83:b8:65`、固定IP `192.168.204.232`。用户保留起点为 `Snapshot 187-固定IP+WindowsMCP开机自启`，初次接纳时snapshot tree必须仅有此一个对象，实际vmsd UID/current=3且marker=`Win10MalBox-Velo-Snapshot3.vmsn`；metadata和guest MAC/IP/DHCP=false须有同轮原件。hostname单独不能认定对象。禁止改固定IP、WindowsMCP自启、删除/改名/覆盖187或操作其他VM。

恢复治理新格式为schema_version=5。schema4仅属未激活的旧PC011候选设计，禁止作为新入口；此处不是声称旧schema3运行状态正常升级，而是对用户删除旧快照后的显式管理接纳。新canonical根exact keys为 `schema_version/workflow_id/epoch/phase/active_snapshot/automatic_restore_allowlist/retired_snapshots/baseline_evidence/activation_evidence`。active_snapshot/allowlist/retired的字段形状沿用§0.2；workflow保持本方案固定值。

| 状态 | schema/epoch/phase | active及唯一automatic allowlist | activation_evidence |
| --- | --- | --- | --- |
| 用户起点已接纳、仅可P05准备 | 5 / 5 / PREPARATION_BASELINE | 用户187及其已实测marker | 必须为null，不得声称产品通过 |
| 验收基线已激活 | 5 / 6 / NETWORK_ACTIVE | Snapshot 188-Velociraptor-MCP可恢复验收基线及创建实值marker | §0.2的完整引用，必须指向§0.7全链激活包 |

两个状态baseline_evidence均为同一不可变接纳记录引用，exact keys `source/evidence_path/evidence_sha256/recorded_at`，不允许null。引用路径限本项目P05固定证据根的无链接普通文件，时间是实际接纳UTC。准备状态retired必须原样保留旧canonical的183/184/Snapshot1顺序并追加186；激活状态再追加187，全部状态值为MANUAL_ONLY_REQUIRES_NEW_USER_AUTHORIZATION。此为自动恢复资格变化，不授权删除快照。188激活后187继续物理保留，但不能常规自动恢复；不把已删除历史快照误称物理存在。

#### 0.8.2 接纳记录与原子写入

接纳记录固定存于P05证据根 `baseline-adoption/<uuid>/adoption.json`。根exact keys为 `schema_version/kind/workflow_id/adoption_id/vmx/snapshot_name/checkpoint_marker/old_canonical/snapshot_tree/metadata/guest_identity/source_inputs`；schema_version=1，kind=user187-preparation-adoption-v1。old_canonical/tree/metadata/guest_identity采用§0.7 Ref，目录自包含且拒绝链接/逃逸。old_canonical保留当前schema3/epoch4/186原字节，不要求其旧激活声明通过新验收；tree/metadata/identity必须来自本次真实只读请求、时间与退出码，不复制旧摘要。source_inputs采用Source形状，包含页首`CURRENT-PC017-018-019-R02`九规范与`PLAN/2026.09.20/current-normative-inputs-pc019-r02.json`、PC012登记、08号用户互传授权、本版完整自评及实际selector源码；旧需求/总纲/Pxx、旧作者组与PC017候选组仅可另列历史输入。

接纳仅可在旧canonical字节与接纳包old_canonical逐字一致、当前没有未知.next、对象身份和全部接纳输入通过时执行。先排他创建证据副本并fsync，再以既有排他.next、完整写/fsync、验证、原子replace、目录fsync和回读流程写5/5准备状态；变更前后字节另留原件。任意旧状态漂移、已接纳重复执行、来源不明.next或非唯一187拒绝。不能删除旧canonical后凭空新建，也不把接纳操作计作候选恢复周期。接纳失败不得触碰VM；有本attempt所有权的.next才可按§0.2隔离留证。

#### 0.8.3 准备、初验及候选周期

先完整回传当前guest本项目验收证据并确认临时传输监听退出；秘密配置及已知可能含秘密的历史环境日志不进入传输包，只列排除理由。按用户既定单次revert→精确VMX marker→单次启动→身份/环境门恢复187，生成新的restore_attempt_id；用户此前自行恢复不代替此轮证据。

恢复后只在P05准备阶段补齐已证明缺失的既定服务/后端/依赖/fixture，不扩大产品或权限。现有凭据只在guest读取使用，不生成新token；部署文件互传按08号执行并校验字节。完整执行§0.4安装/ACL/精确防火墙门和PC007风险接受缓解条件。后端缺失时允许在核对固定二进制/配置、无同角色进程和未知锁、保留数据身份后启动一次对应persistent父角色；存在时按§0.6精确身份检查，不以重启掩盖失败。静态fixture必须与原spec/instance匹配，只有专用进程已不存在才按原确定步骤重建，不杀掉存活fixture制造前提。依赖缺件按原锁定来源/字节准备，定义或身份漂移则回流，不另选版本。

时钟仅沿用§0.6已限定的VMware Tools一次同步路径：实际Disabled才启用，已Enabled只读，±120秒失败不调NTP/时区/IP或伪填时间。完成全部Windows源码测试、ready与ACC-P05-001～007及外部HTTP代表初验后，紧邻创建前再次核实ready及专用进程剩余寿命7200秒。tree中188零匹配才在运行VM上创建一次候选，读取新marker；187保持原样。

候选两轮各从同一188单次完整恢复，分别取得全部ready、四链、正式HTTP代表、权限/ACL及网络正负原件；证据先回传再下一轮，两个restore_attempt_id和report run_id必须独立。canonical全程保持5/5/187。每轮恢复初始进程状态必须直接合格，不能先重建再声称恢复合格；该轮kill后只按原允许条件准备下一次快照前状态，不更换候选快照。任一失败保留原件、不激活；需要改变候选内容时回P05重新裁决候选代次，不能覆盖同名188或悄悄沿用旧一轮。

两轮完整成功且§0.7全图语义校验通过后，唯一业务激活为5/5/PREPARATION_BASELINE/187→5/6/NETWORK_ACTIVE/188，按原.next原子协议执行；baseline_evidence保持不变。失败回读仍为准备状态，仅隔离本attempt.next。不允许5/5直接P06、3/4或4/5冒充、旧marker、缺原件、摘要passed、重复周期或跨代次拼接。

#### 0.8.4 共享配对与来源闭合

report schema2的26根键、current-restore及snapshot-evidence exact keys不改变。固定P05代表业务步骤/参数/断言保持一致，只更新索引/required_snapshot及阶段证据配对，按实际字节重算SHA；无CLI覆盖。新配对如下，旧配对仅供历史读取且不输出正式coverage：

| snapshot_stage | required_snapshot | canonical |
| --- | --- | --- |
| P05_REPAIR_INITIAL | Snapshot 187-固定IP+WindowsMCP开机自启 | 5/5/PREPARATION_BASELINE/187，接纳原件有效 |
| P05_REPAIR_CANDIDATE | Snapshot 188-Velociraptor-MCP可恢复验收基线 | 5/5/PREPARATION_BASELINE/187，同一候选创建marker |
| P06_ACTIVE | Snapshot 188-Velociraptor-MCP可恢复验收基线 | 5/6/NETWORK_ACTIVE/188，接纳及完整激活原件有效 |

所有阶段恢复证据新增kind=baseline_adoption原件（及递归接纳目录）；原有必需kind不删。P06原六类恢复证据因此变为七类，封包/接收/聚合递归带入接纳与激活两个自包含目录，不能仅引用宿主绝对路径。§0.7 activation顶层形状不改，initial和两cycle按本节配对解析，不能接受旧186初验。source_inputs新增PC012登记、本版自评与08授权；19/20仅历史来源，不作为当前通过许可。implementation_sources另必须列入实际参与的velociraptor_transport、p05_ready_evidence、service ABI/lifecycle及恢复/激活校验测试源码，完整集合在首次业务前冻结；直接依赖增加即重新固定和回归。

P06全部scenario/index、资源资格/逐工具、策略和聚合只能消费上述配对，重新资格和五场650关系，旧预算不得沿用；P07只消费新P06最终selection，并从已激活188执行五组上游成本配对。尚未实现的原始证据谓词保持fail-closed，不能先删阻断后补证据。旧14/25项及80/67计数不自动转成新自评；本版源变更后必须重新固定下游身份和受影响保真。

## 1. 已证实入口事实与冻结来源

### 1.0 PC017固定分诊来源、预置与资源门候选

`PC017-TRIAGE-SOURCE`原始来源仍是Velocidex官方URL `https://triage.velocidex.com/docs/windows.triage.targets/Windows.Triage.Targets.zip`；生成物内嵌原标识`Commit cd5eaba on 2026-06-18T17:04:14Z`，这里只保留其追溯含义，不声称本轮独立验证该commit。原ZIP为44427 bytes/SHA-256 `44e59ea3a35db2b1e582cfe589c1f511b69eabc5931c4db96928bb7ba147acbb`，原YAML为80977 bytes/SHA-256 `1a699aa9d5346fe342fb7c646bdfc755e110cf745658fab35499980fc0bbbb1c`。在此来源上追加唯一派生身份`Windows.Triage.Targets+pc017-thumbcache-ref.1`：候选YAML 80877 bytes，SHA-256 `42f396d67dccb06d7e5c4c03d4d24b2434b0bbda2d8a0c7ae1757f0a2a7f44bc`，只把`_BasicCollection`中的`Ref: Thumbcache`改为`Ref: ThumbCache`。候选不得冒充原作者发布；解压语义图从26 targets/122 leaves变为27/123且只新增`ThumbCache/Thumbcache_DB`，其他target、glob、源文件采集与`HashOnly`默认逐项零差异。

安装仍只归P05：离线导入前核对官方来源追溯、候选字节、派生标识和唯一字符差异，导入后核对root Org唯一CLIENT定义、公开参数、raw定义与新锁定版本；MCP不联网安装或运行期替换，P04产品也不增加派生hash运行期gate。不得改其他依赖、恢复/启用旧快照或伪造Snapshot188原件。P05初验和两个候选恢复周期都必须完成真实Triage回填，全部成功后才按§0.8原子激活；P06只消费已激活基线做正式验收。受控现场试验与P06资格分开；PC016历史原件搜索暂停和实施后独立核验保留。

每次P05初验/候选周期的受控真实分诊前，由验收工作流紧邻启动重新取得唯一目标身份、frontend实际datastore/filestore所在卷、该同卷可用字节与可用内存；仅该次执行前preflight要求同卷`>=13958643712` bytes（13 GiB）且内存`>=1073741824` bytes（1 GiB）。13 GiB为`3 × 4294967296 + 1073741824`的保守筛查，不是产品配额、预分配或最大物理占用证明，也不新增产品每次调用的资源阈值错误接口。任一身份/卷绑定不可证或阈值不满足，验收工作流即不创建Flow，且不删历史数据、不恢复快照、不自动扩盘。

### 1.0.1 PC017文件fixture与回填边界

P05只准备可重复fixture和受审期望，不改变生产schema或把fixture结果冒充已运行验收。固定集合必须覆盖：普通data；28data+1个精确idx配对（期望28个逻辑原文件且warnings为`sparse_indexes_internal:1`）；普通天然`.idx`；完全重复data/idx；孤儿idx、未知/非字符串Type、顶层/嵌套Type冲突、同身份异元数据、多义配对、负数/bool/非整数/`uploaded_size>file_size`、client/flow作用域错误，以及天然`.idx`与派生selector冲突和Windows大小写等价别名碰撞。每项保存完整uploads metadata、预期data顺序、file_id identity canonical bytes/hash和预期过滤reason，不保存秘密或取证原件内容。

读取fixture包括普通非空、空文件、逻辑16字节/紧凑7字节且首中尾均含洞的合成稀疏文件、紧凑截短、逻辑长度/hash不符、开始/发布前metadata漂移、ERROR Flow中已完整文件，以及路径/覆盖/I/O故障。故障矩阵按`os.link`提交点分账：提交前读取/长度/fsync/metadata/路径/link失败不得由本请求创建新完成文件，正常清理无part；提交前原始故障叠加part清理失败允许owned part残留，保留原错误并附记`download_temp_cleanup_failed`。link成功后复核注入OSError以及link成功后part unlink注入OSError均期望`BACKEND_ERROR/download_post_publish_failed`且不返回成功payload；前者在无外部替换时保留正确完成字节并正常清part，后者允许正确完成文件与本次part同时保留。路径/所有权不可信时不得继续清理，并以外部哨兵及其他请求对象未删除为断言。并发另设有效fixture：两个请求竞争同一不存在目标且其他前提全部有效，期望恰一正确发布、另一`ALREADY_EXISTS`，败者不覆盖/删除赢家，最终正确目标恰一个、双方part清零；已有目标fixture保持原字节并返回`ALREADY_EXISTS`。不能把提交前零新完成文件泛化到提交后，也不能把合法竞态混写为全失败。fake backend逐调用记录精确identity、Padding、offset、请求长度和返回长度，使P04/P06可证明每块不超过1 MiB、0字节EOF、紧凑预读不落盘、逻辑重建长度/hash正确。fixture不得伪造后端原子快照、索引真实大小、产品源hash或任意本地特权攻击防护；服务器idx原件不删除不改写。P05初验及两个候选恢复周期的真实Triage回填同时保存真实list/download链，仍遵循“真实回填三次→激活→P06”，不因文件fixture提前宣称实现通过。

PC018/019下游fixture：分页准备两个内容不同且各至少两行的合法Flow，以及同一Flow两个合法source；相同`v1:1`在每个显式上下文分别取第二行，同时覆盖未知Flow、非法source、非法编码、超范围offset、多source省略和全页无漏重。分页fixture必须分开三类：空源`offset=0`和`offset==len`均断言`pagination.next_cursor`字段存在且为JSON `null`；正常非终页断言字段存在且为合法规范cursor字符串；无分页操作断言整个`pagination`不存在。三条当前依赖链Autoruns/Triage/KillProcess及四个固定启动SDK转录必须把启动响应与随后业务终态分字段保存：collect_file、collect_forensic_triage、kill_process断言`status="success"`及required非空真实`state`，start_hunt断言Hunt `state`及required非空真实`flow_state`且不可串换；WAITING/IN_PROGRESS/FINISHED/ERROR/其他非空新值逐字保留，缺失/空值为BACKEND_ERROR。启动success不代替FINISHED，初始值与终态不要求相同，ERROR初值不计业务成功；每个启动只允许一个Flow且不得等待终态。独立golden、stdio/HTTP tools/list和报告`tools_schema_sha256`均须从新schema重算，旧hash/旧报告只作历史。本勘误不增加第五个输出字段；PC019相对既有P04契约仍只增三个`state`和一个`flow_state`。

1. 当前唯一自动恢复基线为`Snapshot 184-Velociraptor-MCP测试基线`，状态机schema v1/epoch 2/`INSTALLED_ACTIVE`，checkpoint为`win10h2-MalBox-20241110-Snapshot100.vmsn`。`Snapshot 183-FakenetNG测试专用`为人工回退，未经新授权不得选择。
2. P04最终生产面为118动态+12固定=130，Win10VM 44/44测试、stdio协议和真实Flow/Hunt/file链通过；`Windows.Network.PacketCapture`、`Windows.Sysinternals.Autoruns`、`Windows.Triage.Targets`、`Generic.Utils.KillProcess`是仅有的四项已知依赖缺口。
3. 官方Velociraptor文档确认第三方工具由inventory按名称/版本管理，可由`inventory_add()`运行时预置；自定义artifact可由`artifact_set(definition=...)`写入root Org全局repository。P05只调用这些现有server VQL原语，不给MCP增加依赖管理接口。
4. 来源身份Spike已在`DESKTOP-3FI41GR`核验，原始证据为`Logs/P05/source-spike/p05-source-spike.json`。所有字节只有同时满足下表大小和SHA-256才可进入安装；不允许实施时改为“最新版”。

| 依赖 | 规范来源与身份 | 大小 / SHA-256 | 额外门禁 |
| --- | --- | --- | --- |
| `etl2pcapng` | Microsoft `microsoft/etl2pcapng` release `v1.4.0`，GitHub asset id `24927556`，artifact原URL `https://github.com/microsoft/etl2pcapng/releases/download/v1.4.0/etl2pcapng.zip` | zip 151414 / `2acda1a09c8d2f54086c05cc53f0a120f7ac16ae8ee691154e9bf9f44a8970a9` | x64 exe `e7ce9596e3383b073a42897a2a9f94dcee4e21306ca4a0b5e5d73558a7897cbb`；x86 exe `8eb04014a31981c39a073383f300a02dec8241bdfdb50dfde7957c58027d9ae7`；该历史release未签名，必须以官方asset id+zip/exe双哈希固定 |
| `Autorun_386` | `https://live.sysinternals.com/tools/autorunsc.exe`，取证时产品/文件版本14.3 | 1317184 / `a2394a11272ae38e7fb6ea862e783daa6d704a3d9c73a51dfe978b7692df6b1f` | Authenticode=`Valid`且subject精确`CN=Microsoft Corporation, O=Microsoft Corporation, L=Redmond, S=Washington, C=US` |
| `Autorun_amd64` | `https://live.sysinternals.com/tools/autorunsc64.exe`，取证时产品/文件版本14.3 | 1460024 / `093d1c6b91d280c9644635c667753e0aa0e78c656eb4082147bf30364787d96f` | 同上Microsoft有效签名；运行时只用当前Windows AMD64项，但两项都按artifact定义预置 |
| `Windows.Triage.Targets` | Velocidex官方`https://triage.velocidex.com/docs/windows.triage.targets/Windows.Triage.Targets.zip`；生成物内嵌原标识`Commit cd5eaba on 2026-06-18T17:04:14Z`（仅保留原件追溯，不声称本轮独立验证commit）；在原来源上追加PC017派生`Windows.Triage.Targets+pc017-thumbcache-ref.1` | 原zip 44427 / `44e59ea3a35db2b1e582cfe589c1f511b69eabc5931c4db96928bb7ba147acbb`；原YAML 80977 / `1a699aa9d5346fe342fb7c646bdfc755e110cf745658fab35499980fc0bbbb1c`；派生YAML 80877 / `42f396d67dccb06d7e5c4c03d4d24b2434b0bbda2d8a0c7ae1757f0a2a7f44bc` | 派生身份不冒充上游；只修正Thumbcache Ref大小写；26/122→27/123只新增ThumbCache/Thumbcache_DB；import前后root raw定义与完整_BasicCollection目标图锁定 |
| `Generic.Utils.KillProcess` | 本方案按REQ-014/总纲FB-012定义的本地最小artifact，不从网络取得 | 实施前由独立golden测试锁定正文规范，实施后记录文件和root定义raw SHA-256 | 只能有`Pid: int`一个公开参数；Windows CLIENT；唯一动作是`pskill(pid=Pid)`；不得枚举/匹配/结束其他进程，不得联网、执行外部程序或接收命令文本 |

来源URL只用于追溯，安装必须消费已核验的固定字节。若任何大小、哈希、签名、版本、压缩布局、artifact名称/参数或编译结果不符，停止P05并按FB-P05-001处理，不接受新字节或静默改版本。

### 1.1 P05准备状态机与唯一允许的首次转换

IMP-P05-004开始前必须先用canonical选择器取得Snapshot184，按恢复契约完成一次且仅一次`revertToSnapshot`、checkpoint marker、`vmware -x`、`DESKTOP-3FI41GR`、API和唯一client门；不得从当前可能含P03/P04/Spike临时状态的运行VM直接安装。此次恢复定义一个新的`attempt_id`，所有写入、差集和证据均绑定该ID。

恢复后必须把三项inventory、两个同名artifact查询和fixture根记录为predecessor快照。三项inventory只接受P01 `artifact-inventory.json`已经证明的准确前态：

| 名称 | 只允许的predecessor identity |
| --- | --- |
| `etl2pcapng` | `artifact=Windows.Network.PacketCapture`、`filename=etl2pcapng.zip`、`version/hash/expected_hash/invalid_hash`均空、`admin_override=false`、`materialize=false`、`serve_locally=false`、原始GitHub URL |
| `Autorun_386` | `artifact=Windows.Sysinternals.Autoruns`、`filename=autorunsc.exe`、上述version/hash状态均空、`admin_override=false`、`materialize=false`、`serve_locally=true` |
| `Autorun_amd64` | `artifact=Notebooks.Demo`、`filename=autorunsc64.exe`、上述version/hash状态均空、`admin_override=false`、`materialize=false`、`serve_locally=true` |

只有`predecessor -> 本方案locked identity`的一次转换被允许。已由运行态证据确认：`inventory_add()`不会删除内置来源行，而是为同名工具新增一条`admin_override=true`的有效覆盖行；因此目标态允许每个名称保留其唯一、未修改的predecessor来源行，同时必须恰有一条与本方案locked identity逐字段相符的有效覆盖行。这两行不是冲突或重复安装，工具解析以覆盖行为准。已是全部目标identity的同attempt重跑只能逐字段复核覆盖行并返回`NO_CHANGE`；覆盖行缺失、多于一条、字段不符，或predecessor来源行被改动时立即拒绝，不覆盖。两个artifact只允许从精确0项到本方案定义各1项，或对同一raw/compiled hash执行`NO_CHANGE`；不同的自定义同名定义不得被`artifact_set`无警告覆盖。

每个写操作前后均保存三项inventory、两项artifact和fixture批准根的最小差集。任一写入、编译、readback或后续真实链失败时，先把证据回传host，再按同一Snapshot184完整恢复链开始新的attempt；不得以卸载、删除artifact、手工删fixture或逆向覆盖模拟事务回滚。Snapshot185的创建证据必须反向绑定这一干净起点、唯一转换轨迹和全部成功readback。

## 2. 四项依赖的安装与真实成功契约

### 2.1 固定落盘与inventory

候选字节由只绑定`192.168.204.1:28786`的临时host-only单文件服务传入`C:\VelociraptorMCP\dependencies\p05\staging`，压缩包及其成员双端SHA-256一致后才继续，服务立即停止并回读端口不可达。最终固定字节留在`C:\VelociraptorMCP\dependencies\p05\locked`，任何HTTP/公网行为不得发生在产品MCP或情景运行中。

通过现有root API执行以下等价VQL，不把本地绝对路径写入产品代码：

```vql
SELECT inventory_add(tool="etl2pcapng", version="1.4.0",
                     filename="etl2pcapng.zip", serve_locally=TRUE,
                     hash="2acda1a09c8d2f54086c05cc53f0a120f7ac16ae8ee691154e9bf9f44a8970a9",
                     file=<locked-etl-zip>, accessor="file") AS Result
FROM scope()
```

`Autorun_386`和`Autorun_amd64`分别用版本`14.3`、准确filename、对应hash和locked file执行同型`inventory_add`。`inventory_add`由root server读取locked文件，执行主体必须具备`SERVER_ADMIN`；file参数不是client路径。

每项转换后先完整保存`inventory()`的来源行和覆盖行，再执行一次`inventory_get(probe=TRUE)`验证解析出的实际工具定义，随后重新读取。成功identity以每个名称唯一的`admin_override=true`覆盖行为准；保留的内置来源行必须仍精确匹配第1.1节predecessor。覆盖行必须逐字段记录`name/version/filename/admin_override/materialize/hash/expected_hash/invalid_hash/serve_locally/serve_url/versions/filestore_path`，要求版本、文件名和hash与第1章一致，`admin_override/serve_locally=true`、`materialize=false`、`invalid_hash`为空、serve URL仅为已知server本地public URL。这里`materialize=false`是已证实的成功状态：`inventory_add(file=...)`已把锁定字节直接写入filestore，无远端来源需要再物化；有效性由`inventory_get(probe=TRUE)`返回的Definition、HASH、FILENAME和本地URL精确匹配证明。随后还须读取本地public URL实际字节并计算SHA-256，不以URL外观代替缓存证明。

网络观察分为互不混淆的两个窗口：host-only字节传输窗口单独记账；安装完成后的首次`inventory_get(probe=TRUE)`、首次FetchBinary及PacketCapture/Autoruns真实Flow从调用前到终态后，分别采集Velociraptor server与client的DNS、TCP连接和服务日志。只允许loopback、已知server本地public URL以及既有server/client通道，明确断言`github.com`、`api.github.com`、`live.sysinternals.com`、`triage.velocidex.com`及第1章原始URL零请求；观察证据必须有开始/结束时间、进程PID/start-time和数据源。无法获得等价的server+client观察面时该ACC被阻断，不用“看起来是localhost”判通过。

### 2.2 两个artifact

`Windows.Triage.Targets.yaml`先离线校验唯一entry、名称、参数choices和SHA-256，再以`artifact_set(definition=<exact UTF-8 YAML>)`导入root全局repository。`Generic.Utils.KillProcess.yaml`先通过独立golden、VQL编译与禁止能力扫描，再同样导入。导入前两项精确查询均为0；导入后必须各为唯一CLIENT定义，raw规范哈希、公开参数和P03 metadata映射均与本节相符。

KillProcess的独立手写golden固定为：`name: Generic.Utils.KillProcess`、`type: CLIENT`、Windows precondition `SELECT OS FROM info() WHERE OS = 'windows'`、`required_permissions: [EXECVE]`、唯一公开参数`Pid`且`type: int`、无tools/imports/exports、唯一source名`KillProcess`，唯一query为`SELECT Pid, pskill(pid=Pid) AS Killed FROM scope()`。编译后仍必须只有同一precondition/permission/参数/source/query；结果必须含输入Pid，且`Killed`精确等于输入PID。`p05-attempt-009`已证实当前Velociraptor的`pskill()`成功返回被结束的PID整数而非布尔值；返回值不等于输入PID、VQL错误或Flow `ERROR`均失败，不能只凭Flow结束判成功。

重复执行P05准备脚本必须幂等：相同identity只复核不改变；不同identity拒绝覆盖。P05不提供卸载或删除工具，不修改其他inventory/artifact。

### 2.3 四条真实功能链

- PacketCapture：开始前以固定只读PowerShell `netsh trace show status`取原始输出并要求全局trace为inactive；否则本attempt阻断，绝不调用stop。动态工具以`StartTrace=true`创建并完成Flow，从真实结果取得本attempt ETL路径；只有随后只读状态为active且时间/ETL路径与本次调用一致，才登记`trace_owned_by_attempt=true`。随后同一MCP会话以`StartTrace=false/TraceFile=<引用>`创建第二个Flow，终态`FINISHED`，结果或uploads包含PCAPNG和原ETL。正常路径必须由这个stop/convert Flow结束trace；若MCP失联，仅允许P05外层固定准备脚本在`trace_owned_by_attempt=true`时执行一次`netsh trace stop`，scenario JSON不得携带该命令。所有正常/失败路径最后回读inactive、ETL/PCAPNG identity和批准临时目录差集；无法证明所有权时不停止未知trace，而是回传证据后从Snapshot184重新开始attempt。不得修改VM联网能力。
- Autoruns：调用`Windows.Sysinternals.Autoruns`并等待真实Flow `FINISHED`，读取至少一个结果source；后端工具取自本地inventory，网络观测无原始URL访问。
- Triage：无参数调用`collect_forensic_triage`，确认真实Flow `FINISHED`；`get_flow_results`和`list_flow_files`链均成功。允许fixture环境下某些source为空，但必须存在已知source且整个Flow无错误。
- KillProcess：准备脚本只启动一个命令行含唯一workflow/attempt token、执行固定sleep的专用Python进程；调用紧邻前再次核对PID、creation/start-time、完整命令行token与manifest均相等，任一变化或PID复用都拒绝调用。受保护集合至少含PID 0、PID 4、Velociraptor server/client、本次runner/准备脚本执行器及各自父进程的PID+start-time。调用`kill_process(pid=<instance pid>)`后Flow `FINISHED`且结果`Killed`精确等于输入PID，目标token/PID消失；全部受保护PID及start-time不变。`Killed`不等于输入PID、查询错误、Flow ERROR、错杀或未杀分别记录且均不计成功。

以上任一错误、ERROR终态、伪ID、缺结果链、错误进程、外网取工具或未清理的trace均不计成功并停止进入快照创建。

## 3. 固定fixture与可恢复状态

tracked对象`tests/data/p05_fixture_spec.json`是不可变`fixture-spec-v1`，只含相对名称、固定内容/大小/SHA-256、对象全名和值、生成器版本和禁止范围；它不含绝对guest路径、PID或时间。运行态对象固定为`C:\VelociraptorMCP\fixtures-p05\fixture-instance-v1.json`，不进入Git；它含schema、spec SHA-256、workflow_id、attempt_id、ownership marker、hostname、批准根、各绝对guest路径、实测hash、注册表/事件/任务readback以及进程PID/creation-time/token/interpreter/argv。runner只能在读取已索引scenario后，从scenario固定声明的spec SHA推导这个唯一instance路径并逐字段联结；CLI不能另传manifest路径。

`tests/p05_prepare_fixtures.ps1`只可在Win10VM运行，批准根是脚本内由固定常量组合出的`C:\VelociraptorMCP\fixtures-p05`，CLI只接受workflow_id和attempt_id，不接受根覆盖。它拒绝空/驱动器根/UNC、reparse、已有无正确ownership marker的目录和目录外写入，并按tracked spec幂等创建：

- `files/ascii.txt`固定ASCII字节、`files/utf8 空格.txt`固定UTF-8无BOM字节、`files/dir-a/same-name.txt`和`files/dir-b/same-name.txt`固定不同字节、`files/fixed.bin`为0..255重复四次共1024字节；全部预期SHA-256由spec静态锁定，generator逐项复算；
- 注册表键固定为`HKLM:\SOFTWARE\VelociraptorMCP\P05\<workflow_id>`，值`Owner=<workflow_id>`、`Attempt=<attempt_id>`、`FixtureVersion=1`；只操作该键；
- Application事件固定source=`VelociraptorMCP-P05`、event id=`42005`、entry type=`Information`、message=`<workflow_id>|<attempt_id>|fixture-v1`；同attempt只有一条，按source/id/message三元组判重；
- 计划任务全名固定`\VelociraptorMCP\P05-Fixture`，XML设置`Enabled=false`且无trigger，action仅为`%SystemRoot%\System32\cmd.exe /d /c exit 0`，无网络、下载或外部payload；P05不启用或运行它；
- 专用进程解释器必须是guest repo当前锁定`.venv\Scripts\python.exe`的规范路径，argv固定为`-c "import time; time.sleep(86400)" <workflow_id> <attempt_id>`，不得监听网络；创建快照前紧邻复核其PID、creation-time、完整argv/token且剩余存活门为至少7200秒。

状态转换只有：批准根不存在时首次创建；同spec/同workflow/同attempt且全部静态对象相等时`NO_CHANGE`；只有专用进程不存在时允许在不改静态对象的前提下重建进程并原子改写instance；任何对象冲突均拒绝。instance使用同目录排他临时文件、flush/fsync和无覆盖原子完成；准备前后只比较上述批准对象差集。任一失败按1.1节恢复Snapshot184开始新attempt，不以删除局部对象重试。恶意样本不是P05通过前提，也不创建伪恶意二进制。

## 4. 声明式情景schema与runner契约

### 4.1 schema v1

`tests/scenarios/schema-v1.json`必须是完整Draft 2020-12 JSON Schema。每个情景只允许以下顶层字段且`required`为全部字段、`additionalProperties=false`：

- `schema_version: 1`；
- `scenario_id: string`，跨全部文件唯一且只含`[a-z0-9][a-z0-9_-]{2,63}`；
- `business_purpose: string`，非空；
- `required_snapshot: string`，P05代表情景先写候选`Snapshot 185-Velociraptor-MCP依赖与情景基线`；
- `fixture_spec_sha256: string`，精确绑定tracked fixture spec；
- `steps: array`，至少10个`tool`步骤；sleep不计tools/call；
- `cleanup: array`，仅含tool step，按声明顺序执行。

step联合类型只有：

1. `tool`：`kind/id/tool/arguments/assertions`必需，可选`repeat_until`；`kind="tool"`，id在steps+cleanup全局唯一。tool既须存在于运行时最终`tools/list`，也须与受审scenario文件中的固定值相同。`arguments`是JSON object，叶值只能为JSON scalar/list/object或单键`{"$ref":"/steps/<prior-id>/structuredContent/..."}`/`{"$fixture":"/..."}`引用；两种引用使用RFC 6901 JSON Pointer（只接受`~0`、`~1`转义和非负十进制数组下标），禁止空段、`..`、`__proto__`、`constructor`、`prototype`、私有属性与对象属性遍历。`$ref`只能指向已成功的前序step；`$fixture`只能指向已校验的instance公开字段。
2. `sleep`：`kind/id/seconds`且`additionalProperties=false`，`kind="sleep"`，seconds为0～30的JSON number；runner记录但不计调用。
3. cleanup只允许第1种tool shape，另加必需`when`，枚举`always/on_success/on_failure`；不能含sleep或repeat。cleanup不得调用`run_vql`及任何动态PowerShell/Cmd/命令执行artifact，只能使用scenario schema中列明的安全工具与固定参数，逆序依赖通过显式排列表达；所有匹配when的cleanup都运行，即使前一cleanup失败，失败汇总后令整个报告失败。

assertion是`{actual,op,expected}`或`{actual,op}`两种严格对象：`actual`只能是指向本次调用`/structuredContent/...`或`/isError`的RFC 6901 pointer；`exists/not_exists`不允许expected，其余操作必需expected且expected只允许JSON值或`$fixture`。多个assertion全部为AND，按数组顺序求值但全部记录。缺失路径只有`not_exists`成功、`exists`失败，其余操作失败；类型不匹配失败。`eq/ne`按JSON类型和值深比较，对象键顺序无关、数组顺序有关且bool不等于number；`in`要求actual是expected数组成员；`contains`要求actual为数组且含expected，或actual/expected均为string且actual含expected；`len_eq/len_gte`要求actual为string/array/object且expected为非负整数；`matches`要求两者为string并使用Python `re.fullmatch`、禁用内联flags、pattern最多256字符；`is_error`实际值只能是根`/isError`且expected为boolean。

`repeat_until`严格对象为`max_attempts:1..240`、`interval_seconds:0..30`、`assertions`非空数组。首次调用算attempt 1；每次真实tools/call均计数。调用返回协议错误时仍可被`is_error=true`断言匹配，否则本step立即失败；断言全真即停止，耗尽则step失败。step失败后不运行后续普通steps，只执行匹配的cleanup。前向引用、失败step引用、路径无效或引用类型不能满足目标input schema均在调用前以入口错误拒绝。

canonical report使用UTF-8、LF、末尾单换行、`json.dumps(..., sort_keys=True, separators=(",",":"), ensure_ascii=False, allow_nan=False)`；schema固定根键`schema_version/scenario/source_sha256/index_sha256/fixture_spec_sha256/fixture_instance_sha256/run_id/session_id/server_pid/started_at/ended_at/duration_ms/status/calls/steps/cleanup/failure/unexecuted_step_ids/coverage`且`additionalProperties=false`。时间为UTC RFC3339毫秒，duration为非负整数；不做伪稳定化，两轮比较时只排除文档明确列出的run/session/PID/时间和由它们派生的真实ID字段。

必须提供独立手写golden正反例，覆盖全部操作符、对象/数组相等、路径转义/逃逸、前向/失败引用、类型错、轮询成功/耗尽/调用错误、普通失败、cleanup的三种when/继续执行/失败汇总和unexecuted steps；期望文件不得由runner从实际结果生成。

### 4.2 runner

`tests/scenario_runner.py`以repo根为唯一cwd，只接受一个scenario ID而非任意文件路径。`tests/data/p05_scenario_index.json`逐项锁定scenario ID、规范化repo-relative路径、source SHA-256、required snapshot和fixture spec SHA-256；runner要求目标是`tests/scenarios/`内的普通文件，拒绝绝对路径、`..`、symlink/junction/reparse以及realpath越界，且实际SHA必须命中index。P06新增情景必须先作为受审tracked文件进入该index。fixture instance路径由固定批准根推导并校验普通文件、spec/workflow/attempt/ownership/hash，CLI不能覆盖路径或fixture值。

runner使用锁定的MCP Python SDK创建固定argv `[sys.executable, repo_root / "mcp_velociraptor_bridge.py"]`、`shell=False`的bridge子进程和一个stdio `ClientSession`，在同一情景内只initialize一次并顺序执行全部步骤。子进程cwd固定repo根，环境只继承`SYSTEMROOT/WINDIR/COMSPEC/PATH/PATHEXT/TEMP/TMP`和明确的`VELOCIRAPTOR_API_CONFIG/VELOCIRAPTOR_DOWNLOAD_ROOT/PYTHONUTF8`，其他变量不传。它不调用VMware、不恢复快照、不运行本机任意命令、不安装依赖、不生成调查决策；快照恢复由外层工作流在情景之间执行。

runner启动前验证schema、index、required snapshot参数、fixture instance、tools/list=130及无未知工具；运行后按4.1产生canonical JSON报告。失败立即停止后续正常步骤并执行cleanup；cleanup失败同样使报告失败。报告不得含API配置正文或秘密。

`run_vql`及动态PowerShell/Cmd/命令执行artifact允许出现在P06受审情景，但其tool名与参数只能来自命中index SHA的tracked scenario固定字节，任何CLI/environment/fixture对命令或VQL字符串的插值、覆盖或追加均拒绝；P05代表情景不用`run_vql`和命令执行artifact。未索引、哈希漂移、树外/链接scenario、fixture路径覆盖、tool/argument覆盖均在启动bridge前以退出码2拒绝。

P05提供至少一个人工编写、业务目的明确、>=10次tools/call的代表情景，覆盖动态采集、Flow状态/结果/file链、triage或进程结束中的至少一条依赖成功链以及结构化引用/轮询/断言。该情景在两个独立候选快照恢复周期各运行一次，两轮都必须无错误通过；除真实ID、时间、PID、路径派生字段以及异步`repeat_until`在终态到达前产生的额外同工具轮询次数外，逻辑step/tool序列、断言规格与通过结果、fixture哈希和覆盖行一致。额外轮询仍计入实际tools/call总数并完整保留，不得删改报告来伪造一致。

## 5. 安装后新快照与原子切换

候选名称冻结为`Snapshot 185-Velociraptor-MCP依赖与情景基线`。创建前同时满足：

- 当前active selector仍精确返回Snapshot184，canonical旁无`.next`；
- `vmrun listSnapshots <vmx> showTree`中候选名称0匹配；
- 四项真实功能链、P05完整测试和fixture幂等复核通过；
- 本workflow活动Flow/Hunt为0，netsh trace未运行；专用fixture sleep进程存在；
- server/client/API/唯一Windows client、guest repo和依赖manifest均健康；无host-only临时监听。

VM运行时只执行一次：

```text
/usr/bin/vmrun -T ws snapshot <vmx> "Snapshot 185-Velociraptor-MCP依赖与情景基线"
```

不得预置stop。创建后回读snapshot tree要求精确唯一，并从VMware snapshot metadata实际解析候选`.vmsn`文件名，不假定编号。恢复状态选择器先扩展为同时读取当前schema v1/epoch2和候选schema v2/epoch3，但正常`--emit-active`在原子切换前仍只能给Snapshot184；`.next`存在时正常选择必须fail closed。

候选验证共两个完整周期，每周期开始都先确认snapshot名称唯一，再只执行一次`vmrun -T ws revertToSnapshot`；回读VMX必须出现该候选`.vmsn`的`checkpoint.vmState`，只有marker存在才执行一次`/usr/bin/vmware -x <vmx>`。进度窗口关闭不判失败，不对同一次恢复追加第二个revert；marker缺失绝不执行`-x`。恢复后Win10VM MCP必须返回`DESKTOP-3FI41GR`，再验证API、唯一client、四依赖identity、fixture哈希/进程及代表情景。第一周期证据拉回host并确认接收器停止后，才开始第二周期。

两个周期都通过后才生成schema v2候选。v2根exact keys固定为`schema_version/workflow_id/epoch/phase/active_snapshot/automatic_restore_allowlist/retired_snapshots/activation_evidence`；`active_snapshot`和`activation_evidence`沿用v1 exact keys；`retired_snapshots`为恰好两个`{name,status}`严格对象，按Snapshot编号升序为183、184，name唯一且均为`MANUAL_ONLY_REQUIRES_NEW_USER_AUTHORIZATION`。candidate必须是schema2/epoch3/`DEPENDENCIES_ACTIVE`、active=Snapshot185/从metadata解析的实际非旧marker、allowlist恰含Snapshot185；active、allowlist和retired互不重叠；evidence绝对路径存在且SHA-256相等。唯一允许迁移为canonical有效v1 epoch2到candidate有效v2 epoch3；禁止降级、跳epoch、未知/缺失键、重复retired、错误排序、错误phase/marker/evidence。

`.next`使用同目录排他create临时文件，写完整UTF-8 JSON、flush+fsync后以不覆盖方式显现，随后fsync目录；它一旦存在，普通`--emit-active`仍须fail closed。选择器的`--emit-next`验证上述全部交叉不变量和证据哈希后，使用同目录原子replace canonical并fsync目录；替换后再次验证`--emit-active`只能输出Snapshot185，Snapshot184/183的`--validate-candidate`均退出3。单元测试必须覆盖有效v1/epoch2、有效v2/epoch3及上述每个拒绝分支。

若`.next`验证失败，只允许在证明它由当前workflow/attempt排他创建后，先记录其完整字节SHA和错误，再重新读取并验证canonical仍为完整v1/epoch2/Snapshot184；随后把失败`.next`原子rename到同目录带attempt和SHA前缀的隔离证据名并fsync目录，不覆盖既有文件，最后证明`--emit-active`恢复输出Snapshot184。来源不明的`.next`不得移动或删除，进入阻断。此恢复动作不删除任何VMware snapshot。

候选创建或验证失败时canonical保持epoch2/Snapshot184，并按上一段闭合本attempt生成的失败`.next`；不删除任何快照，不冷启动，不把候选当active；按FB-P05-004处理。若`vmware -x`秒退并消耗marker，必须重新执行一个新的、单次候选revert以重建marker后再试，且单独记录为新恢复attempt。

## 6. 实施项

### IMP-P05-001｜锁定manifest与最小KillProcess artifact

- 落点：dependency manifest、fixture spec、scenario index、KillProcess YAML及其独立golden/禁止能力测试。
- 完成门：四来源身份与第1章一致；KillProcess精确匹配第2.2节的Windows/EXECVE/Pid/单source/pskill/result golden并编译通过。

### IMP-P05-002｜实现fixture生成与幂等验证

- 落点：PowerShell生成器、tracked spec、运行态instance schema和测试。
- 完成门：第3章的首次创建、同态no-op、仅进程重建、冲突拒绝和恢复边界全部通过，无越界写入和网络监听。

### IMP-P05-003｜实现声明式schema和SDK runner

- 落点：schema v1、受审index、runner、fake stdio fixture、独立golden与单元/协议测试。
- 完成门：第4章全部引用、操作符、轮询、断言、清理、覆盖、失败报告、路径/哈希拒绝和同会话证据均有正反例；无eval/本机shell/VMware/安装入口或任意输入代理。

### IMP-P05-004｜预置三项inventory和两个artifact

- 落点：仅验收VM的locked目录、root inventory/repository和脱敏安装证据。
- 完成门：从Snapshot184干净恢复开始，只执行唯一predecessor转换；重复准备为no-op，精确identity/filestore readback，未碰其他项；部分失败按1.1节回到新attempt。

### IMP-P05-005｜回填四项真实成功

- 落点：官方SDK调用记录、Flow/source/upload/进程和网络取工具证据。
- 完成门：第2.3节四链全部通过，server+client网络观察闭合，PacketCapture ownership/紧急清理和KillProcess防PID复用成立，且无遗留trace/Flow/Hunt。

### IMP-P05-006｜创建Snapshot185候选并完成第一轮复现

- 落点：代表情景JSON、候选snapshot及第一次完整恢复/代表报告。
- 完成门：创建前门禁、唯一snapshot、marker/`vmware -x`/hostname/API/依赖/fixture及第一轮同进程同会话>=10 calls全部通过；canonical仍保持Snapshot184。

### IMP-P05-007｜第二轮复现并原子激活Snapshot185

- 落点：两周期恢复证据、schema v2恢复状态和选择器测试。
- 完成门：第二轮同进程/同会话>=10 calls无错误且fixture/覆盖与第一轮一致；第5章schema2迁移、失败`.next`恢复测试和成功fsync/原子切换全部成立，旧快照从automatic allowlist退出。

### IMP-P05-008｜README、Git边界与提交

- 落点：两README、最终测试、二进制/秘密扫描和唯一提交。
- 完成门：index、提交树、相对基线新增reachable blobs和tracked worktree中无秘密、恶意字节、官方工具字节或本机绝对路径；完整worktree把既有PLAN/Logs source字节逐项列为受保护未跟踪排除且不伪称零命中；P06入口说明准确。

### IMP-P05-009｜安装单进程Windows正式服务与28790防火墙

- 落点：目标精确的服务安装/核查脚本、受保护配置引用、单条防火墙规则、单元/幂等/回滚测试和脱敏证据。
- 前置与授权门：从0.2节有效schema2/epoch3/Snapshot185干净恢复开始；P02回流方案及其测试已通过。安装服务与防火墙属于既有实施授权；创建Velociraptor运行API principal、写入Bearer或修改secret/API配置/下载根ACL必须另有用户明确授权，缺任一授权就停在对应写入前且服务保持禁用。
- 完成门：严格按0.4节使用`NT SERVICE\mcp-velociraptor`，分离一次性安装身份与运行API principal，130工具正向权限矩阵及管理面超权负例全部通过；实测证明最低内置角色(reader/analyst/investigator)均无法启动全部130工具后，按PLAN-CHANGE-007在用户明确接受风险的前提下允许administrator角色并固定缓解条件。服务运行回流后P02的正式入口，配置实测guest host-only地址、28790和`/mcp`，只允许实测host来源；其他网卡和`0.0.0.0`无监听。重复安装是no-op；失败只回滚本attempt创建的服务/规则且不触碰其他网络配置。token由部署者提供且不出现在参数列表、仓库、URL、指纹或日志。

### IMP-P05-010｜扩展外部Streamable HTTP runner并完成正负验收

- 落点：与stdio共享场景解析/引用/断言核心的HTTP客户端模式、外部验收脚本和0.3节report schema v2/golden及每个run的`tools-schema.json`。
- 完成门：从虚拟机外官方SDK直接连接VM内`28790/mcp`，合法token完成initialize/tools/list和一个无破坏性tools/call；HTTP与VM内stdio代表报告各自从本会话真实tools/list按0.3节算法生成`tools-schema.json`和`tools_schema_sha256`，两个hash相等且工具数为130。报告必须从真实`Mcp-Session-Id`与`X-MCP-Server-Instance`响应头取得会话/实例身份，并与guest只读服务观察作SHA-256 join。缺/错Bearer和缺/错Host拒绝；无Origin官方SDK成功；显式允许Origin成功、未允许Origin拒绝；0.4节既有非允许来源产生双端拒绝证据。证据证明请求未经过Win10VM MCP，任一清单hash来源错配、身份变化、服务重启、来源负例不可执行或报告schema/golden不符均不通过。

### IMP-P05-011｜创建Snapshot186并完成两轮恢复后原子激活

- 落点：`Snapshot 186-Velociraptor-MCP网络部署基线`候选、两周期全链恢复/外部验收证据、0.2节schema3/epoch4状态及选择器反例。
- 完成门：IMP-009/010及既有依赖/fixture全部通过才创建候选；候选验证期间canonical持续为schema2/epoch3/Snapshot185。每周期唯一匹配、一次revert、marker存在才一次`vmware -x`、hostname/API/service/firewall/28790正向调用和0.3节正式HTTP报告全部通过。第二周期成功后才以0.2节唯一`.next`协议原子激活Snapshot186，回读schema3/epoch4/`NETWORK_ACTIVE`、automatic allowlist恰含186，Snapshot183/184/185均manual-only；任何失败隔离本attempt的`.next`并证明canonical仍为185 active。

## 7. 验收项

### ACC-P05-001｜两项动态外部工具回填（总纲ACC-007）

- 前置：由Snapshot184干净恢复建立唯一predecessor，三项inventory identity锁定、完成probe/filestore hash并本地serve。
- 动作：完成PacketCapture start/stop/convert/upload和Autoruns终态/result链。
- 预期：两项均真实成功，原始公网URL在调用期无访问；P03两项延期正式关闭。
- 证据：predecessor/转换差集、完整inventory与filestore readback、SDK转录、Flow/source/upload、server+client网络观察和trace ownership/cleanup。
- 失败/阻断：任何错误不计成功；全局trace事前active、所有权不可证、外联观察面不全、固定官方字节无法取得或后端不支持时阻断并回流，不替换版本；部分写入从Snapshot184开新attempt。

### ACC-P05-002｜Triage真实回填（总纲ACC-013）

- 前置：§1.0派生YAML受控导入、完整`_BasicCollection`身份/目标图通过；本项分别在P05初验和两个候选恢复周期的激活前状态执行，紧邻调用的唯一目标、实际同卷`>=13958643712` bytes及内存`>=1073741824` bytes筛查通过。§0.8激活门保持，但激活是三次真实回填成功后的结果，不是本项前置。
- 动作：无参数固定工具调用；保存请求中Targets JSON数组字符串、timeout=2400、max_upload_bytes=4294967296及真实flow ID，等待终态并读取results/files；预算证据明确记录压缩前`FileBuffer.DataLength`逐块累计、严格`> cap`后的取消点和可能的在途/并行超出，不以压缩后网络/物理量或uploaded汇总替代。
- 预期：初验及两个候选周期各自完整目标未删减、HashOnly默认未改变，均为真实FINISHED及可读取结果/文件链；产品MCP不下载/安装，其他入口预算不变。三次及其他既有门全部成功后才允许原子激活并交P06。
- 证据：原始/派生来源字节与哈希、唯一差异和26/122→27/123图差异、root raw定义、资源/卷绑定原件、SDK请求、Flow状态/错误/results/files转录。
- 失败/阻断：ERROR、取消、超限、关键采集错误、伪Flow、错误目标、未锁identity或只有行/uploads均失败；4 GiB不足、依赖不兼容、目标图/版本/存储拓扑变化或未知叶子使完整性不可证时停止并回主方，不提限、不删目标、不改HashOnly。

### ACC-P05-003｜专用进程结束（总纲ACC-014）

- 前置：第2.2节golden/编译定义唯一，专用进程及受保护PID集合在调用紧邻前精确复核。
- 动作：调用kill_process并核对前后进程和Flow结果。
- 预期：结果`Killed`精确等于输入PID且只结束指定PID，受保护PID/start-time不变；联网控制接口仍为0。
- 证据：artifact raw/compiled hash及逐字段扫描、目标和受保护PID/token/start-time前后快照、Flow/results和tools/list。
- 失败/阻断：PID身份漂移时调用前拒绝；`Killed`不等于输入PID、Flow ERROR、错杀、未杀、伪成功或扩展能力均失败；pskill语义不成立时阻断重裁决。

### ACC-P05-004｜固定可恢复测试数据（总纲ACC-020）

- 前置：Snapshot184 active且P04工具面冻结。
- 动作：准备/复核fixture，创建Snapshot185并执行两个完整恢复周期。
- 预期：tracked spec与runtime instance一一映射，文件/键/事件/disabled任务/进程断言可重复，恶意/官方工具字节不入Git，新快照原子active。
- 证据：spec/instance及差集、两周期报告、分层Git扫描、snapshot tree/marker/hostname/API/state和`.next`失败恢复测试。
- 失败/阻断：任一不可复现、越界、`.next`封锁无法安全解除、旧snapshot仍automatic或状态非原子均失败；VMware恢复不可用则阻断。

### ACC-P05-005｜声明式runner设施（总纲ACC-023设施部分）

- 前置：MCP SDK 2.1.1、130工具协议、tracked scenario index和已核验fixture instance可用。
- 动作：正反例自测并用代表情景在两个恢复周期运行；装载§1.0.1全部文件fixture及期望，但不把未运行矩阵写成通过。
- 预期：第4章的引用/等待/断言/统计/cleanup/report和信任边界闭合，文件fixture身份、Padding/分块/EOF/漂移与错误分账期望可机械消费；快照只在情景间，无生产入口或任意VQL/命令代理入口。
- 证据：完整schema/独立golden/runner拒绝测试、两代表报告、PID/session/call/coverage。
- 失败/阻断：跨会话拼接、未索引或路径/hash漂移、参数覆盖、任意执行、缺报告或情景中恢复均失败；SDK契约变化才阻断回流。

### ACC-P05-006｜提交与排除边界

- 前置：前五项通过且第10章25项规范稳定ID追溯为25/25/0/0/0/0。
- 动作：Win10VM执行全套测试、Git对象/工作树二进制与秘密扫描、diff/README复核。
- 预期：提交只含本节核心范围；PLAN/Logs/官方工具字节/guest运行态不入Git；整个worktree的既有未跟踪排除逐项报告。
- 证据：测试原文、index/tree/reachable/tracked/worktree分层扫描、已知排除清单、staged diff和commit SHA。
- 失败/阻断边界：任何测试失败、未声明新增排除、秘密/恶意/官方工具字节入Git或生产接口漂移均失败；无法取得Git对象边界时为被阻断而非通过。

### ACC-P05-007｜正式服务、安全网络边界与外部MCP调用（总纲ACC-026）

- 前置：回流后的P02通过；0.2节schema2/epoch3/Snapshot185干净恢复；部署者提供token；host/guest host-only地址紧邻安装实测；用户另行明确授权创建运行API principal及修改必要ACL。
- 动作：先执行0.4节从最低角色开始的130工具能力矩阵和管理面超权负例；再以专用虚拟服务账户安装/启动服务和防火墙，VM内核对身份、ACL、进程、监听和日志；虚拟机外直接执行正式正向调用及Bearer/Host/Origin/来源负例；以0.3节schema v2记录会话与服务身份，比较HTTP/stdio完整工具schema。
- 预期：安装身份不保留，运行身份按PLAN-CHANGE-007修订后可为administrator（用户接受风险+缓解条件固定），管理面可达但不经产品130工具暴露；API配置/Bearer/下载根ACL最小并在服务重启后不漂移。单进程只监听guest host-only `:28790/mcp`，防火墙只允许host来源；合法无Origin SDK与允许Origin成功，未允许Origin及错误Bearer/Host在handler前拒绝；既有非允许源TCP失败且双端口对照实测可关联（PLAN-CHANGE-006修订后证据形式）；正式调用无需Win10VM MCP；HTTP/stdio各自报告的`tools_schema_sha256`均绑定本会话真实清单且相等，同一HTTP会话/服务实例可由真实响应头与guest观察哈希join，secret零泄露。
- 证据：授权记录、能力/超权矩阵、脱敏服务配置、API principal与文件/目录ACL、服务重启前后身份、监听进程、规则、host侧绑定源地址连接失败与双端口对照实测（PLAN-CHANGE-006修订后证据形式）、外部SDK转录、HTTP状态、`Mcp-Session-Id`/`X-MCP-Server-Instance`转录、guest服务观察JSON及SHA-256、HTTP/stdio各自的`tools-schema.json`/报告/重算hash及零diff、handler零调用负例、日志与secret扫描。
- 通过/失败/阻断：全部成立才通过。通配/其他网卡监听、无认证、错误来源可达、未经PLAN-CHANGE-007风险接受流程的LocalSystem/安装凭据长期运行、第二server、schema/身份差异、借道控制面或泄密均失败；token/凭据与ACL授权未提供、客户端无Authorization能力、host-only隔离不成立时被阻断并保持服务禁用。非允许来源证据按PLAN-CHANGE-006修订后的三要素（host连接失败+双端口对照+规则verify）闭合，不再单独要求guest WFP动态事件。

### ACC-P05-008｜Snapshot186安装后网络基线（总纲ACC-020/026）

- 前置：ACC-P05-001～007全部通过，Snapshot185仍是唯一active。
- 动作：创建Snapshot186候选，canonical继续输出Snapshot185；连续两周期按用户runbook恢复，每轮同时验证hostname、Velociraptor API/唯一client、四依赖/fixture、专用服务身份/ACL/防火墙及虚拟机外`28790/mcp`正式schema v2报告；最后按0.2节`.next`协议原子迁移选择器。
- 预期：两轮全部一致；marker缺失绝不直接`-x`，不对运行VM追加revert；成功回读schema3/epoch4/`NETWORK_ACTIVE`，active/automatic唯一为186，183/184/185按序manual-only；旧marker、未知键、跳epoch、错误phase、错误hash、重叠集合和来源不明`.next`均fail-closed。
- 证据：snapshot tree、实际vmsn marker、每attempt命令/状态、VMX/hostname/API/服务/权限/网络报告、两份schema v2正式报告、canonical/candidate字节与hash、选择器正反例和原子切换/目录fsync证据。
- 失败/阻断：任一环节失败不激活、不删除快照、不冷启动；只按0.2节隔离可证明由本attempt创建的`.next`，并回读canonical仍为schema2/epoch3/Snapshot185后才可开新attempt；VMware或外部验收不可用时阻断。

汇总：P05的既有ACC-001～006和回流ACC-007/008全部通过、Snapshot186成为唯一active后才可进入P06；P05不宣称总纲ACC-021/022或ACC-023完整执行部分通过。

## 8. 实际测试入口与证据位置

当前已存在且必须执行的命令：

```powershell
Set-Location C:\mcp-velociraptor
$env:VELOCIRAPTOR_API_CONFIG='C:\VelociraptorMCP\secrets\api_client.yaml'
.\.venv\Scripts\python.exe -m unittest discover -s tests -p 'test_*.py' -v
.\.venv\Scripts\python.exe -m tests.p04_protocol_probe
```

尚未创建的P05 runner不得虚构当前可用命令；冻结其建立后的工作目录为`C:\mcp-velociraptor`，唯一业务输入为scenario ID，snapshot和fixture instance均从受审index/spec与固定批准路径推导，CLI不接受路径、tool或argument覆盖。输出为`Logs/P05/wf-01a05d1d-p05/<run-id>/report.json`，退出码0表示全部断言/cleanup通过，1表示情景失败，2表示schema/环境/身份/入口错误。实现后在实施记录补精确命令。

P05证据根固定为`Logs/P05/wf-01a05d1d-p05/`；source Spike保留在`Logs/P05/source-spike/`。guest证据必须在每次快照恢复前通过host-only令牌+长度+SHA-256接收器拉回host，接收器单请求退出并回读不可达。

## 9. fallback与停止条件

| ID | 触发 | 行为 | 退出条件 |
| --- | --- | --- | --- |
| FB-P05-001 | 任一来源大小/hash/signature/layout不符 | 停止该依赖及后续快照；保留原始证据；不下载替代最新版 | 同一锁定identity重新取得并核验，或规范性来源正式重裁决 |
| FB-P05-002 | inventory/artifact readback、编译或任一步分写入不符 | 停止四链，先回传前后差集；从Snapshot184完整恢复后开新attempt；不以卸载/删除/逆向覆盖回滚 | 新attempt从准确predecessor出发，全部精确identity、唯一CLIENT和no-op重跑通过 |
| FB-P05-003 | 真实链/代表情景失败 | 不计成功；仅在所有权已证时执行固定trace/process cleanup，否则回传证据后从Snapshot184开新attempt；修复后重跑受影响链及回归 | 失败链、系统副作用复原和要求回归无错误通过 |
| FB-P05-004 | Snapshot185创建/恢复/marker/API/第二轮复现或`.next`验证失败 | canonical保持Snapshot184；按第5章隔离仅由本attempt生成的失败`.next`并恢复`--emit-active`；不激活、不冷启动、不删除snapshot；按attempt规则重新武装marker或修复环境 | 两完整周期、state全部反例和成功原子切换通过 |
| FB-P05-005 | host-only证据未回传或hash不符 | 不启动下一次恢复，先重传同一证据；不以对话输出代替 | host文件和guest包SHA一致，接收器停止 |
| FB-P05-006 | Git分层扫描发现秘密、恶意/官方工具字节、绝对用户路径或未声明worktree排除 | 停止提交，只从P05 staged范围移除并重做扫描/测试；不删除受保护PLAN/Logs证据 | index、提交树、新reachable blobs和tracked worktree零命中，完整worktree仅命中已声明的既有未跟踪证据且diff边界准确 |
| FB-P05-007 | 网络部署、权限矩阵、动态非允许来源负例或schema v2身份join失败 | 服务保持禁用；不创建Snapshot186；只修本阶段服务/ACL/规则/runner范围后从Snapshot185开新attempt。最低权限若为SERVER_ADMIN或缺单独凭据/ACL授权则停止等待明确决定 | 130工具最小权限与超权负例、Bearer/Host/Origin/来源边界、报告身份join全部通过，且所需授权可复核 |
| FB-P05-008 | Snapshot186创建/任一恢复周期/schema3 candidate/`.next`验证或原子切换失败 | 不激活、不删快照、不冷启动；canonical保持schema2/epoch3/Snapshot185；只隔离能证明属于current attempt的`.next`，保存hash并回读active仍为185 | 同一候选完成两个全链周期，schema3全部正反例通过并原子切换，回读active/allowlist唯一为186 |

禁止fallback：情景中临时联网补工具、把错误计成功、换用新版本、不验证第二轮恢复、继续使用Snapshot184或Snapshot185作P06常规基线、用GUI手工点击代替可重复脚本、以规则文本替代非允许来源动态负例、让服务长期使用安装期SERVER_ADMIN、把runner扩展成生产编排。

## 10. 需求、OUT、IMP、ACC双向追溯与保真门禁

第一层既有归属保持：OUT-020→IMP-002/006/007→ACC-P05-004；OUT-023→IMP-003/006/007→ACC-P05-005；OUT-032→IMP-002/008→ACC-P05-004/006；OUT-048→IMP-003→ACC-P05-005。既有回填保持：OUT-007/030→IMP-001/004/005→ACC-P05-001；OUT-013/036→IMP-001/004/005→ACC-P05-002；OUT-014/043→IMP-001/002/004/005→ACC-P05-003。v3新增：OUT-053/054及OUT-027/029/031→IMP-P05-009/010/011→ACC-P05-007/008；RACC-026由两项共同闭合，RACC-020的活动基线从185更新为186。外部网络动作仅适用CON-002的明示例外，其余guest动作继续在Win10VM执行。

| 规范ID（共25，RACC镜像独立计数） | 完整实施落点 | 验收落点 |
| --- | --- | --- |
| REQ-007 | IMP-001/004/005；第1、2章 | ACC-P05-001 |
| REQ-013 | IMP-001/004/005；第1、2章 | ACC-P05-002 |
| REQ-014 | IMP-001/002/004/005；第2、3章 | ACC-P05-003 |
| REQ-020 | IMP-002/006/007/011；第0.2、3、5章 | ACC-P05-004/008 |
| REQ-023 | IMP-003/006/007/010；第0.3、4、5章 | ACC-P05-005/007/008 |
| REQ-026 | IMP-009/010/011；第0.2～0.4章 | ACC-P05-007/008 |
| CON-002 | IMP-002～011；全部guest与获准外部网络动作 | ACC-P05-001～008 |
| CON-004 | IMP-003/010；固定index/schema、不接受CLI覆盖、报告exact keys | ACC-P05-005/007 |
| CON-005 | IMP-004～007；真实root API/Flow/marker/state | ACC-P05-001～005 |
| CON-006 | IMP-001～003/008～011；信任边界、最小路径、身份和秘密 | ACC-P05-003～008 |
| CON-007 | IMP-002/008；无恶意样本、分层Git扫描 | ACC-P05-004/006 |
| CON-008 | IMP-002～011；状态机、cleanup、失败恢复 | ACC-P05-001～008 |
| CON-011 | IMP-001/004/005；固定依赖身份和P01回填 | ACC-P05-001～003 |
| CON-012 | IMP-003/010，§0.4下载根部署及代表情景文件链；继承P04/总纲OUT-037：完整下载持续保留、不覆盖、不按时间/容量自动删除、不提供删除工具；失败只尝试清理该次owned临时文件，路径/所有权不可信或清理失败即停止并允许残留，提交后不删除完成目标。快照恢复是已授权测试外层操作，不伪称产品自动清理。旧本行spec/index状态原子写归REQ-020/023，不是CON-012。 | ACC-P05-005/007的单文件链及提交前/提交后故障回归，P04 ACC-010契约复核 |
| CON-015 | IMP-009/010/011；第0.2～0.4章 | ACC-P05-007/008 |
| NON-003 | IMP-008～011；不生成secret、不开放联网能力 | ACC-P05-006～008 |
| NON-004 | IMP-001/005；KillProcess只结束准确PID且不提供联网控制 | ACC-P05-003 |
| NON-009 | IMP-003；runner仅执行受审场景，不是产品编排器 | ACC-P05-005 |
| NON-010 | IMP-009/010；同一server、Win10VM MCP不代理正式请求 | ACC-P05-007/008 |
| RACC-007 | IMP-001/004/005；第1、2章 | ACC-P05-001 |
| RACC-013 | IMP-001/004/005；第1、2章 | ACC-P05-002 |
| RACC-014 | IMP-001/002/004/005；第2、3章 | ACC-P05-003 |
| RACC-020 | IMP-002/006/007/011；第0.2、3、5章 | ACC-P05-004/008 |
| RACC-023 | IMP-003/006/007/010；第0.3、4、5章 | ACC-P05-005/007/008 |
| RACC-026 | IMP-009/010/011；第0.2～0.4章 | ACC-P05-007/008 |

每个IMP和ACC已在上表拥有正向来源，反向没有无来源项；八个fallback分别受CON-005/006/008/011/012/015约束。CON-001只记录为P01已经完成并由Snapshot184继承的部署/API前提，若hostname/API/唯一client失效则P05被阻断并回流P01，不把它重复计成P05产出。

需求保真计数：应承接25、完整25、未落点0、语义弱化0、不可验收0、失效或复核条件缺失0；异常ID无。计数以规范稳定ID为单位，RACC镜像独立计数，不以自然语言分组行数代替。

## 11. 版本变更、实施顺序与审核重点

- v11-candidate-r02：PC017 R02候选恢复官方URL/内嵌Commit追溯，删除产品运行期派生hash职责，明确P05初验+两个候选周期真实回填后才激活，并把13 GiB/1 GiB限定为验收执行前preflight而非产品错误接口；PC016暂停与独立后验不变。待主方采纳/自评。
- v12-candidate-combined：合并05号文件裁决，新增逻辑原文件、idx内部元数据、身份/selector拒绝与普通/稀疏读取fixture；不冒充已运行。按05号勘误纠正triage预算计量为压缩前`FileBuffer.DataLength`累计、严格`> cap`取消并披露并行超出。待主方采纳与完整自评。
- v13-candidate-combined-r02：文件fixture分离合法同目标竞态与零发布错误分支，并绑定唯一当前PC017候选组；R01来源、预算、对象边界及P05三次回填后激活均不变。待主方采纳与完整自评。
- v13（作者修订）：机械采纳已验收v13候选正文，当前组切换为`PC017-AUTHOR-REVISION`；待主会话完整自评，实施门关闭，代码、部署和验收未随文稿通过。
- v14（作者修订）：按08号裁决扩充文件fixture，分别冻结提交前零新完成文件、清理失败残留、提交后过滤错误与赢家/哨兵保护；当前组切换为`PC017-AUTHOR-REVISION-2`，不改变其他fixture、来源或阶段门。
- v15（作者修订）：承接PC018/019，P05三条真实依赖链和SDK转录分别保存启动响应真实Flow状态与随后业务终态，重算新golden/schema hash并绑定九规范当前组；不改变三次回填、激活、PC017或PC014边界，实施门关闭。
- v16（作者修订）：承接PC018终页勘误，在fixture/golden中分开验证终页`next_cursor=null`、非终页规范字符串及无分页时整个`pagination`不存在；不把勘误计为PC019第五个新输出字段，实施门关闭。

- v0：首次送审版本。
- v1：引入首次全面审核AUD-001～010；补齐干净Snapshot184起点和唯一predecessor转换、schema/runner信任边界、fixture spec/instance、`.next`失败恢复、PacketCapture所有权、KillProcess权限/PID防复用、离线网络观察、Git分层扫描和20项稳定ID双向追溯。
- v2：依据`p05-attempt-006`、`007`、`009`及Snapshot185两轮恢复实测纠正运行态语义：`inventory_add()`保留内置来源行并新增唯一管理员覆盖行；直接提供`file`后覆盖行的成功状态为`materialize=false`，实际解析有效性由probe精确输出及本地public字节哈希证明；当前Velociraptor的`pskill()`成功返回被结束的PID整数，因此KillProcess以`Killed == 输入PID`及目标消失双重判定；异步终态到达允许`repeat_until`产生不同数量的连续同工具轮询，但逻辑step/tool序列、断言结果、fixture哈希和覆盖必须一致。上述修订不改变依赖身份、架构、权限、恢复机制、Pxx边界或验收目标。
- v3：Q-072回流。既有Snapshot185、依赖、fixture和stdio runner保持为已完成输入；新增IMP-P05-009～011与ACC-P05-007/008，负责Windows单进程正式服务、host-only 28790防火墙、Bearer/Host/Origin正负外部验收、HTTP/stdio工具schema同一性，以及全部通过后的Snapshot186双周期恢复和原子激活。状态退回未审核；未取得部署者token前不得启用正式服务。
- v4：引入网络回流全面复审AUD-002/003/005/006/007/008。冻结schema2/epoch3/Snapshot185到schema3/epoch4/`NETWORK_ACTIVE`/Snapshot186的唯一原子迁移和`.next`失败隔离；冻结共享report schema v2、真实MCP会话/服务实例响应头与guest观察哈希join；分离安装身份、专用虚拟服务账户和最小运行API principal，要求独立凭据/ACL授权与130工具权限/超权矩阵；给出既有非允许源双端WFP负例及不可构造时的阻断；统一Origin语义；把25个稳定ID逐项列全。状态保持未审核。
- v5：审核循环根因修正。report schema v2新增唯一必需根字段`tools_schema_sha256`，冻结本会话真实tools/list的name/inputSchema/outputSchema排序、JSON规范化、UTF-8 SHA-256与同run `tools-schema.json`三方校验；HTTP/stdio代表报告必须各自产生且hash相等，P06只从该路径消费。其余身份、状态机、权限、网络和工具面契约不变；状态保持未审核。
- v6：引入AUD-009前轮覆盖遗漏。补齐共享scenario/index的P05_INITIAL/P05_CANDIDATE/P06_ACTIVE固定配对和26根report中的snapshot_evidence_sha256，把既定恢复记录按run/session/服务观察机械关联；P05两份受审代表输入及P06五场明确185/186职责。恢复方式、激活顺序、生产接口、权限、Pxx及成功标准均不变。

既有顺序IMP-P05-001→008已完成。回流顺序固定为IMP-P05-009→010→011：009先安装并在VM内自检，010再从虚拟机外完成正式正负验收，只有两者和既有全部ACC通过才允许011创建Snapshot186；011两轮恢复及原子激活后才更新提交并交P06。

审核重点：

1. 四项依赖的来源、哈希、签名、artifact身份和“不改最新版”门是否足够防止漂移；
2. KillProcess是否严格限制为单Pid/pskill且验收不会误杀；
3. runner是否只解释声明式JSON，不形成任意执行或生产编排面；
4. fixture、代表情景、清理与证据是否能在两次快照恢复间复现；
5. 已完成schema v1/epoch2→schema v2/epoch3历史与本轮schema2/epoch3→schema3/epoch4候选验证、marker门、`.next`隔离和原子切换是否清楚分离且fail closed；
6. report schema v2能否用真实响应头、guest观察和SHA-256 join证明同一HTTP会话/服务实例，stdio coverage是否恒为空；
7. 专用服务身份、最小API principal、ACL和非允许来源动态负例是否可执行，缺授权/安全来源或最低权限仍为SERVER_ADMIN时是否正确阻断；
8. P05是否未提前承担P06的650关系全量执行，且没有把任何失败算作通过。

送审成熟度/引入后保真：应承接25、完整25、未落点0、语义弱化0、不可验收0、失效或复核条件缺失0；方向阻断0、验收阻断0、占位决定0、无主依赖0。
