# 复审（闭合确认）：P07 清理与终检（v2 方案，第3次第三方审核）

> 会话角色：sub-plan-reviewer（隔离上下文第三方审核方）
> 审核性质：复审——对 `2026.09.13-03-复审-P07清理与终检.md` 三项条件（AUD-001 修正 / AUD-003 按新裁闭合 / AUD-004 口径定义）在新字节上的闭合确认
> 审核模式：定向复审（三条件逐项闭合核对 + 新矛盾全文扫描）
> 创建日期：2026-09-13（Asia/Shanghai）
> 对象：`PLAN/2026.09.02/2026.09.12-18-实施子方案-P07清理与终检.md`（v2，复审条件引入后字节）
> 结论：**审计通过**（三项复审条件全部闭合，无新矛盾；对应委托方口径"复审通过/闭合确认通过"。范围限定见第 7 节）

## 1. 对象身份与历史链

| 项 | 值 |
| --- | --- |
| Pxx | P07（清理、测量和最终回归） |
| 所属总纲 | `2026.09.02-02-总纲-mcp-velociraptor-Windows-DFIR二次开发.md` v10（SHA `f51ccc07…`） |
| 需求文档 | `2026.09.02-01-需求提炼-mcp-velociraptor全阶段设计.md` v2（SHA `a05916e9…`） |
| 本地基线 HEAD | `5c52f325c7b8c17b0908be22481908109c999161`（工作树仅未跟踪 `Logs/`、`PLAN/`、`tests/__pycache__/`，闭合确认前后两次复核一致） |
| 首次审核 | `2026.09.12-20-审核-P07清理与终检.md`（AUD-001/002/003，有条件通过） |
| 第 2 轮复审 | `2026.09.13-03-复审-P07清理与终检.md`（AUD-002 闭合；AUD-001 部分复开、AUD-003 未按新裁闭合、AUD-004 新增；受审方案彼时 SHA `6406c7d3…`） |
| 作者处理 | `2026.09.13-02-审核处理-P07清理与终检.md`（AUD 处置 + CHK-019~023 引入 + 13 模块全回归表） |
| 本轮对象字节 | `08d4db0b14b3ade13b8030e9327d9a7951eb9c82d25f08510f2c669bbe8aea74`，与委托方声明前缀一致 |

## 2. 路径指纹（SHA-256，本轮独立重算）

| 文件 | 指纹 |
| --- | --- |
| 2026.09.12-18-实施子方案-P07清理与终检.md（受审，闭合后） | `08d4db0b14b3ade13b8030e9327d9a7951eb9c82d25f08510f2c669bbe8aea74` |
| 2026.09.13-03-复审-P07清理与终检.md（第 2 轮复审） | `a445493dd2ac397263233759b96a144d82cd69c5d67b5381d755011ae3cbb4c4` |
| 2026.09.13-02-审核处理-P07清理与终检.md | `3db7b649f380be3ec3a537f2fe9d9b18a17bc9db833f269ab728538e914442f9` |
| 2026.09.12-20-审核-P07清理与终检.md | `c7b272e902cc10666c3130101e7f6dd0035f8bd387414a706046141e0b5dd8d4` |
| 2026.09.13-01-独立核验-mcp-velociraptor-Windows-DFIR二次开发.md | `ce685c02d79c9c5751225873a9c8bf990d05747d2240a0d02f0eed28e73eeafe` |

本轮 diff 范围（`6406c7d3…` → `08d4db0b…`）：抬头状态行、第 4.2 节三段重写/新增、第 6 节整节重写；第 1/2/3/4.1/4.3/5/7/8 节逐节比对无变化。

## 3. 本轮定向复审范围（闭合确认）及逐项覆盖结果

| 复审条件（2026.09.13-03） | 覆盖结果 |
| --- | --- |
| AUD-001：第 6 节枚举修正为实际 13 模块或显式枚举规则，删除幽灵模块，历史基线降为旁注 | **闭合**（见 5.1） |
| AUD-003：4.2 边界裁决替换为升级口径——实测取得可比基线方可使阻断边不命中；披露不构成通过；不可得时按阻断边处置并回流规范 | **闭合**（见 5.2） |
| AUD-004：显式定义上游侧任务实现与映射口径（目标等价、映射披露、重试维度声明），否则回退 CHK-021 路径 2 | **闭合**（见 5.3） |
| 无新矛盾/越界 | 全文扫描通过；AUD-002 闭合状态不受本轮 diff 影响（第 4.3 节逐字未变）（见 5.4/6） |

## 4. 独立复核的事实证据

- 残留扫描：受审正文 grep `边界裁决|披露满足|而非触发阻断|test_p03_contracts` 零命中（退出码 1）——旧读法整段移除、幽灵模块删除属实。
- 模块枚举：第 6 节 13 项与 `tests/` 下 13 个 `test_*.py` 一一对应；本方静态计数 `def test_` 逐模块为 14/2/25/6/4/1/15/3/5/9/7/7/5，与正文枚举项数（p06_contracts 7、p06_aggregate 5）**逐项完全一致**；`test_stdio_and_http_share_identical_toolset` 落点保留；`test_p06_aggregate` 调用方式说明与处理记录第 4 节注一致；历史基线明示"仅作追溯旁注，不替代本轮枚举"。
- 映射上游侧：`collect_artifact` 在 78 工具捕获中在册（参数 client_id/artifact/org_id/parameters/legacy_parameters）；`get_collection_results` 在册且 `inputSchema` 含 `max_retries`、`retry_delay`——正文"上游侧重试维度为其工具内置 max_retries/retry_delay 参数行为"与捕获 schema 逐字对应。
- 阻塞语义：归档上游源 `mcp_velociraptor_bridge.py:2004` `collect_artifact` → `:69 _start_collection_tool` → `velociraptor_api.py:566 start_collection`，其 VQL 以 `collect_client(...)` 经 `run_vql_query` 执行——采集在查询内阻塞至完成，正文"内部阻塞至完成的实时采集语义"与源码一致。
- 映射当前侧：五份选定报告（按 `final-selection.json` report_sha256 定位）calls 中 `get_flow_status`、`get_flow_results`、`list_flow_files` 全部在册。
- 同名面事实：五报告调用的 130 工具与上游 78 面名字相同者恰 4 个（`collect_file`、`collect_forensic_triage`、`kill_process`、`run_vql`），正文"仅少数工具同名、'同一任务'不可按工具名字面执行"表述准确。
- 抬头状态行：改为"已审核（首次有条件通过 2026.09.12-20；复审有条件通过 2026.09.13-03；复审条件已引入本版）"，与历史链记录一一对应，无错引。

## 5. 逐条件闭合裁定

### 5.1 AUD-001｜闭合

第 6 节改为逐项枚举全部 13 个实际存在模块，含每模块项数与调用说明；幽灵模块 `tests.test_p03_contracts` 已删除；历史基线（2026-09-05）降为追溯旁注；"任一模块不可运行时逐模块记录理由与影响，不得静默省略"保留。最小闭合条件（"修正枚举为实际 13 模块或显式枚举规则……在最终实施/验收记录登记"）的正文部分已完成；登记动作随最终实施/验收记录落地，属后续记录职责，不阻断本闭合确认。

### 5.2 AUD-003｜闭合

第 4.2 节"上游阻断边处置（复审后定稿）"明确：不改读总纲阻断边，而是实测取得可比上游固定任务运行基线使阻断边不命中；原"披露满足 REQ-024'如实比较'而非触发阻断"整段移除（残留扫描零命中）；新增兜底"若上游固定任务实测因客观原因不可取得，则按总纲阻断边处置：ACC-024 记为被阻断并回流规范，不以单边披露替代"——即 CHK-021 路径 2 的触发条件与不可单边披露的约束均已入正文。与最小闭合条件逐句对应。

### 5.3 AUD-004｜闭合

第 4.2 节"任务等价与映射口径（AUD-004）"段：等价性按调查目标定义（同一客户端、同一 fixture 状态，"启动指定工件采集 → 轮询至完成 → 取回结果行与文件清单"）；映射为上游 `collect_artifact` + `get_collection_results(max_retries, retry_delay)` 对当前 `start/采集工具` + `get_flow_status` 轮询 + `get_flow_results` + `list_flow_files` 链；比较表逐工具名与逐调用次数如实列出、不折算；上游重试维度口径声明入报告。映射双侧工具名、上游重试参数、阻塞语义均经本方独立实证（第 4 节）。"双方各自以其原生工具面实现该目标"的口径正是 AUD-004 要求的"目标等价而非步骤同一"的显式声明，且不折算原则消除了 silently 转换的风险。

### 5.4 AUD-002｜闭合维持

第 4.3 节本轮逐字未变：垫片/探针内容寻址留存、披露与回退口径入正文的闭合状态不受影响。

## 6. 新矛盾扫描

- 全文复读 + 与 `6406c7d3` 逐节比对：变更严格限于三项条件对应位置与状态行，无越界改写。
- 第 7 节边界未变：上游只读（独立目录运行）覆盖升级处置的执行方式；不新增工具、不改 130 注册面、不推断通用模型成本。
- 重试口径双侧不对称（当前侧按 attempt>1 调用计数、上游侧按工具内置参数行为）属两工具面的结构性差异，正文已声明"口径在报告中声明"且比较表不折算——为如实披露而非隐藏口径，符合 REQ-024"如实比较，不预设更小"与 ACC-024"原始量、口径和差异"。
- 入口门禁（第 3 节）未变；其对 P06 新记录的依赖为既有继承项，非本轮引入。
- 未发现新的占位符、悬空引用、后序依赖或未声明 fallback。

## 7. 范围限定（不预支实施层认证）

本"审计通过"仅覆盖：复审三项条件在受审方案正文中的闭合与无新矛盾。以下不属于本轮结论范围，由后续对象承担：上游固定任务实测的实际执行与证据（`upstream_comparable_baseline` 当前仍为 `none`，实测后方能更新）；CHK-019/020/023 闭合证据对象（新证据根、Snapshot186 资格重跑、绑定新 HEAD 的总体验收）尚未落盘；13/13 全回归与上游实测的最终绑定属新实施/验收记录及后续独立核验。

## 8. 结论

**审计通过**。2026.09.13-03 复审所列三项条件（AUD-001 清单修正、AUD-003 按新裁闭合、AUD-004 口径定义）已在新字节 `08d4db0b…` 全部闭合，AUD-002 闭合维持，未发现新矛盾或越界。P07 方案对象层面的审核链就此闭合；实施层证据按第 7 节范围限定由后续记录与核验收口。本记录交回方案作者/执行方。
