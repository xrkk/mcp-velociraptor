# 总体验收记录：第 4 轮 CHK-019～023 闭合（第7轮，绑定当前 HEAD）

> 会话角色：实施执行方（主会话）
> 创建日期：2026-09-13（Asia/Shanghai）
> 对象：第 4 轮独立核验（2026.09.13-01）五项 CHK 的最小闭合条件
> 本记录绑定 HEAD：`main@ac1c58a`（`653aa8c` → `5c52f32` 账本强化 → `ac1c58a` 成本绑定）

## 1. 核心对象身份（全部为当前字节）

| 对象 | 身份 |
| --- | --- |
| 证据根（新纪元） | `Logs/P06/wf-01a05d1d-p06-r2/`；接收账本 SHA-256 `2b4bb9ce…`；final-selection(v2) `6de64628…`；aggregate `637913e5…`（status=success） |
| 资源资格（CHK-020） | `resource-qualification.json` SHA-256 `790d69c9…`（12 步/99 调用，逐步 before/after 水位、每大调用前 MIN_FINAL_FREE 门禁、deadline 推导；Snapshot186 恢复绑定） |
| 上游任务基线（CHK-021） | `Logs/P07/upstream78-fixed-task.json` SHA-256 `008e4921…`（RecycleBin 目标等价任务：2 调用/5.19s/647B；垫片装置在档） |
| 成本报告（CHK-021） | `Logs/P07/cost-measurement.json` SHA-256 `2abff0d0…`（上游 78/63,474B vs 当前 130/180,964B；任务维度双边实测） |
| 审核链（CHK-022） | 方案 `-18`（08d4db0b）+ 审核处理 `-02`（3db7b649）+ 复审 `-03`（有条件通过）+ 复审闭合 `-04`（审计通过） |
| 全回归（CHK-022） | 13/13 模块全绿（2026-09-13，VM venv，`VELOCIRAPTOR_API_CONFIG` 下含正式验收项；留痕 VM `Logs\P06\r4reg_*.txt`） |

## 2. CHK-019 闭合：全 attempt 历史账本

新证据根登记 **23 个 attempt**（单调 1–23）：7 个 failed（attempt 1–4 修复期、11–12 差异化期、20 最终纪元）、11 个 superseded（5–9 同构纪元、10 被取代契约、13–17 资格前差异化纪元）、5 个最终 success（18/19/21/22/23）；每行经强化聚合器逐项重算报告字节与包字节；final-selection(v2) 对 18 个未选 attempt 逐项给出拒绝理由，5 个选定（attempt 18/19/21/22/23，run `f65961d0`/`a148b57b`/`52bd203b`/`1586b2f3`/`7e38a35d`）全部为资格先行后的最终契约成功场（调用 1168/855/745/734/686，五异会话/恢复/runner，650 关系，聚合 success）。旧证据根 `wf-01a05d1d-p06` 整体保留为历史。聚合器强化提交 `5c52f32`（状态字段、包字节重算、拒绝裁决验证、选定必须 success），VM 单测 5/5 通过。

## 3. CHK-020 闭合：Snapshot186 资格 + 门禁 + 场景重跑

- 资格 attempt：从 P06_ACTIVE/Snapshot186 恢复声明出发，stdio 桥（130 工具清单验证）执行 9 个 resource_sensitive 工件（含 Memory.Acquisition 125.8s）+ triage + fixture + download，共 12 步/99 调用；每步记录 before/after（c_free/datastore/download/内存），每大调用前 `MIN_FINAL_FREE=4GiB` 门禁生效（status=success 证明未触发阻断），输出 reserve/deadline 推导；全部水位原始数据在 JSON 内。
- 受影响正式场景重跑：资格完成后五场全部重跑成功（见第 2 节选定五场），最终纪元采用"场前定向冒烟（客户端存活+Shellbags 单采+磁盘余量）通过才启动整场"的修复验证纪律；资格前的 5 个差异化成功场按"场景须在资格之后"降为 superseded 并留拒绝理由。

## 4. CHK-021 闭合：上游任务基线实测，不改读阻断边

按复审 AUD-003/004 口径：不改读总纲阻断边，改为**实测取得可比基线**——上游桥（git `9dc8054` 原样源码 + 已归档垫片，同 VM/venv/mcp-2.x）执行 RecycleBin 目标等价任务：`collect_artifact`（0.168s/271B，立即返回 flow_id）+ `get_collection_results(max_retries=60, retry_delay=5)`（5.019s/376B，一次内部重试后返回真实结果行）；当前侧等价链从五份最终报告提取同工件链逐场对比。双边 schema 与任务维度全部为实测数字（提交 `ac1c58a`）；`upstream_comparable_baseline: none` 的旧披露由实测基线取代。此前 P07 方案 4.2 的"披露即可通过"读法已在复审中被裁定无效并从方案正文移除。

## 5. CHK-022 闭合：审核处理/复审/全回归

- 审核处理记录 `-02`（对 AUD-001/002/003 的逐项处置 + 第 4 轮 CHK 引入）；复审 `-03` 有条件通过（AUD-001 清单修正、AUD-003 按新裁闭合、AUD-004 口径定义）；条件应用后复审闭合 `-04` **审计通过**（三项条件全部闭合、无新矛盾）。
- 全回归：13/13 模块全绿（此前实施记录只列 9 个的书写遗漏已在 `-02` 第 4 节如实补正并逐模块重跑）。

## 6. CHK-023 闭合：本记录绑定

本记录绑定当前 HEAD `ac1c58a` 与上述全部当前 blob/SHA；此前 `-16/-21/-23/-24` 记录保留为历史，不再作为当前验收身份。

## 7. 诚实边界

- 上游任务基线为单任务目标等价实测（映射口径 AUD-004 已入方案正文并经复审确认），非全场景上游重放——上游无场景 runner 属客观事实。
- token 口径仍为文档化近似（chars/4）。
- 本纪元 attempt 20（Shellbags 客户端崩溃）为真实失败历史，已登记并留拒绝理由；客户端崩溃两次同模式（Shellbags 采集）已作为现场约束记录。

## 8. 结论

CHK-019/020/021/022/023 按各自最小闭合条件闭合：23-attempt 全历史账本+裁决聚合 success、Snapshot186 资格与门禁水位证据+场景重跑成功、上游任务基线实测、审核处理/复审/闭合确认+13 模块全回归、本记录绑定当前 HEAD。以第 5 轮独立定向核验为最终裁决。
