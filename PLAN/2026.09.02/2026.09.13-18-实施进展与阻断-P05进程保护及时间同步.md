# P05 进程保护与时间同步：实施进展及传输授权阻断

> 创建日期：2026-09-13（Asia/Shanghai）；不可变增量记录。
> 平台：Codex；会话角色：作者/执行方。不是独立核验或正式方案审核，不产生/关闭 CHK、AUD，不重置任何旧审核额度。
> 对象 HEAD：`ac1c58a9439aaa303d071a8a040e0f26db85bbdb`，main；当前改动未提交。

## 1. 决定与边界

用户以“我的目标已经告诉你了, 你自己看着办”委托执行方选择安全推进路径。继续 PLAN-CHANGE-011 的 P05 恢复基线回流，不用 Snapshot186 的旧通过声明替代新验收；不降低130工具/650关系/五场/正式网络/双恢复门槛。

总纲当前 v11 草案 blob `dda3e153465331eb1583c5ad109babf263a7a187`；P05 当前 v7 草案 blob `06283d694a535b6560eba8637d756c22c6a9c4ee`；需求 v2 未改，blob `4906941e0698ffe7476838c4479ce344ce30ef27`。P05 §0.6 已写入186→188、schema3/epoch4→schema4/epoch5、保护父链、时钟门、初验/两次完整恢复、原子激活和P06交接的待审规则。草案未完成完整自评/复审；P06 v7、P07 v4 下游新版绑定尚未修改，不能送审为闭合方案。旧文档中的历史保真计数不属于新版本。

## 2. 实施及真实验证

- 已依 diagnosing-bugs 的实际异常路径回归修复 P05 保护集合遗漏：紧邻 kill 的单次快照包含目标、准备器及其父、runner及其父、后端及其父，PID/creation/full command/token 漂移拒绝。没有执行真实 kill。
- 候选 `tests/p05_process_parent.py` 限固定 frontend/client/fixture 角色。普通身份观测失败仍等待子进程，不自动杀进程；等待中断或其他异常保留角色锁，阻止自动重复启动。该 launcher 尚未在真实后端部署。
- 普通观测异常的红测原件：`Logs/P06/resource-gate-regression-370d3d31a9864d68bdd50d96616a883a/`，68项中1失败；修复绿测：`.../resource-gate-regression-d6e85342a2344b3c92e2f33e36a7b501/`，68项通过。
- 等待中断的红测原件：`Logs/P06/resource-gate-regression-a9ea01525b3e41c2adff2108fa21a1f8/`，69项中1失败；修复绿测：`Logs/P06/resource-gate-regression-22b01b72c4a94bd4aade763b01192be1/`，Windows `Ran 69 tests in 26.100s / OK / TEST_EXIT_CODE=0`。各目录 manifest 绑定同步实际字节，response 保存原文。
- 上述69项之后又补了 child PID/ParentPID/executable/command/creation 与本次 launcher 的绑定，以及 fixture 必须由本次 PowerShell 创建、30秒 readiness 门、准备器再次观察不漂移；新增2项测试（下一次应为71项）。**这些最新变更尚未在 Windows 验证，不能引用69项绿测覆盖它们。** 当前 source blob：parent `1973a710d9ecb158dff5380b5546a9a007f59b3c`，guard tests `996ab1ca11e6bcb14f0355b3ce1e3b4d39bcae55`，acceptance `a651e45c41f75d1873bcc06660cd710921a81484`。
- 宿主仅运行 `python3 -m py_compile` 与 `git diff --check`，成功；不计 Windows 行为验收。文档建议的宿主 `.venv/bin/python` 不存在，该次命令退出127，随后用系统python3仅做静态检查，没有据此判 Windows 环境失败。
- 临时同步脚本已改为先冻结全部文件字节，由同一字节集合生成ZIP和manifest，避免测试期间主会话编辑造成清单引用新文件的竞态。旧原件不重写。

## 3. 时间同步实测与副作用

既有 VMware Tools `timesync status` 返回 Disabled，退出码1；Windows时间服务运行但当前未同步。第一次时钟修复脚本把此状态码当控制失败，在只读前态后停止，证据 `Logs/P05/plan-change-011-clock-d71415c9-57a6-4d5d-b196-9ca9fe8ca1db/`；未发生修改。

修正对明确 Disabled 的识别后，只在 `DESKTOP-3FI41GR` 执行一次已安装工具的 `timesync enable`；未改 NTP源、时区、网络、凭据、快照、服务权限。完整前态/命令/后态及包围请求的宿主UTC保存在 `Logs/P05/plan-change-011-clock-8d342270-79c1-4398-ba11-de2e674a2efe/`：guest从 `2026-09-11T02:05:10.6879779Z` 校正到 `2026-09-13T14:14:37.1644736Z`，宿主后读数 `2026-09-13T14:14:37.832442+00:00`，Enabled；偏差不足1秒。此为当前运行VM的隔离修复，不是188恢复稳定性证明。

时间跳变前重建的 fixture PID7144不能凭旧创建时间宣称剩余寿命合格。下一步仍须先保存证据，完整恢复186，再在合法准备状态下重建；未为了绕过第3章规则主动杀7144。此前17号记录中的“完整恢复”指一次真实恢复186，不表示P05两个完整验收周期。未创建188、未恢复/修改用户187、未修改canonical、未重启后端、未调用正式产品业务。

## 4. 子智能体技术复核

`/root/p05_guard_check` 使用 gpt-5.6-terra / xhigh，初派 fork_turns=none，不含主会话上下文；本轮补发仅含必要路径、约束及新证据。只读技术复核，不是正式全面方案审核，不产生AUD/CHK。完成在60分钟上限内。

复核指出普通异常等待、锁异常释放、fixture与persistent launcher来源绑定问题。前两项有Windows红绿证据；第三项已补代码及反例但最新验证被环境权限门阻断。技术复核结论不能替代实施后的主目标独立全面核验。

## 5. 新的执行阻断

运行环境自动审批拒绝最新一次 `python3 /tmp/velo-single-request-sync.py`：认为向未在可信配置明确标记的 `192.168.204.232` Windows VM 发送项目源码可能泄露敏感内部信息，要求用户明确授权该具体载荷/目的地。此次被拒调用未执行；不换传输工具、不改地址、不间接绕过。

待授权范围：经宿主 `192.168.204.1:28786` 单请求 host-only 通道，仅向既定 `DESKTOP-3FI41GR` / `192.168.204.232` / `C:\mcp-velociraptor` 同步固定P05/P06测试源码、对应JSON输入及两README（实际集合见 `/tmp/velo-single-request-sync.py`），不发送PLAN、Logs、凭据、二进制或任意新增目录；双方SHA核对后关闭临时监听，再执行上述Windows测试。风险为仓库文本离开宿主并写入该VM。该授权不包括新账户/ACL、快照切换或拓扑扩展。

当前结论为**实施被传输授权门阻断，未完成总体验收**；不是“核查通过/不通过”的独立核验结论。持续开放CHK保持开放。恢复后先完成71项Windows验证及P05/P06/P07规范对齐、自评和必要正式复审，再进行候选初验/双恢复/激活，随后新P06完整执行、P07及新HEAD独立全面核验。
