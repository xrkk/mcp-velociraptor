# 执行调整：分批源码传输与旧基线门禁

2026-09-14，Codex 主会话规划/执行方。承接08号用户明确互传授权，不新增用户授权请求，不产生或关闭CHK/AUD。

## 测试传输方式

首轮 `Logs/P05/windows-service-unit-4ef9a5b6-d61c-4a96-a8c9-2a9b4c21fb40/` 原始响应证明目标MAC/IP匹配，但PowerShell控制包装层因命令长度产生WinError206，测试未运行。第二轮 `Logs/P05/windows-service-unit-47f920a5-811c-4bb1-821c-2b866e3a2346/` 尝试短内存loader，子智能体报告guest原有源码不匹配冻结身份而拒绝运行。两轮均非产品测试失败，不计通过，不覆盖原件。

主会话调整原08号“无guest文件写入”的子任务实现限制：在用户已授权互传范围内，允许子智能体排他创建UUID临时目录，以长度受限的分批内容传输四份冻结源码包，逐块及整包校验后用Python -B内存加载运行。不得借用guest旧源码冒充当前代次，不覆盖guest仓库，不启动SCM服务或真实HTTP服务，不改网络/快照/秘密。记录实际临时路径、创建事实和内容身份；只清理本次排他创建且身份可证明的精确文件与空目录，保留清理回执。未知文件不得删除，清理失败须保留并报告准确路径。此调整是文件互传的正常临时落地，不解除部署或业务验收门禁。

## 下游规范状态纠正

主会话完整读取P06/P07后，将页首旧“20号自评通过”状态改为PLAN-CHANGE-012回流中，明确当前总纲v12与P05新起点契约优先。旧186及旧186→188配对不得执行；当前文件互传不授权自动恢复、正式资格、五场或成本实测。保留旧条文历史，不假称新状态配对已完成自评。

- P06 v9：`2026.09.05-29-实施子方案-P06逐工具与连续情景全量执行.md`，blob `8ceb3adab8a774da4c7b93880dfa9524863cf7dc`。
- P07 v6：`2026.09.12-18-实施子方案-P07清理与终检.md`，blob `226be3fac6c617676bbc0e45a7b4726f44a67f73`。

130工具/五场650关系、P05真实初验及双周期、P07五组成本/80正式项/67累计结论与新HEAD独立核验均保留。当前工作没有证明产品可用；待取得本轮实际Windows测试结果后继续既定P05回流与修复。
