# PLAN-CHANGE-011：P05 恢复基线前提回流登记

日期：2026-09-13；会话角色：作者/执行方；状态：回流已接受，实施准备中，规范迁移尚未生效。

用户在记录 16 的阻断说明后明确要求“我的目标已经告诉你了，你自己看着办”。执行方据此选择修复 P05 可恢复环境，不降低工具覆盖、安全约束或独立核验要求，不再就同一回流路线重复询问。此授权不推定允许删除旧快照、使用其他 VM、扩大网络暴露或新增高权凭据。

## 来源、证据和根因

固定需求 v2 OID `4906941e0698ffe7476838c4479ce344ce30ef27`，总纲 v10 OID `a2658be62622a373bf65bbebc50ae33e8a99654e`，P05 v6 OID `532844e4cc7892fdb9ee447b8b128b11503478ba`，HEAD `ac1c58a9439aaa303d071a8a040e0f26db85bbdb`。这些 OID 为工作树内容身份。记录 16 及其恢复原件保持不可变。

只读最小复现：恢复后 OS boot_time 为 `2026-09-10T23:50:03.5000000Z`，清单 process.creation_time 为 `2026-09-04T23:42:32.923Z`，PID 1304 查询匹配数 0。清单所指进程早于此次系统启动，不能是该系统运行周期内仍活着的同一进程；不是 P06 产品调用结束了目标。还观察到 Velociraptor 两个父进程缺失。历史 snapshot186 activation 摘要只有端口、代表报告 success 和两次恢复 ID，没有单独证明该 fixture 进程和父进程保护集仍有效。

源码复核发现 `tests/p05_real_acceptance.py` 的 protected_processes 仅选择 runner/父 PID 与 Velociraptor 子进程，没有解析并要求 Velociraptor 父进程存在；kill 前仅核对 token 子串，没有比较 creation time 和完整命令行。Windows 回归原件 `Logs/P06/resource-gate-regression-3a32ea3baa384c2fa0c1f196e4bfbd6c/` 重现旧实现对缺失父进程未抛错（1 test failed）；修复后 `Logs/P06/resource-gate-regression-f0620b3b41de4d96944b6da36d6542f2/` 的 3 tests 通过。这是候选源码修复证据，不是环境修复或新基线通过证据。

## 已选路线和职责

1. 回到 P05 准备阶段，保留 Snapshot186 和所有历史证据；当前已经完整恢复的 Snapshot186 只作为本次获授权修复的明确 predecessor，不得继续把它当作合格 P06 起点。
2. 先落实既有契约保持修复：进程 PID/start-time/完整 argv/token 的紧邻核对；保护集包含实际 Velociraptor server/client、runner/准备执行器及各自父进程，不允许缺失身份静默省略；目标与保护集不得重叠。创建候选前及每次恢复后必须有同一硬门禁。
3. fixture 仅按 P05 §3 既有“同 spec/workflow/attempt、静态对象相等、仅专用进程缺失时可重建”转换准备；先保全 instance 原件，不改静态内容、spec、token 语义或 sleep 参数。不得在 P06 attempt 内执行此操作。
4. 对孤立 Velociraptor 进程，先只读固定原程序、参数、配置来源、API/client 和当前工作状态，评估同一测试环境内以持续存活的父执行器重新启动的最小方案。不得改变后端版本、client identity、API principal、权限或监听范围，不得终止未能精确识别的进程。实际环境操作前要有原件保全和范围确定；未知条件则保留失败，不做猜测性重启。
5. 新基线采用新增、可区分的候选对象，不覆盖/重命名/删除 Snapshot186，不擅用用户现场存在的 Snapshot187。计划名称为 `Snapshot 188-Velociraptor-MCP可恢复验收基线`，创建前必须确认名称未占用，marker 只接受 metadata 实值。
6. 新候选牵涉总纲、P05 选择器与 P06/P07 快照绑定，必须先发布负责层的完整规范修订及保真/自评、必要方案审核；本登记不赋予 selector 接受新对象的能力，也不提前修改 canonical。需求 REQ-020/RACC-020 与 RACC-026/累计结论67的“先初验、再创建候选、再两轮完整恢复、最后原子激活”不变。
7. 新候选的创建前 gate、两轮完整恢复 gate、正式 HTTP/stdio schema 同一性、权限/ACL/防火墙、四依赖、唯一 client、fixture、保护集及实际工具正向/负向证据全部闭合后，才允许按受审新迁移原子激活。任一失败不激活、不冷启动、不把旧报告改写为通过。
8. 激活后 P06 新资格、逐工具、五场和严格聚合完整重跑，随后 P07，最后由无主会话上下文的 gpt-5.6-terra/xhigh 独立核验；等待总上限 60 分钟。作者不关闭 CHK。

## 受影响边、边界和重新裁决

稳定边：REQ-014/020/023/026、RACC-014/020/023/026 → OUT-014/020/023/053/054 → IMP-P05-002/003/005/010/011 → ACC-P05-003/004/005/007/008 → P06 IMP-002/003/005/008 与 ACC-P06-001～006 → P07 入口/ACC-024/025。最终 130 工具、650 关系、五情景、同场同会话/同服务、失败清零、单 root/单 client、host-only 28790、Bearer/Host/Origin、无危险开关/新 fallback 均不变。

适用前提：可在当前 VM 内按既有依赖/身份配置恢复稳定运行，新增内存快照有容量且可重复恢复，原始证据可保全。若必须改变角色权限、对外网络、工具/参数、固定数据内容或覆盖门槛，另行回流负责规范，不把此登记当无限授权。

旧审核批次和旧 CHK 不变；本记录不产生 AUD、不宣称全量保真完成。后续正式方案复审若需要，单独固定对象并登记新批次，不重置旧额度。本登记不是独立核查结论。
