# 执行授权：宿主与目标虚拟机文件互传

- 创建日：2026-09-14（Asia/Shanghai）。Codex 主会话，规划/执行方。
- 用户最新明确指令：“授权, 后续虚拟机与宿主机之间文件互传不许再要权限”。
- 背景：当日 05 记录的私有源码传输权限拒绝，已向用户说明目的地及源码暴露风险；当日 06/07 的本地修复导致测试代次改变。此次明确授权覆盖当前修订版本及后续本项目所需宿主与目标 VM 文件互传，不再重复向用户请求同类授权。

## 执行边界

目标为本项目 Win10MalBox-Velo VM，固定地址 192.168.204.232、MAC 00:0c:29:83:b8:65；传输前仍须核实目标，不能以相同 hostname 代替。目标身份不匹配时停止，不把授权扩张到其他克隆或外部地址。工具级权限仍按环境正常申请，附上本次明确授权，不绕过审查。

此授权解除文件传输的用户许可阻断，不等于允许外传秘密、不等于任意覆盖或删除、不改变受保护快照和固定 IP，也不豁免源码保真、P05 准备/部署、自评及最终独立核验。秘密文件的内容不因本授权进入源码或验收日志。跨机证据回传保持原始字节和校验链；普通文件互传无需再向用户索要许可。

## 当前派遣

`windows_service_units_authorized`：gpt-5.6-terra / xhigh / fork_turns=none，60 分钟上限，独占 VM 窗口，仅运行四份固定源码的 Windows 内存隔离单元测试；不部署、启动服务、改网络、恢复或创建快照。主会话同时处理本地规划，不并发操作 VM。

固定代次：

- tests/p05_service_host.py：0f3983199a0909d83b22872b2acd5e12f79885b6。
- tests/test_p05_service_host_abi.py：a901cbee5bc97aa93bd0e58ba2be54a678566331。
- tests/test_service_transport_lifecycle.py：0e433eca09518350e58336ca860e4ae4cf2043cc。
- velociraptor_transport.py：31753bc5c9424761476fb6129b95c0bdd91fa108。

HEAD 仍为 ac1c58a9439aaa303d071a8a040e0f26db85bbdb，不能用 HEAD 冒充当前 dirty 源码身份。宿主 .venv 和旧 /tmp 控制 helper 当前不存在，子任务须使用现存证据定位控制协议并重新建立受限测试传输；不猜测旧临时文件仍在。

此记录只登记授权和派遣，不预判测试结果、不关闭 CHK/AUD，也不认定 P05/P06/P07 已完成。
