# 实施记录：P06 正式全量（资格、逐工具、五场、selection、aggregate）

- 日期：2026-09-15（记录补写 2026-09-16）
- 执行方：接手执行方（G0→G7 长程执行）
- 入口前提：canonical = schema5/epoch6/NETWORK_ACTIVE，active=Snapshot 188（G4 原子激活回读一致）
- 固定证据根：`Logs/P06/wf-01a05d1d-p06-r3`（只追加；历史与失败 attempt 全保留）

## 1. 恢复与证据根（每 attempt）

- 每个 attempt = 一次对已激活 188 的独立完整恢复（`/tmp/p06-restore.py`：selector 只读断言 6/NETWORK_ACTIVE/188 → vmrun 树/vmsd 双快照断言 → 单次 revert → vmsd current=4 → VMX checkpoint.vmState=Snapshot4 → vmware -x（带重试）→ 控制面等待 → guest 身份四要素）。
- r3 证据根构建（`/tmp/p06-attempt-build.py`）：P06_ACTIVE 声明（七类记录：四类 attempt-绑定摘要 + canonical 字节（激活后规范状态，sha 随激活变更）+ baseline-adoption/919c5de5…（完整自包含收养包）+ activation-188/5d2f20ad…（完整自包含激活包））+ fixture-instance + server-observation；`scenario_runner.load_current_restore` 全链（含激活包再验证）通过后才运行。
- guest 侧 r3 报告根按 G5.1 初始化（快照内不存在，仅建空目录，观察器测量的就是该树）。

## 2. 资格与逐工具

- `resource-qualification/0a8d8417-fc4f-42c6-be23-a958a17ad5e4` success：65 真实调用；9 个资源敏感工具（PC-014 后口径，PacketCapture 排除）+ forensic-triage + fixture-file + download；每步资源 admission（控制面只读观察器：GlobalMemoryStatusEx+disk_usage+三树字节）+ 前后/轮询读数原件 + qualification-payload manifest/zip 冻结 + resource-budget 真实 zip 字节生成。
- 过程修复（保留失败 attempt #1/#2）：p06_resource_gate 观察器对 `netsh trace show status` "无会话进行中"态按退出码 1 误判（该子命令正常期望态即返回 1）；修复为按输出语义判定。guest r3 根缺失导致 tree_bytes 拒绝 → 构建器初始化。
- `individual-acceptance/488afd59-dd24-4b31-a539-83c2231bff48` success：清单 + 固定错误契约分账（coverage=0 为该模块契约本意）。

## 3. 五场情景（每场新恢复、同场同服务进程、同 HTTP session）

| 场景 | 选定 run（ledger attempt） | 轮次说明 |
| --- | --- | --- |
| p06-compromise-scope | `50e207cf…`（#6） | 首轮 d187aa8c 因 r3 缺 resource-qualification-selection 失败（保留 #5） |
| p06-ransomware-root-cause | `1908bd72…`（#7） | 一轮通过 |
| p06-credential-lateral-movement | `b8a93652…`（#8） | 一轮通过 |
| p06-data-exfiltration | `69c817f2…`（#14） | 4 轮（#9/#11/#13 失败保留） |
| p06-remediation-validation | `e9d703be…`（#12） | 2 轮（#10 失败保留） |

- 失败根因（三轮同类）：`fixed-triage-wait` 期间新 urgent triage Flow 被服务端记 `UNKNOWN_FLOW（"Flow not known - maybe the client crashed?"）`——VR client 在紧邻的 hunt 取消序列后偶发（约五成概率）不再处理新 urgent 任务；同场景重跑即过，非确定性。五场全部 129 工具覆盖，每场 coverage=129 关系。

## 4. selection 与 aggregate

- `resource-qualification-selection.json`：显式选择唯一成功资格 attempt（0a8d8417）。
- `final-selection.json`（schema2）：all_attempts=#1..#14 与账本一致；selected=上表五场；rejected=#1/#2/#3/#4/#5/#9/#10/#11/#13（逐条非空理由）。
- `aggregate.json`：**success**；relation_count=645、tool_count=129、scenario_count=5、distinct_restore_attempt_count=5、distinct_session_count=5、server_instance_count=1、runner_lifecycle_count=5。

## 5. 与规范的关系

- P06 数据契约（tests/data/p06_coverage_manifest 645 关系 / tool_count 129）与实测一致；PC-014 三链口径、PC-015 防火墙/EXECVE/Origin 三裁决全部落地并体现在各 attempt 原件。
- 本记录不自评 CHK 关闭；CHK 由独立核查方按 G7 程序处理。
