# PC022作者采纳决定与正式同步范围

2026-09-22。角色：作者采纳决定记录（主方/作者职能，按用户同日明确授权"按 R02 候选执行 G02 正式回流"作出）；不是独立核验，不产生或关闭AUD/CHK，不开放生产实施门。

## 采纳决定

1. 用户于2026-09-22明确授权按 R02 候选执行 G02 正式回流。R02 候选为 GLM 按 codex-R01-verdict（PC22-001～004）从 R01 候选接续修订的隔离稿：`.tmp/velo-glm-20260922-082250/pc022-r02/candidate-r02.md`（SHA-256 `2d01094b8e83bf75848033b3f785039da67a247af98cece885ad38b488ddc266`）、`impact-r02.md`（`26487a7a97342675cbd39a2c864765c509dfe928a530137a4e2211f2bb46f504`）、`validation-r02.md`（`5349445f3e4f38a062e98fde619387ef9d35cf35b039da5bfdf32b511820da47`）。四项缺陷经 G01 验收闭合（`.tmp/velo-glm-20260922-082250/G01-pc022-r02-verdict.md`）。
2. 采纳边界即05号登记的方向：限定 Windows 运行环境、实际原生刷新确认、失败停止；不忽略同步错误、不提权、不卷级 flush、不做 VM 断电试验。成功口径是规定 API 全部实际成功且身份/字节复核成立；该保证边界作为规范差异公开，不称 POSIX 等价，不冒称硬件掉电保证。
3. 本采纳仅同步规范文本；不表示主方完整保真自评（G03）、实现（G04 起）、部署或现场通过。实施门保持关闭。

## 正式同步范围（五份业务规范，P01–P04 不变）

应用方式为 G01 已验证的 21 处锚点（`.tmp/velo-glm-20260922-082250/pc022-r02/apply_candidate.py`，before/after 副本与完整 diff 存同目录 `R02-evidence/`）：

- requirement：`PC021当前执行条款` 内插入 Windows 刷新保证与累计结论边界两段；无删除。
- master：`0. PC021签发与可移植下载总纲` 第二段因果链整段替换（含 R01 丢失末句补回）；`0.2` 稳定边后插入说明。
- P05：`0.PC021.1` 后插入 PC022 helper/新建层级/同卷边界节；`0.PC021.3.3` 发布 primitive Windows 化；`0.PC021.2.1` receipt 版本 v2；`0.PC021.2.2` issuer 六步→五步+六事件表；`0.PC021.2.3` 后插入 source 分层；§5 Windows 路径与 FAILED_BEFORE_REPLACE 语义；`0.PC021.4.2/4.3/4.4` activation receipt v3 与 Windows 替换。
- P06：`0.PC020 P06消费契约/入口和配对` 末尾插入 PC022 接收门；无删除。
- P07：`0. PC021 P07交接契约` 第一段整段替换（现行 5/4键 manifest、`downloads/<flow_key>/<file_id>/content.bin` 读取、禁解引用旧 `local_path`、Triage 不下载与两 fixture 链全部原文级保留）。

版本分配：需求 v9→v10、总纲 v22→v23、P05 v18→v19、P06 v19→v20、P07 v17→v18；P01 v4、P02 v7、P03 v3、P04 v11 版本不变。P04 业务语义不变，仅按 R04 同步先例更新页首"唯一当前九规范组"指向（P04 在 manifest 中记 modified=true，属组指向性质，不升版）。当前九对象组切换为 `CURRENT-PC022-R01`，manifest 为 `PLAN/2026.09.22/current-normative-inputs-pc022-r01.json`；`CURRENT-PC021-R04` 保留为历史父组原字节。页首组引用、历史条款中的当前组指向与 P07 版本记录随版本分配更新；历史组名（`CURRENT-PC020-R01`、`CURRENT-PC021-R03`）与既有版本记录不改。

## 不变义务与勘误声明

- P04 产品 `download_flow_file` 不因 P05 包复制要求增加目录刷新、hard-link 或 package 义务。
- 80正式项/67累计结论、130注册、129实测+1排除、645关系、187/189阶段、Triage 4 GiB/2400秒、OUT/ACC/Pxx 编号与稳定边全部不变。
- issuance receipt v2（13键）、epoch6→7 migration receipt v2（13键独立kind）、epoch7→8 activation receipt v3（15键独立kind）、intent 10键不升版；六事件名与顺序、exact keys、Ref 解析、并发 loser、失败保全语义保持。
- 若正式来源被解释为不可限定的硬件掉电持久性要求，按05号重新裁决回主方，不由执行端移除。
