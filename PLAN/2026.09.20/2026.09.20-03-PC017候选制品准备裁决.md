# PLAN-CHANGE-017：候选制品准备裁决

创建日2026-09-20；主会话角色：作者/规划/修复/调度。本文只批准隔离候选制品准备，不是当前规范覆盖块、完整自评或产品实施放行。

## 已完成与限制

版本源码及合成读取已完成，见PC017-SOURCE/PC017-VFS主方验收。PC017-MAP提供147条冻结原文和依赖导航；其模板化拆解未获主方认可，不能当80/67语义保真通过。主方接手解释与裁决，不继续派表格整理。

## 依赖候选的唯一改动

采用现有锁定ZIP `Logs/P05/source-spike/Windows.Triage.Targets.zip`（SHA256 `44e59ea3a35db2b1e582cfe589c1f511b69eabc5931c4db96928bb7ba147acbb`），其中唯一YAML为80977字节、SHA256 `1a699aa9d5346fe342fb7c646bdfc755e110cf745658fab35499980fc0bbbb1c`。

仅修复从 `_BasicCollection` 可达的未解析引用 `Thumbcache`，使其精确指向既有 `ThumbCache` 定义；不做全局大小写归一化，不改target名、glob、其他引用、采集策略、参数默认值、VQL或工具面。候选须保留原始字节/换行，仅允许可定位的引用字节差异；生成新hash和派生来源记录，明确不是未经修改的上游原件。

采纳理由：真实版本字典查找大小写敏感，原基础包漏掉已意图包含的ThumbCache；纠正引用比删除目标、创建替代复合采集或全局忽略大小写更符合REQ-013/CON-011/CUM-035/056。代价：必须在P05锁定与部署链登记派生版本，不能复用旧hash/成功证据。

可准备内容只有`.tmp`新候选YAML、精确补丁、源链manifest及离线引用图校验。不修改正式manifest、Pxx、README或产品，不导入VM、不采集、不提交。此准备是来源对象定型，供主方正式回流填写准确hash，不是绕过自评实施门。

## 正式实施仍需闭合

1. 主方把候选来源、适用边界和新hash同步需求/总纲/P05及P06/P07直接消费者，完成自评与需求保真；未来方案第三方复审已取消，但实施后独立核验保留。
2. 1GiB仍为当前执行上限；旧容量盘点约2.27GiB可能上传且有15条unknown，不能把估计当完整上界或直接批准反复撞限。资源限额变更是另一待裁决子项，不因本候选准备自动提限。
3. 文件身份与读取须同时解决data/idx可区分、真实selector冲突、逻辑/紧凑输出及大小校验；不得只加Type、任取重复行或隐去idx来冒充完成。此处不冻结尚未闭合的公共schema。
4. 候选引用图必须无新增未解析引用/环，新增可达范围只能是既有ThumbCache及其既有叶子。若源hash不符、需改其他行为或存在新歧义，停止该候选并回主方，不扩张本次补丁。
5. 新源正式生效后，仍须真实Windows固定入口、完整BasicCollection、成功Flow及文件链和受影响回归；局部离线候选通过不等于A08/总纲通过。PC014排除、PC016暂停原件搜索、唯一VM/固定网络、无自动删除均不变。
