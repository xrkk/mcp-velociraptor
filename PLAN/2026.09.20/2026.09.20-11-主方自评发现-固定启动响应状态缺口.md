# 主方自评发现：固定启动响应状态缺口

2026-09-20；作者/修复/调度方。HEAD 326d54a57b89a1e8623bb9486298402e2047fc57。本记录为SELF-FIDELITY-002 / PLAN-CHANGE候选，不是实施后独立CHK，也不关闭任何审核记录。

## 已确认反例

当前需求REQ-014明确kill_process返回真实flow_id与初始状态，总纲§3及OUT-014/ACC-014同样要求真实Flow和初始状态；累计32及累计8也承接该语义。

P04v7（blob 845e1577d6afaf6f1b64368fb9a038da2731aeb8）§3.1把固定成功status冻结为字面success，§3.1.1把kill_process成功模型限定为基字段加flow_id，并且additionalProperties=false，没有携带后端state的字段。当前velociraptor_mcp_core.py的FixedFlowReferenceResult仅新增flow_id；velociraptor_fixed_tools.py的_dependency_collection在取得flow后只复制flow.flow_id，丢弃flow.status，kill_process复用它。故“启动调用成功”无法让消费者恢复“后端初始状态”。这不是当前PC017文稿同步引入，应归于旧P04承接缺口。

## 范围与最小闭合

至少REQ-014/RACC-014、累计32、总纲OUT/ACC-014 → P04固定启动输出/golden → P05真实KillProcess回填 → P06固定成功链 → P07README与终检受影响。累计8的所有固定启动工具及累计35需要一并核对，不能只改kill_process后便宣称全部启动状态恢复。

保持上位真实初始状态要求，不以额外调用get_flow_status或字面success替代。候选修复是给固定启动结果加入非空真实state，并使后端缺state仍为错误；这将改变冻结输出schema/golden及下游绑定，必须先由主方完成受影响规范裁决和自评后实施，不作为纯实现小修绕过。尚未修改P04或产品，尚未登记已完成的PLAN-CHANGE编号，不能声称缺口关闭。

## 当前自评进度

本轮完整阅读P01、P02、P03，核对P04接口/算法/验收切片及P05当前覆盖和入口，发现本缺口与独立的PC018游标冲突。80/67全量保真计数尚未完成；已有异常非零，因此当前不得自评通过。其余条目仍继续核查，不把未读/未验项填为完整，也不沿用旧版9/26/19项通过数字。
