# PC017 主方自评发现：发布后故障边界

创建日：2026-09-20。角色：作者/修复/调度方；不是第三方审核或实施后的独立核验，不产生AUD/CHK，不改变审核额度。HEAD：326d54a57b89a1e8623bb9486298402e2047fc57。

## 对象与范围

当前作者组PC017-AUTHOR-REVISION；身份以同目录pc017-author-sync-manifest.json固定，其SHA-256为9be201f76b2195cadc7a0dc33b57babba23e09c100518ada032a85c74c0de235。P04v6的SHA-256为848a9ef757c9cdb5b2951f350fc67ac4e77c0c83f55200ecac899d9b4d73f248。

本记录仅是一条已确认的自评发现，不是完整80/67自评；应承接总数147，完整/未落点/弱化/不可验收/复核缺失各类最终计数尚未完成，不能记为零或全通过。

## SELF-PC017-001：发布后的失败不能同时满足只清part和零发布

- 需求/总纲：REQ-010、CON-006/012、RACC-010 → OUT/ACC-010。
- 负责切片：P04 §3.2步骤7、ACC-P04-002；下游P05文件fixture、P06文件安全矩阵、P07终检。
- 当前算法：先os.link(temp, content.bin)，再resolve复核root、父目录和完成文件；任何失败只清本次temp。
- 当前验收：路径逃逸及后端/I/O故障均不发布完成文件。
- 反例：os.link已经成功，随后resolve发生OSError；照算法只清temp，content.bin仍存在，故不能兑现该错误分支的零发布承诺。
- 主方在本机TemporaryDirectory中执行标准库最小状态机：xb写入valid-content、flush/fsync、os.link、注入发布后OSError、只unlink本次part。输出为 own_part_exists=false、completed_file_exists=true、acc_p04_002_zero_publish=false；临时目录由上下文管理器清理，没有触碰产品或VM。
- 证据限制：这是契约状态机反例，不是当前产品实测漏洞，也不是Windows权限/重解析点竞态已被实测利用。无需因此扩张为新的威胁模型或重新查找历史原件。
- 归因：矛盾来自主方采纳的上游安全算法与验收措辞。AUTHOR-SYNC机械同步没有引入此语义，不判执行端任务失败。

## 最小闭合条件

主方必须先明确os.link成功这一提交点前后不同的失败语义：发布前故障保持零新完成文件；发布后检查/清理失败是否保留已发布对象、如何返回过滤错误、如何向验收披露残留，并保证不删除赢家/既有文件。若坚持所有故障零发布，则须给出能够兑现该承诺且不会错误删除其他请求对象的算法，不能只追加一句清理要求。

上述取舍须在PC017现有回流内由主方作出，同步受影响规范与版本身份并加入故障注入验收；不得由实现端自行选择fallback，也不能简单删掉发布后检查。此记录尚未批准任一修订方案。

## 当前结论与下一步

六规范作者稿同步任务通过；主方完整自评未通过、尚未完成。已确认的契约冲突阻断PC017产品实施、4 GiB部署及真实分诊复跑。主方负责修正这一冲突并继续147项自评，无需用户重复授权；不把本发现冒充总纲全面核查结论，也不声称其为唯一剩余问题。
