# PC017 分诊来源与预算切片：作者裁决（待规范同步及自评）

日期：2026-09-20。主会话为作者/修复/调度方。本记录冻结规范补丁编写输入，不是新契约生效、自评通过、部署授权或总纲验收结论；其余 PC017 文件身份/稀疏读取切片仍由主方裁决。

## 已作决定

1. 保留无参数 collect_forensic_triage、Windows.Triage.Targets、完整 _BasicCollection、2400秒 timeout、root Org 与唯一Windows目标。Targets 在 collect_client env 边界用 JSON 数组字符串编码，不改公开工具参数。
2. 该固定入口专用 max_upload_bytes 请求预算采用 4294967296 bytes（4GiB）。它是本次规范修改的确定目标，不是无限额，也不是物理磁盘上界；不改变其他入口/动态工具/全局默认值，不增加用户可调参数，不做自动重试提限。当前运行限额仍1GiB，正式规范采纳并自评通过前禁止切换。
3. 保留所有目标、glob、源文件采集及 HashOnly 默认；不能通过排除 WindowsIndexSearch、删大文件或 HashOnly 来凑成功。
4. 派生来源只修正 _BasicCollection 的 Ref: Thumbcache -> ThumbCache。候选 YAML 为 .tmp/velo-codex-20260920-pc017-dependency/candidate/Windows.Triage.Targets.yaml，80877 bytes，SHA256 42f396d67dccb06d7e5c4c03d4d24b2434b0bbda2d8a0c7ae1757f0a2a7f44bc；派生标识 Windows.Triage.Targets+pc017-thumbcache-ref.1。原始 ZIP 44e59ea3a35db2b1e582cfe589c1f511b69eabc5931c4db96928bb7ba147acbb、原始 YAML 1a699aa9d5346fe342fb7c646bdfc755e110cf745658fab35499980fc0bbbb1c 保留来源地位，不伪称候选是原作者发布。解压表仅一个字符变更，外层 token 外字节不变；26/122 -> 27/123 的目标/叶子差集只新增 ThumbCache/Thumbcache_DB，其他不变。
5. 安装仍由P05受控完成，MCP不得联网安装或运行期静默替换依赖；导入前后核对精确定义与新锁定版本。不擅自改其他依赖，不自动启用旧快照或伪造Snapshot188原件。当前运行环境测试与P05正式基线激活分开，既有激活门、PC016暂停状态和独立终验不变。
6. 受控真实重跑前重新核对目标身份、实际datastore/filestore卷、空闲空间和内存；同卷至少13GiB可用且内存至少1GiB为本次执行前筛查，不是产品下载配额或永久通用需求。13GiB来自3×4GiB+1GiB保守筛查，不证明最大占用。容量R01/R02已经完成规划准备，无需再派准备盘点；临执行才复查。未达门时不启动采集、不清历史数据、不恢复快照、不自动扩盘。
7. 同时最多1个新主动Flow，完整记录真实ID与请求预算。ERROR/取消/超限/关键采集错误不能由有行数或uploads改判成功；必须真实终态和实际结果/文件链共同通过。cap按后端压缩数据累计检查且可能并行超出，不能拿uploaded汇总、索引元数据或逻辑大小硬等同预算计量。
8. 若4GiB仍不足、依赖不兼容、目标图变化、版本/存储拓扑变化或未知叶子导致完整性不可证，停止受影响验收并回主方再裁决；不反复提限或缩小目标，不把未验证算通过。

## 验收及稳定追溯

切片稳定标识 PC017-TRIAGE-SOURCE / PC017-TRIAGE-BUDGET / PC017-TRIAGE-SUCCESS，属于 PLAN-CHANGE-017，不冒充既有规范依赖边ID。
承接 REQ-013 / CON-011 / RACC-013 及完整目标、来源锁定、成本取舍和重新裁决累计结论；执行端补丁必须定位其准确现行原文及现有稳定边，禁止生成式通用摘要代替语义。
P04负责入口/预算/状态；P05负责来源、预置、资源与激活门；P06负责缺失负例、编码/预算范围回归、完整目标真实成功、超限明确失败及受影响情景；P07负责双README、成本和终检。P04/P05 -> P06 -> P07 原顺序不变。
文件身份/稀疏下载的未决门明确保留；当前真实分诊仍未通过。本切片补丁完成也不代表PC017整体可实施。

## 委派边界

执行端只按本裁决制作隔离候选规范全文和可审阅补丁，不改正式PLAN/产品/VM；主会话负责阅读、语义取舍、实际采纳、规范一致性与自评。此批是规范文本落实，不再做来源、VFS或容量研究。下一步是主方合并剩余文件契约并完成自评，不能用此批绕过。
