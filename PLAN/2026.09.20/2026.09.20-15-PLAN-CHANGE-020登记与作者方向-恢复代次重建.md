# PLAN-CHANGE-020 登记与作者方向：恢复代次重新资格化

2026-09-20。作者：当前Codex主会话，角色为规划/回流作者。状态：候选设计，尚未写入当前需求/总纲/Pxx，尚未完成保真自评，不开放实施门。不是AUD/CHK，不关闭旧核验异常。

## 事实与触发

证据为 `.tmp/velo-codex-20260920-deploy-preflight/` 两轮调查及主方裁决。当前canonical实际schema5/epoch6/NETWORK_ACTIVE/188，SHA256=2d5f378d20c163401b1c4ce7087db102a4719b394fc7a9408baa7d63d7ca4385；实际树187→188，current UID4。旧activation引用字节存在，但当前selector因缺attempt绑定raw命令原件拒绝，且旧来源与当前九规范/三个新代码包不一致。不能从5/5重复接纳，不能覆盖同名188，也不能把旧激活通过继承给当前对象。停止追历史创建原件的用户取向保持。

## 主方拟定方向

1. 保留187、188、旧canonical/接纳/激活及失败原件。拒绝修补旧证据使其看似当前通过，拒绝关闭raw谓词。旧188不得作为当前P06合格入口。
2. 为这一个已知旧状态设计显式、单次、可审计的重新资格化迁移，epoch严格递增，不回写epoch5、不通用化任意恢复开关。迁移以旧canonical精确字节、当前真实树/marker/VM身份、当前规范及来源冻结和排他原子写为前提；未知漂移/未知.next均拒绝。接纳的是可恢复的准备起点，不是旧activation有效性。
3. 拟以保留的187作为新准备起点，定义从retired到本轮准备资格的显式例外与审计理由；物理恢复前先保全当前非秘密项目证据并验证回传。新准备代次不具备P06资格。实际恢复后依现行P05准备/身份/服务/数据保护门部署，而不是在当前现场先部署再盲目恢复覆盖成果。
4. 初验合格后创建新的唯一候选名称，优先评估189（仅候选名称，必须先证明零匹配；marker只能创建后实读，绝不推算UID）。两次完整恢复验证及3次真实Triage/file回填、安全/资源/ready门保持；之后才原子激活新代次。187/188物理保留，旧失败代次不可混拼。
5. 优先评估epoch7准备、epoch8激活；schema是否需升版及新接纳引用形状必须经现有exact-key/schema/报告/selector依赖影响核对后定稿，不把这些建议值当运行参数。旧状态历史可读，操作选择保持fail-closed。
6. 正式服务开机启动未知按现行职责补证；控制面既有自启保持，不能借本变更重建或改网络。只有现有规范要求且事实证明缺失时，另定最小修复动作。不新增无依据产品要求。

## 受影响边界与下一步

规范：需求中的恢复/来源/验收条目与累计结论、总纲阶段/恢复门、P05迁移/原件/配对、P06报告与资格、P07来源与终检；P01–P04逐项判是否受影响，不批量替换历史188文字。实施：selector、接纳/激活/raw验证、runner/index/report/聚合及相应测试。稳定依赖边沿用现有ID，下一份映射逐项列准确定位，不发明既有边ID。

作者取舍：选择新代次重新资格化而非降低旧包校验、冒用5/5或覆盖188；必要代价是一次显式回流与新证据，不重做已经匹配当前字节的三个代码包。若187实际身份/恢复能力或保全条件不成立、新候选名称冲突、迁移无法安全保持原子性或现有字段表达不充分，停止受影响实施并重新裁决，不自动选择其他快照。

下一步只做有限规范/代码影响映射和最小迁移形状比较，主方据此完成正式回流、保真自评和当前治理规则要求的审核，再开放限定实现。本文自身不授权任何canonical、快照、服务或部署写入。
