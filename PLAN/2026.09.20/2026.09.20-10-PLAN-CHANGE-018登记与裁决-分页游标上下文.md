# PLAN-CHANGE-018：分页游标上下文

2026-09-20；主会话作者/修复/调度方；用户已授权总纲验收所需回流。HEAD 326d54a57b89a1e8623bb9486298402e2047fc57。状态：已登记并作出主方裁决，P02作者稿已修正；整体保真、自评和受影响验收未完成，不授权产品实施，不产生AUD/CHK。

## 事实与反例 SELF-018-001

P02v4 blob e7523ce8898f3cfd21296af9eb2a4dd3c3726ac8 的§3.2第8项及§4.3同时规定cursor只能为v1:offset、并要求跨Flow/source复用必须拒绝。P04v7 §3.2和§7.1沿用v1:0/1/2，未定义携带对象身份或登记簿。总纲§3工具链/OUT-009与当前REQ-009要求按source及cursor分页，没有要求游标签发来源认证。

两个合法上下文A、B都生成v1:1后，请求(B, v1:1)可以来自B正常续页，也可以来自调用方复制A的文本；两者当前请求和任何相同先前请求历史都可完全相同。服务无法按没有携带的信息区分来源。全局把v1:1绑定到首次上下文会反而拒绝另一上下文的正常分页。此为规范冲突，不是已完成部署的新增故障归因。

## 裁决及取舍

保留当前v1纯偏移格式、四项上限和显式flow_id/source参数。每次在内部唯一目标中独立验证本次Flow/source，offset只解释本次明确选择的结果集；不承诺识别复制游标。调用方需维持分页链上下文不变，source省略则使用P04既定多source全局偏移。未知Flow、非法source、非法编码、超范围offset仍拒绝；不新增签名、状态登记、隐式目标切换、自动归零或旧格式fallback。

这是显式撤回P02中无法由既定编码兑现的额外承诺，不冒称零语义变化；不是降低上位需求的分页正确性。收益是两个正常上下文可独立分页、继续保持已有接口；代价是不能检测客户端混用来源游标，也不提供后端原子快照保证。若以后需要来源绑定、跨页快照或防混用，重新裁决token格式及生命周期，而非在实现中偷偷加状态。

## 稳定依赖边与验收

REQ-009/RACC-009 → OUT/ACC-009 → P02 IMP-P02-004/ACC-P02-004 → P04 get_flow_results/ACC-P04-001 → P06分页回归 → P07双README/终检。REQ-012/RACC-012、公共字节/行数限制及动态调用无变更。

两个合法Flow及一个Flow的两个合法source分别复用文本v1:1，各自得到本次所选源的第二行；未知Flow、非法source、超范围offset分别拒绝。全页合并仍无漏重，source省略多source顺序仍按P04执行。新矩阵须由P04实际服务层在Windows执行并经stdio/HTTP回归，不以规范自洽代替运行通过。

主方已完整阅读P02全文后修订为v5，旧通过计数保留历史。该变更未触碰PC017六文档，因此旧六文档manifest仍真实；后续整体自评身份集合必须另外固定P02v5，不能误称只六文件覆盖全部输入。P04及下游需核对承接和README同迭代说明后才能闭合此PLAN-CHANGE。
