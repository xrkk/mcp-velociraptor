# PC020主方设计裁决：迁移形状与因果顺序

2026-09-20，当前主方作者。承接15号候选；是作者定稿输入，仍须五规范同步、需求保真与完整自评，不直接开放代码或现场实施，不关闭AUD/CHK。

## 五项选择

1. 新代使用schema6，epoch7/PREPARATION_BASELINE/187与epoch8/NETWORK_ACTIVE/189；schema1–5保留历史解析，但旧5/6/188不得操作选择。唯一迁移前驱精确绑定预检中的canonical原字节SHA2d5f378d20c163401b1c4ce7087db102a4719b394fc7a9408baa7d63d7ca4385；漂移另裁决，不通用化任意旧状态入口。
2. 唯一新候选名固定为`Snapshot 189-Velociraptor-MCP可恢复验收基线`。名称在规范中固定，创建前必须实时零匹配，创建一次后恰一匹配并实读marker。任何冲突停止，不能自动改190、覆盖/改名188，187/188均保留。此命名不是声称189当前不存在。
3. canonical exact root keys固定为`schema_version/workflow_id/epoch/phase/active_snapshot/automatic_restore_allowlist/retired_snapshots/migration_evidence/preparation_evidence/activation_evidence`。migration引用键为`kind/source/evidence_path/evidence_sha256/predecessor_sha256/migrated_at`，kind=`pc020-requalification-migration-v1`，source=`pc020-migration`。preparation引用键为`kind/source/evidence_path/evidence_sha256/qualified_at`，kind=`pc020-snapshot187-preparation-v1`，source=`pc020-preparation-admission`。activation引用沿用四键`source/evidence_path/evidence_sha256/activated_at`，source=`snapshot189-activation`，根包kind=`snapshot189-activation-evidence-v1`。目录分别限定P05固定证据根下`pc020-migration/<uuid>/`、`pc020-preparation/<uuid>/`、`activation-189/<uuid>/`。引用时间是对应不可变证据签发时间，不冒充后续canonical写入已成功时间；实际提交时间记另行执行回执，避免自引用。
4. replace前失败保全canonical，只隔离本attempt owned .next；replace后fsync/回读失败统一`TRANSITION_INDETERMINATE`，保留全部字节，停止任何恢复/业务动作，由主方只读核对实际落点再决定；不得自动回滚或fallback。不新增一个伪造成功/失败的canonical相位。
5. 阶段名保留`P05_REPAIR_INITIAL/P05_REPAIR_CANDIDATE/P06_ACTIVE`；配对分别schema6/epoch7/187、schema6/epoch7/189、schema6/epoch8/189。报告业务根契约保持，证据kind集合及递归封包精确更新，不允许按旧七类硬数漏新证据。

## 消除循环与防止虚假准入

preparation_evidence是**恢复前准入**：仅含187当前真实存在、唯一名称/marker/VM身份、保全条件及retired重新赋予准备资格的明确依据；不含revert、恢复后ready或产品验收通过。先冻结准入证据，再冻结引用它的迁移证据，验证完整图并原子写epoch7。所有Ref只指向先完成的普通无链接不可变对象，不允许相互引用。

epoch7只授予按P05既有方法恢复187并准备的资格，不授予P06资格。其后生成新的实际187恢复attempt→部署/依赖/fixture准备→ready与完整初验→创建189→两次各自完整恢复验证→新activation图→epoch8。恢复后ready/初验是真实后置证据，由activation包含，不回填或改写恢复前准入证据。三次Triage/file链、129+1/645、安全/资源门和原始命令谓词不降低。

epoch7 retired从旧列表中精确移除187（理由绑定migration）并追加188，其他项及顺序保持；epoch8再追加187，active/allowlist/retired始终互斥。原始旧列表仍保存在前驱原件，不篡改历史。旧baseline/activation原字节随迁移包作为历史输入保存，不要求其旧通过成立，也不得将历史失败引用当新成功引用。

准入/迁移包的exact keys、跨目录自包含复制、来源集合和签发先后须在P05候选全文明确；与当前graph/parser相冲突时回主方，不自行增加字段/阶段或更改裁决。新来源不能包含尚未写成的本包/本回执自身hash；源代码/规范可先冻结。实现及现场门仍关闭，待正式回流自评。
