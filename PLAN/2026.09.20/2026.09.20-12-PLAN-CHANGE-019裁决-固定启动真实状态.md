# PLAN-CHANGE-019：固定启动真实状态

2026-09-20；主方作者/修复/调度方，用户已授权必要回流。承接11号SELF-FIDELITY-002；HEAD 326d54a57b89a1e8623bb9486298402e2047fc57。状态：规范裁决及P04作者修订，完整自评/实施门未通过。

## 裁决

不削弱REQ-014及累计8/32/35要求。保留固定工具status="success"表示本次控制调用完成，同时在collect_file、collect_forensic_triage、kill_process的成功输出增加required非空string字段state，原样承接本次创建Flow后立即回读的真实后端状态。start_hunt原有state继续表示Hunt状态，另增加required非空string字段flow_state承接创建的主要Flow初始状态；不能用Hunt状态冒充Flow状态。动态工具既有status承接后端状态的契约不变。

初始化状态是创建后首次实际读回值，不保证返回时仍不变，不同步等待终态；FINISHED和ERROR也必须真实保留，控制调用success不等于业务验收成功。后端缺失或空状态仍为BACKEND_ERROR，不合成SUBMITTED/RUNNING，不自动重建Flow，不以稍后get_flow_status替代启动响应字段。Hunt既定部分失败收敛规则不变。

影响：REQ-007/008/010/011/013/014、CON-005、RACC-008/010/011/013/014及累计8/9/21/22/32/35/38/55的适用切片 → 总纲OUT/ACC-008～014 → P02真实FlowReferenceResult → P04四个固定启动输出及独立golden → P05三链初验/双恢复 → P06固定成功链/五场 → P07schema成本、双README及终检。公开输出schema发生显式变化，旧schema哈希和依赖该哈希的报告不得被当成新版验收。

## 可观察验收与边界

四工具分别注入WAITING、IN_PROGRESS、FINISHED、ERROR及非空后端新状态，断言Flow状态逐字保留，且只创建一次、不等终态。三个固定启动的state与Hunt的flow_state必须来自同一启动返回的Flow；Hunt.state仍来自真实Hunt。缺state/空state必须失败。保持root/唯一client、参数、timeout、预算、Hunt单端点和独立取消、依赖缺失零Flow等不变量。

golden按本文手工扩展required字段；官方SDK stdio/HTTP的tools/list、成功/错误样本必须匹配。P05真实链保存启动响应状态和随后终态，分别判断，不要求两者相同；P06同样，不把返回ERROR状态的调用计作业务成功。两README须随产品修复同迭代说明status/state/flow_state三者含义和兼容变化，不拖至P07。

不添加新工具、目标参数、轮询器、状态枚举猜测或旧客户端兼容层。后端不能提供真实状态或SDK不能承载输出时停止受影响项回流，不删字段逃避验收。本裁决不是测试通过或产品部署授权。

## 当前输入版本覆盖

PC017-AUTHOR-REVISION-2六文件manifest仍作为当时不可变快照保留，不重写其中hash。本裁决与10号PC018使当前P02升v5、P04升v8；需求v5、总纲v18、P05v14、P06v15、P07v12及P01v4/P03v3不变。P04旧组v7身份从此仅为增量前基底。完整自评必须固定本裁决、PC018和全部当前Pxx实际blob，不能将旧六文件组误称完整现行输入。
